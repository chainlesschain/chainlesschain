# npm configuration for Electron packaging and Vite module resolution
# Use legacy peer deps to avoid peer dependency conflicts
legacy-peer-deps=true

# Increase network timeout for large packages
fetch-timeout=60000
fetch-retries=3
