# 分享功能快速测试指南

**测试时间**: 5-10分钟  
**前置要求**: 已启动开发服务器

---

## 快速开始

```bash
cd desktop-app-vue
npm run dev
```

应用会在 http://localhost:5173 打开

---

## 完整测试流程

### ✅ 测试1: 创建分享 (在浏览器Console执行)

```javascript
// 1. 创建测试项目
const project = await window.electronAPI.project.create({
  name: '测试分享项目',
  project_type: 'web',
  user_id: 'test'
});

// 2. 创建公开分享
const share = await window.electronAPI.project.shareProject({
  projectId: project.projectId,
  shareMode: 'public'
});

console.log('🔗 分享链接:', share.shareLink);
// 复制这个链接
```

### ✅ 测试2: 访问分享页面

1. 在新标签页打开分享链接
2. 应该看到项目信息页面
3. 检查项目名称、类型等信息是否正确

### ✅ 测试3: 添加文件并预览

```javascript
// 添加测试文件
await window.electronAPI.project.saveFiles(project.projectId, [{
  file_name: 'README.md',
  file_type: 'md',
  content: '# 测试\n这是测试内容'
}]);
```

刷新分享页面，点击文件预览。

### ✅ 测试4: 测试错误场景

```javascript
// 改为私密
await window.electronAPI.project.shareProject({
  projectId: project.projectId,
  shareMode: 'private'
});
// 刷新页面应显示403错误
```

---

## 预期结果

✅ 分享链接可访问  
✅ 项目信息正确显示  
✅ 文件列表可查看  
✅ 文件可预览  
✅ 错误提示友好  

**测试通过** → 功能正常！🎉
