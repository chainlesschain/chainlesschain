# 快速开始指南

## 第一步：安装依赖

```bash
cd desktop-app-vue
npm install
```

安装时间可能较长，请耐心等待。如果遇到网络问题，可以配置淘宝镜像：

```bash
npm config set registry https://registry.npmmirror.com
```

## 第二步：启动开发服务器

```bash
npm run dev
```

这个命令会：
1. 启动 Vite 开发服务器（监听 5173 端口）
2. 等待服务器就绪
3. 启动 Electron 应用

**首次启动可能需要等待 10-30 秒**

## 第三步：使用应用

### 登录

1. 应用启动后会显示登录页面
2. U盾状态会显示为"已连接"（MVP模式）
3. 输入PIN码：`123456`
4. 点击"登录"按钮

### 主界面

登录成功后，你会看到：

- **左侧侧边栏**：知识库列表和搜索框
- **顶部导航栏**：操作按钮和用户菜单
- **主内容区**：显示首页或详情页

### 创建笔记

1. 点击左侧边栏的"新建笔记"按钮
2. 输入标题、选择类型、填写内容
3. 点击"确定"创建

### 查看和编辑笔记

1. 在左侧边栏点击任意笔记
2. 在详情页点击"编辑"按钮
3. 修改内容后点击"保存"

### 搜索笔记

在左侧边栏的搜索框输入关键词，列表会实时过滤。

### 删除笔记

1. 打开笔记详情页
2. 点击"删除"按钮
3. 确认删除

## 常用命令

```bash
# 开发模式
npm run dev

# 只启动渲染进程（调试UI）
npm run dev:renderer

# 构建生产版本
npm run build

# 打包成可执行文件
npm run package

# 制作安装包
npm run make
```

## 开发技巧

### 1. 修改代码后如何看到效果？

- **渲染进程代码**（src/renderer目录）：保存后会自动热重载
- **主进程代码**（src/main目录）：需要关闭应用重新运行 `npm run dev`

### 2. 如何查看控制台输出？

- **渲染进程**：应用会自动打开 DevTools
- **主进程**：查看运行 `npm run dev` 的终端输出

### 3. 如何修改UI样式？

- 全局样式：编辑 `src/renderer/style.css`
- 组件样式：在 `.vue` 文件的 `<style scoped>` 标签中编辑

### 4. 如何添加新页面？

1. 在 `src/renderer/pages/` 创建新的 `.vue` 文件
2. 在 `src/renderer/router/index.js` 添加路由配置
3. 在需要的地方使用 `<router-link>` 或 `router.push()` 跳转

### 5. 如何添加新的API？

1. 在 `src/main/index.js` 添加 `ipcMain.handle()`
2. 在 `src/preload/index.js` 暴露 API
3. 在 `src/renderer/utils/ipc.js` 封装调用
4. 在组件中导入并使用

## 常见问题

### 问题1: npm install 失败

**解决方案**：
```bash
# 清理缓存
npm cache clean --force
rm -rf node_modules package-lock.json

# 使用淘宝镜像
npm config set registry https://registry.npmmirror.com

# 重新安装
npm install
```

### 问题2: 应用启动白屏

**可能原因**：
- Vite 开发服务器还没启动完成
- 端口被占用

**解决方案**：
- 等待更长时间（首次启动需要更久）
- 检查 5173 端口是否被占用
- 查看终端是否有错误信息

### 问题3: 热重载不工作

**解决方案**：
- 确保文件保存成功
- 尝试刷新页面（Ctrl+R 或 Cmd+R）
- 如果还不行，重启开发服务器

### 问题4: 构建失败

**解决方案**：
```bash
# 确保主进程代码已构建
npm run build:main

# 然后构建渲染进程
npm run build:renderer
```

## 项目结构说明

```
desktop-app-vue/
├── src/
│   ├── main/               # Electron主进程
│   │   └── index.js        # 主进程入口
│   │
│   ├── preload/            # 预加载脚本
│   │   └── index.js        # 暴露安全API
│   │
│   ├── renderer/           # Vue应用（渲染进程）
│   │   ├── components/     # 可复用组件
│   │   │   └── MainLayout.vue
│   │   │
│   │   ├── pages/          # 页面组件
│   │   │   ├── LoginPage.vue
│   │   │   ├── HomePage.vue
│   │   │   └── KnowledgeDetailPage.vue
│   │   │
│   │   ├── stores/         # Pinia状态管理
│   │   │   └── app.js
│   │   │
│   │   ├── router/         # 路由配置
│   │   │   └── index.js
│   │   │
│   │   ├── utils/          # 工具函数
│   │   │   └── ipc.js      # IPC通信封装
│   │   │
│   │   ├── App.vue         # 根组件
│   │   ├── main.js         # Vue应用入口
│   │   ├── style.css       # 全局样式
│   │   └── index.html      # HTML模板
│   │
│   └── shared/             # 主进程和渲染进程共享
│       └── types.js        # 类型定义
│
├── scripts/                # 构建脚本
│   └── build-main.js
│
├── package.json            # 项目配置
├── vite.config.js          # Vite配置
└── README.md               # 详细文档
```

## 下一步学习

1. **Vue 3 文档**: https://cn.vuejs.org/
2. **Ant Design Vue**: https://www.antdv.com/
3. **Pinia 文档**: https://pinia.vuejs.org/zh/
4. **Electron 文档**: https://www.electronjs.org/zh/docs/latest/

## 获取帮助

如果遇到问题：
1. 查看 README.md 的详细文档
2. 检查控制台错误信息
3. 搜索相关框架的官方文档
4. 在项目仓库提交 Issue

祝你开发愉快！ 🎉
