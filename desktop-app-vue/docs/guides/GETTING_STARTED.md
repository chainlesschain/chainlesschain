# ChainlessChain Desktop Vue 版本 - 入门指南

欢迎使用 ChainlessChain Desktop Vue 版本！这是一个基于 Electron + Vue 3 的个人 AI 知识库桌面应用。

## 🚀 快速开始（5分钟）

### 步骤 1: 安装依赖

```bash
cd desktop-app-vue
npm install
```

⏱️ 预计耗时：2-5 分钟（取决于网络速度）

### 步骤 2: 启动应用

```bash
npm run dev
```

⏱️ 首次启动需要 15-30 秒

### 步骤 3: 登录测试

1. 应用启动后会自动打开登录页面
2. 输入 PIN 码：`123456`
3. 点击"登录"按钮

🎉 恭喜！你已经成功启动应用了！

## 📖 完整教程

### 一、环境准备

#### 系统要求

- **操作系统**：Windows 10+, macOS 10.13+, Ubuntu 18.04+
- **Node.js**：18.0.0 或更高版本
- **npm**：9.0.0 或更高版本

#### 检查环境

```bash
node --version   # 应该显示 v18.x.x 或更高
npm --version    # 应该显示 9.x.x 或更高
```

如果版本不符，请访问 [Node.js 官网](https://nodejs.org/) 下载安装。

### 二、安装项目

#### 1. 克隆或下载项目

如果你还没有项目代码：

```bash
git clone <项目地址>
cd chainlesschain/desktop-app-vue
```

#### 2. 安装依赖

```bash
npm install
```

**可能遇到的问题**：

**问题 1：网络慢或超时**
```bash
# 使用淘宝镜像
npm config set registry https://registry.npmmirror.com
npm install
```

**问题 2：权限错误（Linux/macOS）**
```bash
sudo npm install
# 或者使用 nvm 管理 Node.js 版本
```

**问题 3：依赖冲突**
```bash
# 清理并重新安装
rm -rf node_modules package-lock.json
npm install
```

### 三、启动开发环境

#### 方法 1：使用 npm 脚本（推荐）

```bash
npm run dev
```

这个命令会：
1. 启动 Vite 开发服务器（端口 5173）
2. 等待服务器就绪
3. 启动 Electron 应用
4. 自动打开 DevTools

#### 方法 2：分步启动

**终端 1**（启动渲染进程）：
```bash
npm run dev:renderer
```

**终端 2**（启动主进程）：
```bash
npm run dev:electron
```

#### 启动成功的标志

你应该看到：
- ✅ Vite 输出："Local: http://localhost:5173/"
- ✅ Electron 窗口自动打开
- ✅ 显示登录页面
- ✅ DevTools 自动打开

### 四、使用应用

#### 1. 登录

![登录页面示意]

- **U盾状态**：显示"U盾已连接"（绿色）
- **PIN 码**：输入 `123456`
- **登录按钮**：点击"登录"

> **MVP 说明**：当前版本使用模拟验证，实际生产环境会连接真实 U 盾硬件。

#### 2. 首页

登录成功后，你会看到首页：

- **统计卡片**：总笔记数、今日新增、同步状态
- **最近更新**：显示最近 10 条更新的笔记
- **侧边栏**：知识库列表和搜索框

#### 3. 创建笔记

**方法 1：使用侧边栏按钮**
1. 点击左侧边栏的"新建笔记"按钮
2. 填写表单：
   - 标题（必填）
   - 类型（笔记、文档、对话、网页剪藏）
   - 内容（可选）
3. 点击"确定"

**方法 2：使用快捷键**（待实现）
- `Ctrl/Cmd + N`：新建笔记

#### 4. 查看笔记

1. 在左侧边栏点击任意笔记
2. 或在首页"最近更新"列表点击"查看"
3. 进入详情页，显示完整内容

#### 5. 编辑笔记

1. 在详情页点击"编辑"按钮
2. 修改标题、类型或内容
3. 点击"保存"按钮

> 💡 **提示**：编辑过程中可以点击"取消"放弃修改。

#### 6. 搜索笔记

1. 在左侧边栏顶部的搜索框输入关键词
2. 列表会实时过滤匹配的笔记
3. 支持标题和内容搜索

#### 7. 删除笔记

1. 打开要删除的笔记
2. 点击"删除"按钮
3. 确认删除操作

> ⚠️ **警告**：MVP 版本删除后无法恢复（数据存在内存中）。

### 五、开发调试

#### 1. 查看日志

**渲染进程日志**（前端）：
- 打开 DevTools（自动打开）
- 切换到 Console 标签

**主进程日志**（后端）：
- 查看运行 `npm run dev` 的终端
- 所有 `console.log` 会在这里显示

#### 2. 修改代码

**修改页面（热重载）**：
1. 编辑 `src/renderer/` 下的任何文件
2. 保存后自动刷新
3. 无需重启应用

**修改主进程（需重启）**：
1. 编辑 `src/main/` 或 `src/preload/` 文件
2. 关闭应用
3. 重新运行 `npm run dev`

#### 3. 调试技巧

**Vue DevTools**：
1. 安装 Vue DevTools 浏览器扩展
2. 在 DevTools 切换到 Vue 标签
3. 查看组件树和状态

**断点调试**：
1. 在 DevTools Sources 标签
2. 找到相应的 `.vue` 或 `.js` 文件
3. 设置断点

**网络请求**：
1. 切换到 Network 标签
2. 查看 IPC 通信（虽然不会显示在这里）
3. 查看外部 HTTP 请求

### 六、构建和打包

#### 1. 开发构建

```bash
npm run build
```

输出目录：
- `dist/main/` - 主进程代码
- `dist/renderer/` - 渲染进程代码

#### 2. 打包应用

```bash
npm run package
```

生成可执行文件：
- Windows: `out/ChainlessChain-win32-x64/`
- macOS: `out/ChainlessChain-darwin-x64/`
- Linux: `out/ChainlessChain-linux-x64/`

#### 3. 制作安装包

```bash
npm run make
```

生成安装包：
- Windows: `.exe` 安装程序
- macOS: `.zip` 压缩包
- Linux: `.deb` 和 `.rpm` 包

### 七、常见问题

#### Q1: 端口被占用

**现象**：`Error: Port 5173 is already in use`

**解决**：
```bash
# 查找占用进程
netstat -ano | findstr 5173   # Windows
lsof -i :5173                 # macOS/Linux

# 杀死进程或修改端口
# 编辑 vite.config.js，修改 port 值
```

#### Q2: 应用启动白屏

**原因**：
- Vite 服务器未就绪
- 代码错误

**解决**：
1. 等待更长时间（30-60秒）
2. 检查终端错误信息
3. 打开 DevTools 查看 Console

#### Q3: 热重载不工作

**解决**：
1. 确认文件已保存
2. 手动刷新：`Ctrl/Cmd + R`
3. 重启开发服务器

#### Q4: npm install 失败

**解决**：
```bash
# 1. 清理缓存
npm cache clean --force

# 2. 删除 node_modules
rm -rf node_modules package-lock.json

# 3. 使用淘宝镜像
npm config set registry https://registry.npmmirror.com

# 4. 重新安装
npm install
```

#### Q5: Electron 无法启动

**检查**：
1. 主进程代码是否有错误
2. 是否正确构建了主进程
3. 端口是否被占用

**解决**：
```bash
# 重新构建主进程
npm run build:main

# 检查主进程日志
node dist/main/index.js
```

### 八、学习资源

#### 官方文档

- [Vue 3 文档](https://cn.vuejs.org/) - Vue 框架
- [Ant Design Vue](https://www.antdv.com/) - UI 组件库
- [Pinia](https://pinia.vuejs.org/zh/) - 状态管理
- [Vue Router](https://router.vuejs.org/zh/) - 路由
- [Electron](https://www.electronjs.org/zh/docs/latest/) - 桌面应用框架
- [Vite](https://cn.vitejs.dev/) - 构建工具

#### 推荐教程

- [Vue 3 入门教程](https://cn.vuejs.org/tutorial/)
- [Electron 快速入门](https://www.electronjs.org/zh/docs/latest/tutorial/quick-start)
- [Ant Design Vue 实战](https://www.antdv.com/docs/vue/introduce-cn)

#### 社区资源

- [Vue 论坛](https://forum.vuejs.org/)
- [Electron Discord](https://discord.com/invite/electron)
- [Stack Overflow](https://stackoverflow.com/questions/tagged/vue.js)

### 九、下一步

#### 完成入门后，你可以：

1. **自定义 UI**
   - 修改 `src/renderer/style.css` 全局样式
   - 调整组件样式

2. **添加新功能**
   - 创建新页面
   - 添加新的 API
   - 扩展数据模型

3. **优化性能**
   - 实现虚拟滚动
   - 添加缓存策略
   - 优化大文件处理

4. **集成真实服务**
   - 连接 SQLite 数据库
   - 集成 Ollama LLM
   - 实现 Git 同步

### 十、获取帮助

#### 遇到问题？

1. **查看文档**
   - 阅读 README.md
   - 查看 PROJECT_SUMMARY.md

2. **搜索问题**
   - 在项目 Issues 中搜索
   - Google / Stack Overflow

3. **提问**
   - 在 GitHub 提交 Issue
   - 在社区论坛发帖

4. **联系我们**
   - Email: support@chainlesschain.org
   - GitHub: @chainlesschain

## 🎓 总结

恭喜你完成了入门教程！现在你应该能够：

- ✅ 启动开发环境
- ✅ 使用基本功能
- ✅ 理解项目结构
- ✅ 进行代码调试
- ✅ 构建和打包应用

继续探索，祝你开发愉快！ 🚀

---

**最后更新**：2024-01-XX
**文档版本**：1.0.0
