/**
 * Session module — Desktop shim
 *
 * 实现已迁移至 @chainlesschain/session-core,本文件仅为保留原导入路径兼容。
 * 新代码请直接 `require("@chainlesschain/session-core")`。
 */
module.exports = require("@chainlesschain/session-core");
