{
  "id": "tpl_data_visualization_003",
  "name": "data_visualization_dashboard_template",
  "display_name": "数据可视化看板",
  "description": "创建交互式数据可视化看板，包含图表设计、仪表盘布局和数据更新",
  "icon": "📈",
  "category": "data-science",
  "subcategory": "visualization",
  "tags": ["可视化", "仪表盘", "图表", "BI"],
  "project_type": "app",
  "prompt_template": "请帮我生成数据可视化看板项目，要求:\n\n**看板信息：**\n- 看板名称：{{dashboardName}}\n- 可视化工具：{{vizTool}}\n- 数据源：{{dataSource}}\n- 更新频率：{{updateFrequency}}\n- 目标用户：{{targetAudience}}\n\n**项目结构：**\n\n```\n{{dashboardName}}/\n├── data/\n│   ├── raw/              # 原始数据\n│   └── processed/        # 处理后的数据\n│\n├── src/\n│   ├── data_loader.py    # 数据加载\n│   ├── data_processor.py # 数据处理\n│   └── visualizations.py # 图表生成\n│\n{{#eq vizTool \"Plotly Dash\"}}\n├── app/\n│   ├── app.py           # Dash应用主文件\n│   ├── layouts.py       # 布局定义\n│   ├── callbacks.py     # 交互回调\n│   └── assets/\n│       ├── styles.css\n│       └── logo.png\n{{/eq}}\n{{#eq vizTool \"Streamlit\"}}\n├── streamlit_app.py     # Streamlit主文件\n├── pages/               # 多页面\n│   ├── 01_Overview.py\n│   ├── 02_Details.py\n│   └── 03_Settings.py\n{{/eq}}\n{{#eq vizTool \"Tableau\"}}\n├── workbooks/           # Tableau工作簿\n│   └── dashboard.twb\n├── data_sources/        # 数据源连接\n{{/eq}}\n│\n├── config/\n│   └── config.yaml      # 配置文件\n│\n├── requirements.txt\n└── README.md\n```\n\n**requirements.txt：**\n\n```\n# 数据处理\npandas==2.1.3\nnumpy==1.26.2\n\n{{#eq vizTool \"Plotly Dash\"}}\n# Dash框架\ndash==2.14.0\ndash-bootstrap-components==1.5.0\nplotly==5.18.0\n{{/eq}}\n\n{{#eq vizTool \"Streamlit\"}}\n# Streamlit框架\nstreamlit==1.29.0\nplotly==5.18.0\naltair==5.2.0\n{{/eq}}\n\n{{#eq vizTool \"Python可视化库\"}}\n# 可视化库\nmatplotlib==3.8.2\nseaborn==0.13.0\nplotly==5.18.0\nbokeh==3.3.0\n{{/eq}}\n\n# 数据库连接\nsqlalchemy==2.0.23\npsycopg2-binary==2.9.9\n\n# 工具\npyyaml==6.0.1\npython-dotenv==1.0.0\n```\n\n{{#eq vizTool \"Plotly Dash\"}}\n**app/app.py（Dash应用示例）：**\n\n```python\nimport dash\nfrom dash import dcc, html, Input, Output\nimport plotly.express as px\nimport plotly.graph_objects as go\nimport pandas as pd\nimport dash_bootstrap_components as dbc\n\n# 初始化应用\napp = dash.Dash(__name__, external_stylesheets=[dbc.themes.BOOTSTRAP])\n\n# 加载数据\ndef load_data():\n    df = pd.read_csv('data/processed/{{dataFile}}')\n    return df\n\ndf = load_data()\n\n# 布局\napp.layout = dbc.Container([\n    dbc.Row([\n        dbc.Col(html.H1('{{dashboardName}}', className='text-center mb-4'), width=12)\n    ]),\n    \n    # KPI卡片\n    dbc.Row([\n        dbc.Col([\n            dbc.Card([\n                dbc.CardBody([\n                    html.H4('{{kpi1Name}}', className='card-title'),\n                    html.H2(id='kpi-1', className='text-primary')\n                ])\n            ])\n        ], width=3),\n        dbc.Col([\n            dbc.Card([\n                dbc.CardBody([\n                    html.H4('{{kpi2Name}}', className='card-title'),\n                    html.H2(id='kpi-2', className='text-success')\n                ])\n            ])\n        ], width=3),\n        dbc.Col([\n            dbc.Card([\n                dbc.CardBody([\n                    html.H4('{{kpi3Name}}', className='card-title'),\n                    html.H2(id='kpi-3', className='text-warning')\n                ])\n            ])\n        ], width=3),\n        dbc.Col([\n            dbc.Card([\n                dbc.CardBody([\n                    html.H4('{{kpi4Name}}', className='card-title'),\n                    html.H2(id='kpi-4', className='text-info')\n                ])\n            ])\n        ], width=3),\n    ], className='mb-4'),\n    \n    # 筛选器\n    dbc.Row([\n        dbc.Col([\n            html.Label('日期范围：'),\n            dcc.DatePickerRange(\n                id='date-range',\n                start_date=df['date'].min(),\n                end_date=df['date'].max(),\n                display_format='YYYY-MM-DD'\n            )\n        ], width=4),\n        dbc.Col([\n            html.Label('类别筛选：'),\n            dcc.Dropdown(\n                id='category-filter',\n                options=[{'label': cat, 'value': cat} for cat in df['{{categoryColumn}}'].unique()],\n                multi=True,\n                placeholder='选择类别'\n            )\n        ], width=4),\n    ], className='mb-4'),\n    \n    # 图表区域\n    dbc.Row([\n        dbc.Col([\n            dcc.Graph(id='time-series-chart')\n        ], width=8),\n        dbc.Col([\n            dcc.Graph(id='pie-chart')\n        ], width=4),\n    ], className='mb-4'),\n    \n    dbc.Row([\n        dbc.Col([\n            dcc.Graph(id='bar-chart')\n        ], width=6),\n        dbc.Col([\n            dcc.Graph(id='scatter-chart')\n        ], width=6),\n    ], className='mb-4'),\n    \n    # 数据表\n    dbc.Row([\n        dbc.Col([\n            html.H4('详细数据', className='mb-3'),\n            dcc.Graph(id='data-table')\n        ], width=12)\n    ]),\n    \n    # 自动刷新\n    dcc.Interval(\n        id='interval-component',\n        interval={{refreshInterval}}*1000,  # 毫秒\n        n_intervals=0\n    )\n], fluid=True)\n\n# 回调函数：更新KPI\n@app.callback(\n    [Output('kpi-1', 'children'),\n     Output('kpi-2', 'children'),\n     Output('kpi-3', 'children'),\n     Output('kpi-4', 'children')],\n    [Input('date-range', 'start_date'),\n     Input('date-range', 'end_date'),\n     Input('category-filter', 'value')]\n)\ndef update_kpis(start_date, end_date, categories):\n    # 筛选数据\n    filtered_df = df[\n        (df['date'] >= start_date) & \n        (df['date'] <= end_date)\n    ]\n    \n    if categories:\n        filtered_df = filtered_df[filtered_df['{{categoryColumn}}'].isin(categories)]\n    \n    # 计算KPI\n    kpi1 = f\"{filtered_df['{{metric1}}'].sum():,.0f}\"\n    kpi2 = f\"{filtered_df['{{metric2}}'].mean():.2f}\"\n    kpi3 = f\"{len(filtered_df):,}\"\n    kpi4 = f\"{filtered_df['{{metric3}}'].max():.1f}\"\n    \n    return kpi1, kpi2, kpi3, kpi4\n\n# 回调函数：时间序列图\n@app.callback(\n    Output('time-series-chart', 'figure'),\n    [Input('date-range', 'start_date'),\n     Input('date-range', 'end_date'),\n     Input('category-filter', 'value')]\n)\ndef update_time_series(start_date, end_date, categories):\n    filtered_df = df[\n        (df['date'] >= start_date) & \n        (df['date'] <= end_date)\n    ]\n    \n    if categories:\n        filtered_df = filtered_df[filtered_df['{{categoryColumn}}'].isin(categories)]\n    \n    fig = px.line(\n        filtered_df.groupby('date')['{{metric1}}'].sum().reset_index(),\n        x='date',\n        y='{{metric1}}',\n        title='{{metric1}}趋势'\n    )\n    \n    fig.update_layout(\n        hovermode='x unified',\n        template='plotly_white'\n    )\n    \n    return fig\n\n# 回调函数：饼图\n@app.callback(\n    Output('pie-chart', 'figure'),\n    [Input('date-range', 'start_date'),\n     Input('date-range', 'end_date')]\n)\ndef update_pie_chart(start_date, end_date):\n    filtered_df = df[\n        (df['date'] >= start_date) & \n        (df['date'] <= end_date)\n    ]\n    \n    category_sum = filtered_df.groupby('{{categoryColumn}}')['{{metric1}}'].sum().reset_index()\n    \n    fig = px.pie(\n        category_sum,\n        values='{{metric1}}',\n        names='{{categoryColumn}}',\n        title='类别分布'\n    )\n    \n    return fig\n\n# 回调函数：柱状图\n@app.callback(\n    Output('bar-chart', 'figure'),\n    [Input('category-filter', 'value')]\n)\ndef update_bar_chart(categories):\n    filtered_df = df.copy()\n    \n    if categories:\n        filtered_df = filtered_df[filtered_df['{{categoryColumn}}'].isin(categories)]\n    \n    top_10 = filtered_df.nlargest(10, '{{metric1}}')\n    \n    fig = px.bar(\n        top_10,\n        x='{{nameColumn}}',\n        y='{{metric1}}',\n        title='Top 10 {{metric1}}',\n        color='{{metric1}}',\n        color_continuous_scale='Blues'\n    )\n    \n    return fig\n\n# 回调函数：散点图\n@app.callback(\n    Output('scatter-chart', 'figure'),\n    [Input('category-filter', 'value')]\n)\ndef update_scatter(categories):\n    filtered_df = df.copy()\n    \n    if categories:\n        filtered_df = filtered_df[filtered_df['{{categoryColumn}}'].isin(categories)]\n    \n    fig = px.scatter(\n        filtered_df,\n        x='{{metric2}}',\n        y='{{metric3}}',\n        color='{{categoryColumn}}',\n        size='{{metric1}}',\n        title='{{metric2}} vs {{metric3}}',\n        hover_data=['{{nameColumn}}']\n    )\n    \n    return fig\n\nif __name__ == '__main__':\n    app.run_server(debug=True, port=8050)\n```\n{{/eq}}\n\n{{#eq vizTool \"Streamlit\"}}\n**streamlit_app.py（Streamlit应用示例）：**\n\n```python\nimport streamlit as st\nimport pandas as pd\nimport plotly.express as px\nimport plotly.graph_objects as go\nfrom datetime import datetime, timedelta\n\n# 页面配置\nst.set_page_config(\n    page_title='{{dashboardName}}',\n    page_icon='📊',\n    layout='wide'\n)\n\n# 标题\nst.title('{{dashboardName}}')\n\n# 加载数据\n@st.cache_data\ndef load_data():\n    df = pd.read_csv('data/processed/{{dataFile}}')\n    df['date'] = pd.to_datetime(df['date'])\n    return df\n\ndf = load_data()\n\n# 侧边栏筛选\nst.sidebar.header('筛选条件')\n\ndate_range = st.sidebar.date_input(\n    '日期范围',\n    value=(df['date'].min(), df['date'].max()),\n    min_value=df['date'].min(),\n    max_value=df['date'].max()\n)\n\ncategories = st.sidebar.multiselect(\n    '类别',\n    options=df['{{categoryColumn}}'].unique(),\n    default=df['{{categoryColumn}}'].unique()\n)\n\n# 筛选数据\nfiltered_df = df[\n    (df['date'] >= pd.Timestamp(date_range[0])) &\n    (df['date'] <= pd.Timestamp(date_range[1])) &\n    (df['{{categoryColumn}}'].isin(categories))\n]\n\n# KPI指标\ncol1, col2, col3, col4 = st.columns(4)\n\nwith col1:\n    st.metric(\n        label='{{kpi1Name}}',\n        value=f\"{filtered_df['{{metric1}}'].sum():,.0f}\",\n        delta=f\"{((filtered_df['{{metric1}}'].sum() / df['{{metric1}}'].sum() - 1) * 100):.1f}%\"\n    )\n\nwith col2:\n    st.metric(\n        label='{{kpi2Name}}',\n        value=f\"{filtered_df['{{metric2}}'].mean():.2f}\"\n    )\n\nwith col3:\n    st.metric(\n        label='{{kpi3Name}}',\n        value=f\"{len(filtered_df):,}\"\n    )\n\nwith col4:\n    st.metric(\n        label='{{kpi4Name}}',\n        value=f\"{filtered_df['{{metric3}}'].max():.1f}\"\n    )\n\n# 图表区域\nst.markdown('---')\n\n# 时间序列图\nst.subheader('趋势分析')\n\ntime_series_df = filtered_df.groupby('date')['{{metric1}}'].sum().reset_index()\n\nfig_time = px.line(\n    time_series_df,\n    x='date',\n    y='{{metric1}}',\n    title='{{metric1}}时间趋势'\n)\n\nst.plotly_chart(fig_time, use_container_width=True)\n\n# 两列布局\ncol1, col2 = st.columns(2)\n\nwith col1:\n    st.subheader('类别分布')\n    \n    category_df = filtered_df.groupby('{{categoryColumn}}')['{{metric1}}'].sum().reset_index()\n    \n    fig_pie = px.pie(\n        category_df,\n        values='{{metric1}}',\n        names='{{categoryColumn}}'\n    )\n    \n    st.plotly_chart(fig_pie, use_container_width=True)\n\nwith col2:\n    st.subheader('Top 10 排名')\n    \n    top_10 = filtered_df.nlargest(10, '{{metric1}}')\n    \n    fig_bar = px.bar(\n        top_10,\n        x='{{nameColumn}}',\n        y='{{metric1}}',\n        color='{{metric1}}',\n        color_continuous_scale='Blues'\n    )\n    \n    st.plotly_chart(fig_bar, use_container_width=True)\n\n# 散点图\nst.subheader('相关性分析')\n\nfig_scatter = px.scatter(\n    filtered_df,\n    x='{{metric2}}',\n    y='{{metric3}}',\n    color='{{categoryColumn}}',\n    size='{{metric1}}',\n    hover_data=['{{nameColumn}}']\n)\n\nst.plotly_chart(fig_scatter, use_container_width=True)\n\n# 数据表\nst.subheader('详细数据')\n\nst.dataframe(\n    filtered_df.sort_values('{{metric1}}', ascending=False).head(20),\n    use_container_width=True\n)\n\n# 下载按钮\ncsv = filtered_df.to_csv(index=False).encode('utf-8')\n\nst.download_button(\n    label='下载数据 (CSV)',\n    data=csv,\n    file_name='filtered_data.csv',\n    mime='text/csv'\n)\n```\n{{/eq}}\n\n**可视化最佳实践：**\n\n### 1. 图表选择指南\n\n**时间序列数据**：\n- 折线图：展示趋势\n- 面积图：强调数量变化\n- 柱状图：对比不同时期\n\n**类别对比**：\n- 柱状图：少量类别（<10个）\n- 条形图：多类别或长标签\n- 饼图/环形图：占比展示\n\n**分布分析**：\n- 直方图：数值分布\n- 箱线图：识别异常值\n- 小提琴图：分布形态\n\n**关系分析**：\n- 散点图：两变量关系\n- 气泡图：三变量关系\n- 热力图：相关性矩阵\n\n**地理数据**：\n- 地图：区域分布\n- 热力地图：密度展示\n\n### 2. 仪表盘设计原则\n\n**布局**：\n- F型布局：重要信息左上角\n- 黄金分割：主次分明\n- 留白：避免拥挤\n\n**配色**：\n- 主色调：2-3种\n- 强调色：突出关键数据\n- 无障碍：考虑色盲用户\n\n**交互**：\n- 筛选器：日期、类别、指标\n- 下钻：点击查看详情\n- 提示：Hover显示详细信息\n\n### 3. 性能优化\n\n**数据优化**：\n```python\n# 使用缓存\n@st.cache_data\ndef load_data():\n    return pd.read_csv('data.csv')\n\n# 聚合后再可视化\ndf_agg = df.groupby('date').agg({\n    'metric1': 'sum',\n    'metric2': 'mean'\n}).reset_index()\n```\n\n**采样**：\n```python\n# 数据量过大时采样\nif len(df) > 10000:\n    df_sample = df.sample(10000)\nelse:\n    df_sample = df\n```\n\n### 4. 响应式设计\n\n```python\n# Dash响应式布局\nimport dash_bootstrap_components as dbc\n\nlayout = dbc.Container([\n    dbc.Row([\n        dbc.Col(component1, xs=12, sm=12, md=6, lg=3),\n        dbc.Col(component2, xs=12, sm=12, md=6, lg=3),\n    ])\n], fluid=True)\n```\n\n**部署方式：**\n\n{{#eq vizTool \"Plotly Dash\"}}\n### Dash部署\n\n**本地运行**：\n```bash\npython app/app.py\n```\n\n**生产部署**：\n```bash\n# 使用Gunicorn\ngunicorn app.app:server -b 0.0.0.0:8050\n\n# 或使用Docker\ndocker build -t {{dashboardName}} .\ndocker run -p 8050:8050 {{dashboardName}}\n```\n{{/eq}}\n\n{{#eq vizTool \"Streamlit\"}}\n### Streamlit部署\n\n**本地运行**：\n```bash\nstreamlit run streamlit_app.py\n```\n\n**Streamlit Cloud部署**：\n1. 推送代码到GitHub\n2. 访问 share.streamlit.io\n3. 连接仓库并部署\n{{/eq}}\n\n**监控与维护：**\n\n1. **性能监控**\n   - 页面加载时间\n   - 查询响应时间\n   - 用户访问量\n\n2. **数据质量**\n   - 数据更新检查\n   - 异常值监测\n   - 数据完整性验证\n\n3. **用户反馈**\n   - 收集使用反馈\n   - A/B测试新功能\n   - 持续优化",
  "variables_schema": [
    {"name": "dashboardName", "label": "看板名称", "type": "string", "required": true, "placeholder": "例如：销售数据分析看板"},
    {"name": "vizTool", "label": "可视化工具", "type": "select", "required": true, "options": [
      {"value": "Plotly Dash", "label": "Plotly Dash"},
      {"value": "Streamlit", "label": "Streamlit"},
      {"value": "Tableau", "label": "Tableau"},
      {"value": "Python可视化库", "label": "Python可视化库"}
    ]},
    {"name": "dataSource", "label": "数据来源", "type": "string", "required": false, "default": "CSV文件", "placeholder": "例如：数据库、API、CSV"},
    {"name": "updateFrequency", "label": "更新频率", "type": "select", "required": false, "default": "每日", "options": [
      {"value": "实时", "label": "实时"},
      {"value": "每小时", "label": "每小时"},
      {"value": "每日", "label": "每日"},
      {"value": "每周", "label": "每周"}
    ]},
    {"name": "targetAudience", "label": "目标用户", "type": "select", "required": false, "default": "业务团队", "options": [
      {"value": "高管", "label": "高管/决策层"},
      {"value": "业务团队", "label": "业务团队"},
      {"value": "数据分析师", "label": "数据分析师"},
      {"value": "技术团队", "label": "技术团队"}
    ]},
    {"name": "refreshInterval", "label": "自动刷新间隔(秒)", "type": "number", "required": false, "default": 60, "min": 10, "max": 3600}
  ],
  "file_structure": {
    "root": "{{dashboardName}}",
    "files": [
      {"path": "README.md", "type": "markdown"},
      {"path": "requirements.txt", "type": "document"},
      {"path": "config/config.yaml", "type": "document"},
      {"path": "data/", "type": "directory"}
    ]
  },
  "default_files": [],
  "is_builtin": true,
  "author": "ChainlessChain Team",
  "version": "1.0.0",
  "usage_count": 0,
  "rating": 0,
  "rating_count": 0
}
