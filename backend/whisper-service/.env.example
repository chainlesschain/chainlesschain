# Whisper Service Configuration

# Server settings
WHISPER_HOST=0.0.0.0
WHISPER_PORT=8000

# Model settings
# Available models: tiny, base, small, medium, large
# tiny: ~39M params, fastest, lowest accuracy
# base: ~74M params, fast, good accuracy (recommended for most use cases)
# small: ~244M params, slower, better accuracy
# medium: ~769M params, slow, high accuracy
# large: ~1550M params, very slow, highest accuracy
WHISPER_MODEL=base

# Device settings
# auto: automatically detect CUDA/CPU
# cuda: force GPU usage
# cpu: force CPU usage
WHISPER_DEVICE=auto

# Logging
LOG_LEVEL=INFO
