# ChainlessChain Mobile App - 架构文档

## 📋 项目概述

ChainlessChain移动应用是一个功能完整的知识管理系统，基于UniApp框架开发，支持H5、小程序和App多端运行。

**核心特性：**
- 📝 强大的知识编辑和组织能力
- 🤖 AI智能助手（摘要、标签建议、内容扩展）
- 📊 数据统计和可视化分析
- 🔗 知识关联和图谱
- 📤 多种分享方式
- ☁️ 云端同步和备份
- 🏷️ 灵活的标签系统
- 📁 文件夹层级管理

## 🛠 技术栈

### 前端框架
- **UniApp** - 跨平台应用开发框架
- **Vue 3** - 渐进式JavaScript框架
- **SCSS** - CSS预处理器

### 数据存储
- **SQLite** (App模式) - 移动端结构化数据库
- **localStorage** (H5模式) - 浏览器本地存储
- 双模式自动适配

### 第三方库
- **crypto-js** - 数据加密
- **pinia** - Vue 3状态管理（可选）

## 📁 目录结构

```
mobile-app-uniapp/
├── pages/                      # 页面目录
│   ├── index/                  # 首页
│   ├── login/                  # 登录页
│   ├── knowledge/              # 知识管理模块
│   │   ├── list/              # 知识列表
│   │   ├── detail/            # 知识详情
│   │   ├── edit/              # 知识编辑
│   │   └── statistics/        # 知识统计
│   ├── chat/                   # AI对话
│   ├── social/                 # 社交模块
│   │   ├── friends/           # 好友列表
│   │   ├── posts/             # 动态
│   │   ├── messages/          # 消息
│   │   └── friend-chat/       # 好友聊天
│   ├── trade/                  # 交易市场
│   │   ├── market/            # 市场
│   │   ├── orders/            # 订单
│   │   └── assets/            # 资产
│   ├── backup/                 # 备份管理
│   │   ├── backup.vue         # 数据备份
│   │   └── cloud-sync.vue     # 云同步设置
│   └── settings/               # 设置
│
├── services/                   # 服务层
│   ├── database.js            # 数据库服务（核心）
│   ├── ai.js                  # AI服务
│   ├── cloudSync.js           # 云同步服务
│   ├── backup.js              # 备份服务
│   └── cloud/                 # 云存储适配器
│       └── cloudStorage.js
│
├── utils/                      # 工具函数
│   └── crypto.js              # 加密工具
│
├── static/                     # 静态资源
│   └── images/                # 图片资源
│
├── App.vue                     # 应用主组件
├── main.js                     # 应用入口
├── manifest.json               # 应用配置
├── pages.json                  # 页面配置
├── uni.scss                    # 全局样式变量
└── package.json                # 依赖配置
```

## 🗄 数据库设计

### 核心表结构

#### 1. knowledge_items（知识项表）
```sql
CREATE TABLE knowledge_items (
  id TEXT PRIMARY KEY,
  title TEXT NOT NULL,                    -- 标题
  type TEXT,                               -- 类型：note/document/conversation/web_clip
  content TEXT,                            -- 内容
  encrypted_content TEXT,                  -- 加密内容
  is_favorite INTEGER DEFAULT 0,          -- 是否收藏
  folder_id TEXT,                          -- 所属文件夹
  created_at INTEGER NOT NULL,            -- 创建时间
  updated_at INTEGER NOT NULL,            -- 更新时间
  device_id TEXT,                          -- 设备ID
  sync_status TEXT DEFAULT 'pending',     -- 同步状态
  FOREIGN KEY (folder_id) REFERENCES folders(id)
);
```

#### 2. tags（标签表）
```sql
CREATE TABLE tags (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL UNIQUE,              -- 标签名
  color TEXT NOT NULL,                     -- 颜色
  created_at INTEGER NOT NULL
);
```

#### 3. knowledge_tags（知识-标签关联表）
```sql
CREATE TABLE knowledge_tags (
  knowledge_id TEXT NOT NULL,
  tag_id TEXT NOT NULL,
  PRIMARY KEY (knowledge_id, tag_id),
  FOREIGN KEY (knowledge_id) REFERENCES knowledge_items(id) ON DELETE CASCADE,
  FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE
);
```

#### 4. knowledge_links（知识关联表）
```sql
CREATE TABLE knowledge_links (
  id TEXT PRIMARY KEY,
  source_id TEXT NOT NULL,                -- 源知识ID
  target_id TEXT NOT NULL,                -- 目标知识ID
  relation_type TEXT DEFAULT 'related',  -- 关联类型
  created_at INTEGER NOT NULL,
  FOREIGN KEY (source_id) REFERENCES knowledge_items(id) ON DELETE CASCADE,
  FOREIGN KEY (target_id) REFERENCES knowledge_items(id) ON DELETE CASCADE
);
```

#### 5. folders（文件夹表）
```sql
CREATE TABLE folders (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,                     -- 文件夹名
  parent_id TEXT,                          -- 父文件夹ID
  color TEXT,                              -- 颜色
  icon TEXT,                               -- 图标
  sort_order INTEGER DEFAULT 0,          -- 排序
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL,
  FOREIGN KEY (parent_id) REFERENCES folders(id) ON DELETE CASCADE
);
```

#### 6. conversations（对话表）
```sql
CREATE TABLE conversations (
  id TEXT PRIMARY KEY,
  title TEXT NOT NULL,
  knowledge_id TEXT,                       -- 关联知识
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL,
  FOREIGN KEY (knowledge_id) REFERENCES knowledge_items(id)
);
```

#### 7. messages（消息表）
```sql
CREATE TABLE messages (
  id TEXT PRIMARY KEY,
  conversation_id TEXT NOT NULL,
  role TEXT,                               -- user/assistant/system
  content TEXT NOT NULL,
  timestamp INTEGER NOT NULL,
  tokens INTEGER,
  FOREIGN KEY (conversation_id) REFERENCES conversations(id) ON DELETE CASCADE
);
```

### 其他表
- **friendships** - 好友关系
- **posts** - 动态
- **post_comments** - 评论
- **market_listings** - 市场商品
- **orders** - 订单
- **user_assets** - 用户资产
- **transactions** - 交易记录

## 🏗 核心模块架构

### 1. 数据库服务层 (`services/database.js`)

**设计模式：** 单例模式

**核心职责：**
- 数据库初始化和表创建
- H5/App双模式自动适配
- CRUD操作封装
- 数据加密/解密
- 统计和查询

**主要方法分类：**

```javascript
// 初始化
- init(pin)                      // 初始化数据库
- createTables()                 // 创建表结构

// 知识管理
- addKnowledgeItem(data)         // 添加知识
- getKnowledgeItems(options)     // 查询知识列表
- getKnowledgeItem(id)           // 获取单个知识
- updateKnowledgeItem(id, data)  // 更新知识
- deleteKnowledgeItem(id)        // 删除知识
- toggleKnowledgeFavorite(id)    // 切换收藏

// 标签管理
- getTags()                      // 获取所有标签
- createTag(name, color)         // 创建标签
- deleteTag(id)                  // 删除标签
- getKnowledgeTags(knowledgeId)  // 获取知识的标签
- setKnowledgeTags(knowledgeId, tagIds)  // 设置知识标签

// 知识关联
- createKnowledgeLink(sourceId, targetId, type)  // 创建关联
- getKnowledgeLinks(knowledgeId)                 // 获取关联
- deleteKnowledgeLink(linkId)                    // 删除关联

// 文件夹管理
- createFolder(name, parentId, color, icon)  // 创建文件夹
- getFolders()                               // 获取所有文件夹
- updateFolder(id, updates)                  // 更新文件夹
- deleteFolder(id)                           // 删除文件夹
- moveKnowledgeToFolder(knowledgeId, folderId)  // 移动知识

// 统计分析
- getKnowledgeStatistics()       // 获取统计数据
- getDailyCreationStats()        // 获取每日创建统计

// 对话管理
- createConversation(title)      // 创建对话
- getConversations(limit)        // 获取对话列表
- addMessage(conversationId, role, content)  // 添加消息

// 社交功能
- addFriend(friendDid, nickname) // 添加好友
- getFriends()                   // 获取好友列表
- createPost(authorDid, content) // 创建动态

// 交易市场
- createListing(knowledgeId, sellerDid, price)  // 创建商品
- buyKnowledge(listingId, buyerDid)             // 购买知识
```

**双模式实现原理：**
```javascript
// H5模式：使用localStorage
if (this.isH5) {
  this.h5Data.knowledge_items.push(newItem)
  this.saveH5Data()
}

// App模式：使用SQLite
else {
  await this.executeSql(sql, params)
}
```

### 2. AI服务层 (`services/ai.js`)

**核心功能：**
- 智能摘要生成
- AI标签建议
- 内容扩展生成
- 内容润色改进
- 问答对生成

**方法列表：**
```javascript
- generateSummary(content, maxLength)      // 生成摘要
- suggestTags(title, content, existingTags)  // 标签建议
- expandContent(title, outline, style)     // 内容扩展
- improveContent(content)                  // 内容改进
- generateQA(content)                      // 生成问答
```

**扩展性设计：**
- 支持切换不同AI模型
- 模拟模式用于演示
- 完整的错误处理

### 3. 云同步服务 (`services/cloudSync.js`)

**同步策略：**
- 自动同步（应用启动后3秒）
- 手动同步
- 冲突检测和解决

**核心方法：**
```javascript
- autoSyncOnStartup()       // 启动时自动同步
- syncNow()                 // 立即同步
- detectConflicts()         // 检测冲突
- resolveConflict(strategy) // 解决冲突
```

### 4. 备份服务 (`services/backup.js`)

**备份方式：**
- 本地备份（导出JSON文件）
- 云端备份（WebDAV/云存储）

**方法：**
```javascript
- createLocalBackup()       // 创建本地备份
- createCloudBackup()       // 创建云端备份
- restoreFromBackup(data)   // 恢复备份
- listBackups()             // 列出备份
```

## 🎨 UI/UX设计

### 主题系统

**CSS变量架构：**
```scss
// 浅色主题
page {
  --bg-page: #f8f8f8;
  --bg-card: #ffffff;
  --bg-input: #f5f5f5;
  --text-primary: #333333;
  --text-secondary: #666666;
  --color-primary: #3cc51f;
  // ...
}

// 深色主题
page[data-theme='dark'] {
  --bg-page: #121212;
  --bg-card: #1f1f1f;
  --bg-input: #2a2a2a;
  --text-primary: #e0e0e0;
  // ...
}
```

**主题切换：**
- 支持浅色/深色/自动三种模式
- 系统主题跟随
- 全局统一样式变量

### 组件设计原则

1. **模态框统一设计**
   - 底部滑出动画
   - 遮罩层点击关闭
   - 统一的关闭按钮

2. **列表项统一样式**
   - 卡片式布局
   - 圆角阴影
   - 统一的点击反馈

3. **按钮层级**
   - Primary: 主要操作
   - Secondary: 次要操作
   - Tertiary: 辅助操作

## 📱 功能模块详解

### 1. 知识管理模块

#### 知识列表 (`pages/knowledge/list/list.vue`)
**功能：**
- 搜索（实时防抖）
- 多条件筛选（标签、类型、收藏）
- 标签横向滚动条
- 下拉刷新
- 快速收藏切换

**核心数据流：**
```
用户操作 → loadItems() → database.getKnowledgeItems() → 渲染列表
```

#### 知识详情 (`pages/knowledge/detail/detail.vue`)
**功能：**
- 内容展示
- 关联知识列表
- 分享功能（4种方式）
- 编辑/删除操作

**关联知识流程：**
```
加载详情 → loadRelatedItems() → 渲染关联列表 → 点击跳转
```

#### 知识编辑 (`pages/knowledge/edit/edit.vue`)
**功能：**
- 标题/类型/标签/内容编辑
- AI助手集成（4个核心功能）
- 标签选择弹窗（搜索/创建）
- 自动保存草稿

**AI助手菜单：**
1. 📝 生成摘要
2. 🏷️ 标签建议
3. ✨ 内容扩展
4. 💡 内容润色

### 2. 数据统计模块 (`pages/knowledge/statistics/statistics.vue`)

**统计维度：**
- 总览卡片（4个关键指标）
- 类型分布（横向进度条）
- 热门标签（Top 10）
- 创建趋势（6个月柱状图）
- 最近活动（7天内）

**图表类型：**
- 进度条图表
- 柱状图
- 标签云

### 3. 备份同步模块

#### 数据备份 (`pages/backup/backup.vue`)
- 本地备份列表
- 云端备份列表
- 一键备份/恢复
- 备份历史管理

#### 云同步设置 (`pages/backup/cloud-sync.vue`)
- WebDAV配置
- 同步策略设置
- 冲突解决策略
- 同步状态显示

## 🔐 安全设计

### 数据加密
- PIN码派生密钥
- 内容加密存储（可选）
- 传输加密（HTTPS）

### 隐私保护
- 本地优先存储
- 可选云同步
- 数据所有权归用户

## 🚀 性能优化

### 数据库优化
- 索引优化（主键、外键）
- 分页查询
- 延迟加载

### UI性能
- 虚拟列表（长列表）
- 图片懒加载
- 防抖/节流

### 缓存策略
- 列表缓存
- 统计数据缓存
- 定时刷新

## 📊 已实现功能清单

### ✅ 核心功能（100%）
- [x] 知识CRUD
- [x] 标签系统（12色）
- [x] 搜索和筛选
- [x] 收藏/星标
- [x] 知识关联
- [x] 文件夹管理（数据层）

### ✅ AI增强（100%）
- [x] 智能摘要
- [x] 标签建议
- [x] 内容扩展
- [x] 内容润色

### ✅ 数据分析（100%）
- [x] 统计仪表板
- [x] 类型分布
- [x] 标签分布
- [x] 创建趋势
- [x] 最近活动

### ✅ 分享导出（100%）
- [x] 复制文本
- [x] 复制Markdown
- [x] 导出文件
- [x] 社交分享

### ✅ 云同步备份（100%）
- [x] 本地备份
- [x] 云端备份
- [x] 自动同步
- [x] 冲突处理

### ✅ 用户体验（100%）
- [x] 深色主题
- [x] 下拉刷新
- [x] 空状态提示
- [x] 加载状态
- [x] 错误处理

## 🎯 待开发功能

### 📁 文件夹管理UI（优先级：高）
- [ ] 文件夹列表页面
- [ ] 创建/编辑文件夹
- [ ] 文件夹层级展示
- [ ] 知识移动界面
- [ ] 拖拽排序

### ⚡ 效率工具（优先级：中）
- [ ] 快速笔记浮动按钮
- [ ] 笔记模板系统
- [ ] 批量操作
- [ ] 快捷键支持

### 📖 编辑器增强（优先级：中）
- [ ] Markdown实时预览
- [ ] 富文本编辑器
- [ ] 图片上传
- [ ] 语音转文字

### 🎨 视图优化（优先级：低）
- [ ] 卡片视图
- [ ] 时间轴视图
- [ ] 看板视图
- [ ] 阅读模式

### 📥 导入导出（优先级：低）
- [ ] 批量导入
- [ ] PDF导出
- [ ] Excel导出
- [ ] Notion导入

### 🔗 知识图谱（优先级：低）
- [ ] 关系图可视化
- [ ] 交互式探索
- [ ] 路径查找

## 🔄 数据流架构

```
┌─────────────┐
│  Vue组件层   │ ← 用户交互
└──────┬──────┘
       │
       ↓
┌─────────────┐
│  服务层      │ ← 业务逻辑
│  - database │
│  - ai       │
│  - cloudSync│
│  - backup   │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│  数据存储层  │
│  - SQLite   │ ← App模式
│  - localStorage│ ← H5模式
└─────────────┘
       │
       ↓
┌─────────────┐
│  云存储      │ ← 可选同步
│  - WebDAV   │
│  - 对象存储  │
└─────────────┘
```

## 🧪 测试策略

### 单元测试
- 数据库CRUD操作
- 数据加密/解密
- AI服务mock测试

### 集成测试
- 知识完整流程
- 同步和备份流程
- 关联功能测试

### E2E测试
- 用户关键路径
- 跨页面交互
- 多端兼容性

## 📦 部署架构

### H5部署
```
前端静态资源 → CDN → 用户浏览器
数据存储 → localStorage
```

### App部署
```
App安装包 → 应用商店 → 用户设备
数据存储 → SQLite本地数据库
云同步 → WebDAV/云存储（可选）
```

### 小程序部署
```
小程序包 → 微信/支付宝等平台 → 用户
数据存储 → 小程序storage
```

## 🔧 开发规范

### 代码风格
- ESLint配置
- Vue风格指南
- 命名规范

### Git提交规范
```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式
refactor: 重构
perf: 性能优化
test: 测试
chore: 构建/工具链
```

### 注释规范
- JSDoc函数注释
- 复杂逻辑注释
- TODO标记

## 📈 未来规划

### 短期目标（1-2周）
1. 完成文件夹管理UI
2. 添加快速笔记功能
3. 实现模板系统

### 中期目标（1-2个月）
1. Markdown编辑器
2. 图片管理系统
3. 知识图谱可视化

### 长期目标（3-6个月）
1. 多人协作
2. 权限管理
3. API开放平台

## 🤝 贡献指南

### 开发环境搭建
```bash
# 安装依赖
npm install

# H5开发
npm run dev:h5

# 小程序开发
npm run dev:mp-weixin

# App开发
npm run dev:app
```

### 项目结构说明
详见[目录结构](#-目录结构)章节

## 📄 许可证

本项目采用 MIT 许可证

---

**文档版本：** v1.0
**最后更新：** 2025-12-20
**维护者：** ChainlessChain Team
