# 火山引擎（Doubao）快速测试指南

## 🎯 目标
使用PC端获取的火山引擎API Key，在Android端完成配置和对话测试。

---

## 📋 前提条件

### 已有资源：
- ✅ 火山引擎API Key（从PC端获取）
- ✅ Android设备或模拟器
- ✅ 网络连接

### 需要完成：
- [ ] 编译Android应用
- [ ] 安装到设备
- [ ] 配置导航路由（一次性工作）

---

## 🚀 快速开始（5分钟）

### 步骤1：编译应用 (1分钟)

```bash
cd android-app
./gradlew assembleDebug

# 或使用Android Studio
# File → Build → Build Bundle(s) / APK(s) → Build APK(s)
```

**预期输出**:
```
BUILD SUCCESSFUL in 1m 23s
APK: android-app/app/build/outputs/apk/debug/app-debug.apk
```

---

### 步骤2：安装应用 (30秒)

```bash
# 方式1：ADB安装
adb install app/build/outputs/apk/debug/app-debug.apk

# 方式2：Android Studio安装
# Run → Run 'app'
```

**验证安装**:
```bash
adb shell pm list packages | grep chainlesschain
# 输出: package:com.chainlesschain.android.debug
```

---

### 步骤3：完成导航集成 (2分钟)

**如果尚未集成导航，需要添加以下代码：**

打开主导航文件（通常是 `MainActivity.kt` 或 `MainNavigation.kt`），添加：

```kotlin
// 在NavHost中添加
composable("llm_settings") {
    LLMSettingsScreen(
        onNavigateBack = { navController.popBackStack() }
    )
}

composable("llm_test") {
    LLMTestChatScreen(
        onNavigateBack = { navController.popBackStack() },
        provider = LLMProvider.DOUBAO
    )
}

// 在ProfileScreen调用中添加
ProfileScreen(
    onLogout = { /* ... */ },
    onNavigateToLLMSettings = {
        navController.navigate("llm_settings")
    },
    viewModel = hiltViewModel()
)
```

**重新编译**:
```bash
./gradlew assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
```

---

### 步骤4：配置火山引擎API Key (1分钟)

#### 4.1 打开AI配置
1. 启动App
2. 登录/注册
3. 进入"我的"页面
4. 点击"AI配置"

#### 4.2 选择提供商
1. 在提供商列表中找到"火山引擎Doubao"
2. 点击选择

#### 4.3 输入API Key
1. 在"API Key"输入框粘贴密钥
2. 点击眼睛图标确认密钥正确
3. 点击"测试连接"按钮

#### 4.4 验证连接
**预期结果**:
- 加载动画（2-5秒）
- 显示："✅ 连接成功！可以开始使用"
- 连接状态卡片变为绿色

**如果失败**:
- 检查API Key是否正确
- 确认网络连接
- 查看错误信息

#### 4.5 保存配置
1. 点击"保存"按钮
2. 看到提示："✅ 配置已保存"

---

### 步骤5：测试AI对话 (1分钟)

#### 5.1 进入测试界面
- 方式1：返回主界面 → 进入"LLM测试"（如果已添加）
- 方式2：在AI配置界面点击"测试对话"（如果已添加按钮）

#### 5.2 发送测试消息

**测试1：基础对话**
```
输入: 你好，请介绍一下自己
预期: 流式输出自我介绍，约2-3秒响应
```

**测试2：代码生成**
```
输入: 用Python写一个冒泡排序
预期: 输出完整的Python代码
```

**测试3：中文理解**
```
输入: 用一句话总结火山引擎的优势
预期: 简洁准确的回答
```

#### 5.3 观察性能指标

**性能统计卡片**:
- **响应时间**: 应该 < 3000ms
- **总Token**: 随对话增加
- **消息数**: 正确计数
- **成功率**: 100%

---

## 🔍 详细测试场景

### 场景1：流式响应测试

**目的**: 验证逐字输出效果

**步骤**:
1. 发送："写一篇100字的文章，主题是AI"
2. 观察输出过程：
   - ✅ 文字逐个显示
   - ✅ 有光标动画（▊）
   - ✅ 输出流畅不卡顿
3. 完成后：
   - ✅ 光标消失
   - ✅ 消息添加到历史记录
   - ✅ Token统计更新

---

### 场景2：RAG功能测试

**前提**: 已有知识库数据

**步骤**:
1. 启用"知识库检索 (RAG)"开关
2. 发送："ChainlessChain项目的主要功能？"
3. 观察响应：
   - ✅ 回答包含项目相关内容
   - ✅ 引用知识库信息
   - ✅ 响应时间略长（需要检索）

**对比测试**:
1. 关闭RAG开关
2. 重新发送相同问题
3. 对比两次回答的差异

---

### 场景3：性能压力测试

**步骤**:
1. 连续发送10条消息
2. 观察：
   - ✅ 所有消息都成功
   - ✅ 成功率保持100%
   - ✅ 平均响应时间稳定
   - ✅ 总Token正确累加

---

### 场景4：错误处理测试

**测试网络错误**:
1. 关闭网络
2. 发送消息
3. 预期：显示"❌ 发送失败: xxx"
4. 重新连接网络
5. 再次发送，应该成功

**测试API Key错误**:
1. 修改API Key（随意改几个字符）
2. 点击"测试连接"
3. 预期：显示"❌ 连接失败"
4. 恢复正确的Key
5. 重新测试，应该成功

---

## 📊 验收标准

### 功能验收：

- [ ] ✅ API Key配置成功保存
- [ ] ✅ 连接测试通过
- [ ] ✅ 基础对话正常
- [ ] ✅ 流式响应逐字显示
- [ ] ✅ 性能统计准确
- [ ] ✅ 清空对话功能正常
- [ ] ✅ RAG开关有效（如有知识库）
- [ ] ✅ 错误提示友好

### 性能验收：

- [ ] ✅ 首次响应 < 3秒
- [ ] ✅ 后续响应 < 2秒
- [ ] ✅ 流式输出流畅
- [ ] ✅ UI无卡顿
- [ ] ✅ 内存占用正常（< 200MB）

### UI验收：

- [ ] ✅ 界面美观符合Material Design 3
- [ ] ✅ 消息气泡区分明确
- [ ] ✅ 性能统计清晰易读
- [ ] ✅ 错误提示醒目
- [ ] ✅ 加载动画流畅

---

## 🐛 常见问题排查

### 问题1：无法打开AI配置界面

**症状**: 点击"AI配置"无反应

**原因**: 导航未配置

**解决**:
```kotlin
// 检查是否添加了onNavigateToLLMSettings回调
ProfileScreen(
    onNavigateToLLMSettings = {
        navController.navigate("llm_settings")  // 确保此行存在
    }
)

// 检查是否添加了路由
composable("llm_settings") {  // 确保此路由存在
    LLMSettingsScreen(...)
}
```

---

### 问题2：测试连接失败

**症状**: 显示"❌ 连接失败"

**排查步骤**:

1. **检查API Key**
   ```
   - 是否完整复制
   - 是否包含多余空格
   - 是否已过期
   ```

2. **检查网络**
   ```bash
   adb shell ping -c 3 8.8.8.8
   # 确认网络连通
   ```

3. **查看详细错误**
   ```bash
   adb logcat | grep -E "ChainlessChain|LLMSettings"
   # 查看具体错误信息
   ```

4. **验证API服务**
   ```bash
   # 在PC端测试
   curl -X POST https://ark.cn-beijing.volces.com/api/v3/chat/completions \
     -H "Authorization: Bearer YOUR_API_KEY" \
     -H "Content-Type: application/json" \
     -d '{"model":"doubao-pro-32k","messages":[{"role":"user","content":"Hello"}]}'
   ```

---

### 问题3：流式响应不显示

**症状**: 消息一次性显示，无逐字效果

**原因**: Stream参数可能未启用

**检查**:
```kotlin
// 在LLMTestChatViewModel.sendMessage中
currentAdapter?.streamChat(
    messages = conversationHistory,
    stream = true,  // 确保为true
    ...
)
```

---

### 问题4：性能统计不准确

**症状**: Token数量明显错误

**说明**:
当前使用简化的估算算法（中文1字符=1token，英文4字符=1token）

**优化**:
未来版本将集成tokenizer进行精确计算

---

## 📝 测试日志模板

复制以下模板记录测试结果：

```
# 火山引擎API测试日志

**测试日期**: 2026-01-25
**测试设备**: [设备型号]
**Android版本**: [系统版本]
**App版本**: v0.17.0

## 配置测试
- [ ] API Key配置: ✅/❌
- [ ] 连接测试: ✅/❌ (响应时间: ___ ms)
- [ ] 保存配置: ✅/❌

## 对话测试
- [ ] 基础对话: ✅/❌ (响应时间: ___ ms, Token: ___)
- [ ] 代码生成: ✅/❌ (响应时间: ___ ms)
- [ ] 中文理解: ✅/❌ (响应时间: ___ ms)

## 流式响应
- [ ] 逐字显示: ✅/❌
- [ ] 光标动画: ✅/❌
- [ ] 输出流畅: ✅/❌

## RAG功能
- [ ] RAG开关: ✅/❌
- [ ] 知识检索: ✅/❌
- [ ] 回答准确: ✅/❌

## 性能指标
- 平均响应时间: ___ ms
- 总Token使用: ___
- 成功率: ___%

## 问题记录
1. [问题描述]
   - 重现步骤:
   - 预期结果:
   - 实际结果:
   - 解决方案:

## 总体评价
- 功能完整性: ⭐⭐⭐⭐⭐ (1-5星)
- 性能表现: ⭐⭐⭐⭐⭐
- 用户体验: ⭐⭐⭐⭐⭐

## 备注
[其他说明]
```

---

## 🎉 测试成功标志

当你看到以下结果时，说明测试完全成功：

1. ✅ **配置界面**
   - 火山引擎卡片选中状态
   - API Key已保存且显示部分内容（****）
   - 连接状态显示绿色对勾

2. ✅ **测试对话**
   - 消息逐字流畅输出
   - 性能统计实时更新
   - 响应时间 < 3秒
   - Token统计合理

3. ✅ **用户体验**
   - 界面美观无卡顿
   - 错误提示清晰
   - 操作流畅自然

**恭喜！🎊 Android端LLM配置和测试功能已完全就绪！**

---

## 📞 获取帮助

遇到问题？
1. 查看 [ANDROID_LLM_CONFIG_GUIDE.md](./ANDROID_LLM_CONFIG_GUIDE.md)
2. 查看 [IMPLEMENTATION_SUMMARY.md](./IMPLEMENTATION_SUMMARY.md)
3. 运行日志查看：`adb logcat | grep ChainlessChain`
4. 提交Issue：附上测试日志和截图

---

**祝测试顺利！🚀**
