# Phase 9: 文件传输与编辑器增强 - 实施计划

**状态**: 🚧 进行中 | **开始时间**: 2026-01-26 | **预计完成**: Week 17-18

---

## 📋 目标概览

Phase 9聚焦于两大核心功能模块：

1. **P2P文件传输系统** - 支持分块传输、断点续传、队列管理
2. **代码编辑器增强** - 代码补全、智能提示、多文件编辑

---

## 🎯 功能模块

### 模块A: P2P文件传输系统 (70%)

#### A1: 文件分块传输管理器 ✅ 已规划

**功能需求**:

- 大文件分块传输（1MB块大小）
- 传输进度实时回调（百分比 + 传输速度）
- 支持多文件并发传输
- 传输错误自动重试（指数退避）
- MD5/SHA-256文件完整性校验

**技术方案**:

- `FileTransferManager.kt` - 传输管理器核心类
- `ChunkManager.kt` - 分块读取和组装
- `TransferProgressTracker.kt` - 进度跟踪器
- `FileIntegrityChecker.kt` - 完整性校验器

**核心API**:

```kotlin
class FileTransferManager {
    suspend fun sendFile(
        file: File,
        peerId: String,
        chunkSize: Int = 1024 * 1024, // 1MB
        onProgress: (TransferProgress) -> Unit
    ): Result<TransferResult>

    suspend fun receiveFile(
        transferId: String,
        outputPath: String,
        onProgress: (TransferProgress) -> Unit
    ): Result<File>

    fun pauseTransfer(transferId: String)
    fun resumeTransfer(transferId: String)
    fun cancelTransfer(transferId: String)
}
```

#### A2: 断点续传支持 ✅ 已规划

**功能需求**:

- 传输中断自动保存进度
- 恢复传输从断点继续
- 已接收分块持久化存储
- 断点元数据管理（JSON格式）

**技术方案**:

- `TransferCheckpoint.kt` - 断点数据模型
- `CheckpointManager.kt` - 断点管理器
- Room数据库表：`transfer_checkpoints`

**数据模型**:

```kotlin
@Entity(tableName = "transfer_checkpoints")
data class TransferCheckpointEntity(
    @PrimaryKey val transferId: String,
    val fileId: String,
    val fileName: String,
    val totalSize: Long,
    val receivedChunks: String, // JSON array [0,1,2,5,7...]
    val lastChunkIndex: Int,
    val createdAt: Long,
    val updatedAt: Long
)
```

#### A3: 文件传输队列管理 ✅ 已规划

**功能需求**:

- 传输队列（优先级队列）
- 并发控制（最多3个同时传输）
- 队列状态监控（等待/进行中/完成/失败）
- 队列持久化（重启后恢复）

**技术方案**:

- `TransferQueue.kt` - 队列管理器
- `TransferScheduler.kt` - 调度器
- Room数据库表：`transfer_queue`

**队列状态**:

```kotlin
enum class TransferStatus {
    QUEUED,      // 队列中等待
    TRANSFERRING, // 传输中
    PAUSED,      // 已暂停
    COMPLETED,   // 已完成
    FAILED,      // 失败
    CANCELLED    // 已取消
}
```

#### A4: P2P文件传输UI ✅ 已规划

**功能需求**:

- 拖拽上传文件（Compose DragAndDrop）
- 传输列表界面（上传/下载分组）
- 实时进度条和速度显示
- 暂停/恢复/取消按钮
- 传输完成通知

**技术方案**:

- `FileTransferScreen.kt` - 传输列表界面
- `FileDropZone.kt` - 拖拽上传区域
- `TransferListItem.kt` - 传输项组件
- `TransferProgressBar.kt` - 进度条组件

---

### 模块B: 代码编辑器增强 (30%)

#### B1: 基础代码补全 ✅ 已规划

**功能需求**:

- 关键字补全（if, fun, class, import等）
- 代码片段补全（快速输入模板代码）
- 变量名补全（基于当前文件作用域）
- 触发符号：输入字符自动触发

**技术方案**:

- `CodeCompletionEngine.kt` - 补全引擎
- `KeywordProvider.kt` - 关键字提供者（14种语言）
- `SnippetProvider.kt` - 片段提供者
- `ScopeAnalyzer.kt` - 作用域分析器（变量提取）

**代码片段示例**:

```kotlin
val kotlinSnippets = mapOf(
    "fun" to "fun \${1:functionName}(\${2:params}): \${3:ReturnType} {\n    \${4:// TODO}\n}",
    "class" to "class \${1:ClassName}(\${2:params}) {\n    \${3:// TODO}\n}",
    "if" to "if (\${1:condition}) {\n    \${2:// TODO}\n}",
    "for" to "for (\${1:item} in \${2:collection}) {\n    \${3:// TODO}\n}"
)
```

#### B2: 智能提示（基于上下文） ✅ 已规划

**功能需求**:

- 上下文感知补全（根据当前位置）
- 导入语句自动建议
- 函数参数提示
- API文档悬浮提示

**技术方案**:

- `ContextAnalyzer.kt` - 上下文分析器
- `ImportSuggester.kt` - 导入建议器
- `DocumentationProvider.kt` - 文档提供者

**上下文分析**:

```kotlin
sealed class CompletionContext {
    object ImportStatement     // import com.example.|
    object ClassDeclaration    // class MyClass |
    object FunctionCall        // myFunction(|
    object MemberAccess        // myObject.|
    object General             // 其他情况
}
```

#### B3: 多文件标签页编辑 ✅ 已规划

**功能需求**:

- 文件标签页（Tab）切换
- 最多10个同时打开的文件
- 标签拖拽排序
- 关闭标签（确认未保存）
- 标签状态指示（已修改/未保存）

**技术方案**:

- `EditorTabManager.kt` - 标签页管理器
- `EditorTab.kt` - 标签数据模型
- `TabBar.kt` - 标签栏Compose组件

**标签状态**:

```kotlin
data class EditorTab(
    val id: String,
    val file: ProjectFileEntity,
    val content: String,
    val isDirty: Boolean,
    val cursorPosition: Int = 0,
    val scrollPosition: Int = 0
)
```

#### B4: 代码折叠功能 ⏳ 待实施

**功能需求**:

- 函数/类折叠展开
- 代码块折叠（{...}）
- 折叠状态持久化
- 折叠图标显示

**技术方案**:

- `CodeFoldingManager.kt` - 已存在，需增强
- `FoldableRegion.kt` - 折叠区域数据模型
- 正则检测可折叠区域（class, fun, if, for等）

#### B5: 行号和缩进指示 ✅ 已规划

**功能需求**:

- 左侧行号显示
- 当前行高亮
- 缩进参考线（竖线）
- 代码对齐辅助

**技术方案**:

- `LineNumberGutter.kt` - 行号侧边栏组件
- `IndentGuide.kt` - 缩进参考线组件
- Canvas绘制优化

---

## 📊 进度跟踪

### 模块A: P2P文件传输 (0% → 100%)

| 子模块   | 功能               | 状态      | 预计工时   | 实际工时  |
| -------- | ------------------ | --------- | ---------- | --------- |
| A1       | 文件分块传输管理器 | ⏳ 待开始 | 6小时      | -         |
| A2       | 断点续传支持       | ⏳ 待开始 | 4小时      | -         |
| A3       | 文件传输队列管理   | ⏳ 待开始 | 4小时      | -         |
| A4       | P2P文件传输UI      | ⏳ 待开始 | 6小时      | -         |
| **总计** |                    |           | **20小时** | **0小时** |

### 模块B: 代码编辑器增强 (0% → 100%)

| 子模块   | 功能               | 状态      | 预计工时   | 实际工时  |
| -------- | ------------------ | --------- | ---------- | --------- |
| B1       | 基础代码补全       | ⏳ 待开始 | 8小时      | -         |
| B2       | 智能提示（上下文） | ⏳ 待开始 | 6小时      | -         |
| B3       | 多文件标签页编辑   | ⏳ 待开始 | 5小时      | -         |
| B4       | 代码折叠功能       | ⏳ 待开始 | 3小时      | -         |
| B5       | 行号和缩进指示     | ⏳ 待开始 | 3小时      | -         |
| **总计** |                    |           | **25小时** | **0小时** |

**Phase 9 总工时**: 45小时 (约2周，按每天4-5小时开发)

---

## 🧪 测试计划

### 单元测试 (预计35个用例)

- `FileTransferManagerTest` - 15个用例
- `ChunkManagerTest` - 8个用例
- `CheckpointManagerTest` - 6个用例
- `CodeCompletionEngineTest` - 12个用例
- `ContextAnalyzerTest` - 8个用例
- `EditorTabManagerTest` - 10个用例

### 集成测试 (预计12个用例)

- P2P文件传输端到端测试 - 6个用例
- 多文件编辑器状态管理测试 - 6个用例

---

## 📦 依赖管理

### 新增依赖

```kotlin
// build.gradle.kts (app模块)

// 文件传输（无需新增，使用现有OkHttp/Kotlin Coroutines）

// 代码补全（可选：使用Treesitter或Language Server Protocol）
// implementation("io.github.tree-sitter:tree-sitter:0.20.8")  // 待评估

// 拖拽上传（Compose DragAndDrop - 已包含在Compose Foundation）
// 无需新增
```

---

## 🎨 UI/UX设计

### 文件传输界面布局

```
┌─────────────────────────────────────┐
│ 文件传输                      [+]   │ TopAppBar
├─────────────────────────────────────┤
│ ┌───────────────────────────────┐   │
│ │  拖拽文件到这里上传           │   │ DropZone
│ │  📁 或点击选择文件            │   │
│ └───────────────────────────────┘   │
├─────────────────────────────────────┤
│ 上传中 (2)                          │ Tab
│ ┌───────────────────────────────┐   │
│ │ 📄 report.pdf                 │   │ Transfer Item
│ │ ████████░░ 85% | 2.5 MB/s     │   │ Progress
│ │ [⏸] [✖]                       │   │ Actions
│ └───────────────────────────────┘   │
│ ┌───────────────────────────────┐   │
│ │ 📄 video.mp4                  │   │
│ │ ██░░░░░░░░ 23% | 1.8 MB/s     │   │
│ │ [⏸] [✖]                       │   │
│ └───────────────────────────────┘   │
├─────────────────────────────────────┤
│ 已完成 (5)                          │ Tab
│ ┌───────────────────────────────┐   │
│ │ ✅ image.png | 2.3 MB          │   │ Completed
│ └───────────────────────────────┘   │
└─────────────────────────────────────┘
```

### 代码编辑器增强界面

```
┌─────────────────────────────────────┐
│ 文件标签                            │ TabBar
│ [MainActivity.kt*] [ViewModel.kt]   │ Tabs (* = dirty)
│ [Composable.kt] [+ 新建]            │
├─────────────────────────────────────┤
│ 1  │ package com.example           │ Line numbers
│ 2  │ ├─                            │ Indent guides
│ 3  │ │ import androidx.compose    │
│ 4  │ │                             │
│ 5  │ │ ▼ fun onCreate() {         │ Code folding
│ 6  │ │ ├─  setContent {           │
│ 7  │ │ │    MyApp()      <────────│ Auto-complete popup
│ 8  │ │ │    ┌──────────────┐      │
│ 9  │ │ │    │ MyApp()      │      │
│10  │ │ │    │ MyAppTheme() │      │
│11  │ │ │    │ MyAppBar()   │      │
│12  │ │ │    └──────────────┘      │
│13  │ │ ├─}                        │
│14  │ │ }                           │
└─────────────────────────────────────┘
```

---

## 🚀 实施顺序

### Week 1 (Day 1-5): P2P文件传输核心

**Day 1**: 文件分块传输管理器

- 实现ChunkManager（分块读取）
- 实现FileTransferManager核心逻辑
- 单元测试

**Day 2**: 传输进度追踪

- 实现TransferProgressTracker
- 实现文件完整性校验
- 单元测试

**Day 3**: 断点续传支持

- 实现CheckpointManager
- 数据库表设计和DAO
- 断点恢复逻辑

**Day 4**: 传输队列管理

- 实现TransferQueue
- 实现TransferScheduler
- 并发控制逻辑

**Day 5**: P2P文件传输UI (Part 1)

- FileTransferScreen布局
- FileDropZone拖拽区域
- 传输列表基础UI

### Week 2 (Day 6-10): 代码编辑器增强

**Day 6**: P2P文件传输UI (Part 2)

- TransferListItem组件
- 进度条和速度显示
- 暂停/恢复/取消逻辑

**Day 7**: 基础代码补全

- KeywordProvider实现
- SnippetProvider实现
- ScopeAnalyzer变量提取

**Day 8**: 智能提示上下文

- ContextAnalyzer实现
- ImportSuggester实现
- 补全弹窗UI

**Day 9**: 多文件标签页

- EditorTabManager实现
- TabBar UI组件
- 标签状态管理

**Day 10**: 行号和缩进指示

- LineNumberGutter实现
- IndentGuide实现
- CodeFoldingManager增强

---

## 📝 文档计划

### 技术文档

- [ ] `FILE_TRANSFER_API_REFERENCE.md` - 文件传输API文档
- [ ] `CODE_COMPLETION_GUIDE.md` - 代码补全使用指南
- [ ] `EDITOR_CUSTOMIZATION.md` - 编辑器定制指南

### 用户文档

- [ ] `FILE_TRANSFER_USER_GUIDE.md` - 文件传输用户手册
- [ ] `EDITOR_SHORTCUTS.md` - 编辑器快捷键参考

---

## 🔧 技术挑战

### 挑战1: 大文件传输性能

**问题**: 传输大文件（>100MB）时可能导致内存溢出

**解决方案**:

- 使用流式读取（InputStream）而非一次性加载
- 分块大小动态调整（网络慢时减小块大小）
- 使用`okio`库优化IO性能

### 挑战2: 断点续传数据一致性

**问题**: 网络中断时可能导致部分分块损坏

**解决方案**:

- 每个分块单独校验（CRC32）
- 损坏分块自动重新请求
- 接收完成后整体SHA-256校验

### 挑战3: 代码补全性能

**问题**: 大文件（>10,000行）补全响应慢

**解决方案**:

- 补全请求防抖（300ms延迟）
- 作用域分析限制在当前类/函数
- 补全结果缓存（LRU Cache）

### 挑战4: 多文件编辑器内存占用

**问题**: 同时打开多个大文件内存占用高

**解决方案**:

- 限制最多10个文件同时打开
- 超出限制时提示关闭未修改文件
- 使用虚拟滚动优化长文件渲染

---

## ✅ 完成标准

### P2P文件传输模块

- [ ] 支持传输任意大小文件（测试10MB-1GB）
- [ ] 传输速度≥1MB/s（良好网络条件）
- [ ] 断点续传成功率>95%
- [ ] 文件完整性校验100%准确
- [ ] 传输队列并发控制正常（最多3个）
- [ ] UI响应流畅（60fps）
- [ ] 所有单元测试通过（15个用例）

### 代码编辑器增强模块

- [ ] 关键字补全支持14种语言
- [ ] 代码片段补全≥20个常用模板
- [ ] 补全响应时间<300ms
- [ ] 多文件标签页≤10个同时打开
- [ ] 标签拖拽排序流畅
- [ ] 行号和缩进指示正确显示
- [ ] 代码折叠功能正常工作
- [ ] 所有单元测试通过（12个用例）

---

## 📅 里程碑

| 日期       | 里程碑           | 完成度  |
| ---------- | ---------------- | ------- |
| 2026-01-26 | 规划文档完成     | ✅ 100% |
| 2026-01-28 | 文件传输核心完成 | ⏳ 0%   |
| 2026-01-31 | 传输UI完成       | ⏳ 0%   |
| 2026-02-03 | 代码补全完成     | ⏳ 0%   |
| 2026-02-06 | 编辑器增强完成   | ⏳ 0%   |
| 2026-02-07 | Phase 9 100%完成 | ⏳ 0%   |

---

**文档版本**: v1.0
**创建时间**: 2026-01-26 07:00
**Phase 9状态**: 规划完成，待实施 (0%)
**下一步**: 开始实施模块A1 - 文件分块传输管理器
