# 2026-09-06 文档与官网同步部署记录

## 代码与发布依据

- 核对增量：`0b497ce521..eb7cc93dce`，覆盖上次 9 月 4 日文档同步之后的代码。
- 文档部署提交：`384565f2e4ece40da82cd1dce19cd2fd54ac3681`。
- CLI：`0.166.24`，不可变标签 `v-npm-0-166-24`，提交 `9cf9c7bfd70ddb8b12b0d157dd6faa1ceb152436`。
- CLI 同提交的 [CLI CI](https://github.com/chainlesschain/chainlesschain/actions/runs/34006348566)、[CLI Strict Sandbox](https://github.com/chainlesschain/chainlesschain/actions/runs/34006348459) 三平台矩阵与 [npm 发布/公共安装检查](https://github.com/chainlesschain/chainlesschain/actions/runs/34007761162) 成功。
- 官方 npm 回读：CLI `0.166.24`、Session Core `0.3.12`、Core DB `0.1.5`。`0.166.23` 未单独发布，其候选改动由 `0.166.24` 承接。
- Open VSX `0.37.84`：公开标签指向 `eb7cc93dce34bb2495fe56cfcfc82a82a9b4e2f8`，配套 CLI `0.166.24`。
- JetBrains Marketplace `0.4.111`：公开标签指向 `9a06eb722b04fd8529ebb9fed189fa17b9cfd0d2`，内置推荐仍为 CLI `0.166.22`；后续主线的 v2 批审回执适配不冒充已发布插件能力。

## 更新范围

本次内容提交更新 34 个文件，包括中英文 README、CLI README、CHANGELOG、共享设计源、用户指南、导航、官网发布元数据和中英文首页/CLI/IDE 页面。

新增说明覆盖长任务预算、大文件分页与压缩续读、IDE 慢命令保活、持久 Workbench 装配和审核/回滚恢复、跨 Wiki 多级来源撤销、候选 quarantine/reject 区分、typed artifact、受治理市场候选安装及只读 Eval 徽章。设计源通过两个站点的同步脚本生成镜像。

## 验证

- 三站 `npm run build` 均成功。用户文档最终构建为单独运行，Node heap 上限 6144 MiB；完整构建耗时约 301 秒。设计站约 195 秒，官网最后一次 Astro 构建约 9 秒。
- 用户文档结构检查：301 个文件均包含 11 个必需模块。
- Playwright / headless Chromium：15 个关键页面 HTTP 200、版本/功能文本正确、无页面脚本错误、站内及三站交叉链接均可解析到构建文件。
- 官网长任务区域检查 1365px 桌面和 390px 手机宽度，无横向溢出。
- Prettier 与 `git diff --check` 通过。
- VitePress 保留既有大于 500 kB 的 chunk 提示及 `excel`/`conf` 语法高亮回退提示，不影响构建成功。

## 部署结果

部署标识：`20260906-1327-cli016624`。服务器：`47.111.5.128`。2026-09-06 完成部署并回读。

| 站点                                          | HTML 页面 | 文件数（不含 deployment.json） | 全树 SHA-256                                                       |
| --------------------------------------------- | --------: | -----------------------------: | ------------------------------------------------------------------ |
| [官网](https://www.chainlesschain.com)        |        20 |                             35 | `5e47ff3ba4031ad238dbc56fece8a869d316d7471900d1942f170c0c945e41b3` |
| [用户文档](https://docs.chainlesschain.com)   |       612 |                           1863 | `afe36d44c2906edad8af8a187d39d435843589c2a43837434432038fa9363d49` |
| [设计文档](https://design.chainlesschain.com) |       266 |                            824 | `a1b2603001d296cdaa585263b61cabe67dfb6b1d71e3826b8584c854da890dd5` |

三站均使用 `scripts/remote-deploy-docs.sh` 复用已验证产物，上传后切换目录并保留旧版本。首轮两个文档站打包检测到发布清单仍在写入，tar 拒绝该次包；待清单完成后重新打包成功，失败的包未上传。最终 898 个 HTML 页面全部上线。

线上 9 个关键页面与 3 份 `deployment.json` 均回读 HTTP 200，并核对新内容、提交与摘要。服务器另逐文件重新计算 SHA-256 聚合，三站均与本地发布清单完全一致。

公开发布清单：[官网](https://www.chainlesschain.com/deployment.json)、[用户文档](https://docs.chainlesschain.com/deployment.json)、[设计文档](https://design.chainlesschain.com/deployment.json)。全树摘要按排序后的相对路径与文件 SHA-256 聚合，不包含清单本身。

## 回滚位置

- `/www/wwwroot/www.chainlesschain.com.bak-20260906-1327-cli016624`
- `/www/wwwroot/docs.chainlesschain.com.bak-20260906-1327-cli016624`
- `/www/wwwroot/design.chainlesschain.com.bak-20260906-1327-cli016624`

备份均来自本轮部署前的线上目录。需要回滚时先保留当前目录，再将对应备份恢复为原站点目录，并重新核对 HTTPS 页面。

本次部署仅替换三个静态站点，未发布 npm、Desktop、IDE 插件或后端服务，也未配置生产演进 authority。
