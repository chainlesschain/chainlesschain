# 三站文档更新与部署记录（2026-09-26）

## 内容与源码基线

- 代码核对基线：`main@a231bc79df9661ea2fbcbbb9e806661b54299113`。
- 部署内容提交：`d752e79e60`（`docs: sync latest runtime releases and website guidance`）。
- 更新中文/英文 README、CLI README、系统主设计与模块 110/112 入口、运行时/升级/IDE 用户指南，以及官网中英文首页、CLI/IDE 页面。
- 新增运行时与评测证据增量设计、PM 效果评测用户指南；移除两个文档站覆盖首页版本的过期配置。
- 公开版本：CLI `0.166.76`、Open VSX `0.37.117`、JetBrains `0.4.137`。JetBrains `0.4.138` 工作流成功，公开列表尚未回读到；npm `0.166.73/0.166.75` 未单独发行。产品版本保持独立的 `v5.0.3.138`。

## 验证结果

| 检查                         | 结果                                                  |
| ---------------------------- | ----------------------------------------------------- |
| 官网 `npm run build`         | 通过，20 个页面                                       |
| 用户文档 `npm run build`     | 通过；305 个用户文档均满足 11 个必需章节              |
| 设计站 `npm run build`       | 通过；从 `docs/design` 同步 270 个文件                |
| Prettier、`git diff --check` | 通过                                                  |
| 离线 PM 工具 `--help`        | 六个子命令与文档一致                                  |
| Chromium 本地页面检查        | 13 页通过；无页面 JavaScript 错误或本地资源 HTTP 错误 |
| 新增文档内部链接             | 均能解析到构建文件                                    |
| 官网 CLI 手机布局            | 390 px 视口无横向溢出，已检查截图                     |
| 公网 HTTPS 回读              | 13 页均为 200，HTML SHA-256 与本地构建完全一致        |

VitePress 的既有提示包括部分代码块语言回退为纯文本以及较大资源包；构建退出码均为 0。本次未修改运行时产品代码，未重新发布 npm 或 IDE 制品。

## 服务器部署

使用仓库现有脚本：

```bash
bash scripts/deploy-sites-tar.sh official user design
```

| 站点                                           | 本地构建目录                            | 远端目录                                 | 文件数 |
| ---------------------------------------------- | --------------------------------------- | ---------------------------------------- | ------ |
| [官网](https://www.chainlesschain.com/)        | `docs-website-v2/dist`                  | `/www/wwwroot/www.chainlesschain.com`    | 35     |
| [用户文档](https://docs.chainlesschain.com/)   | `docs-site/docs/.vitepress/dist`        | `/www/wwwroot/docs.chainlesschain.com`   | 1896   |
| [设计文档](https://design.chainlesschain.com/) | `docs-site-design/docs/.vitepress/dist` | `/www/wwwroot/design.chainlesschain.com` | 842    |

目标服务器为既有部署服务器 `47.111.5.128`。每站先上传到 `.new`、检查文件数量与首页，再切换目录；上一个线上版本保存在对应 `.old`。三个站点全部部署成功，文件数量与本地一致。

公网核验覆盖官网中英文首页、CLI 页、IDE 页，用户站首页、发布指南、PM 指南、IDE 指南及同步的增量设计，以及设计站首页和增量设计。

## 回滚

在服务器上先核对目标站点与对应 `.old/index.html`。如需回滚，将当前站点目录改名保留为独立故障备份，再将对应 `.old` 改回正式目录；完成后重新核验 HTTPS 首页与关键页面。三个站点分别拥有上一版备份，不应混用其目录或静态资源。

生产 PM/Pilot 结果、完整 cohort 启动覆盖和总成本仍需目标环境独立验收；文档与网站部署不改变 automatic active promotion 的 HOLD 状态。
