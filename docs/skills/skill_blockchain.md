---
id: skill_blockchain
name: 区块链工具
category: network
enabled: true
---

# Blockchain

## 📝 概述

智能合约交互、钱包管理、链上查询、交易签名

**分类**: 网络请求
**标签**: 区块链, 智能合约, 钱包, 交易
**状态**: ✅ 已启用

## 💡 使用场景

1. 根据需求使用相关工具
2. 完成特定领域的任务
3. 提高工作效率
## ⚙️ 配置选项

```json
{
  "network": "mainnet",
  "chain": "ethereum"
}
```

**配置说明**:

- `network`: string 类型，当前值: "mainnet"
- `chain`: string 类型，当前值: "ethereum"

## 📖 使用示例

### 示例1: 使用 区块链工具

```javascript
// 通过AI引擎调用技能
const result = await aiEngineManager.processUserInput(
  "请帮我...",  // 用户输入
  { skillId: "network" }  // 指定使用的技能
);
```

### 示例2: 通过IPC调用

```javascript
// 在渲染进程中
const tools = await window.electronAPI.invoke('skill:get-tools', skillId);
console.log('技能包含的工具:', tools);
```

## 🔗 相关技能

暂无相关技能
---

**文档生成时间**: 2025/12/30 17:19:40
**技能类型**: 内置
