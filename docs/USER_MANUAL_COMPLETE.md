# ChainlessChain 完整用户手册

**版本**: v0.16.0
**更新日期**: 2026-01-03

---

## 📖 目录

- [快速开始](#快速开始)
- [系统概述](#系统概述)
- [安装与配置](#安装与配置)
- [核心功能](#核心功能)
  - [1. 知识库管理](#1-知识库管理)
  - [2. AI智能助手](#2-ai智能助手)
  - [3. Git版本控制](#3-git版本控制)
  - [4. 项目管理](#4-项目管理)
  - [5. U-Key安全](#5-u-key安全)
  - [6. P2P社交网络](#6-p2p社交网络)
  - [7. 去中心化交易](#7-去中心化交易)
  - [8. Skill-Tool系统](#8-skill-tool系统)
- [高级功能](#高级功能)
- [常见问题](#常见问题)
- [故障排除](#故障排除)

---

## 🚀 快速开始

### 第一次使用

1. **下载安装**
   - Windows: 下载并运行 `ChainlessChain-Setup-*.exe`
   - macOS: 下载 `.dmg` 文件并安装
   - Linux: 下载 `.AppImage` 或使用包管理器

2. **初始配置**
   - 首次启动会进入初始化向导
   - 设置主密码（用于数据库加密）
   - 选择数据存储位置
   - 配置LLM服务（本地/云端）

3. **创建第一个笔记**
   - 点击左侧边栏 "新建笔记"
   - 输入标题和内容
   - 使用 Markdown 格式编写
   - 保存（Ctrl+S）

4. **开始对话**
   - 点击顶部 "AI助手"
   - 输入问题或指令
   - AI会基于你的知识库回答

### 界面导航

```
┌─────────────────────────────────────────┐
│  顶部菜单栏                              │
├──────┬──────────────────────────────────┤
│      │                                  │
│ 侧边 │                                  │
│ 导航 │        主工作区                   │
│ 栏   │                                  │
│      │                                  │
│      │                                  │
└──────┴──────────────────────────────────┘
```

**顶部菜单栏**：
- 文件、编辑、视图、工具、帮助
- 快速搜索框（Ctrl+P）
- 用户头像和设置

**侧边导航栏**：
- 知识库：笔记列表、标签、收藏
- AI助手：对话历史
- 项目管理：项目列表
- 社交：联系人、消息
- 交易：钱包、商店
- 设置：系统配置

---

## 📋 系统概述

### ChainlessChain 是什么？

ChainlessChain 是一个去中心化的个人AI管理系统，集成了：

1. **知识管理** - 个人第二大脑，支持 Markdown、图片、PDF、Word 等多种格式
2. **AI助手** - 集成本地和云端LLM，支持RAG增强检索
3. **版本控制** - 基于Git的自动版本控制和多设备同步
4. **隐私保护** - U-Key硬件加密，端到端加密通信
5. **去中心化社交** - DID身份系统，P2P加密消息
6. **数字资产交易** - 知识付费、信用评分、智能合约

### 核心优势

- **数据主权**：数据完全属于你，存储在本地加密数据库
- **隐私安全**：U-Key硬件加密 + 端到端加密通信
- **离线优先**：所有核心功能离线可用
- **AI增强**：本地LLM，无需担心数据泄露
- **开放生态**：支持插件扩展，开源可审计

---

## 🔧 安装与配置

### 系统要求

**最低配置**：
- CPU: 4核心
- 内存: 8GB RAM
- 硬盘: 20GB 可用空间
- 系统: Windows 10/11, macOS 11+, Ubuntu 20.04+

**推荐配置**（用于本地LLM）：
- CPU: 8核心+
- 内存: 16GB+ RAM
- GPU: NVIDIA RTX 2060+ (6GB+ VRAM)
- 硬盘: 50GB+ SSD

### 安装步骤

#### Windows

1. 下载 `ChainlessChain-Setup-*.exe`
2. 双击运行安装程序
3. 选择安装路径（默认：`C:\Program Files\ChainlessChain`）
4. 完成安装，启动应用

#### macOS

1. 下载 `.dmg` 文件
2. 打开镜像，拖动到 Applications
3. 首次启动需要在"系统偏好设置 > 安全性与隐私"中允许
4. 启动应用

#### Linux

```bash
# 使用 AppImage
chmod +x ChainlessChain-*.AppImage
./ChainlessChain-*.AppImage

# 或使用包管理器（Ubuntu/Debian）
sudo dpkg -i chainlesschain_*.deb
chainlesschain
```

### 初始配置

#### 1. 设置主密码

首次启动时，系统会要求设置主密码：

- 密码长度至少8位
- 建议包含大小写字母、数字和特殊字符
- 此密码用于加密本地数据库
- **请务必牢记，遗忘后数据无法恢复**

#### 2. 配置数据存储

选择数据存储位置：

- **默认位置**：`~/ChainlessChain/data`
- **自定义位置**：选择其他路径（如外置硬盘、NAS）

#### 3. 配置LLM服务

选择AI模型服务：

**选项A：本地LLM（推荐，隐私优先）**
```
模型：Ollama
下载模型：qwen2:7b（约4GB）
GPU加速：自动检测
```

**选项B：云端LLM（性能优先）**
```
服务商：阿里云通义千问 / 智谱GLM / OpenAI等
API Key：输入你的API密钥
模型选择：qwen-max / glm-4 / gpt-4等
```

**选项C：混合模式**
- 日常对话使用本地模型
- 复杂任务使用云端模型

#### 4. U-Key 配置（可选）

如果你有U-Key硬件设备：

1. 插入U-Key到USB接口
2. 系统自动检测设备
3. 输入PIN码（默认：`123456`）
4. 完成设备绑定

**注意**：
- Windows：完整硬件支持
- macOS/Linux：仅模拟模式

---

## 💡 核心功能

### 1. 知识库管理

知识库是你的个人数字大脑，用于存储、组织和检索所有信息。

#### 1.1 创建笔记

**方式一：直接创建**
1. 点击侧边栏 "新建笔记" 按钮
2. 输入标题
3. 编写内容（支持 Markdown）
4. 保存（Ctrl+S）

**方式二：导入文件**
1. 点击 "导入" 按钮
2. 选择文件类型：
   - Markdown (.md)
   - PDF (.pdf)
   - Word (.docx)
   - 纯文本 (.txt)
   - 图片 (.png, .jpg) - 自动OCR识别文字
3. 选择文件，自动导入并创建笔记

**方式三：浏览器剪藏**（需安装浏览器扩展）
1. 在浏览器中点击扩展图标
2. 选择剪藏模式：
   - 完整页面
   - 选中内容
   - 截图
3. 添加标签和备注
4. 保存到知识库

#### 1.2 编辑笔记

**Markdown编辑器功能**：

- 实时预览（分屏/同步滚动）
- 语法高亮
- 快捷插入：
  - 标题（Ctrl+1 到 Ctrl+6）
  - 粗体（Ctrl+B）
  - 斜体（Ctrl+I）
  - 代码块（Ctrl+Shift+K）
  - 链接（Ctrl+K）
  - 图片（Ctrl+Shift+I）
  - 表格（Ctrl+Shift+T）

**高级功能**：

- **双向链接**：`[[笔记标题]]` 自动创建笔记间链接
- **标签系统**：`#标签名` 自动创建标签
- **任务列表**：`- [ ] 待办事项`
- **代码块**：支持100+编程语言语法高亮
- **数学公式**：支持 LaTeX 语法 `$...$` 或 `$$...$$`
- **流程图**：支持 Mermaid 语法

#### 1.3 组织笔记

**文件夹管理**：
- 创建多层级文件夹
- 拖拽移动笔记
- 文件夹收藏

**标签系统**：
- 为笔记添加多个标签
- 标签自动补全
- 按标签筛选
- 标签云可视化

**智能分类**：
- AI自动提取关键词
- 自动分类建议
- 相似笔记推荐

#### 1.4 搜索笔记

**全文搜索**：
- 支持中英文分词
- 模糊搜索
- 正则表达式搜索

**高级筛选**：
```
标题：项目
标签：#工作 #重要
日期：最近7天
类型：Markdown
排序：修改时间降序
```

**AI语义搜索**（RAG增强）：
```
问题：如何配置Docker网络？
结果：返回最相关的笔记段落，按相关度排序
```

#### 1.5 笔记导出

支持导出格式：
- Markdown (.md)
- PDF (.pdf)
- HTML (.html)
- Word (.docx)
- 纯文本 (.txt)

批量导出：
- 选择多个笔记
- 导出为ZIP压缩包
- 保持文件夹结构

---

### 2. AI智能助手

AI助手是你的智能对话伙伴，能够回答问题、协助写作、分析数据等。

#### 2.1 开始对话

1. 点击顶部 "AI助手" 图标
2. 新建对话或选择历史对话
3. 输入问题或指令
4. AI回复基于：
   - 通用知识（LLM预训练）
   - 你的知识库（RAG检索）
   - 对话历史（上下文）

#### 2.2 对话模式

**普通对话**：
```
你：什么是Docker？
AI：Docker是一个开源的容器化平台...
```

**知识库增强**（默认开启）：
```
你：我之前记录的Docker配置是什么？
AI：[检索你的笔记]
    根据你的笔记《Docker学习笔记》，你的配置是...
```

**多轮对话**：
```
你：帮我写一篇关于AI的文章
AI：好的，文章主题是什么？
你：AI在教育领域的应用
AI：[生成文章大纲]
    这是我为你准备的大纲...
你：展开第二部分
AI：[展开内容]
```

#### 2.3 AI功能

**文本生成**：
- 写作助手（文章、报告、邮件）
- 代码生成（支持多种编程语言）
- 创意生成（诗歌、故事、广告文案）

**内容分析**：
- 文本摘要
- 关键词提取
- 情感分析
- 翻译（支持50+语言）

**数据处理**：
- 表格分析
- 数据可视化建议
- 正则表达式生成

**知识问答**：
- 基于知识库的问答
- 多文档对比分析
- 知识图谱可视化

#### 2.4 提示词模板

系统内置100+提示词模板，覆盖场景：

**办公场景**：
- 会议纪要生成
- 邮件撰写
- 报告总结

**学习场景**：
- 论文阅读助手
- 学习计划制定
- 知识点梳理

**创作场景**：
- 故事创作
- 诗歌生成
- 剧本编写

**编程场景**：
- 代码审查
- Bug修复建议
- 算法优化

**自定义模板**：
1. 创建新模板
2. 定义变量（如 `{{主题}}`, `{{风格}}`）
3. 保存并分享

#### 2.5 RAG高级检索

RAG（Retrieval-Augmented Generation）是AI增强的核心技术。

**工作原理**：
1. 用户提问
2. 向量检索相关笔记
3. 重排序（Rerank）选出最相关内容
4. 结合上下文生成回答

**配置选项**：
```yaml
检索设置:
  相似度阈值: 0.7 (0-1)
  返回结果数: 5
  重排序: 启用
  包含来源: 启用
```

**检索范围**：
- 全部笔记
- 指定文件夹
- 指定标签
- 指定时间范围

---

### 3. Git版本控制

ChainlessChain 内置 Git 版本控制，自动记录所有变更，支持多设备同步。

#### 3.1 自动版本控制

**默认行为**：
- 每次保存笔记自动提交（Auto-commit）
- 提交信息自动生成：`Update: 笔记标题`
- 支持关闭自动提交，手动控制

**查看历史**：
1. 右键点击笔记
2. 选择 "版本历史"
3. 浏览所有历史版本
4. 对比差异（Diff视图）
5. 恢复到任意历史版本

#### 3.2 Git同步

**配置远程仓库**：

1. 打开 "设置 > Git配置"
2. 选择仓库类型：
   - GitHub
   - GitLab
   - Gitee
   - 自建Git服务器
3. 输入仓库URL：`https://github.com/username/repo.git`
4. 认证方式：
   - HTTPS（用户名+密码/Token）
   - SSH（公钥认证）

**同步操作**：

- **推送（Push）**：将本地更改上传到远程
- **拉取（Pull）**：从远程下载更改
- **自动同步**：设置定时自动推拉（如每30分钟）

**冲突解决**：

当多设备同时修改同一笔记时：

1. 系统检测到冲突
2. 弹出冲突解决界面
3. 三栏对比视图：
   - 左侧：本地版本
   - 中间：合并结果
   - 右侧：远程版本
4. 手动选择保留内容
5. 确认解决，完成合并

#### 3.3 分支管理（高级）

```bash
创建分支: feature/new-idea
切换分支: main ↔ feature/new-idea
合并分支: feature/new-idea → main
删除分支: feature/new-idea
```

**应用场景**：
- 实验性内容：创建分支测试，满意后合并
- 多人协作：每人一个分支，定期合并
- 版本发布：维护release分支

---

### 4. 项目管理

项目管理功能帮助你组织工作、跟踪进度、协同团队。

#### 4.1 创建项目

1. 点击侧边栏 "项目管理"
2. 点击 "新建项目"
3. 填写项目信息：
   - 项目名称
   - 项目描述
   - 开始/结束时间
   - 项目成员
   - 项目标签
4. 选择项目根目录（关联文件夹）
5. 保存创建

#### 4.2 项目视图

**看板视图**（Kanban）：
```
待办 | 进行中 | 已完成
─────┼────────┼─────────
任务1 │ 任务3  │ 任务5
任务2 │ 任务4  │ 任务6
```

**列表视图**：
- 任务列表
- 支持多级子任务
- 勾选完成

**日历视图**：
- 按日期显示任务
- 拖拽调整时间
- 查看截止日期

**甘特图**：
- 任务时间轴
- 依赖关系
- 关键路径

#### 4.3 任务管理

**创建任务**：
```yaml
标题: 完成API文档
描述: 编写RESTful API文档
优先级: 高
截止日期: 2026-01-10
指派给: 张三
标签: 文档, API
检查清单:
  - [ ] 接口列表
  - [ ] 参数说明
  - [ ] 示例代码
```

**任务状态**：
- 待办（Todo）
- 进行中（In Progress）
- 待审核（Review）
- 已完成（Done）
- 已取消（Cancelled）

**任务关联**：
- 关联笔记
- 关联文件
- 关联代码提交
- 子任务依赖

#### 4.4 项目统计

**进度统计**：
- 任务完成率
- 工时统计
- 燃尽图（Burndown Chart）

**成员统计**：
- 任务分配情况
- 完成效率
- 贡献排行

**代码统计**（如果是代码项目）：
- 代码行数
- 提交频率
- 文件变更热力图

#### 4.5 项目分享

**生成分享链接**：
1. 打开项目详情
2. 点击 "分享"
3. 配置分享选项：
   - 公开/加密访问
   - 有效期（7天/30天/永久）
   - 访问权限（只读/编辑）
4. 生成链接：`https://share.chainlesschain.com/project/abc123`

**协作模式**：
- **只读**：他人可查看，不可编辑
- **评论**：可查看和评论，不可编辑
- **编辑**：完整编辑权限

---

### 5. U-Key安全

U-Key 是硬件安全设备，提供企业级加密和身份认证。

#### 5.1 U-Key 功能

**数据加密**：
- AES-256 硬件加密
- 密钥存储在U-Key中，不可导出
- 数据库自动加密

**身份认证**：
- 双因素认证（2FA）
- 硬件证书（X.509）
- DID身份绑定

**数字签名**：
- 笔记签名（防篡改）
- 交易签名（去中心化交易）
- 合约签名（智能合约）

#### 5.2 使用流程

**初始化U-Key**：
1. 插入U-Key
2. 系统检测设备
3. 设置PIN码（6-16位）
4. 生成密钥对
5. 完成初始化

**日常使用**：
1. 启动应用
2. 插入U-Key
3. 输入PIN码
4. 自动解密数据库
5. 正常使用

**备份密钥**（重要）：
1. 打开 "设置 > U-Key管理"
2. 点击 "导出恢复密钥"
3. 输入PIN码
4. 保存到安全位置（如纸质备份、密码管理器）
5. **切勿将恢复密钥存储在电脑上**

#### 5.3 安全最佳实践

1. **PIN码管理**：
   - 不使用生日、手机号等易猜测密码
   - 定期更换PIN码
   - 连续输错3次锁定设备

2. **设备保管**：
   - U-Key妥善保管，防止丢失
   - 不与他人共用U-Key
   - 丢失后立即远程撤销设备

3. **多设备备份**：
   - 准备备用U-Key
   - 导出恢复密钥
   - 定期测试恢复流程

#### 5.4 模拟模式（无硬件）

如果没有U-Key硬件，可以使用模拟模式：

1. 打开 "设置 > U-Key管理"
2. 选择 "模拟模式"
3. 设置模拟PIN码
4. 系统使用软件加密（安全性降低）

**注意**：模拟模式仅用于测试，生产环境请使用硬件U-Key。

---

### 6. P2P社交网络

ChainlessChain 支持去中心化社交，基于DID身份和P2P网络。

#### 6.1 创建DID身份

DID（Decentralized Identifier）是你的去中心化身份标识。

**创建流程**：
1. 打开 "社交 > 身份管理"
2. 点击 "创建DID"
3. 填写信息：
   - 昵称
   - 头像
   - 个人简介
4. 系统生成DID：`did:chain:abc123...`
5. 使用U-Key签名（可选）

**DID特性**：
- 全球唯一
- 自主控制
- 可验证
- 跨平台通用

#### 6.2 添加好友

**方式一：扫描二维码**
1. 面对面时，打开对方的DID二维码
2. 扫描添加
3. 发送好友请求
4. 对方同意后成为好友

**方式二：搜索DID**
1. 输入对方DID：`did:chain:abc123...`
2. 搜索并查看资料
3. 发送好友请求

**方式三：通过论坛**
1. 在社区论坛看到有趣的发言
2. 点击用户头像
3. 添加好友

#### 6.3 加密消息

**发送消息**：
1. 选择好友
2. 输入消息
3. 消息自动端到端加密（Signal Protocol）
4. 发送

**消息类型**：
- 文本消息
- 图片/文件
- 笔记分享
- 位置（可选）

**离线消息**：
- 对方离线时，消息存储在P2P网络
- 对方上线后自动接收
- 保证消息不丢失

**消息销毁**：
- 阅后即焚（设置倒计时）
- 自动删除（如7天后）

#### 6.4 社交论坛

**发布帖子**：
1. 打开 "社交 > 论坛"
2. 点击 "发帖"
3. 选择板块（技术/生活/随笔等）
4. 编写内容（支持Markdown）
5. 添加话题标签
6. 发布（可选择匿名）

**互动功能**：
- 点赞/踩
- 评论/回复
- 转发/引用
- 收藏

**信誉系统**：
- 发布有价值内容获得积分
- 积分提升信誉等级
- 高信誉用户获得更多权限

---

### 7. 去中心化交易

ChainlessChain 支持知识付费和数字资产交易，基于区块链技术。

#### 7.1 知识付费

**发布付费内容**：
1. 选择要出售的笔记
2. 点击 "知识付费"
3. 设置价格（如 0.5 CHAIN币）
4. 设置试读内容（可选）
5. 发布到市场

**购买内容**：
1. 浏览知识市场
2. 筛选分类（技术/商业/生活等）
3. 查看试读内容和评价
4. 支付购买
5. 内容自动添加到知识库

#### 7.2 信用评分

**评分机制**：
- 发布内容质量
- 交易诚信度
- 社区贡献
- 好友评价

**信用等级**：
```
★☆☆☆☆ (0-20分) 新手
★★☆☆☆ (21-40分) 普通
★★★☆☆ (41-60分) 良好
★★★★☆ (61-80分) 优秀
★★★★★ (81-100分) 卓越
```

**信用用途**：
- 高信用用户优先展示
- 降低交易手续费
- 参与社区治理投票

#### 7.3 智能合约

**创建合约**：
1. 打开 "交易 > 智能合约"
2. 选择合约模板：
   - 知识付费合约
   - 协作分成合约
   - 订阅服务合约
3. 配置参数
4. 部署合约

**合约示例 - 知识付费**：
```javascript
合约内容:
  卖方: Alice (did:chain:alice)
  买方: Bob (did:chain:bob)
  标的: 《Docker高级教程》
  价格: 1.0 CHAIN
  版权: 个人使用，不可转售
执行条件:
  - Bob支付1.0 CHAIN
  - Alice自动获得0.95 CHAIN（扣除5%手续费）
  - Bob自动获得内容访问权
```

#### 7.4 数字钱包

**查看余额**：
- CHAIN币（平台通证）
- 其他资产

**充值/提现**：
1. 打开 "交易 > 钱包"
2. 选择充值/提现
3. 输入金额
4. 确认交易（U-Key签名）

**交易记录**：
- 查看所有交易历史
- 按类型筛选（收入/支出/转账）
- 导出账单

---

### 8. Skill-Tool系统

Skill-Tool系统是AI助手的能力扩展系统。

#### 8.1 技能管理

**查看技能**：
1. 打开 "设置 > Skill-Tool系统"
2. 切换到 "技能" 标签
3. 浏览所有技能（按分类）

**启用/禁用技能**：
- 点击技能卡片的开关
- 禁用后AI不会使用该技能

**创建自定义技能**：
1. 点击 "创建技能"
2. 填写技能信息
3. 关联工具
4. 编写提示词模板
5. 保存

#### 8.2 工具管理

**内置工具**：
- 文件读取（file_reader）
- 代码执行（code_runner）
- 网络搜索（web_search）
- 数学计算（calculator）
- 100+工具

**测试工具**：
1. 点击工具的 "测试" 按钮
2. 输入测试参数
3. 查看执行结果

**创建自定义工具**：
1. 点击 "创建工具"
2. 选择工具类型：
   - 函数（JavaScript）
   - API调用
   - 命令行
3. 定义参数Schema
4. 编写执行逻辑
5. 测试和保存

#### 8.3 统计分析

**使用统计**：
- 技能调用次数
- 成功率
- 平均耗时

**依赖关系图**：
- 可视化技能和工具的依赖
- 识别核心工具
- 优化性能

---

## 🚀 高级功能

### 插件系统

**安装插件**：
1. 打开 "设置 > 插件管理"
2. 浏览插件市场
3. 搜索需要的插件（如"番茄钟"、"Markdown增强"）
4. 点击安装
5. 重启应用生效

**开发插件**：
参见 [插件开发指南](./PLUGIN_DEVELOPMENT.md)

### 多设备同步

**配置同步**：
1. 所有设备配置相同的Git仓库
2. 启用自动同步
3. 数据实时同步

**冲突解决**：
- 自动合并无冲突的更改
- 有冲突时弹出解决界面

### 备份与恢复

**自动备份**：
1. 打开 "设置 > 备份"
2. 配置备份：
   - 备份频率（每天/每周）
   - 备份位置（本地/云端）
   - 保留份数（如最近7份）
3. 启用自动备份

**手动备份**：
- 点击 "立即备份"
- 导出为ZIP文件

**恢复数据**：
1. 打开 "设置 > 备份"
2. 查看备份列表
3. 选择恢复点
4. 确认恢复（会覆盖当前数据）

### 快捷键

| 功能 | Windows/Linux | macOS |
|------|---------------|-------|
| 新建笔记 | Ctrl+N | Cmd+N |
| 保存 | Ctrl+S | Cmd+S |
| 搜索 | Ctrl+P | Cmd+P |
| AI助手 | Ctrl+Shift+A | Cmd+Shift+A |
| 切换侧边栏 | Ctrl+B | Cmd+B |
| 全屏 | F11 | Cmd+Ctrl+F |
| 设置 | Ctrl+, | Cmd+, |

完整快捷键列表：设置 > 快捷键

---

## ❓ 常见问题

### 1. 数据安全性如何保证？

- **本地加密**：SQLCipher AES-256加密数据库
- **传输加密**：TLS 1.3 + 端到端加密
- **硬件保护**：U-Key硬件加密（可选）
- **开源审计**：代码开源，可自行审计

### 2. 可以多人协作吗？

可以，通过以下方式：
- **Git同步**：共享Git仓库
- **项目分享**：生成分享链接
- **P2P协作**：添加好友，实时协作（开发中）

### 3. 支持哪些LLM模型？

**本地模型**（Ollama）：
- Qwen2 (7B/14B/72B)
- Llama3 (8B/70B)
- Mistral
- 等100+模型

**云端模型**：
- OpenAI (GPT-4, GPT-3.5)
- 阿里云通义千问
- 智谱GLM
- 百度千帆
- 等14+服务商

### 4. 数据存储在哪里？

- **默认位置**：`~/ChainlessChain/data`
- **可自定义**：设置中更改路径
- **完全本地**：不强制上传云端

### 5. 如何迁移数据？

**方式一：备份恢复**
1. 旧设备：设置 > 备份 > 导出
2. 新设备：设置 > 备份 > 导入

**方式二：Git同步**
1. 旧设备：配置Git远程仓库，推送
2. 新设备：配置相同仓库，拉取

**方式三：直接复制**
- 复制 `data` 文件夹到新设备

### 6. 支持移动端吗？

- **当前版本**：仅桌面端（Windows/macOS/Linux）
- **规划中**：移动端开发进度10%，预计v1.0发布

### 7. 是否收费？

- **个人版**：完全免费
- **企业版**：高级功能（如团队管理、审计日志）需付费
- **云服务**：可选购买云端LLM额度

### 8. 如何卸载？

**Windows**：
- 控制面板 > 程序和功能 > 卸载ChainlessChain
- 数据不会自动删除，手动删除 `~/ChainlessChain`

**macOS**：
- 拖动应用到废纸篓
- 手动删除 `~/ChainlessChain`

**Linux**：
```bash
sudo apt remove chainlesschain
rm -rf ~/ChainlessChain
```

---

## 🔧 故障排除

### 问题1：应用无法启动

**可能原因**：
- 端口被占用（5173）
- 数据库损坏
- 权限不足

**解决方法**：
1. 检查端口占用：`netstat -ano | grep 5173`
2. 重置数据库：删除 `data/chainlesschain.db`（会丢失数据）
3. 以管理员身份运行

### 问题2：LLM无法连接

**本地Ollama无法连接**：
1. 检查Ollama服务：`curl http://localhost:11434`
2. 启动Ollama：`ollama serve`
3. 拉取模型：`ollama pull qwen2:7b`

**云端API无法连接**：
1. 检查API Key是否正确
2. 检查网络连接
3. 查看控制台错误日志

### 问题3：同步失败

**Git推送失败**：
1. 检查网络连接
2. 检查仓库URL和认证信息
3. 查看Git日志：设置 > Git配置 > 查看日志

**冲突无法解决**：
1. 备份本地数据
2. 重置到远程版本
3. 手动合并内容

### 问题4：U-Key无法识别

**Windows**：
1. 检查驱动：设备管理器查看USB设备
2. 重新安装驱动：运行 `SIMKeySDK/driver-install.exe`
3. 换个USB接口

**macOS/Linux**：
- 当前仅支持模拟模式
- 硬件支持开发中

### 问题5：性能问题

**应用卡顿**：
1. 关闭不需要的功能（如自动同步）
2. 清理缓存：设置 > 高级 > 清理缓存
3. 减少开启的笔记数量

**LLM响应慢**：
1. 本地模型：检查GPU是否启用
2. 云端模型：切换到更快的服务商
3. 减少RAG检索数量

### 问题6：数据丢失

**紧急恢复**：
1. 查看自动备份：设置 > 备份
2. 从Git历史恢复：版本历史 > 恢复
3. 使用恢复密钥（如果有U-Key备份）

**预防措施**：
- 启用自动备份
- 配置Git远程仓库
- 定期导出重要笔记

---

## 📞 获取帮助

### 官方支持

- **文档中心**：https://docs.chainlesschain.com
- **GitHub Issues**：https://github.com/chainlesschain/desktop-app/issues
- **社区论坛**：https://community.chainlesschain.com
- **Email**：support@chainlesschain.com

### 社区资源

- **Discord**：https://discord.gg/chainlesschain
- **微信群**：添加客服微信 chainless-bot
- **Telegram**：@chainlesschain

### 贡献文档

发现文档错误或有改进建议？

1. 访问 GitHub仓库
2. 提交Issue或Pull Request
3. 我们会及时审核和更新

---

**文档版本**: v0.16.0
**最后更新**: 2026-01-03
**维护团队**: ChainlessChain Documentation Team

感谢使用 ChainlessChain！🎉
