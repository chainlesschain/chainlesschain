# ChainlessChain 对照 WikiSkill、Claude Code 与 Codex 的自进化差距与优化建议

> 审计日期：2026-09-01<br>
> ChainlessChain 仓库基线：`b7aab9d3c0f92ddcaaebcbad7ca3b24b14af9f14`<br>
> 产品版本基线：根项目 `5.0.3.54`、CLI `0.166.15`、Desktop `5.0.3-alpha.135`<br>
> WikiSkill 基线：arXiv `2608.27454v1`，2026-08-27<br>
> Claude Code 基线：`2.1.251`，2026-08-28<br>
> OpenAI Codex 基线：Codex CLI `0.151.0`，2026-08-29<br>
> 二次复审：2026-09-01，重点核对“可直接复用的本项目底座”、最新版本增量与论文结论边界<br>
> 增量复审：2026-09-04，仓库 HEAD `8f704b5425929e5c65afb9868de26b5bc78e1294`、CLI `0.166.21`；重点核对演化四阶段、WikiSkill 数值摘要与生产闭环剩余任务<br>
> 参考章节格式：[`CODEX_OPEN_SOURCE_GAP_ANALYSIS_2026-08-24.md`](./CODEX_OPEN_SOURCE_GAP_ANALYSIS_2026-08-24.md)

## 1. 结论先行

WikiSkill 对 ChainlessChain 最重要的启发，不是再增加一个名为“自进化”的模块，而是把现有学习零件收敛为一套**受控的软件发布系统**：

```text
不可变经验 Raw
    ↓
可纠错、可积累的 Wiki
    ↓
一次只改一个 Skill 的 candidate patch
    ↓
独立 Eval / 安全 / 权限门禁
    ↓
shadow / canary / active / rollback
```

其中，经验是证据，Wiki 是可维护的中间表示，Skill 是编译产物，Eval、权限和人工审阅是发布门。

当前 ChainlessChain 已有 Trajectory、Outcome Feedback、Reflection、Skill Synthesizer、Skill Improver、Eval、Rollout、Record & Replay、Memory、Hooks 和 Desktop Phase 20 等零件，但还没有形成完整的：

`Raw → persistent Wiki → candidate Skill → validation gate → promote / rollback`

这里的“没有形成”是指**尚无覆盖 Synthesizer、Improver、普通 Skill、CLI/Desktop/Graph 的 canonical 闭环**，不是说仓库完全没有生命周期原语。Record & Replay 已实现 `draft→approved→validated→enabled→revoked` 的窄域纵切，Plugin runtime 也已有不可变版本、active pointer 与事务恢复；正确方向是推广和统一这些实现，而非从零开始。

二次复审后，最值得本项目借鉴的并不是再造新基础设施，而是复用现有底座补五个“连接件”：

1. 在现有 [`CAPABILITY_MANIFEST`](../packages/cli/src/lib/capability-manifest.js#L32) 声明 evolution capability ID/静态 gate，再由 `cc agent --capabilities` 输出带 manifest digest 的运行时 status projection，如实公开 `implemented/wired/verified/defaultEnabled/mutationScope/lastEvidence`；不要另建第二套 capability 清单，也不要把易变证据写进静态 manifest digest。
2. 把 Record & Replay 已有的 approval/replay/CAS 生命周期与 Plugin runtime 的不可变版本、active pointer、journal/recovery 抽成通用制品激活端口，不从零重写 Skill Registry 事务。
3. 复用 [`GoalConditionEngine`](../packages/cli/src/lib/goal-condition-engine.js#L301) 的确定性完成条件、token/cost/time/turn 预算，以及现有 worktree/checkpoint，先做“单候选→评分→保留更优→预算停止”的离线纵切闭环。
4. 修复现有 Desktop Skill metrics 接线，并结合 CLI name-level attribution 补 `SkillInvocationReceipt`，把 Skill digest、路由理由、模型/工具/环境/权限指纹和真实 outcome 连接起来；否则无法判断“用了哪个 Skill”是否真的改善结果。
5. 把模型切换、工具结果进入模型前的投影、基础设施故障分类设为证据边界，防止混合模型归因、工具注入和瞬时 MCP/权限失败被错误编译成 Skill 规则。

这五项的投入产出比高于先建设完整 Wiki UI 或新的“自进化”服务；其中第 1、2、5 项属于自动 mutation 开启前的 P0，第 3、4 项属于最小可用闭环的 P1。

源码审计显示，当前被“自进化”命名覆盖的实现实际分为三类：

1. **指标或演示壳**：按公式递增 accuracy、返回固定预测或状态字符串，并没有训练模型或进化 Skill。
2. **尚未接入生产主链的真实组件**：例如 `SkillImprover`，静态引用扫描只命中定义，没有找到生产调用点；历史 CLI learning hooks 已因相同问题且绕过认证 ingress 而退役。
3. **接入后可能产生高风险副作用的 writer**：若 `SkillImprover.skillsDir` 指向当前运行时 Skill 树，它会在缺少 candidate/Eval/promotion 语义时原地覆盖 `<skillsDir>/<name>/SKILL.md>`。当前未找到生产构造点，且 CLI loader 在后续 materialize 时有 digest drift/再授权防线；这降低了静默执行风险，但没有补上 mutation-time gate。

这里的优先级按“何时成为发布阻断”定义：当前高风险 writer 尚未接入主链，因此 P0 主要是**启用任何自主 active Skill mutation 之前必须关闭的门禁**；P1 是闭环主能力；P2 是闭环稳定后的产品化与规模化。建议顺序为：

- **P0：先保证真实、安全、可恢复。** 冻结任何未经门禁的 active Skill 自动写入，消除幻影成功，建立 candidate、独立验证、原子晋级、回滚、可信审计和演化数据安全边界。
- **P1：再建立统一演化内核。** 形成 canonical Raw/Wiki/Skill 三层控制面，统一 CLI、Desktop、Graph、Eval、Memory 与 Skill Registry，补齐模型与运行环境兼容矩阵。
- **P2：最后扩展产品体验和规模。** 建立演化工作台、Skill 检索路由、跨设备团队知识、跨模型来源/目标适配和长时在线适应。

在 P0 门禁关闭前，不应把当前 `learning synthesize`、`SkillImprover`、CLI/Desktop `self-evolving` 指标壳描述为生产级自主进化。

### 1.1 当前实施完成情况（截至 2026-09-04）

本节记录本报告转入实施后的当前状态，实施基线截至提交 `4381aece2c`。状态采用两层口径：**“已提交”只表示一个可独立审查、已验证的基础批次完成，不等于对应路线项已经达到第 9 节的生产验收标准**；只有剩余项全部关闭后，路线项才可标记为“完成”。

2026-09-02 已提交窄纵切 `881abf6090`：类型化 matrix receipt envelope 只携带有界 `receiptDigest`，`SkillPromotionController.promoteEvaluated()` 在消费 mutation authority、创建 release prepare 或改写 active state 之前，使用独立 verifier 校验完整 signed matrix receipt，并把 candidate content、dependency lock、runtime manifest、target matrix、active digest/revision 和 `accepted` decision 绑定到同一次晋级。既有 release intent 继续通过 `evalReceipt` digest 固定这份证据。

同日提交 `1a70a880fa` 补齐 typed、digest-bound receipt-resolution 端口：evaluated promotion 不再接受调用者直传完整 matrix receipt，而是用授权 envelope 中的 tenant 与 `receiptDigest` 向声明为 trusted 的类型化 resolver 取回证据；resolver authority/revision/handler digest、tenant、receipt digest 和规范时间戳必须完全匹配，之后仍须由独立 verifier 对 receipt 做密码学认证。该批次只建立 fail-closed resolver 契约与连接点，不把 resolver 的自声明描述符等同于密码学 attestation，也不代表已有真实跨进程持久存储 adapter 或持久 child-receipt resolver。

提交 `0feb536d82` 继续关闭 production composition 的直接绕过面：resolver 与独立 verifier 被收敛到构造期创建的 branded evaluated-promotion provider，`SkillPromotionController` 新增显式 `evaluated-only` 策略；启用该策略后，普通 `promote()` 会在 mutation authority consume、ledger prepare 和 active state 修改前失败关闭，`promoteEvaluated()` 也不再接受逐调用替换 resolver/verifier。该批次提供的是可被生产 adapter 强制选用的组合原语；仓库尚未发现真实生产构造点，因此不能宣称所有生产入口已经完成切换。

提交 `face4e9250` 新增 attested、bounded、freshness-checked durable retain/resolve 生命周期适配器：它在 Eval 后向构造期捕获的外部 durability authority 提交完整 matrix receipt，并要求 retain acknowledgement 与后续 resolution 同时满足 `authenticated=true`、`durable=true`，且完整绑定 authority/revision/handler digest、tenant、matrix receipt digest 和独立 persistence/resolution receipt digest。上述字段被规范 payload digest 与独立 attestation trust/verifier 共同承诺；retain/resolve 受构造期最大操作时限约束，超时会触发 AbortSignal；签名时间还必须落在可信时钟、最大证据年龄和固定时钟偏差构成的窗口内。密钥轮换采用一个 active trust 加至多 8 个有界 grace trust，所有 keyId 全局唯一，旧密钥只在显式 `notAfter` 窗口内可用。重新创建 adapter 后可通过同一外部 authority 恢复 receipt，非 durable 确认、receipt substitution、无效签名、过期重放、过期 grace key、重复 keyId 或永久悬挂均不能进入 promotion provider。该适配器不自建第二套文件存储，也不把测试 HMAC/shared backend 等同于生产 PKI、数据库或 WORM；协作取消也不等同于进程级 hard kill。真实跨进程后端、密钥生命周期运营和故障恢复 E2E 仍未完成。

提交 `3b76883a81` 进一步补上 authority attestation 撤销基线：精确描述符必须提供正整数 revision 与至多 64 个唯一 revoked keyId；当前 active key 已被撤销时 adapter 拒绝构造，grace key 即使仍在 `notAfter` 窗口内也会以独立 `REVOKED` 结果失败关闭。revocation revision 会传入构造期捕获的 verifier，避免 verifier 在不知道撤销快照版本的情况下给出认证结论。该批次只建立 fail-closed 数据模型与组合契约，不宣称已经具备在线 CRL/OCSP、撤销发布认证、跨进程刷新、应急轮换或生产演练。

提交 `23125c71e4` 同时纳入 narrow evaluated-promotion control-plane facade：构造时必须提供 branded evidence provider，并强制创建 `requireEvaluatedPromotion=true` 的 controller；返回面只暴露 `promoteEvaluated()`、授权 `rollback()` 及不可变 provider identity，不暴露 direct `promote()` 或底层 controller。该原语减少了生产 adapter 误用宽控制器表面的机会，但仓库仍没有真实生产构造点，因此不能把“安全装配入口已可用”改写为“生产入口已切换”。

提交 `23125c71e4` 合并关闭 EVO-P0-1 truth cutover、EVO-P0-2 transaction acceptance 与上述受限 evaluated-promotion facade：正式 learning/evolution/Desktop 表面不再报告 phantom success；Candidate/Promotion/Rollback 通过 100 并发单赢家、五阶段 crash/recovery、真实子进程重启和 `<60s` 字节级回滚验收。提交 `be547ba42f` 随后关闭 EVO-P1-8 SkillInvocationReceipt 跨端纵切：canonical schema 移入共享 `session-core`；Desktop Registry→BaseSkill→MetricsCollector 将 receipt 持久到数据库 `context_json`；CLI `run_skill` 将同一 receipt 随 tool result 持久到 transcript；automatic-candidate/canary 缺完整归因字段会在执行前失败关闭；共享 verifier 与 trace projection 可确定性回答选择、路由、环境、判定和成本。

2026-09-03 的后续持久化审计纠正了上述 CLI 证据边界：`be547ba42f` 已证明 receipt 随内存中的 tool result 返回，但当时 canonical compact transcript writer 并未保存该字段。当前修正批次让 JSONL store 在 append 前调用共享 verifier，且只允许 receipt 绑定 `run_skill`；REPL、普通 headless、stream headless 与 canonical WebSocket 四类正式驱动（含嵌套 tool settlement）均把同一 receipt 写入 hash-chain `tool_call` event。REPL 遇到带 receipt 的持久化失败不再按 legacy telemetry 静默忽略。真实 JSONL 与四入口定向回归 4 文件 **124/124** 通过，因此“CLI transcript 持久化”现已有直接证据，而非由 tool-result 内存形状间接推断。

提交 `4381aece2c` 继续关闭 EVO-P1-9 candidate-only 有界评分循环：固定 baseline/split/grader/runtime/gate/root budget 后，每轮只接受一个候选，按确定性 grader→隔离 evaluator 顺序执行并持久确认 receipt；best 只有在预注册 gate、根预算及 active-state 不变式同时通过后才更新。provider/MCP transient、sandbox/evaluator crash 与 permission/policy denial 被明确归类且默认不记为 Skill 负样本；未知证据、receipt 未持久确认和 active 漂移均失败关闭。循环复用 `GoalConditionEngine` 的预算和 snapshot/restore，支持确定性 round key 与恢复重放，不接受 active/promotion writer。

本批次完成 EVO-P1-3 Wiki-informed Single-Skill Proposer：控制面先按固定顺序只读 Wiki index、Skill impact、active Skill digest 与训练摘要，证据矛盾或样本不足直接 `needs-evidence`；模型只能按显式请求选择性追加 pattern/raw，且最多重试一次。通过校验的 proposal 必须固定为单一 Skill，携带等价 `PURPOSE.md` 结构、digest-bound pattern/source lineage、适用/禁用条件、失败反例、回退、验证、requested capabilities、target runtimes、上下文预算和 candidate-root 内 machine diff，最后只写 candidate sink。

本批次继续实现 EVO-P1-2 Evidence-backed Wiki Maintainer 的仓库控制协议：新增 tenant-scoped、digest-bound Wiki state/revision/evidence schema，Maintainer 构造期权限固定为只读 trusted projection 与写 Wiki revision，显式拒绝 raw evidence、active Skill、shell、network 和 secret 权限。确定性 reducer 已覆盖 pattern/index/evolution-log/skill-impact/evidence reverse-dependency，支持等价 cluster 去重、同 kind merge、counterevidence/contradiction、半衰期 confidence decay、expiry/stale、revoke/tombstone 以及 proposal decision impact；单一 model inference 只能保留为不可操作 hypothesis，只有多 trust-domain 来源或真实 grader receipt 才能使 procedure 进入 corroborated/actionable。proposal rejection 追加 impact、降低 operational confidence 并保留历史，不回滚 Wiki revision；decision 字段必须与认证 receipt digest 精确绑定，禁止替换 candidate/outcome/pattern/reason。持久端口使用 expected state digest 做 CAS，并要求 acknowledgement 精确回绑 revision/state digest 与 canonical EvolutionRun ID。13/13 定向测试通过。该批次尚未提供真实跨进程 Wiki authority/ArtifactStore+Ledger adapter、并发 writer/restart/迁移验收和真实 ingress，因此路线项保持“部分完成”，不把端口 mock 当作持久生产纵切。

后续批次新增 `WikiMaintainerLedgerAdapter`，不自建第二套文件存储：完整 Wiki revision 以新增的有限产品类型 `wiki-revision` 写入现有 `EvolutionArtifactPorts`，且只有 `evolution-ledger` purpose 可取得 ledger retention；随后用现有 `EvolutionLedger.appendDomainEvent()` 的 head/sequence CAS 追加 `wiki.revision.committed`，事件 subject 精确绑定不可变 artifact ref，前一 revision ref 进入 source lineage。读取时只接受 ArtifactPorts 工厂产生的 branded read-only resolver，并复核 durable record、artifact tenant、audience、purpose、revision/state/EvolutionRun digest；伪造 resolver 在读取前即被拒绝。响应在 ledger append 后丢失时，相同 revision 可从持久事件恢复为幂等成功；同一 Wiki baseline 的不同并发 revision 只能有一个提交。新增真实组合测试让 revision 经过实际 EvolutionLedger 文件、签名/witness 与 ArtifactStore 后，由全新的 Ledger/adapter 实例恢复同一 state digest。适配器 6/6、Maintainer 13/13、真实 ArtifactPorts 25/25 和 EvolutionLedger 45/45 通过（另 1 项按平台条件跳过）；结合 Ledger 既有进程退出/重启与并发验收，本路线项在仓库内可独立关闭。真实 Agent evidence ingress/生产切换归 EVO-P1-6，通用旧账本迁移归 EVO-P0-5，敏感 evidence retention/crypto-shredding 归 EVO-P0-4/P2-6，不再在本项重复挂账。

本批次开始实现 EVO-P1-7 结构化 Memory 与权力分离：共享 `session-core` 新增 canonical append-only memory event/projection/snapshot，明确 `episodic/semantic/procedural/policy` 四层。episodic 只允许首次追加或 governor tombstone；子 Agent/Proposer 的 semantic 事实只进入带 evidence 的提炼队列，必须由独立 critic+evaluator receipt 和 governor 才能接受，且接受时不可替换 content/artifact/evidence lineage；procedural 只接受 promotion-controller 与 promotion receipt；policy 只接受非自动 human governor 与 policy receipt。memoryId 不得跨层复用，metadata 拒绝 raw content/secret，增量投影 state 必须由上一 projection digest 绑定。运行时 append 不再信任调用者自报 actor/role，只消费 session-core 工厂签发的 tenant-bound branded authority，伪造或跨 tenant authority 在 persistence 前失败。Compaction 强制保留 requirements、decisions、open risks、failed attempts、tests、goal state、delegated tasks 和 memory lineage；`PostCompact` verifier 或持久确认失败/抛错均保持并返回上一快照。后续持久批次新增 `StructuredMemoryLedgerAdapter`：event/snapshot 分别作为有限 `structured-memory-event`/`structured-memory-snapshot` 类型写入真实 `ArtifactStore`，只允许 `evolution-ledger` purpose 使用 ledger retention，再以 head/sequence CAS 追加类型化 Ledger event；读取只接受 ArtifactPorts branded resolver，并复核 durable record、tenant/audience/purpose/type 与 subject ref。相同 event 在 append 响应丢失后可恢复为幂等成功，不同并发 event 不能占用同一 memory sequence；新控制面实例会校验 event/snapshot digest、边界 event root 和 projection digest 后恢复。控制协议 14/14，adapter 4/4，连同 ArtifactPorts 共 29 通过、1 项按平台条件跳过。critic/evaluator/promotion/policy receipt 的 branded resolver 与真实 PostCompact hook 的 production composition 接线仍未完成，因此 P1-7 保持部分完成。

后续验收批次把该 adapter 接到实际 `EvolutionLedger` 文件、签名与 durable witness，而不是只使用端口级 fake：event 与 snapshot 写入后，重新创建 Ledger 和 Memory adapter，可恢复完全一致的 projection/snapshot，并验证两个类型化 domain event 的 sequence/head。Memory adapter 5/5 通过。由此关闭 P1-7 的实际 Ledger 文件/witness 专项 reopen 缺口；剩余仅为四类 receipt 的 branded resolver 和真实 PostCompact hook/production composition。

后续权力分离批次不再允许运行时调用者直接提交 critic/evaluator/promotion/policy digest：新增 tenant-bound branded receipt provider，构造期固定 resolver、独立 verifier 与 authority id/revision/handler digest；按 transition 所需的精确 receipt kind 集合解析完整 receipt，并在持久化前复核 accepted decision、tenant、memory/layer/action、content/artifact/evidence 全绑定。PostCompact 同样改为 branded verifier，必须调用构造期固定 hook，并校验 authenticated result、authority identity、candidate/projection/previous snapshot digest、decision、时间与独立 attestation；普通返回 `true` 的伪 hook 不再可装入控制面。`StructuredMemoryLedgerAdapter` 在构造期同时固定这两个 provider，`createMemory()` 不暴露逐调用替换点。控制协议 19/19、`session-core` 全量 538/538、adapter 6/6 通过。真实 receipt resolver 后端、真实 CLI/Desktop PostCompact hook adapter 及最终 Agent production composition 仍未接线，因此继续保持部分完成。

后续真实 authority 批次先为 receipt 增加 canonical content-addressed digest 与 issuer id/revision/handler 绑定，再新增 `StructuredMemoryAuthorityLedgerAdapter`：完整 critic/evaluator/promotion/policy receipt 进入有限 `structured-memory-authority-receipt` ArtifactPorts ledger-retention 类型，以类型化 Ledger event 建立 digest→不可变 subject 索引；retain 与 resolve 均执行独立签名 verifier，响应丢失可按同一 digest 恢复。实际 Ledger 文件/签名/witness 测试覆盖 receipt→semantic proposal acceptance→snapshot→全新 authority/memory adapter 恢复。CLI `PostCompact` adapter 已等待现有 Hooks V2 的真实 decision-capable producer；零 hook、block、失败 result 均拒绝，成功结果固定 hook outcome digest 并由构造期 attestor 签名后才能更新快照。Memory/authority adapter 8/8、PostCompact adapter 5/5、ArtifactPorts 25/25 通过，另 1 项按平台条件跳过。剩余为实际 critic/evaluator/promotion/policy producer 写入、Desktop PostCompact adapter 与最终 Agent production composition。

Desktop 后续批次复用同一 session-core branded PostCompact 契约，并接到现有 `HookSystem.trigger("PostCompact")`：完整等待所有匹配 hook，要求至少一个 hook、执行数等于注册数、无 prevent/error，结果摘要与 candidate/projection/previous snapshot digest 一并由构造期 attestor 签名；disabled/零 hook、prevent、error 和不可信签名均失败关闭。Desktop adapter 5/5 通过。由此 CLI/Desktop hook adapter 均已形成；P1-7 余项缩减为四类实际 producer 写入与最终 Agent production composition。

后续 producer 批次先关闭 promotion 写入：新增 branded `StructuredMemoryPromotionReceiptWriter`，并由真实 `SkillPromotionController` 在 `SkillReleaseRegistry` 已完成 CAS、prepare/finalize 与 durable transition receipt 后调用。writer 重新计算 release transition receipt digest，复核 tenant/Skill/active release/state/content 绑定；evaluated 路径还要求 matrix binding 与 transition receipt 中的 eval digest 完全一致。生成的 procedural Memory receipt 固定 release content/ref、release receipt 与 matrix receipt evidence，由构造期 attestor 签名并交现有 authority ledger 持久确认；伪造 writer 在 mutation 前拒绝，伪造 transition receipt 不会进入 authority store。PromotionController 与 Memory ledger 联合回归 26/26 通过。该写入发生在 release commit 之后，若 authority ledger 返回未知结果，调用方会收到失败且可按同一 canonical digest 重试恢复，不会伪报 Memory 成功；跨 release/Memory 的最终编排与 reconciliation 仍归 Agent production composition。P1-7 余项缩减为 critic/evaluator/policy 三类 producer 写入与最终 Agent production composition。

后续 producer 批次关闭 policy 写入：新增 branded `StructuredMemoryPolicyReceiptWriter`，由真实 `RemoteApprovalBridge.consumeAuthorization()` 在 coordinator 已线性消费 lease、`DurableRemoteMembershipHostStore` 已验证并持久采用 signed `lease.consumed` statement 后调用；同步响应与事件监听器竞速时，只接受同一 durable statement 的 first-adopt/replay 二选一结果。writer 复核 consumed status、request/fingerprint/approval binding 和 host statement/receipt hash，固定完整 operation tuple 的 content digest，以 coordinator statement 为 artifact ref，由构造期 attestor 签名并经 authority ledger 持久确认；未品牌化 writer 在连接前拒绝。既有 `consumeAuthorization() === true` 契约保持不变，Memory 写入失败则不放行工具 dispatch。真实 loopback WebSocket、coordinator、host fsync store 联合回归连同 decision/state failure 共 17/17 通过。P1-7 余项缩减为 critic/evaluator 两类 producer 写入与最终 Agent production composition。

后续 semantic producer 批次新增 `StructuredMemorySemanticReviewPipeline`：child/proposer 先通过共享 Memory append `semantic/propose`，pipeline 从持久投影读取且不允许替换 content/artifact/evidence，再并发调用 branded critic 与 evaluator。两类 reviewer 的 producer/attestor/verifier authority id 与 handler digest 四方必须全部互异；只有各自返回 accepted、canonical receipt attestation 经独立 verifier 认证、且两份 receipt 均获 authority store durable acknowledgement 后，固定 governor authority 才以精确 receipt refs append `semantic/accept`。任一 reviewer 拒绝、认证失败或持久确认缺失均不追加 accept，队列继续保持 proposed；已持久的单边 receipt 只是不可消费的孤立证据，不会越权激活 Memory。Semantic pipeline 与共享 Memory/authority resolver 联合回归连同 Ledger adapter 共 12/12 通过。至此四类 producer 写入路径均已形成，P1-7 只剩最终 Agent production composition 与其真实后端配置/恢复验收。

后续 composition 批次先把 policy 路径接入真实 Agent 启动链：`AgentRuntime` 构造期只接受 branded `StructuredMemoryPolicyReceiptWriter`，并沿 `startAgentRepl → startHeadlessRemoteApproval → RemoteApprovalBridge` 原样传递，不进入可序列化 policy/config、UI 或日志。未配置时保持既有行为，伪 writer 在 REPL/网络启动前失败；配置后 remote approval 的工具 dispatch 必须等待 policy receipt 持久确认。Runtime、remote bootstrap 与真实 WebSocket 联合回归 28/28 通过。仓库当前没有可复用的生产 EvolutionLedger/ArtifactPorts 密钥和独立 witness 构造根，本批次没有用测试 HMAC 补默认值；P1-7 仍需把恢复后的统一 control plane 接到 semantic/procedural Agent 入口，并完成真实密钥、witness、重启与 reconciliation 验收。

后续 composition-root 批次新增 branded `StructuredMemoryAgentControlPlane`：构造时必须同时提供实际 `StructuredMemoryLedgerAdapter` 与 `StructuredMemoryAuthorityLedgerAdapter`，并要求 tenant、artifact tenant、stream、audience、purpose 完全相同；critic/evaluator、proposer/governor authority 以及 promotion/policy writer 只能一次性固定，writer tenant 不一致即拒绝。root 在暴露任何 producer capability 前调用 `createMemory()` 完成 event/snapshot 权威恢复，再以该同一 Memory 和 authority store 构造 semantic pipeline；输出只包含恢复摘要和四个窄控制面，不暴露底层 ledger/artifact ports。联合测试让 proposal、两份 authority receipt 与 accept event 进入同一 ArtifactStore+Ledger stream，随后重建 authority adapter、memory adapter 与 control root，恢复 sequence 2、active semantic memory 和精确 receipt refs；相关 Memory/Semantic 联合回归 13/13 通过。剩余为把完整 branded root 接入 AgentRuntime 的 semantic/procedural 使用点，以及生产密钥/witness 与进程重启/reconciliation 验收。

后续 Agent-root 接线批次让 `AgentRuntime` 构造期直接捕获完整 branded control plane，并从其中派生唯一 policy writer；禁止同时配置 standalone writer，避免两个 authority stream 分叉。`startAgentSession()` 将同一对象传入 REPL，REPL 在创建 session host lease、预算根或网络服务前再次验证品牌，并拒绝与 root 内 writer 不同的 policy capability；remote approval 因而继续使用 root 所属 authority stream。测试使用上批实际 adapter 恢复出的 control root 启动 AgentRuntime，并验证伪 root、双 writer 配置和 REPL 直接注入均失败关闭。完整 root 的 semantic/procedural 使用点仍未触发，生产密钥/witness 与进程级恢复也仍待完成。

后续 Agent-use 批次为 `AgentRuntime` 增加窄的 `proposeSemanticMemory()` 与 `reviewAndAcceptSemanticMemory()`，二者只能调用构造期 branded root；联合测试从已恢复 sequence 2 的实际 runtime 追加第二条 proposal、通过独立双 receipt 门并把同一持久流推进到 sequence 4。procedural 侧不向 Agent 暴露 writer，而由 root 的 `createEvaluatedPromotionControlPlane()` 强制注入自身 promotion writer 并继续返回 evaluated-only facade；调用方只要携带 `memoryPromotionReceiptWriter` 覆盖字段即在构造 controller 前拒绝。由此 semantic/procedural Agent 使用面已建立；当时尚缺 root 路径下完整 evaluated promotion 的端到端执行，以及生产密钥/witness、真实进程退出重启与跨 release/Memory reconciliation 验收。

后续 durable-witness 批次将 EvolutionLedger 测试中已验证的文件 witness 语义提炼为正式 `createEvolutionFileWitness()` 端口：签名和验证必须由外部同步 authority 注入，模块不持有 ledger key 或内置共享秘密；owner-only 目录/文件、严格跨进程锁、单调 CAS、完整认证 history、discard accumulator/fence、临时文件 fsync→原子 rename→目标文件及父目录 fsync 共同构成持久边界。store/record/signature/discard 使用精确 schema 与容量上限，genesis、状态字段、history 连续性、当前指针、ancestry checkpoint 和重复 discard 均失败关闭。正式端口已通过 6/6 专项测试，并替换一条真实 EvolutionLedger 崩溃恢复用例，证明新实例能读取 fence 且拒绝已丢弃孤儿尾复活；EvolutionLedger 全量连同专项共 51/51 通过。该实现关闭本机独立密钥 durable witness 缺口，但 signer/verifier 的生产密钥托管和跨主机/远程 witness fault domain 仍需部署配置，不能以测试 HMAC 代替。

后续 evaluated-promotion execution preflight 批次修正了此前唯一 accepted matrix fixture 的不可能状态：`baselineReleaseDigest=null` 不再同时宣称非空 active content 和 revision 7，而可表达 PromotionController 实际接受的空 active CAS（`EMPTY_SKILL_ACTIVE_DIGEST`、revision 0）。同一测试中的两个真实隔离 Gate cell 先生成并验证 signed conjunction receipt，再由 branded durable resolver/provider 重新解析，进入 evaluated-only control plane；真实 `SkillMutationAuthority` 完成 authorize/nonce/audit/consume，一次性 registry transition capability 由绑定 registry 消费，且 facade 不暴露 direct `promote`。Matrix 全量 3/3 通过。该批已证明 accepted evidence 不只停留在 parser/provider，但最终验收仍须把 transition 换成真实 `SkillReleaseRegistry` prepare/finalize，并证明 Agent root 强制注入的 promotion writer 将 procedural receipt 写回同一 Memory/authority stream。

后续 evaluated-release execution 批次将上述 transition 换成真实 `SkillReleaseRegistry`：canonical candidate 与 matrix plan 共用 dependency lock/runtime manifest/target matrix，accepted receipt 绑定实际 candidate/content digest；promotion 经真实 prepare/finalize ledger projection、active pointer CAS 和全新 registry 实例 reopen 后仍恢复相同 release/state。该组合首次发现并修复真实阻断：release receipt 的 `receiptDigests.eval` 保存的是 canonical matrix envelope 的 mutation-domain digest，而 Memory promotion writer 错把它与 matrix receipt 本体 digest 直接比较，导致 release 已提交后 procedural receipt 必然失败。writer 现重建同一 canonical envelope 后计算相同域摘要再校验，同时让 procedural receipt evidence 保留 release transition receipt 与 matrix receipt 两个原始 digest。TargetMatrix/Promotion/Memory 受影响三组 30/30 通过。剩余窄缺口为由实际 `StructuredMemoryAgentControlPlane`/`AgentRuntime` 调用同一路径，并证明 root 强制注入的 writer 将 receipt 写入同一持久 authority/Memory stream，而非测试内独立 authority store。

后续 Agent-root promotion completion 批次关闭上述窄缺口：shared session-core 新增 branded authority capture，composition root 构造期要求 tenant-bound `promotion-controller/service`，伪造、跨 tenant 或错误角色在暴露 producer capability 前失败。`AgentRuntime.createEvolutionPromotionControlPlane()` 返回的 evaluated-only facade 在真实 release commit 与 authority receipt durable ack 后，自动以固定 promotion authority 追加 `procedural/accept`；事件的 content/artifact/evidence/receipt ref 全部从已认证 promotion receipt 派生，调用方不能注入 digest。组合测试贯穿两个真实隔离 Gate cell、signed matrix receipt、durable resolver/provider、mutation authority、真实 `SkillReleaseRegistry` prepare/finalize/CAS/reopen、root-owned promotion writer、共享 ArtifactStore/Ledger 及 procedural projection。测试还模拟 Memory ledger 已提交但响应丢失：调用返回 `CC_PROMOTION_MEMORY_COMMIT_PENDING` 和 `release-committed-memory-pending`，随后 `recordPromotionMemory()` 仅凭已持久 receipt 幂等恢复；全新 root 能恢复同一 active procedural memory，再次 reconciliation 返回 recovered 而不重复追加。CLI 受影响四组 39/39、session-core 专项 20/20 通过。仓库内 Agent composition 已闭合，剩余是生产 signer/verifier 密钥托管、跨主机 witness fault domain，以及真实 OS 进程退出/重启下的 release/Memory reconciliation 演练。

后续 file-backend composition 批次新增正式 `createEvolutionLedgerFileBackend()` 构造根：ledger 与 witness 的 signer/verifier 继续只以外部端口注入，强制使用不同 signer、verifier、keyId、trust policy，并要求 event、authority 与 witness 路径互不重叠；返回的 branded descriptor 不暴露签名端口。专项测试通过两个顺序启动、PID 不同的真实 Node 进程重开同一 ledger identity、epoch 与 durable witness；随后仅删除本地 event/authority 根，第三个进程会因仍在的 witness 以 `CC_EVOLUTION_LEDGER_CORRUPT` 拒绝账本“转世”。新增 unit/integration 共 3/3 通过。该批次关闭本机 production-style 构造端口与通用 Ledger OS 进程重启证据；测试 HMAC 仅存在于 fixture，不能替代生产密钥托管，也未证明跨主机 witness fault domain 或完整 release/Memory 的 OS 进程 reconciliation。

后续 promotion-reconciliation 批次移除跨进程恢复对进程 A 内存中 `promotionResult` 的依赖：`StructuredMemoryAuthorityLedgerAdapter.listReceipts()` 只枚举指定 kind 的同 tenant Ledger events，逐项重新解析 ArtifactStore durable record、复核 canonical receipt digest，并以既有独立 verifier 按 resolve 路径重新认证；Agent root 的 `reconcilePromotionMemories()` 仅消费这些已认证 promotion receipts，以 receipt digest 派生确定性 event id 和 metadata，缺失时补写，已存在时核对完整 content/artifact/receipt 绑定后返回 recovered。原 accepted matrix→真实 release CAS 测试现改为 Memory event pre-commit 失败，证明 release 与 authority receipt 已提交后，全新 root 可自行发现并补齐，而非由调用者重传结果。新增三 PID 集成用例进一步让进程 A 写 durable promotion receipt 后退出、进程 B 从真实 ArtifactStore+Ledger+file witness 补写 procedural Memory、进程 C 重开验证幂等且 sequence 保持 1。受影响四组 31/31 通过。剩余 OS 演练已收窄为让进程 A 在同一场景中实际执行完整 release producer 后硬退出；生产密钥托管/部署和跨主机 witness 仍是外部部署边界。

最终 process-kill 批次关闭该窄缺口：集成测试不再用合成 receipt 启动进程 A，而是在独立 Vitest/Node 进程内复用既有两-cell 真实 Gate、signed matrix receipt、mutation authority、`SkillReleaseRegistry` prepare/finalize/CAS 和 root-owned promotion writer；只有 release 与 authority receipt 已 durable commit、Memory event 明确 pre-commit failure 后才发布同步 marker，父进程随后强制终止整个 threads-pool 进程。全新进程 B 不读取 marker 中的授权数据，仅从真实 ArtifactStore+EvolutionLedger+file witness 枚举、解析并重新认证 receipt 后补写 procedural Memory；进程 C 再次重开得到 recovered 且 sequence 仍为 1。process-kill 1/1 及正常 accepted-matrix 路径均通过。至此 EVO-P1-7 的代码、确定性恢复和本机进程级演练已达到仓库闭环；生产 signer/verifier 密钥托管/部署与跨主机 witness fault domain 必须由目标环境提供，继续作为部署发布门而不是用测试 HMAC 伪装成仓库交付。

后续 evidence-artifact composition 批次把 P0-4 既有安全协议接到真实持久边界：`ArtifactStoreEncryptedRawStore` 只把 canonical plaintext 交给构造期捕获的外部 encryptor，要求其返回 tenant-scoped KMS ref 与 AES-256-GCM sealed bytes，ArtifactStore 仅接收密文、cipher digest 和无敏感内容的 lineage，并在返回 receipt 前完成 immutable publication 与字节 readback。`EvolutionEvidenceArtifactAdapter` 先运行既有 source verification、keyed commitment、storage policy、secret/PII redaction、prompt-injection 检测、trust/quarantine 和 attestation，再由独立 bundle verifier 复核；随后分别写入 raw metadata、model-visible projection、trusted projection 与 projection receipt，最后才发布包含四个认证 envelope/ref/digest、source commitment、ruleset digest 和 attestation 的 derivation manifest 作为提交点。真实 ArtifactStore 集成用例证明 Raw 文件中不存在 secret/PII 明文但可由测试 KMS authority 正确解密，五个 projection artifacts 中不存在 canary，新 adapter 实例可逐项认证恢复，注入证据保持 quarantined，manifest digest 替换失败关闭；与 ArtifactPorts 联合回归 72 通过、1 项按平台条件跳过。该批关闭仓库内双层持久化、脱敏/quarantine 与完整 projection derivation manifest 缺口，但真实 Agent/tool pre-model ingress、生产 KMS/HSM、删除/撤销传播、Candidate realpath/symlink/capability 安全、人工 quorum 和 1,000 条生产攻击轨迹仍未验收，故 P0-4 继续保持部分完成。

后续 candidate plaintext guard 批次复用同一 evidence redaction policy，而不是在 Registry 内维护第二套弱正则：projector 导出只读的 `assertEvolutionContentContainsNoKnownSecrets()`，除原 secret/PII 规则外还以 NFKC/default-ignorable 清理和 confusable skeleton 再检查一次；`SkillCandidateRegistry` 的 canonical `normalizeContent()` 在 candidate digest、临时文件和最终文件产生前调用该门。因而 Wiki、record-replay 和 manual-import 共用的唯一 `create()` 路径均不能把 credential、email/phone/government-id/payment-card 或 Unicode 混淆秘密写入 SkillCandidate 明文。新增真实 Registry 负测连续提交 password、email 与全角 password 三个 canary，均返回 `SKILL_CANDIDATE_SECRET_LEAK` 且候选列表保持空；Candidate+Projector 74/74 通过，提交为 `f668598a42`。既有 Registry 已覆盖 strict schema、kebab-case、1 MiB content/2 MiB artifact 上限、realpath containment、non-symlink、single-link、descriptor readback，因此这些不再重复列为 P0-4 未实现；仍缺的是语义恶意指令审查、相对 parent 的 capability/permission diff 与人工 quorum。

后续 human-review promotion 批次把 capability diff 与 quorum 接入真实 active mutation，而非只生成一个 UI DTO：`buildSkillPromotionReviewPacket()` 从认证 Candidate、当前 active release/state 和已验证 matrix binding 确定性生成 evidence 摘要、完整 candidate unified replacement diff、parent-relative capability added/removed/retained/high-risk、Eval receipt 摘要与 target runtimes，并以 packet/candidate-diff/capability-diff 三个 digest 固定。branded `SkillPromotionReviewProvider` 只解析 mutation request 中同一个 canonical `policyReceipt` envelope，经 durable resolver 取得完整 decision，要求 `automated=false`、唯一 human reviewer、签名 verifier、短时效和 exact candidate/packet binding；任何 capability 增量一律要求至少两名不同真人，无增量仍要求至少一名。Agent root 使用的 evaluated-only facade 构造期现强制同时捕获 matrix provider 与 review provider，review 在 mutation authority consume、release prepare 和 active CAS 前完成；review receipt 又通过 release 的 policy receipt digest 与 procedural Memory evidence refs 持久回绑。两-cell Gate→signed matrix→human review→release CAS→Memory pending 正常纵切、producer hard-kill→新进程 reconciliation 均通过；review/controller/registry/release/matrix 五组 82/82，process integration 1/1。顺带修复 process fixture 从仓库根启动时错误解析 `C:\node_modules\vitest` 的 cwd 假设。提交为 `2d7fa726f8`。P0-4 的 capability diff/quorum 仓库门已关闭；真实用户可操作 reviewer UI、生产 human decision resolver/PKI、kill switch/canary 和唯一最终用户入口仍属于 P1-5/P1-6 的未完成产品接线。

后续 content-risk acknowledgement 批次复用证据投影器已有的 NFKC/default-ignorable/confusable prompt-injection 检测，而不是在 reviewer 中复制一套关键词：认证 Candidate 的稳定 finding IDs、是否命中及 `contentRiskDigest` 被纳入 review packet/packet digest；存在 finding 时至少需要两名不同真人，signed decision 必须逐摘要显式确认，否则在 signature verifier、mutation authority consume 和 active 写入前失败关闭。该设计不把包含攻击反例的合法安全 Skill 一概硬拒绝，而是确保混淆恶意指令无法在审阅面中静默消失；review binding 和 procedural Memory promotion receipt 也保留同一风险摘要。全角 `Ｉｇｎｏｒｅ previous instructions` 负测证明未确认时拒绝，review 7/7、Projector+Candidate 74/74、其余 promotion/release/matrix 回归 56/56、process integration 1/1 通过，提交为 `534ca5f4e5`。这关闭 Candidate 静态语义风险发现与人工确认的仓库门，但不替代更广的语义分类器、真实用户 UI、生产 resolver/PKI 或 1,000 条生产攻击轨迹。

后续 adversarial-corpus 批次扩展统一 ruleset 的越权、伪造成功、凭据外传、自授权、关闭护栏及英中同义意图，并同时比较规范词序和去分隔 compact skeleton，使逐字符标点/符号/空白插入不能绕过已有 NFKC/default-ignorable/confusable 处理。确定性语料以 20 类攻击意图 × 10 种大小写/全角/零宽/逐字符分隔/换行/混合脚本变体 × 5 种工具输出/外部内容包装形成 1,000 条唯一候选轨迹，全部必须产生 content-risk finding；Projector+review 55/55、Candidate 单独 27/27、其余晋级链 56/56、process integration 1/1 通过，提交为 `bd3c29b843`。这关闭仓库内 1,000 条合成对抗候选门，不把它表述为真实生产流量、未知攻击召回率或误报率证明；生产 ingress 后的攻击观测和更广语义分类仍未完成。

后续 durable human-review authority 批次不再依赖进程内 decision mock：新增 `SkillPromotionReviewLedgerAdapter`，只有受信 packet builder 产生的 packet 才能进入有限 `skill-promotion-review-packet` ArtifactPorts ledger-retention 类型；approve/reject decision 在写入 `skill-promotion-review-decision` 前必须通过完整 packet/时效/quorum/content-risk binding 与构造期独立签名 verifier。两类不可变 artifact 分别由 `skill.promotion-review.requested/decided` Ledger event 建索引，decision event 必须精确回指 packet subject；同 packet 只允许一个决策，响应丢失可由 event 恢复为幂等成功。新 adapter/真实 Ledger 文件与独立 witness 实例可恢复 pending/approved/rejected 队列，并向既有 branded review provider 提供完全匹配的 decision resolver；伪造 packet、无效签名和 lineage substitution 均在 active mutation 前失败关闭。adapter+review 13/13、ArtifactPorts 25/25（另 1 项按平台跳过）、完整 promotion/release/matrix/process 链 50/50 通过，提交为 `1b30ca030e`。仓库内持久 resolver 已关闭；生产 PKI/密钥部署、用户身份认证、最终 CLI/Desktop surface 与唯一产品入口仍未接线。

后续 offline-audit 批次为真实 `EvolutionLedger` 新增 `exportAuditBundle()` 与无账本/无 ArtifactStore 依赖的 `verifyEvolutionLedgerAuditBundle()`。导出包固定 signed identity、store incarnation marker、完整 legacy/domain event hash chain、逐序号 anchor chain、独立 durable witness，以及逐事件 tenant/ref/digest/resolution receipt/原字节证据；整体另有 canonical export digest。离线 verifier 在调用外部 ledger/witness trust port 前捕获完整 plain-data graph 和 verifier 实现，逐条重算签名域、event/anchor/segment/witness payload、artifact validation aggregate 与原字节 digest，并要求零缺失、零冗余、规范顺序。空账本 genesis、混合事件和删除两个在线 store 后的验证均通过；即使攻击者重算未签名的外层 export digest，截断尾部、修改 signed record、替换 artifact bytes/resolution receipt、重排或追加证据仍全部失败关闭。EvolutionLedger 全量 48/48，连同 ArtifactPorts、Ledger ports、Wiki/Memory/review adapters 共 104 通过、1 项按平台跳过；提交 `27b7e267e9`。这关闭 P0-5 的仓库内离线审计导出/独立验证缺口，但不等同于持久索引/快照、旧数据迁移、规模与故障注入、生产跨进程 authority 或最终 wiring。

后续 authenticated-prefix query-index 批次把公开 `query()`、`queryMany()`、`read()`、receipt verification、audit export 和 ledger verification 接到同一条重新认证的增量加载路径；缓存命中仍逐个重算既有 segment/anchor 文件摘要并校验 HEAD 与独立 witness，外部进程只要追加合法尾部，本进程就只验证新增事件签名后更新状态。认证 state 通过私有 `WeakMap` 建立 eventId/eventDigest O(1) 索引，sequence 直接按连续数组定位；新增 `queryMany()` 将最多 10,000 个 selector 合并到一次锁和一次 state 认证中，默认不批量签发 receipt。5,000 个混合命中/未命中查询在 `<5s` 门内完成且历史 event/domain-event 签名验证次数为 0，另一实例追加后只新增 1 次 event 签名验证；Ledger 49/49、六个相关文件共 105 通过、1 项按平台跳过，提交 `00686ceda8`。这关闭进程内认证增量 prefix 与查询索引缺口，不把内存缓存表述成持久 snapshot；重启后的持久索引/快照、长账本 reopen 基准和迁移仍待完成。

后续 persistent state-snapshot 批次新增显式 `checkpointState()`：以当前 witness digest 命名的不可变 snapshot 同时签名绑定 ledger identity、store incarnation/entry、HEAD/anchor、witness generation/digest 和完整 state digest；state 又逐序号绑定 legacy/domain event chain、anchor chain、内容寻址文件名及每个 canonical 文件字节摘要。重启仍先独立验证 identity、store marker、HEAD 和 witness，再使用 snapshot；命中后重算全部真实 segment/anchor 文件摘要和 schema/chain/digest 关系，但不再逐条调用历史 event/anchor 签名 authority。损坏 snapshot 安全回退到权威日志全验，旧 witness snapshot 即使复制到当前文件名也不能被采用；危险 symlink/hard-link 仍失败关闭。既有 100 并发账本在 checkpoint 后新实例 `<5s` 恢复、历史 event/domain-event 签名调用为 0；Ledger 50/50、六个相关文件共 106 通过、1 项按平台跳过，提交 `9c77318e59`。这关闭可跨重启复用的持久认证 snapshot 与仓库内 100-event reopen 基准；生产量级/资源上限、旧 snapshot retention/清理和旧数据迁移仍未关闭。

后续 snapshot-retention 批次让每次成功 `checkpointState()` 只保留当前 witness 对应的唯一 snapshot；删除严格限制在 authority 根目录、完整匹配 `state-snapshot-v1-<64 hex>.json` 的旧缓存，当前文件永不删除，目录项与 `lstat` 必须同时证明 regular/non-symlink/single-link，删除后同步目录。当前 witness 文件若被 stale replay 占用，checkpoint 明确失败而不覆盖；移除攻击文件后可重建当前 snapshot，并确定性清理损坏旧缓存。Ledger 50/50、ESLint 与差异检查通过，提交 `d6c645aefd`。这关闭旧 snapshot 无界增长与清理边界缺口；生产量级/资源上限仍待目标环境验收。

后续 legacy Candidate migration 批次从仓库历史提交恢复 v1 的精确 15 字段 schema 与原始 digest domain，新增 `verifyLegacySkillCandidateDraft()` 和 tenant Registry 拥有的 `migrateLegacy()`。迁移先重算旧 candidate/content digest、规范数组与 provenance，并复用当前 secret/PII plaintext guard；再要求受信 target-matrix admission 补齐同 tenant dependency lock、runtime manifest 和 matrix，且 target runtime scope 不得相对旧 Candidate 缩减或替换。生成的 v2 仍只是不可激活 draft；迁移完成必须取得与 legacy/v2 ID、内容、三个 execution digest、tenant、authority handler 精确绑定的同步 authenticated/durable audit receipt，审计失败明确返回 `candidate-only`，幂等重试不会产生第二个制品。Candidate 28/28，Candidate+Release+Promotion 74/74，ESLint 与差异检查通过，提交 `c96b6da007`。这关闭单个旧 Candidate schema→tenant v2 的显式迁移协议；旧 unscoped store 批迁移、Release/state/journal 与 projection 迁移仍待完成。

后续 legacy Candidate store 批迁移新增 Registry-owned `migrateLegacyStore()`：旧 unscoped 根必须与目标 base 物理分离并保持同一 canonical directory identity，采用有界同步目录枚举，只接受 content-addressed v1 JSON；逐文件执行 non-symlink/single-link/descriptor identity、fatal UTF-8、规范字节、历史 digest 与 16 MiB aggregate budget 校验。全部源先验证后才逐项解析 execution authority、调用同一 durable-audited 单制品迁移；完成前再次核对每个源文件 identity/bytes，源目录从不删除。中途失败只会留下 candidate-only 幂等草稿；重跑得到同一 store migration digest，`createdCount=0`。Candidate 29/29、ESLint 与差异检查通过，提交 `2c11930ef5`。这关闭旧 Candidate unscoped store 的安全批迁移和回滚保留缺口；Release/state/journal/projection 迁移仍待完成。

后续 legacy Release migration 批次从历史实现恢复 v3 的精确 15 字段 schema 与 `chainlesschain.skill-release/v3\0` digest domain，新增显式 verifier 和 Registry-owned `migrateLegacyRelease()`。迁移只接受已取得 authenticated/durable receipt 的 Candidate v1→v2 迁移结果，并精确核对旧 release 的 candidate/content/parent/capability/runtime，以及旧 raw dependency lock 与新 tenant-bound lock artifact 内层 `lock/lockDigest`；随后生成 inactive v4 release，保留原 mutation/receipt lineage，同时用新的 durable migration receipt 绑定旧/新 release digest、旧/新 candidate、tenant、Candidate migration receipt 与 handler。审计失败明确留下 `inactive-release-only`，不会改写 active state；幂等重试复用同一 release/migration digest。Release 29/29、Candidate+Release+Promotion 76/76、ESLint/Prettier/差异检查通过，提交 `383e35a598`。Release artifact 迁移已关闭；state v2、journal v3 与 ledger projection 的事务迁移仍待完成。

后续 legacy state planning 批次新增可独立重验的 Release migration result 与历史 state v2 verifier，并构造不可变 `SkillReleaseStateMigrationPlan`：plan 精确绑定旧 state digest/revision/fence/transaction、active/LKG 的旧新 release digest、两份 release migration record/receipt，以及新 tenant-bound dependency-lock digest。该 API 明确携带 `requiresAuthenticatedLedgerMigration=true`，只产出计划、不写 active 指针，避免在尚无账本迁移事件时伪造已认证 state。Release 29/29、Candidate+Release+Promotion 76/76 通过，提交 `ccf52dfe14`。该批关闭 state 输入与映射证明，state pointer 提交、journal v3 处置及 ledger projection 仍待真实 EvolutionLedger adapter 完成。

后续 legacy state ledger baseline 批次把上述 plan 接入真实 `EvolutionLedger`，新增受 durable authority receipt 约束的 `skill.release.state-migration` 制品与 domain event。迁移只允许发生在该 tenant/Skill 的空 lineage，保留旧 revision/fence，将新 active/LKG/dependency-lock 写为认证基线；相同完整证据幂等，替换 receipt、重复基线或已有 finalize lineage 均失败关闭。Ledger query/reopen 可恢复同一 current projection，后续正常 prepare/finalize 从旧 revision 连续增长；Registry 只在 ledger commit 和 projection 复核后写 active pointer，若 ledger 已提交而 pointer 写入前崩溃，显式重跑会从同一事件收敛。相关 113 项组合回归中迁移相关 95 项通过、1 项按平台跳过；100 并发 promotion 的慢机 grant 有效期测试另以 18/18 验证，提交 `065f9d277f`、`2dc0520fea`。state pointer 与 ledger projection 的显式事务迁移已关闭；journal v3 的认证处置、迁移专用 journal 和启动期自动 reconciliation 仍未关闭。

后续 legacy journal verification 批次从历史实现恢复 v3 journal 及其 tenantless state v2 的精确 schema/digest domain，新增只读 `verifyLegacySkillReleaseJournal()`。verifier 重新认证嵌套 mutation request/consumption receipt 和六类 receipt binding，重算 transition subject、intent/journal/state digest，复核 previous→next revision/CAS、active/authority/dependency-lock 与可选 prepared projection；它只证明旧文件内部一致，不执行 finalize、写 pointer 或将 journal phase 当作提交事实。Release 全量 30/30 通过，提交 `b0d9769675`。旧 journal 输入验证已关闭；下一步仍须用独立 authenticated ledger resolution 决定 abort、迁移 committed state 或 quarantine，并补迁移专用 journal 与启动自动 reconciliation。

后续 legacy journal disposition 批次新增 branded `SkillReleaseJournalResolutionAuthority` 与 content-addressed disposition plan。构造根捕获的同步 resolver 必须返回与 tenant、journal/transaction、handler 及 authenticated/durable ledger projection 精确绑定的 receipt；复制 descriptor 会丢失内部 brand，普通 JSON 自报 `authenticated=true` 不再可用。只有 `absent` 生成 `abort`，只有 `committed + current` 且 state migration plan 精确对应 journal next-state 才生成 `migrate-committed-state`，`prepared` 或非 current committed 一律 `quarantine`；plan 自身可独立重验。Release 全量 30/30 通过，提交 `65b71c36b9`。认证分类已关闭，剩余为对源文件实际执行保留/隔离、迁移 journal 以及启动自动 reconciliation。

后续 state migration recovery 批次新增 tenant-scoped `state-migrations/` 持久 journal，并把它纳入 Registry 构造期 recovery mutex。迁移在调用 ledger 前先持久化 plan/state/durable receipt/lease 的 content-addressed journal；若 ledger 明确 absent，重启只中止并清理，不会依据本地 receipt 自行提交；若 ledger 已 committed/current 但调用响应丢失，重启从认证 projection 补写 active pointer、复核 release/state 字节后清理 journal/lease。重新计算 journal digest 也不能掩盖 receipt 篡改。Release 全量 30/30、聚焦恢复/篡改测试及静态检查通过，提交 `6055b210b6`。ledger→pointer 的迁移 journal 与启动自动 reconciliation 已关闭；剩余为按已认证 disposition 对 legacy journal 源文件执行保留/隔离/退休。

后续 legacy journal archive 批次新增显式 `archiveLegacySkillReleaseJournal()`：只接受已经重验的 legacy journal、包含完整 durable resolution receipt/ledger projection 的 disposition plan，以及 committed-state 分支由 Registry 实例产生、不可 spread/序列化伪造的迁移完成证明。源必须是 canonical `journals/<skill>.json` 的 non-symlink/single-link 文件，archive 必须位于物理分离的既有安全目录；完整 journal+plan+迁移 state digest 先写 content-addressed envelope，经 fsync、hard-link publication、readback 和离线 verifier 复核后才 unlink 源并 fsync 源目录。并发 EEXIST 只接受完全相同 archive，损坏/碰撞会保留源文件。abort 与 committed-state、幂等退休和碰撞失败路径均通过，Release 全量 30/30，提交 `76906bc88f`。legacy journal v3 的验证、认证分类、迁移恢复与文件级退休链已关闭。

后续 migration fault-injection 批次把 archive 的四个 durability 边界变为显式同步 checkpoint，并允许构造根注入受控 fault hook。测试逐一在 archive temp fsync、hard-link publication、readback、source retirement 后模拟进程中断：首阶段保证源仍在且无目标；中间阶段保证完整 archive 与源同时存在；最后阶段保证源已删但完整 archive 已持久。所有阶段用无 fault 的同一输入重跑后都只剩一个可验证 archive 且源已退休。Release 全量 30/30、ESLint/Prettier/差异检查通过，提交 `07b0e8cd10`。legacy migration/retirement 纵切的系统故障矩阵已关闭；P0-5 更广的长账本、witness、authority 与生产 wiring 故障演练仍未关闭。

当前 Workbench governance visibility 批次把 EvolutionRun status、active/LKG release、冲突数和 Pilot stage/revision/kill-switch/reconciliation-required 从 canonical verified projection 贯穿到 Desktop、VS Code 与 JetBrains reviewer surface，并在各客户端展示前失败关闭非法结构。CLI 16/16、Desktop 主进程代理/IPC/renderer 三文件 62/62（其中 renderer 7/7）、VS Code 9/9、JetBrains JDK 21 JUnit 6/6 及相关 ESLint 已通过。该批只关闭产品表面对既有治理态不可见的窄缺口，不改变 P2-2“目标部署 authority/source 与部署验收仍未完成”的判断；详见 §7.2。

后续 registrar/transport 接线批次把调用方显式提供的 branded Workbench host 从 `registerServeCommand(..., dependencies)` 贯穿到 stdio 与 WebSocket App Server；WebSocket host 在打开监听和接受连接前即拒绝未 branded 的 Workbench/knowledge review host，每个连接创建的 `CcAppServer` 只能接收构造期捕获的 host。未注入时仍明确报告 unavailable，不从环境变量、本地 JSON 或测试密钥回退。App Server 主协议、WebSocket 真连接、stdio `serve` registrar 与 Workbench host 联合 **30/30** 通过，相关 ESLint/Prettier 通过。该批只关闭内部注入缝：公开 npm binary 的 `lazy-dispatch.js` 仍以 `registerFn(program)` 注册命令，eager 入口也未装载 deployment dependencies，所以真实默认 `cc serve --app-server` 仍得不到 host；目标部署 loader、human identity/PKI/projection/transition/metrics authority 与生产 receipt source 均未完成，详见 §7.2。

2026-09-04 的公开版 `0.37.81` VSCodium 实测暴露了上述默认链断点：扩展成功启动独立 App Server，但服务端能力正确声明 `evolutionWorkbench.available=false`，旧 UI 仍直接调用 `evolution/workbench/list`，因而弹出 “Evolution Workbench is not configured for this App Server”。`0.37.82` 修复候选在任何 Workbench RPC 前读取 initialize capability；未启用 pilot 时给出设置指引，未配置 host 时给出安全禁用说明，部分只读部署只展示服务端实际声明的方法，并为三类提示提供简体中文本地化。定向 **9/9**、VS Code 全量 unit **161/161**、VSIX metadata **18/18** 与打包源码回读已通过。该补丁修复用户可见的命令报错，不把 unavailable 伪装为可用，也不等同于完成默认生产 host 接线。

| 优先级 | 当前判断                                                                       | 已完成并写入仓库                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 当前仍需完成                                                                                                                                                                                                                                                                                                                                                                                            |
| ------ | ------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P0     | 已完成 41 个基础提交；EVO-P0-1、EVO-P0-2 已完成，P0 整体未关闭                 | 能力真值与 mutation freeze 已完成产品表面切换；Candidate/Promotion/Rollback 已完成不可变制品、CAS、journal/recovery、100 并发单赢家和字节级 LKG 回滚验收；可信 mutation authority、独立监督 Eval Gate、train/validation/test 防泄漏、split-blind handles 与 holdout provenance、signed target-matrix 全 cell 合取门及预注册 Bonferroni 双侧 family-wise confidence 校正、accepted matrix receipt→promotion/release intent 窄绑定、typed/digest-bound receipt-resolution 端口、evaluated-only controller 与受限 control-plane facade 组合、attested durable receipt lifecycle adapter 及撤销快照基线、密文 Raw→model/trusted projection 的 ArtifactStore 持久组合与 derivation manifest、统一 Candidate secret/PII plaintext guard、parent-relative capability diff、Unicode-aware content-risk digest、1,000 条合成对抗候选与 mutation 前显式 human acknowledgement/quorum、进程级 Eval supervisor/Gate hard-kill、child evidence ArtifactStore+Ledger resolver、tamper-evident ledger、类型化领域事件、规范执行清单、持久账本端口契约、可离线独立验证的完整 signed audit bundle、认证增量 prefix/O(1) 查询索引、witness-bound 持久 state snapshot、安全 retention、durable-audited legacy Candidate 单制品/批 store migration、inactive Release migration、state migration plan、真实 ledger baseline/pointer 提交及 legacy journal 验证/认证处置/文件退休与 state migration 自动恢复及迁移四阶段 fault matrix | P0-3～P0-5 仍缺真实跨进程 Eval durability authority/生产 PKI 后端、Desktop 直接 Coding Agent/其他非 App Server pre-model ingress、目标部署的生产 KMS/PKI/policy/witness authority、跨平台 sandbox/资源/网络攻击验收、删除传播、更广语义恶意指令分类和生产流量攻击观测，以及长账本/authority/witness 故障演练；human-review 持久 resolver 已落地，最终用户 UI、生产 PKI 与统一控制面接线归 EVO-P1-5/P1-6 |
| P1     | EVO-P1-1、P1-3、P1-8、P1-9 已完成，P1-2、P1-7 已达仓库闭环；其余主能力仍待验收 | shared EvolutionRun 已统一 CLI/Desktop/Graph 的 Raw/Wiki/Registry/Eval 投影；Wiki Maintainer 已闭环认证 evidence、确定性 pattern/index/log/impact、生命周期、ArtifactPorts 不可变 revision 与真实 Ledger CAS/event/reopen；结构化 Memory 已具备四层写入规则、branded actor/receipt/PostCompact gate、可回退 compaction、ArtifactStore+Ledger event/snapshot、真实 receipt ledger resolver、CLI Hooks V2/Desktop HookSystem PostCompact adapters、critic/evaluator/promotion/policy 四类 producer 写入、同 stream Agent root、authority-ledger 自动 reconciliation、外部 authority 驱动的本机 durable file witness、正式 file-backend 构造端口与实际 release process-kill 恢复；SkillInvocationReceipt 与有界评分循环已闭环；CLI Agent durable ingress、显式 production composition root、canonical Graph App Server 与 legacy WebSocket Agent ingress 已纵切；不可达 Desktop/CLI 壳已退役，公式型训练壳已收口为 metrics-only API/CLI                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 生产目标矩阵 Eval 与统一 Registry ingress 仍未完成；剩余目标部署 authority/默认 launcher、Maintainer/transition durable 调度及 production adapter 迁移；P1-7 的生产密钥托管/部署与跨主机 witness fault domain 作为目标环境发布门验收，真实流量接入归 P2                                                                                                                                                 |
| P2     | 已完成 36 个基础批次；P2-1～P2-6 均为部分完成                                  | Pilot、Workbench、Retrieval Router、受治理知识同步、跨模型市场与 Wiki pruning 已形成 fail-closed 控制协议和定向验证；Workbench 治理态可见性、App Server registrar/transport 注入缝与 VS Code unavailable capability preflight 已补齐；未把任一 P2 项提前声明为生产交付                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 接入真实租户/流量、默认 CLI deployment host loader、Desktop/CLI/IDE/Cloud 宿主、生产 PKI/KMS/Eval/transition authority、跨进程持久状态与产品界面，并完成部署级 Pilot、跨设备同步、市场撤销、隐私删除和长期在线观测                                                                                                                                                                                      |

P0 的逐项状态如下；“基础完成”特指底层安全原语已提交，不代表生产接线已经完成：

| 路线项                                         | 状态                                                                                                                                                                                                                                                            | 已完成部分                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 提交证据                                                                                                                                                                                                                                                                 | 未关闭项                                                                                                                                                                                                                                                                                                                 |
| ---------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| EVO-P0-1 能力真实性与 mutation freeze          | ✅ 已完成                                                                                                                                                                                                                                                       | canonical capability/runtime status；CLI learning/evolution fail-closed 与 metrics-only help；Desktop TechLearning 无制品不再成功；Phase 20 改为 metrics 接线且旧 simulator 未注册；learning/proposer/import writer 全部 candidate-only；产品文档与生成参考完成真值切换；13 文件 295/295 定向回归通过                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `3fdff6c1ee`、`0da1f36a8b`、`c16e1a3912`、`23125c71e4`                                                                                                                                                                                                                   | 无；通用 Agent/插件等 legacy writer 的统一单写者迁移归 EVO-P1-5，不再作为 learning/proposer 能力声明                                                                                                                                                                                                                     |
| EVO-P0-2 Candidate、Promotion 与 Rollback 事务 | ✅ 已完成                                                                                                                                                                                                                                                       | tenant-scoped immutable candidate/release、完整 execution manifest、可信 mutation authority、active CAS、lease/fence、prepare/finalize ledger、journal/staging/pointer recovery、session pin、LKG+dependency lock rollback；五个写入阶段崩溃矩阵与真实子进程退出恢复已覆盖；新增 100 并发候选严格 1 成功/99 拒绝及字节级回滚 `<60s` 断言，三文件 70/70 通过                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `3fdff6c1ee`、`fe16c72d5e`、`ed7882d004`、`233e1bdc3a`、`4cffc53054`、`dfa21b4ba4`、`4f22d70bb5`、`23125c71e4`                                                                                                                                                           | 无；shadow/canary 产品编排及唯一最终用户 production writer 的实际构造接线归 EVO-P1-5，跨进程 PKI/Eval authority 归 EVO-P0-3                                                                                                                                                                                              |
| EVO-P0-3 独立真实 Eval Gate                    | 单 cell 监督、matrix 合取证据、family-wise confidence 校正、evaluated-promotion 窄绑定、receipt resolver、evaluated-only 组合、受限 control-plane facade 及 attested/bounded/fresh/rotatable/revocation-aware durable lifecycle adapter 基础完成，生产接线 HOLD | 隔离 target、角色/信任分权、签名 receipt v4、调用/撤销独立证据、hard-termination 收敛、全 run 单调 deadline、descriptor-bound authority root、后验防 TOCTOU；预注册 train/validation/test、跨 split group/exact-input 防泄漏、split-blind 单 run opaque handles、authenticated training partition/holdout provenance、paired validation/hidden test 与 safety hard gate；signed matrix plan v2 预注册 family-wise error rate，按 `2 × cell 数` 对 validation/test paired comparisons 做 Bonferroni 双侧校正，实际 `confidenceZ` 进入 child receipt/evidence root/cell result/decision commitment，低于最低 z 时 fail-closed 为 `needs-more-evidence`；tenant durable reserve/finalize 端口契约、verified full child receipt（含 attestation）的有序摘要根、因果时间线与全 cell 一致通过判定；accepted receipt 在 authority consume/release prepare 前绑定 candidate/runtime/target/active CAS，并由 release intent 固定 eval envelope digest；按 tenant/digest 解析完整 receipt 的 fail-closed 端口；构造期 branded provider、拒绝直接 `promote()` 的策略及不暴露宽 controller 的窄 facade；要求 authenticated/durable acknowledgement、验证规范 payload attestation、证据新鲜度、全局唯一 keyId/有界 grace trust、版本化撤销快照，并对 authority 调用施加根时限/AbortSignal 的 retain/resolve 生命周期适配器；进程 supervisor、Gate hard-kill 与 child invocation/revocation ArtifactStore+Ledger 持久 resolver/reopen 已落地 | `52427b742c`、`adde8f98e6`、`5c2de980d3`、`881abf6090`、`1a70a880fa`、`0feb536d82`、`face4e9250`、`3b76883a81`、`23125c71e4`                                                                                                                                             | 真实跨平台 grader/runner、生产隔离部署与更广分布级 anti-gaming；现有生产 adapter 实际构造受限 control plane、真实跨进程 durability authority/PKI 后端；跨平台网络/OS 资源攻击与完整 Matrix 部署演练；跨进程 plan resolver/reservation/finalization 持久性；认证撤销分发/刷新及生产密钥轮换/撤销演练与目标平台 grace 校准 |
| EVO-P0-4 Raw、入模投影与 Skill 编译安全边界    | 持久组合、Candidate plaintext 与 promotion review gate 基础完成，生产接线 HOLD                                                                                                                                                                                  | attested source→Raw/model/trusted projection；ciphertext-only Raw ArtifactStore；secret/PII 脱敏、injection trust/quarantine 与 derivation manifest；Candidate strict schema/name/size/path/symlink/single-link + Unicode-aware plaintext guard；认证 parent-relative capability diff、evidence/diff/Eval/runtime reviewer packet、Unicode-aware content-risk digest、1,000 条唯一合成对抗候选，以及 mutation 前签名 human acknowledgement/quorum；Projector+review 55/55、Candidate 27/27、其余相关回归 56/56、process integration 1/1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `b8490faa94`、`b4dca1ee05`、`4cffc53054`、`bc24db7a0b`、`f668598a42`、`2d7fa726f8`、`534ca5f4e5`、`bd3c29b843`                                                                                                                                                           | 真实 Agent/tool pre-model ingress 与旧直通路径退役、生产 KMS/HSM、删除/撤销依赖传播、更广语义恶意指令分类与生产流量攻击观测/误报校准；最终用户 reviewer UI 与生产 PKI/用户身份归 P1-5/P1-6                                                                                                                               |
| EVO-P0-5 Fail-closed 证据与审计                | 账本、离线导出、认证查询索引、持久 snapshot 与显式 Candidate/Release/state/journal/projection 迁移链完成，生产可用性 HOLD                                                                                                                                       | append-only tamper-evident ledger、typed domain events、subject-bound transition、认证制品解析与持久账本组合端口；signed identity/event/anchor/witness/artifact 自包含导出及无在线 store 独立验证；重新认证的增量 prefix、私有 O(1) event 索引、有界 `queryMany()`、witness-bound 不可变 state snapshot、single-current retention、durable-audited v1→tenant-v2 Candidate 单制品/只读批 store migration、v3→tenant-v4 inactive Release migration、state v2 mapping/ledger baseline/pointer、migration journal/reconciliation，以及 legacy journal 验证/认证处置/archive retirement 与四阶段 fault matrix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `d073bdf3c7`、`233e1bdc3a`、`d098a64253`、`b4dca1ee05`、`dfa21b4ba4`、`27b7e267e9`、`00686ceda8`、`9c77318e59`、`d6c645aefd`、`c96b6da007`、`2c11930ef5`、`383e35a598`、`ccf52dfe14`、`065f9d277f`、`b0d9769675`、`65b71c36b9`、`6055b210b6`、`76906bc88f`、`07b0e8cd10` | 真实跨进程持久 authority、生产量级/资源上限、长账本/authority/witness 系统故障注入以及生产 wiring                                                                                                                                                                                                                        |

P1/P2 当前状态按路线项展开如下，防止提前把“已有依赖”计为“能力完成”：

| 路线项                                   | 当前状态                                                                                                                                                                                                                    | 已复用或提前完成的底座                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 下一关闭条件                                                                                                                                                           |
| ---------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EVO-P1-1 Canonical Raw/Wiki/Skill        | ✅ 已完成                                                                                                                                                                                                                   | shared `EvolutionRun` 事件/投影/快照状态机统一 Raw/Wiki/Registry/Eval；CLI/Desktop/Graph 三适配器返回同一状态与 digest；重复、乱序、crash replay、compaction replay、tombstone、敏感内容拒绝及 record-replay direct candidate 共 7/7 通过                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 无；真实 Agent 事件生产接入和旧壳退役归 EVO-P1-6，外部持久 authority 运维归 P0-5                                                                                       |
| EVO-P1-2 Evidence-backed Wiki Maintainer | 🟢 仓库闭环                                                                                                                                                                                                                 | trusted/digest-bound evidence ingress、最小权限 Maintainer、确定性 pattern/index/evolution-log/skill-impact/reverse-dependency、去重/merge/conflict/decay/expiry/revoke/tombstone、digest-bound proposal impact；`wiki-revision` ArtifactPorts ledger-retention 类型、branded resolver、不可变 revision→Ledger domain event、head/sequence CAS、响应丢失幂等以及真实 Ledger 文件/witness 跨实例恢复已接通；Maintainer 13/13、adapter 6/6、ArtifactPorts 25/25、Ledger 45/45 通过                                                                                                                                                                                                                                                                                                                                                                                                   | 本项无仓库内剩余；生产 Agent ingress 归 P1-6，通用迁移归 P0-5，敏感 evidence 删除基础设施归 P0-4/P2-6                                                                  |
| EVO-P1-3 Single-Skill Proposer           | ✅ 已完成                                                                                                                                                                                                                   | 固定最小 Wiki/impact/active/training 读取；矛盾/样本不足 abstain；pattern/raw 选择性读取；单 Skill PURPOSE、digest lineage、适用/禁用边界、反例、回退、验证、capability/runtime/context cost、safe machine diff 与 exact candidate binding；1 文件 10/10 通过                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 无；真实 Wiki authority 的统一生产来源归 EVO-P1-1/P1-2，不重复计入 proposer 控制协议                                                                                   |
| EVO-P1-4 目标运行时 Eval                 | 数据模型、单 cell 成对评测、matrix 合取、family-wise confidence 校正、accepted→promotion 窄绑定、receipt-resolution、evaluated-only 及 attested/bounded/fresh/rotatable/revocation-aware durable lifecycle adapter 基础完成 | 预注册 train/validation/test split、group/exact-input 防泄漏、split-blind opaque handles、paired validation/hidden test、holdout provenance；dependency lock、runtime manifest、target matrix canonical schema、独立 target/grader/safety receipt 验证、signed plan/tenant-scoped reserve/finalize 持久化端口契约、无缺格的 all-cell conjunction、Bonferroni 双侧 family-wise confidence 门、matrix receipt 与当前 candidate/active CAS 的晋级前校验、tenant/digest-bound receipt resolver 契约、构造期 evidence provider、外部 durability authority retain/resolve、acknowledgement attestation、最大操作时限、证据新鲜度、keyId/grace 轮换及版本化撤销快照基准；完整 child receipt 已由 authority-root-bound ArtifactStore+Ledger resolver 持久并支持 witness 重开                                                                                                               | 接入真实跨平台 runtime/grader 与生产隔离部署；现有生产入口构造 evaluated-only controller，落地真实跨进程 durability authority/store/PKI 与认证撤销刷新及 shadow/canary |
| EVO-P1-5 Registry 与单写者治理           | tenant 存储、evaluated+human-reviewed controller、持久 review authority、受限 facade 与最终用户 reviewer surface 基础完成，生产接线 HOLD                                                                                    | content-addressed tenant candidate/release、promotion controller、lease/CAS/journal/recovery；root facade 拒绝 direct promotion，强制 matrix+human review provider；review packet 已包含 evidence、candidate diff、权限变化、Eval receipt、target runtime 与 content-risk digest；ArtifactStore+Ledger 持久 pending/approve/reject 队列、真实文件/witness 重开与 provider resolver 已落地，且 active 只接受 signed non-automated quorum approval 和风险摘要显式确认；CLI/Desktop/VS Code/JetBrains reviewer surface 不持有 writer/identity，现又可见 active/LKG/Pilot 安全态                                                                                                                                                                                                                                                                                                       | 部署生产 PKI/用户身份；唯一 production writer 的真实入口构造、跨进程部署权威和目标环境 kill-switch/canary 验收                                                         |
| EVO-P1-6 统一生产接线                    | 🟡 部分完成                                                                                                                                                                                                                 | CLI Agent durable ingress 与显式 production composition root 已纵切交互、单轮及 stream 三种入口，canonical Graph App Server 与 legacy WebSocket Agent 共用宿主 factory 接线缝；`serve` registrar 与 stdio/WebSocket transport 已能贯穿显式注入的 branded Workbench/knowledge review host，并在缺失或伪造时 fail-closed；公开 npm lazy/eager 入口已新增 Ed25519 descriptor、module/trust-root digest 与 exact-byte execution 的 deployment loader；Agent completion 与真实 SchedulerStore occurrence 已分别形成 SessionEnd/GoalEnd/ScheduledBatch source/producer，并进入同一 durable Maintainer queue；Candidate/Eval/HumanTask 三事件链已通过 durable request/attempt/settlement 接到 evaluated+human-reviewed Registry control plane；外部 authority 缺失即关闭，Raw/Artifact/Ledger/witness 分域存储并支持新实例重开；两个不可达旧壳已退役，训练壳已收口为 metrics-only API/CLI | 目标部署须签发 descriptor 并提供真实 KMS/PKI/policy/witness/scheduler/transition authority，配置三类 resolver/worker、迁移其他最终入口并完成 production adapter 迁移   |
| EVO-P1-7 Memory 与多 Agent 权力分离      | 🟢 仓库闭环                                                                                                                                                                                                                 | shared canonical 四层 gate、真实 ArtifactStore+Ledger resolver、独立 verifier、四类 producer、同 stream Agent root、新实例恢复、AgentRuntime semantic API、accepted matrix→真实 release CAS/reopen→root-owned procedural event、authority-ledger 自动 pending reconciliation、remote bridge、外部 authority 驱动的 durable file witness、正式 file-backend 构造端口及实际 release process-kill→新进程恢复已接通                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 仓库内无剩余；生产密钥托管/部署与跨主机 witness fault domain 作为目标环境发布门验收                                                                                    |
| EVO-P1-8 SkillInvocationReceipt          | ✅ 已完成                                                                                                                                                                                                                   | shared canonical receipt、Desktop DB adapter、CLI hash-chain transcript adapter、candidate/canary attribution gate、exact-structure/digest verifier 与 deterministic trace projection；原纵切 5 文件 208/208，持久化修正 4 文件 124/124，verifier 加固定向 5/5、session-core 全量 544/544、Desktop 消费 8/8、CLI 消费 131/131 通过                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 无；上层 Eval/Wiki 只需使用已固定的 evolutionRunId/trace/segment/grader receipt refs 做领域 join                                                                       |
| EVO-P1-9 有界评分改进循环                | ✅ 已完成                                                                                                                                                                                                                   | candidate-only 内部控制面已串接 GoalConditionEngine、单候选 proposer、确定性 grader、隔离 evaluator、durable receipt、预注册 best gate、根预算、失败分类、active 不变式及 snapshot/resume/replay；与 GoalConditionEngine 合计 2 文件 31/31 通过；提交 `4381aece2c`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 无；真实流量、人工 review、shadow/canary 明确归 EVO-P2-1，不属于本离线路线项                                                                                           |
| EVO-P2-1～P2-6                           | 6 项均为部分完成                                                                                                                                                                                                            | 已分别建立受控 Pilot、Workbench projection/逐项批审/版本对比/受控回滚/CLI surface/长期指标 snapshot、CLI Retrieval Router、受治理知识同步、跨模型市场和 Wiki pruning/无提权在线适应控制协议；均有定向测试，且未把控制协议冒充生产部署                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 接入真实跨进程 authority、目标宿主/界面和生产流量，完成部署级安全、效果、撤销、隐私删除传播及长期观测验收                                                              |

当前验证快照如下；各行均对应已提交的基础批次。测试均采用串行执行以避免 Windows 并发测试进程造成误判：

| 批次                                                                                       | 验证快照                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 结论                                                                                                                                                                                                                                                                                                 |
| ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EVO-P0-1 truth cutover                                                                     | capability/learning/writer/authority/Desktop/Phase/keyword/跨表面契约共 13 文件 295/295 通过；真实 CLI help 已核对；manifest/help/reference 生成物无漂移；ESLint 0 error；`git diff --check` 通过                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 路线项已由 `23125c71e4` 提交：正式入口不再把 unavailable、metrics 或内存对象报告为已训练/已进化/已创建 active Skill                                                                                                                                                                                  |
| EVO-P0-2 transaction acceptance                                                            | candidate/release/promotion 3 文件 70/70 通过；100 并发完整候选严格单赢家；rollback 原字节与 dependency lock 一致且 `<60s`；五阶段 crash matrix、commit-unknown、真实子进程退出/重启恢复保持通过                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 路线项已由 `23125c71e4` 提交：active CAS、LKG、journal 和 ledger 事务原语达到自身确定性验收                                                                                                                                                                                                          |
| EVO-P0-5 offline audit                                                                     | EvolutionLedger 48/48，相关 Ledger/Artifact/Wiki/Memory/review 六文件 104 通过、1 项按平台跳过；覆盖空 genesis、legacy/domain 混合链、store 删除后独立验证，以及重算 export digest 后的截断、signed record、artifact bytes/receipt、排序和额外证据篡改                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 提交 `27b7e267e9` 关闭离线导出/验证缺口；索引与 snapshot 后续分别由 `00686ceda8`、`9c77318e59` 关闭                                                                                                                                                                                                  |
| EVO-P0-5 authenticated prefix/index                                                        | EvolutionLedger 49/49，相关六文件 105 通过、1 项按平台跳过；5,000 个批量 selector `<5s`，缓存 prefix 不重验历史事件签名，外部实例追加后只验证 1 个新 event                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 提交 `00686ceda8` 关闭进程内认证增量 prefix 与 O(1) 查询索引缺口；持久 snapshot 随后由 `9c77318e59` 关闭                                                                                                                                                                                             |
| EVO-P0-5 persistent state snapshot                                                         | EvolutionLedger 50/50，相关六文件 106 通过、1 项按平台跳过；覆盖 mixed legacy/domain state、snapshot/file 逐项绑定、损坏回退、stale witness replay，以及 100 并发事件 checkpoint 后新实例 `<5s` 恢复且历史事件签名调用为 0                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 提交 `9c77318e59` 关闭持久认证 snapshot 与仓库内 100-event reopen 基准；retention 随后由 `d6c645aefd` 关闭                                                                                                                                                                                           |
| EVO-P0-5 snapshot retention                                                                | EvolutionLedger 50/50；覆盖 stale-current checkpoint 失败、攻击文件移除后重建、只保留当前 witness snapshot；ESLint 与 `git diff --check` 通过                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 提交 `d6c645aefd` 关闭旧 snapshot 无界增长和清理限域；生产量级/资源上限、迁移、系统化故障注入及生产 authority/wiring 仍未关闭                                                                                                                                                                        |
| EVO-P0-5 legacy Candidate migration                                                        | Candidate 28/28、Candidate+Release+Promotion 74/74；覆盖真实 v1 digest 重算、tenant/execution 补齐、runtime scope 不变、篡改拒绝、durable audit binding、失败后 candidate-only 与幂等重试                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 提交 `c96b6da007` 关闭单 Candidate v1→tenant v2 迁移协议；store 批迁移随后由 `2c11930ef5` 关闭                                                                                                                                                                                                       |
| EVO-P0-5 legacy Candidate store migration                                                  | Candidate 29/29；覆盖独立 canonical source root、有界枚举、文件/目录 identity、规范字节、aggregate budget、逐项 durable audit、源只读保留、幂等 rerun 与 mixed-entry 拒绝                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 提交 `2c11930ef5` 关闭 unscoped v1 Candidate store 的安全批迁移；Release/state/journal/projection、系统化故障注入及生产 wiring 仍未关闭                                                                                                                                                              |
| EVO-P0-5 legacy Release migration                                                          | Release 29/29；Candidate+Release+Promotion 76/76；覆盖历史 v3 digest、Candidate migration receipt 绑定、raw lock→tenant lock artifact 映射、inactive-only publication、durable audit、幂等重跑与 tamper 拒绝                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 提交 `383e35a598` 关闭 v3 Release artifact migration；state v2、journal v3、ledger projection、系统化故障注入及生产 wiring 仍未关闭                                                                                                                                                                  |
| EVO-P0-5 legacy state migration plan                                                       | Release 29/29；Candidate+Release+Promotion 76/76；覆盖历史 state v2 digest、active/LKG 双迁移 proof、旧 revision/fence/transaction lineage、新 tenant lock digest 与 active-pointer 零写入                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 提交 `ccf52dfe14` 关闭 state 输入验证与映射计划；真实 ledger commit/projection、journal v3 处置、系统化故障注入及生产 wiring 仍未关闭                                                                                                                                                                |
| EVO-P0-5 legacy state ledger baseline                                                      | Ledger ports 12/12、ArtifactPorts 25/25、Release 29/29，相关组合 112 通过、1 项按平台跳过；覆盖 durable migration artifact/event、空 lineage 单基线、旧 revision/fence 保留、current query/reopen、正常 finalize 延伸、精确幂等、receipt/state 篡改与碰撞拒绝、ledger 后 pointer 提交及显式重跑收敛                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 提交 `065f9d277f` 关闭 state ledger baseline/projection/pointer 显式迁移；journal v3 认证处置、迁移 journal/启动自动 reconciliation、系统化故障注入及生产 wiring 仍未关闭                                                                                                                            |
| EVO-P0-5 legacy journal verification                                                       | Release 30/30；覆盖历史 v3 journal、tenantless state v2、mutation request/consumption receipt、六类 receipt、transition subject、intent/journal/state digest、previous→next CAS 与 prepared projection 的精确只读重验                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 提交 `b0d9769675` 关闭旧 journal 输入验证；authenticated ledger resolution 驱动的 abort/committed-state migration/quarantine、迁移 journal 与启动自动 reconciliation 仍未关闭                                                                                                                        |
| EVO-P0-5 legacy journal disposition                                                        | Release 30/30；覆盖 branded resolver authority、durable resolution receipt、absent→abort、prepared/non-current→quarantine、committed-current→state migration、state-plan 精确绑定、伪 authority/receipt 与非法 action/status 组合拒绝                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 提交 `65b71c36b9` 关闭 authenticated ledger disposition 分类；源文件 quarantine/abort 或 committed migration 的持久执行、迁移 journal 与启动自动 reconciliation 仍未关闭                                                                                                                             |
| EVO-P0-5 state migration recovery                                                          | Release 30/30；覆盖 ledger 前 durable journal、absent 自动中止、commit 响应丢失后新实例自动补 pointer、release/state 字节复核、journal/lease 清理、路径与 receipt 篡改后重算外层 digest 的拒绝                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 提交 `6055b210b6` 关闭 ledger→pointer migration journal 与启动 reconciliation；legacy journal 源文件的归档/quarantine/retirement 执行仍未关闭                                                                                                                                                        |
| EVO-P0-5 legacy journal archive                                                            | Release 30/30；覆盖完整 resolution receipt/projection 入档、branded committed migration proof、canonical/single-link 源、物理分离 archive、fsync+hard-link publication、readback 后退休、幂等 EEXIST 及损坏 archive 保留源                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 提交 `76906bc88f` 关闭 legacy journal 的文件级 archive/retirement；显式 Candidate/Release/state/journal/projection 迁移链已形成，剩余生产 authority、规模、故障注入与 wiring                                                                                                                         |
| EVO-P0-5 migration fault injection                                                         | Release 30/30；覆盖 temp fsync、archive publication、readback、source retirement 四阶段中断，以及每阶段重跑后的单 archive/源退休收敛不变式                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 提交 `07b0e8cd10` 关闭 legacy migration/retirement 纵切的系统故障矩阵；长账本、authority/witness 与 production composition 的跨进程故障验收仍未关闭                                                                                                                                                  |
| EVO-P1-1 canonical EvolutionRun                                                            | shared core + CLI/Desktop/Graph adapters 共 7/7 通过；覆盖跨端同状态/digest、重复、乱序、crash replay、compaction replay/append、annotation/tombstone、record-replay direct candidate、冲突/跨 run/敏感内容拒绝                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 路线项已随本批次提交；生产 ingress/旧壳切换归 P1-6，跨进程持久 authority 归 P0-5                                                                                                                                                                                                                     |
| EVO-P1-2 Wiki Maintainer 控制协议与持久组合                                                | Maintainer 13/13、adapter 6/6、ArtifactPorts 25/25、EvolutionLedger 45/45 通过；覆盖多源/grader corroboration、单模型 hypothesis、去重/反证/decay/revoke、proposal/commit substitution、真实不可变 artifact 写入与 branded resolve、Ledger head CAS/文件/witness、新实例恢复及响应丢失幂等                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 路线项仓库闭环；生产 ingress、通用迁移与敏感数据删除分别由 P1-6、P0-5、P0-4/P2-6 统一验收                                                                                                                                                                                                            |
| EVO-P1-7 结构化 Memory 持久与权力分离纵切                                                  | session-core 控制协议 20/20、既有全量 538/538；Memory/authority adapter 9/9、Semantic pipeline 联合回归 12/12、CLI PostCompact 5/5、Desktop PostCompact 5/5、ArtifactPorts 25/25、PromotionController/TargetMatrix/AgentRuntime/reconciliation 受影响回归 31/31、process-kill 1/1、RemoteApproval 联合回归 17/17、FileWitness 6/6、file-backend unit/integration 3/3 通过，另 1 项按平台条件跳过；覆盖 canonical receipt/issuer、四类持久 resolve、独立 critic/evaluator 双门、CLI Hooks V2 与 Desktop HookSystem decision/attestation、四层 reducer、branded actor、真实四类 producer、root-owned procedural append、authority-ledger 自动 pending reconciliation、hydration、ArtifactStore、Ledger CAS/并发/响应丢失、正式 durable file witness、实际文件新实例恢复、本地 Ledger 转世拒绝及实际 release 进程硬终止后的新进程 Memory 恢复 | 路线项仓库闭环；生产 signer/verifier 密钥托管/部署与跨主机 witness fault domain 由目标环境发布门验收                                                                                                                                                                                                 |
| EVO-P1-3 Wiki-informed proposer                                                            | proposer 1 文件 10/10 通过；覆盖最小初始读取、矛盾/样本不足 abstention、pattern/raw 选择性加载、单 Skill、PURPOSE lineage、边界/反例/回退/验证、safe diff、tamper、candidate ack failure 与 target runtime substitution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 路线项已随本批次提交；只写 candidate sink，不接收 active writer；统一 Wiki authority 来源归 P1-1/P1-2                                                                                                                                                                                                |
| EVO-P1-8 SkillInvocationReceipt                                                            | shared receipt、Desktop DB、CLI hash-chain transcript、collector/Phase/pipeline 原纵切 5 文件 208/208；后续真实 JSONL 与 REPL/headless/stream/WS 持久化修正 4 文件 124/124 通过；verifier 加固定向 5/5、session-core 全量 544/544、Desktop 消费 8/8、CLI 消费 131/131；canary 完整归因与缺字段前置拒绝、trace ID 贯穿、digest tamper、重算 digest 后的额外字段/非法嵌套/矛盾归因/重复候选、错绑拒绝、失败不追加及 deterministic trace join 均有断言                                                                                                                                                                                                                                                                                                                                                                                        | 路线项已关闭；普通 incomplete receipt 明确禁止 outcome attribution                                                                                                                                                                                                                                   |
| EVO-P1-9 bounded improvement pilot                                                         | 新循环与 GoalConditionEngine 2 文件 31/31 通过；覆盖单候选、grader 顺序、best gate、root budget、provider/MCP transient、permission denial、evaluator crash、unknown evidence、receipt ack failure、active 漂移、snapshot/resume 与相同输入重放                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 路线项已由 `4381aece2c` 提交；仅允许 candidate sink，不暴露 active/promotion writer；真实流量准入继续属于 EVO-P2-1                                                                                                                                                                                   |
| 证据投影与认证制品持久组合                                                                 | evidence projector + encrypted Raw/derivation adapter 47/47 通过；与 artifact ports 联合为 72 通过，Windows 无权限创建 symlink 时另有 1 项按条件跳过；覆盖真实密文文件、projection canary 扫描、新 adapter 恢复、quarantine 与 manifest substitution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 持久组合由 `bc24db7a0b` 提交；真实 Agent/tool pre-model ingress、生产 KMS/HSM 与删除传播仍 HOLD；Candidate 静态语义风险发现已转由下一行的人工确认门处理                                                                                                                                              |
| Candidate capability diff、content risk 与 human-review promotion gate                     | Projector+review 55/55、Candidate 27/27、controller/release/matrix 等其余相关回归 56/56；真实 release producer hard-kill→新进程 Memory reconciliation 1/1；覆盖 evidence/diff/permission/Eval/runtime、1,000 条英中/Unicode/分隔混淆候选、content-risk digest、policy envelope、签名/时效、自动/拒绝/低 quorum/candidate substitution、capability 增量双人门及风险摘要未确认拒绝                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `2d7fa726f8`、`534ca5f4e5`、`bd3c29b843` 已让 Agent-root evaluated facade 在 active mutation 前强制 branded human review，并把 review/content-risk receipt 回绑 release/Memory；生产流量攻击观测/误报校准、最终用户 UI 和生产 PKI/用户身份仍归 P0-4/P1-5/P1-6                                        |
| Durable human-review authority                                                             | adapter+review 13/13、ArtifactPorts 25/25（另 1 项平台跳过）、完整 promotion/release/matrix/process 链 50/50；覆盖真实 Ledger 文件/witness 重开、pending/approve/reject、响应丢失幂等、伪造 packet、无效签名与 decision→packet lineage substitution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `1b30ca030e` 已把 packet/decision 放入有限 ArtifactPorts 类型和类型化 Ledger 索引，并直接提供既有 review provider 所需 resolver；生产 PKI/用户身份、最终 CLI/Desktop surface 与唯一入口部署仍归 P1-5/P1-6                                                                                            |
| Tamper-evident ledger 与 typed events                                                      | 相关账本测试 45/45 通过                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 数据模型与 fail-closed 校验基础已提交                                                                                                                                                                                                                                                                |
| Canonical execution manifests                                                              | manifest + mutation authority 测试 57/57 通过                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | canonicalization、tenant-bound lock、runtime manifest、target matrix 基础已提交                                                                                                                                                                                                                      |
| Durable ledger ports + release/promotion compatibility                                     | ledger ports 10/10、release registry 14/14、promotion controller 11/11 通过                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 端口契约可发布为 foundation；当前 O(N) 扫描和缺真实 durability authority 使生产接线继续 HOLD                                                                                                                                                                                                         |
| 独立监督 Eval Gate                                                                         | Gate + target-matrix 131/131 通过；split/holdout/anti-gaming 与 Bonferroni family-wise confidence 门复核通过，终轮独立复审 RELEASE                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 提交 `52427b742c`、`adde8f98e6`；可发布为监督/证据 foundation，但真实生产 grader/runner、跨进程终止、attested loader 与 matrix decision→promotion 生产事务接线仍为阻断                                                                                                                               |
| Tenant Candidate/Release/Promotion                                                         | candidate + release + promotion 65/65 通过，终轮独立复审 RELEASE                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 提交 `4f22d70bb5`；tenant 隔离、恢复和制品绑定基础可发布，生产持久 authority、adapter 组合权与同权限外部篡改防护仍 HOLD                                                                                                                                                                              |
| Signed target-matrix Eval 合取门                                                           | 3/3 大场景测试通过（含真实 branded Gate 两 cell accepted/needs-more 及攻击断言），两轮独立复审最终 RELEASE                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 提交 `5c2de980d3`；可发布为 trusted-composition/all-cell conjunction foundation，不是跨 cell 统计生产门；attested settlement 绑定、真实 hard kill、生产 durable plan resolver/reservation/finalization adapters 与 Promotion 事务接线仍 HOLD                                                         |
| Evaluated promotion 窄纵切                                                                 | 提交前 matrix eval + promotion controller 定向回归 16/16 通过；相关 evolution 回归 324/324 通过、1 项按条件跳过；另有 ESLint、Node 语法、模块加载与 `git diff --check` 通过                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 已提交 `881abf6090`；accepted matrix receipt 已在 mutation authority consume 和 release prepare 前绑定当前 candidate/runtime/target/active CAS，失败路径保持 revision 0、无 ledger transaction                                                                                                       |
| Typed receipt-resolution 端口                                                              | matrix eval 3/3、promotion controller 13/13 定向回归通过，包含 resolver tenant substitution 在 verifier 前失败关闭                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 已提交 `1a70a880fa`；调用者不再直传完整 receipt，resolver authority/revision/handler digest 与 tenant/digest 结构绑定已落地，receipt 仍由独立 verifier 认证；resolver attestation、真实 durable adapter 与跨进程恢复仍 HOLD                                                                          |
| Evaluated-only controller 与受限 control-plane facade 组合                                 | promotion controller 15/15 定向回归通过；既有真实 provider 验证和 direct promote 前置拒绝保持通过，新增断言证明 facade 不暴露 `promote` 或底层 controller                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | controller 基础已提交 `0feb536d82`，窄 facade 已随 `23125c71e4` 提交；生产 adapter 尚未实际构造该 facade，接线仍 HOLD                                                                                                                                                                                |
| Attested/bounded/fresh/rotatable/revocation-aware durable matrix receipt lifecycle adapter | 13/13 定向测试通过，覆盖跨 adapter 实例恢复、非 durable acknowledgement、receipt substitution、无效 attestation、永久悬挂/AbortSignal、过期签名重放、grace key 窗口、重复 keyId、超长 grace policy、grace key 撤销、active key 已撤销及重复撤销项                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | adapter 与版本化撤销快照已分别提交 `face4e9250`、`3b76883a81`；已建立外部 authority retain/resolve、规范 payload attestation、根操作时限、证据新鲜度、唯一 keyId/有界 grace 及 fail-closed revocation 的组合契约；真实跨进程后端、生产 PKI/撤销分发运营、进程级 hard kill、崩溃恢复和生产接线仍 HOLD |

2026-09-03 的 Agent ingress 批次开始关闭 EVO-P1-6：新增 `EvolutionRunLedgerAdapter`，把 canonical `EvolutionRun` event 作为有限 `evolution-run-event` 产品类型写入现有 `EvolutionArtifactPorts` 的 ledger retention，再以 `evolution.run.event.committed` 类型化事件和前一制品 source lineage 进入同一 `EvolutionLedger`。重开时必须重新认证每个 subject，复核 artifact tenant/audience/purpose/type、连续 sequence、唯一 eventId、首事件 `run-started`、前序 lineage 和完整投影；响应在 ledger 已提交后丢失可恢复为幂等成功，同一 baseline 的竞争 writer 通过“全局 head 捕获→Run baseline 复核→CAS”失败关闭，不能提交过期 lineage。`createAgentEvolutionIngress()` 只接受真实 evidence artifact adapter 与 run ledger adapter，source envelope 由构造期 authority 签发；Raw 明文只进入既有加密/投影边界，EvolutionRun 只保留认证 derivation manifest 引用。branded ingress 已接入交互 REPL、单轮 headless、长驻 stream headless 和 `AgentRuntime`：UserPrompt 在模型前持久确认，工具 requested/completed/failed 及 response/run end 在继续消费 generator 前确认，跨租户或未确认结果会阻止后续模型/工具执行；只有完整会话无 ingress 缺口时才写 `run-completed`。新增 adapter/三入口/竞争/响应丢失/篡改/跨租户回归 10/10，连同 ArtifactPorts 为 35/35 通过、另 1 项按平台条件跳过，既有 headless/runtime/REPL 回归 169/169 通过。该批次没有使用测试 HMAC 作为产品默认值，也没有自动打开演化采集；CLI 命令层生产 KMS/PKI/witness composition、Desktop/Graph ingress、Maintainer trigger 与旧壳退役仍待完成，因此 P1-6 只升级为“部分完成”。

后续 production composition-root 批次新增 `createAgentEvolutionRuntimeComposition()`，将 encrypted Raw store、evidence projector/verifier、ArtifactPorts、EvolutionRun adapter、真实文件 Ledger 与 durable witness 组成单一 branded root；artifact envelope、Raw encryptor、source envelope/verifier、keyed commitment、storage policy、attestation、ledger 和 witness authority 全部必须由部署方显式提供，ledger/witness 的 trust root 与存储路径必须独立，缺端口或伪 composition 会在创建目录、模型或网络启动前失败。`registerAgentCommand()` 只通过可选的 composition factory 向交互、单轮 headless 和 stream headless 传递不可序列化 ingress；未提供 factory 时保持关闭，命令选项、环境变量和本地测试密钥都不能隐式启用。真实全链路测试已验证 UserPrompt 只以密文进入 Raw、三事件 run 完成、同一外部 authority 下的新 composition 实例可恢复完全一致的投影，并验证命令/runtime 拒绝未品牌化对象；新增 composition 测试 2/2、受影响 runtime/命令入口回归 42/42 通过。由此 CLI 仓库内 composition 与三模式接线缺口关闭；剩余是目标环境供应及轮换生产 KMS/PKI/policy/witness authority、Desktop/Graph ingress、Maintainer/transition 调度和旧壳退役，P1-6 仍为“部分完成”。

2026-09-04 的 EVO-OPT-1 首批实现新增 canonical WikiSkill benchmark plan/report/envelope 与从零执行器：计划精确固定 Git SHA、runner/model/container、推理参数、硬件、工具/API、prompt/Skill/Wiki digest，强制五个带逐题 split digest 的数据集、至少三个互异 seed 和固定 1,000 次 paired bootstrap；执行器对每个 seed/split 依次发出同环境 `no-skill`/`skill` 请求，报告保存逐题 trace、grader receipt、失败分类、token/cost/latency，机器重算五集等权均值、delta、95% CI 与 p50/p95/p99。缺 split/receipt、seed 重复、计划漂移、报告替换或签名拒绝均失败关闭；没有可信签名 envelope 时公开投影只能返回 `external-paper-only / HOLD`。定向测试 8/8 通过。该批次完成可复现协议、统计内核和文案门，不伪造论文缺失数据；真实五数据集 loader、目标模型 runner、独立 grader/PKI、不可变制品持久化和至少三次完整运行仍待目标环境执行，因此 EVO-OPT-1 升级为“部分完成”。

后续 EVO-OPT-1 不可变持久化批次新增 `WikiSkillBenchmarkLedgerAdapter`。Canonical plan 现在可重新构造并精确复核所有字段、逐数据集 split digest 与 plan digest；完整 report 必须从原始 runs 重算五集等权统计、paired bootstrap、成本/时延和失败分类，不能只凭调用方携带的 report digest 入库。经独立 verifier 认证的签名 envelope 使用 ArtifactStore 的四种有限 ledger-retention 类型保存：plan 与 execution manifest 单独持久，report 按 512 KiB 分块并逐块固定原始字节摘要，bundle manifest 绑定 tenant、Skill、plan/execution/report digest、所有 chunk ref、完整 report byte digest、attestation 与时间；单报告上限 64 MiB，避免把测试小报告能放入 1 MiB 单制品误当成真实数据可用。Ledger 只提交 bundle manifest subject，读取时从全新 ArtifactStore 端口恢复 execution authority、逐块恢复并重算 report，再重新认证 attestation；append 已落盘但回执丢失可按同一 manifest digest 恢复。真实 Ledger 文件与独立签名 witness reopen、跨单制品上限分块、响应丢失和伪签名前置拒绝均已覆盖。不可变制品持久化缺口由此关闭；真实五数据集、目标模型和生产 PKI 下的完整运行仍未发生，公开 claim 继续 `HOLD`，EVO-OPT-1 保持部分完成。

后续 EVO-OPT-1/EVO-OPT-3 执行 authority 批次将 Benchmark plan 升为 v2，并用 `executionManifestDigest` 固定四方独立 authority 与目标环境：Dataset Provider、Target Runner、Grader、Report Attestor 都必须匹配 manifest 中的 authority id/revision/handler artifact digest，runner handler 还必须等于计划的 runner digest，目标容器/vLLM/硬件重算后必须等于 manifest environment digest。新的 `executeWikiSkillBenchmarkProduction()` 不接受一个同时自报结果和分数的宽 runner：Dataset Provider 的 authenticated+durable resolution 精确覆盖计划 split 并固定逐题 input digest；Runner 对每个 seed/split/arm 固定环境、model、工具、输入、Skill/Wiki 与 output/trace/cost/latency；独立 Grader 只对绑定该 Runner receipt 的输出签发 score；Report Attestor 最后固定 plan、execution manifest 与 report digest。四类 provider 都由构造期品牌捕获并独立重算 receipt digest、验证 attestation；替换一个合法但未预注册的 grader 会在数据读取前拒绝，伪造 Runner receipt 不会触发 grader 或报告签名。执行 host、Benchmark、持久 adapter 与 ArtifactPorts 合计 42 passed/1 平台条件 skip。仓库内独立执行角色与持久协议已建立；剩余是为真实五数据集、目标模型/容器和独立 grader/PKI 提供部署 provider，并在同一固定环境实际完成至少三次运行。

后续 Benchmark 产品入口批次新增 branded `WikiSkillBenchmarkCliHost`，将四方 production execution、Report Attestor、`WikiSkillBenchmarkLedgerAdapter.commit()`、全新 readback 与公开 claim 投影收敛为同一个 `run()`；只有持久 bundle 重新加载后仍精确匹配 plan/execution/report digest 且 attestation 再次通过，才返回 `VERIFIED`。`show()` 同样从 Ledger manifest 与分块 ArtifactStore 重建，不接受调用方直接传 report。正式 `cc evolution benchmark run <plan-file> <execution-manifest-file>` 与 `show <report-digest>` 已注册；未配置 Host、结构相似对象或 prototype 伪造均明确 unavailable，且 `run` 在读取用户文件前先验证 Host。公开 npm lazy/eager 已使用的 Ed25519 deployment loader 同时向签名单文件模块提供 Dataset/Runner/Grader/ReportAttestor/ExecutionManifest/LedgerAdapter/CliHost 七个内建品牌工厂，部署 bundle 无需复制内部品牌或回退内存实现。联合测试贯穿真实 Host 执行→签名→ArtifactStore/Ledger→新端口 show，Deployment/CLI/Benchmark 六文件 53 passed/1 条件 skip。至此仓库内可信产品入口已接通；没有目标部署提供实际 provider 时命令仍失败关闭，真实数据/模型/生产 PKI 和三次完整运行仍是 EVO-OPT-1 的外部验收缺口。

后续 descriptor-to-callable 加固把上述七个工厂进一步绑定到刚刚通过 Ed25519 descriptor 和 SHA-256 校验的 deployment module 原始字节。Dataset/Runner/Grader/ReportAttestor 工厂只接受 `handlerArtifactDigest === descriptor.moduleDigest`，Execution Manifest 也要求四个 authority 的 handler digest 全部等于同一已认证模块；因此签名 bundle 不能把任意摘要写进计划、再用另一段 callable 执行。合法 provider 与 manifest 从 exact-byte `data:` module 创建并保留内部品牌，声称其他实现摘要会在 provider 暴露前拒绝；Deployment loader 定向 10/10 通过。该批关闭仓库入口的 attested descriptor↔callable 绑定缺口；外部服务、容器镜像和模型 checkpoint 是否与部署 adapter 所调用目标一致，仍须由目标 runner receipt/PKI 和部署验收证明。

同日的 EVO-OPT-2 首批实现新增 canonical `EvolutionPlan`、候选触发门和 `EvolutionReleaseTrain`：单一计划一次性绑定 tenant/Skill、Git commit、baseline/candidate/Wiki/Eval/target matrix、risk/rollout/metric/permission/policy、触发证据、有效期与 token/cost/time/turn 根预算；requested capability 必须是 baseline capability 的子集。触发器只接受带归因 receipt 的重复 procedure failure、成功模式或用户纠正，先排除 provider/MCP/sandbox/permission/policy/infrastructure 噪声并拒绝跨 Skill 聚合。固定八阶段 `Wiki Maintainer→Proposer→Candidate→Eval→Review→Pilot→Promotion→Wiki impact` 逐步消费前一 output digest，每步必须返回 plan/operation/input 全绑定且 durable+accepted 的 receipt，状态通过构造期 branded CAS store 持久确认；缺 receipt、预算超限或有效期到期均在后续阶段前失败关闭。全新控制面从 durable state/receipt 恢复后不会重复既有阶段副作用；定向测试 5/5、ESLint 和格式检查通过。该批次形成统一协议与可恢复编排内核，仍需以现有 ArtifactStore+EvolutionLedger 实现生产 state store、把八个现有控制面装配到默认 Agent composition，并补实际进程强杀/双进程竞争验收，因此 EVO-OPT-2 保持“部分完成”。

EVO-OPT-2 持久批次随后新增 `EvolutionReleaseTrainLedgerAdapter`，将每步 state+receipt 作为单一有限产品类型 `evolution-release-train-checkpoint` 写入现有 ArtifactStore，并通过 `evolution.release-train.checkpoint.committed` 事件、全局 Ledger head/sequence CAS、前一 checkpoint subject lineage 和 durable readback 建立不可变链；没有新增旁路 JSON store。读取端只接受 ArtifactPorts 工厂产生的 branded ledger resolver，并逐项复核 artifact tenant/audience/purpose/type、plan/Skill、state/receipt/output 与 checkpoint digest。Ledger 已提交但响应丢失时，全新 adapter 从事件恢复而不重复该阶段；两个竞争控制面从同一 baseline 产生不同 output 时只有一个可写入，失败方不能覆盖 winner。ReleaseTrain、adapter 与 ArtifactPorts 联合 33 passed/1 平台条件 skip。至此仓库持久 state store 缺口关闭；剩余是把八个现有领域控制面装配为默认 stage ports，并补真实 OS 进程强杀、跨进程并发和 release/Wiki 双向 reconciliation 验收。

后续 EVO-OPT-2 领域装配批次先拆开 Proposer 与 Candidate 的实际持久化边界：`WikiInformedSkillProposer.draft()` 只产出 canonical proposal 与摘要，不触达 Candidate sink；`createCandidateFromDraft()` 支持从跨阶段持久制品恢复，并在写入前重新规范化整份 proposal，严格复核摘要、tenant、EvolutionRun、Skill、Wiki revision、proposer model、能力、runtime、证据 lineage 与 machine diff。原有 `propose()` 仍保持兼容并串联两步。定向测试 12/12 通过，且覆盖持久草稿被替换时 Candidate sink 零调用。该拆分为默认 `propose`/`candidate` stage adapter 提供了可独立 checkpoint 的真实领域边界；八阶段适配器及双向 reconciliation 仍未全部完成。

同一 EVO-OPT-2 批次继续落地前三个真实领域 stage adapter。`wiki-maintain` 调用 `EvidenceBackedWikiMaintainer.maintain()` 并要求持久 state digest 等于计划预授权的 Wiki digest；`propose` 先从新的 `WikiSkillProposalLedgerAdapter` 恢复，只有不存在时才调用 `draft()`，随后把完整草稿作为有限类型 `wiki-skill-proposal` 写入 ArtifactStore，并用独立 Ledger 事件绑定 plan/operation/input/output/tenant/run/Skill 后才签发 stage receipt；`candidate` 只消费相邻 propose output，并由 `createCandidateFromDraft()` 复核后要求实际 content digest 等于计划的 candidate digest。Ledger 已提交但响应丢失时新 adapter 可恢复且不重新生成；同计划替换草稿失败关闭。领域 stage/提案 adapter/Proposer 定向 20/20，ArtifactPorts 25 passed/1 平台条件 skip，ReleaseTrain 回归 7/7。至此默认领域转换完成 3/8；仍需 Eval、Review、Pilot、Promotion、Wiki impact 五个 adapter 与 release/Wiki 双向 reconciliation 验收。

EVO-OPT-2 默认领域装配批次随后完成余下 5 个 adapter，并修正了统一计划对目标矩阵的绑定强度。`EvolutionPlan v2` 除 candidate/baseline release 外显式固定 candidate ID+content、baseline artifact/content/revision 与 signed matrix-eval plan digest，防止用单 cell Gate 或另一份矩阵计划代替预授权的全 cell 合取评测。`eval` 只调用 branded `SkillTargetMatrixEvalAggregator` 与独立 receipt verifier，逐 cell 复核同一 suite/policy，并在外部 durability authority 确认后写入有限类型 `evolution-release-train-stage-output`；`review` 调用 canonical packet builder 与 `SkillPromotionReviewLedgerAdapter`，pending/reject 均不产生成功 receipt；`pilot` 驱动持久 progressive Canary，缺 gate evidence 保持 pending，只有 stable active 才继续；`promotion` 只调用 evaluated+human-reviewed controller，并从真实 ReleaseRegistry active pointer 恢复；`wiki-impact` 要求该 release 的 transition 确实存在于 branded source，且 reconciliation checkpoint 已越过对应 sequence。Agent production composition 新增 `releaseTrain.domain`，由根使用同一 ArtifactStore/EvolutionLedger/witness 自动创建 Proposal 与 stage-output Ledger，不再要求部署方手写八个任意函数；原 `stages` 入口暂保兼容。领域/持久适配器/Proposer 定向 38/38、默认/兼容 composition 2/2、ReleaseTrain+ArtifactPorts 33 passed/1 平台条件 skip，既有真实 OS 强退/双进程竞争 2/2。后续真实控制器纵向验收又覆盖：两个真实 Gate cell 经 matrix aggregator/verifier 进入 Eval adapter；真实 ArtifactStore+Ledger 人审 adapter 进入 Review；真实四级 signed progressive Canary 进入 Pilot；真实 mutation authority、evaluated+human-reviewed controller、ReleaseRegistry 与 Memory pending/reconciliation 进入 Promotion；真实 Wiki reconciler 将匹配 release transition 推进到 checkpoint 后进入 Wiki impact。接入时发现并修复 Promotion adapter 克隆 opaque mutation capability、导致 WeakMap 身份失效的问题：普通输入仍使用冻结副本，但 authority capability 保留原始不可伪造对象。受影响 9 文件 51/51 通过。后续双向 lineage 验收又证明 release transition 经真实 Wiki reconciler 形成固定 active release/transition digest 的 `proposal-decision` evidence 与 `skill-impact`，下一轮真实 Wiki-informed proposer 再把该 evidence ref/digest 和新 Wiki revision 固定进 proposal；Wiki/Proposer 联合 20/20 通过。最终跨进程纵切把真实 Wiki Maintainer、Wiki-informed Proposer、Candidate adapter、双 cell matrix Eval、持久 Review、四级 signed Pilot、evaluated-only Promotion/磁盘 ReleaseRegistry 与 Wiki reconciliation 全部装入同一 ReleaseTrain。matrix plan 先签名并进入 `EvolutionPlan`，随后才执行真实 Eval；Review 从已持久 Eval 收据延迟构造，Pilot 从持久审批恢复，Promotion 再由同一确定性 Gate authority 重新认证矩阵收据并完成 mutation authority、prepare/finalize 与 active CAS，Wiki impact 最后只接受该精确 release/state 并推进 reconciliation checkpoint。进程 A 在 Review packet、双人决策和 stage output 落盘但主 checkpoint 前以退出码 75 硬终止，进程 B 从真实 ArtifactStore、EvolutionLedger、文件 witness、Wiki state、proposal/review artifact 恢复后收敛到 8 个唯一阶段副作用和 16 个账本事件。跨进程整组 3/3、matrix/Promotion/Wiki-impact 定向 10 passed/1 环境条件 skip，跨进程真实控制器覆盖达到 8/8；EVO-OPT-2 的仓库内交付与验收项完成，目标部署的生产 PKI、runner、traffic authority 与跨主机演练继续归 EVO-OPT-7。

Workbench deployment-loader 批次开始关闭 EVO-P2-2/EVO-OPT-7 的公开 npm 入口缺口：lazy dispatcher 与 eager `createProgram()` 均可为 `agent/evolution/serve` 装载目标部署依赖；部署方必须同时提供绝对路径的 descriptor 与 Ed25519 trust root。canonical descriptor 固定 revision、命令白名单、单文件 ESM 路径/module digest 和 trust-root digest；CLI 先验证 trust root、签名与模块 SHA-256，再通过 `data:` URL 执行刚刚认证的同一字节，避免 hash 后路径替换窗口，最后只接受模块导出的 `createChainlessChainCommandDependencies()` 返回值。调用上下文按命令下放 Workbench、knowledge review 和 Agent composition 的窄内置 factory，使部署 bundle 无需重新导入可变包路径即可构造带 CLI 内部品牌的对象。未配置保持原行为，半配置、签名/摘要漂移、模块超限或缺导出均失败关闭，且不生成测试 identity、KMS、PKI、Ledger 或内存 fallback。loader 6/6、loader/lazy-eager 合计 28/28 通过。该批次让“已安装 CLI 如何取得部署 Host”有了可发布入口，但截图中的 Workbench 只有在管理员签发 descriptor 且目标模块实际返回 branded Workbench/identity/projection/transition/metrics authority 后才会变为可用；生产 authority、灾备和部署演练仍属于 EVO-OPT-7。

EVO-OPT-4 的统计化渐进 Canary 首批新增 canonical risk-tier plan 与 branded assignment authority：rollout 不再内置固定 `1%`，而由计划预注册 `shadow→一个或多个单调递增 Canary step→active-probation` 的流量比例、样本量和观察窗，只有末步 gate 通过才可投影为 `stable`。服务端 authority 用 plan-bound salt 和 subject digest 生成稳定万分桶，receipt 固定 tenant/pilot/step/subject/bucket/decision、authority revision/handler、签发/过期时间与签名；客户端增删字段、换 subject、伪造 assigned 或提交未分配流量均被拒绝。每步只消费带独立 outcome receipt 的同期 paired baseline/candidate 样本，固定执行 1,000 次 deterministic paired bootstrap，并同时检查预注册置信下界、mean cost、p95/p99 latency、tool-call 非劣门；任何 security 或 permission event 都直接阻断扩量。

后续 Pilot transition-integration 批次把 plan/assignment/gate report 协议提升为 v2，并在计划内固定独立 gate authority 的 id/revision/handler digest。新增 branded gate authority 重新验证每条 assignment 后生成完整 report，再对 tenant/pilot/plan/step/report bytes、签发/过期时间做签名回执；跨计划 assignment authority、报告替换、字段增删与过期签名均失败关闭。`ControlledSkillProductionPilot` 的新 v2 progressive state 不再接收旧 observation 路径，只按签名 gate receipt 依次执行 `shadow→Canary[n]→active-probation→active`，每次先将 report/receipt digest 写入 durable prepare state，再调用既有 transition authority 并保持 finalize/reconciliation 语义；同 stage 的多级 Canary 也各自形成独立 transition。既有 v1 Pilot 行为保持兼容。其后新增 `ProgressiveCanaryTrafficWorker`：服务端自行取得 assignment，未分配请求不会触发任一 arm；traffic manifest 固定 baseline/candidate runner 与 paired-outcome authority 的 id/revision/handler digest，同一 canonical request 并行执行两条 arm，逐条验证 authenticated+durable runner receipt，再由独立 outcome authority 对完整 paired receipt 签名。worker 在执行前以 plan/manifest/step/subject/request 的 operation digest 取得 durable exclusive reservation，阻止跨 worker 重复副作用；提交后要求 authenticated+durable acknowledgement 和 fresh readback，响应丢失可按同请求恢复，不同请求不能替换同主体。持久观测可无转换进入既有 gate，security/permission event 仍直接失败。worker/统计/Pilot 联合 30/30 通过，并含真实临时文件 readback、并发抢占、runner replay 拒绝和 gate 串接。由此仓库内 traffic execution/observation 协议缺口关闭；EVO-OPT-4/P2-1 仍因目标流量入口部署、watchdog 独立进程/存储部署、目标租户观测窗和扩量验收而保持“部分完成”。

外部失联保护批次新增与 Pilot 宿主无进程内依赖的 `ProgressiveCanaryExternalWatchdog` 协议。预注册 v2 plan 固定 tenant/pilot/descriptor/LKG baseline/host、租约长度，以及 heartbeat/rollback authority 的 id/revision/handler digest/Ed25519 SPKI digest；watchdog 只从 durable latest-heartbeat source 读取，宿主心跳对 sequence、stage、active-state、issued/expires 和 plan identity 签名，再用 watchdog 自己的 `observedAt` 判断租约，不能由调用者重放旧心跳或由宿主自报“仍健康”。只有 shadow/Canary/active-probation 属于监控期，终态 active/rolled-back 不会因租约停止而被误回滚。租约超时后以稳定 incident digest 取得 durable exclusive reservation，严格先调用外部 kill authority、验证 authenticated+durable receipt，再调用 LKG rollback authority并验证 active-state receipt，最后持久提交并 fresh readback；相同事故重看从记录恢复，两个 watchdog 并发只允许一个发出 effect。Node PKI adapter 允许宿主进程持私钥签发 heartbeat，而 watchdog 进程只持 plan-pinned public key 验证；kill/LKG effect 读回后由独立 rollback key 签名，公钥或私钥替换在 authority 构造前失败关闭。新增物理文件 store 以 immutable sequence 文件保存 latest heartbeat、以 plan/incident digest 命名 reservation/incident，限制文件类型、硬链接数、大小和历史上界，写入后 `fsync` 并 fresh-instance 读回；事故 reservation 带租约，watchdog 在真实子进程已被 kill、但自身于 rollback 前崩溃时，重启后可在租约过期接管同一 incident，再确认进程已不存在并完成 LKG 回滚。真实 Node 子进程只持 heartbeat 私钥，父 watchdog 只持公钥；测试确实终止不同 PID、注入 kill 后崩溃、确认 candidate 尚未回滚，再接管、回滚、重开 incident。控制协议 5/5、Node PKI 3/3、file store 3/3、process fault drill 1/1，与 worker/统计/Pilot 合计 42/42。跨进程 PKI、真实 kill 与本机存储恢复的仓库缺口已关闭；目标环境仍需把 watchdog 和 durable store 部署到独立故障域并完成目标基础设施故障演练，因此路线项仍为部分完成。

EVO-OPT-3 的进程执行面新增 `EvolutionEvalProcessSupervisor`。构造时只接受 `isolation=process` 的目标描述符，读取单链接普通模块并要求 SHA-256 等于 `handlerArtifactDigest`；已认证字节被捕获为不可变 data-module，执行前后仍复核来源文件，避免路径在 descriptor 与 callable 间被替换。每次调用通过宿主 Process Broker（测试显式注入 native spawn）以 `shell:false`、空环境、独立 PID、1 MiB 输入/输出边界运行；target result 与 invocation/revocation/supervision evidence 分别绑定 request/capability/target/operation/timeline/result 和独立 authority revision。deadline 触发 `SIGKILL` 后必须等到 child `close` 才签发 hard-terminate receipt，故障进程也必须先观察到退出；测试证明成功目标运行在不同 PID、hang 被 hard kill 后迟到文件永不出现、模块漂移在 spawn 前拒绝、grader-style crash 失败关闭且 PID 已消失。签名 deployment loader 现将该 supervisor 作为 evolution/serve 内建品牌工厂提供，并强制 supervisor authority 的 `handlerArtifactDigest` 等于已认证 deployment module digest；替换 digest 在读取 target 前失败。后续 composition 批次又让正式 Gate 的 process executor 走同一主管，同时用既有主管作为非进程 target 的窄 fallback；完整 Gate 在一秒 wall deadline 后取得 hard-terminate settlement，确认真实 child PID 已退出、两秒后的迟到文件未产生且进程内 executor 从未被调用。sandbox 批次为每个 process target 固定有界策略：Node permission mode 默认不授予文件、子进程、Worker、原生扩展或 WASI 权限，文件读写仅允许至多 32 个显式绝对路径，V8 heap 限制为 32–1024 MiB（默认 128 MiB）；负测确认未授权文件读取和派生子进程均返回 `ERR_ACCESS_DENIED`，而 hard-kill 专项目录可显式放行。最新持久 child evidence/receipt 批次新增 `EvolutionEvalChildEvidenceLedgerAdapter`：invocation、revocation 与完整 Gate receipt 按各自规范 digest 写入有限 `evolution-eval-child-evidence` ArtifactPorts ledger-retention 类型，再以 tenant/stream/digest 唯一事件追加真实 EvolutionLedger；supervisor 与 Matrix 在消费前都必须取得 authenticated+durable acknowledgement，并经 resolver fresh readback 精确复核 authority/revision/handler、scope、kind、digest 和完整字节。Matrix 的 child receipt store descriptor 进入 authority root v2，每个独立验证后的完整 cell receipt 必须持久回读后才计算 `childFullDigest`；未品牌化 store、非 durable acknowledgement、readback 替换或 descriptor digest 替换均失败关闭。真实 ArtifactStore、Ledger 文件与独立 witness 重开后可解析三类证据/receipt；完整 Gate hard-kill 与两 cell Matrix 均经过持久端口。主管 9/9、adapter 2/2、Matrix 6 passed/1 平台条件 skip、Gate 129/129、loader 12/12、ArtifactPorts 25 passed/1 平台条件 skip，去重合计 183 passed/2 skip。该批关闭真实 process isolation/hard-kill、签名构造入口、完整 Gate/Matrix composition、本机权限基线与持久 child receipt resolver/reopen；尚需在 Linux/Windows/macOS 的目标平台 sandbox 上覆盖网络越权、OS 级 CPU/内存/文件资源限制和真实目标部署演练，EVO-OPT-3 继续部分完成。

EVO-OPT-6 的首批收敛新增共享 `@chainlesschain/session-core/evolvable-artifact`：统一固定 tenant/type/content digest、parent/lineage、dependency lock、runtime/permission manifest、candidate/release、Eval/review/promotion receipt 与 active/LKG 字段；Skill、Prompt、Hook、Knowledge authority 由构造期 branded type policy 隔离，policy 必须分别提供 admission、evaluator、activation 和 rollback，Knowledge authority 不能消费 Prompt/Skill 制品。Hook candidate 额外强制 code-signature、SBOM、sandbox 与 network-egress-policy digest，activation 必须有两名不同身份的高风险签名审批；Prompt candidate 强制 data-policy digest。依赖 release/content 漂移会确定性级联标记直接和传递依赖制品为 stale，并阻断继续评测/激活；stale 状态不能原地清除，必须由同类型 authority 生成绑定新 dependency lock、全新 candidateId 与 revalidation receipt 的候选，旧 active/LKG 保持不变，Eval/review/promotion 清空后重走全链。Desktop `PromptOptimizer.createVariant()` 已改为异步写入统一 candidate gate，SQLite 本地投影固定 `is_active=0`；缺 branded gate 时拒绝而非恢复旧 auto-active。Renderer `hooks:register` 只可持久化统一 Hook candidate，不再调用 `HookSystem.register()`；`set-enabled(true)`、全局 enable 与 renderer reload 直接拒绝，disable/cancel 保留。后续 Skill Sync 适配让部署方可注入同一 branded Skill gate：已验证并规范化的完整 package 作为 content 写入，artifact 固定 Skill type、tenant、package digest、runtime/signature-lock、permission capability 与 dependency lock；有声明依赖但缺可信 release resolver、覆盖旧 Skill 但缺可信 lineage resolver 时失败关闭，返回结果继续明确 `candidateOnly/activeMutation=false/hotLoaded=false`。原 Skill-only candidateStore 已降为统一 gate 内部的持久 backend adapter：必须显式 tenant，仍执行原 exact schema/readback/tamper 验证，但不能作为平行成功路径；同步 IPC 又强制校验共享 artifact digest 与 persistence receipt，只有旧 `candidate-staged` 文案不能冒充成功。最新持久发布门要求先验证候选落盘 receipt，再由同类型 authority 消费 Eval/review/promotion 或 rollback receipt；canonical transition request 以 previous/next artifact digest 导出稳定 operationId，写入响应无论成功或丢失都必须经独立 durable readback 精确复核 request、完整 artifact 与 receipt，替换回读不能激活。Governed Knowledge 的人工 conflict merge 唯一真实 sync publisher 入口也已强制注入 tenant-bound branded Knowledge candidate gate：候选绑定 local/remote/merged lineage、merge plan、人工 receipt、scope/vector clock、权限与父 release，并且 candidate persistence 失败时 transport 不会被调用、merge 不会 settlement；带依赖的知识在可信 artifact release resolver 接入前明确失败关闭。后续新增通用 `EvolvableArtifactLedgerAdapter`，把 candidate 与 promote/rollback transition 纳入 ArtifactPorts 的有限 ledger-retention 类型：完整制品和 request/receipt 先写真实 ArtifactStore，再以 signed EvolutionLedger event 串联精确前驱，transition 重算 request digest，candidate、promotion 和 rollback 可由全新的 ports/ledger/adapter 实例恢复且重复提交幂等；真实文件测试 1/1、ArtifactPorts 25/25 通过并有 1 项平台跳过。统一 candidate gate 又新增 `stageRevalidationCandidate()`：只有同类型 authority 能把 stale 制品转为绑定新 dependency lock/revalidation receipt 的候选，随后必须取得同一 candidate writer 的 persistence receipt，不能停留在内存态或绕过持久门。后续 release gate 增加同 authority identity 与受 gate 品牌保护的两阶段 promotion：`preparePromotion()` 只完成 candidate receipt、Eval/review/promotion policy 校验并计算 next active，不写持久状态；外部副作用成功后才可由同一 gate 的 `commitPreparedPromotion()` 落盘，克隆或跨 gate prepared 对象失败关闭。Ledger adapter 同时提供 tenant-bound branded transition reader，供独立 verifier 从全新 adapter 回读。Knowledge 人工 merge 随后完成 release 纵切：publisher v2 在 transport 前完成同 authority Knowledge activation policy 预授权，sync publication durable readback 后才提交 active transition；独立 verifier 从不同 reader scope 的第二个 branded reader 复核 candidate→active digest、release、plan/content 和 transition receipt，同 scope 复用在构造期失败，以上字段全部进入 publish result digest 与 merge settlement。transport 失败或 activation 拒绝均不会写 transition，旧 v1 settlement 只读兼容而新发布只能生成 v2。共享包全量 551/551，Prompt/Hook/IPC/phase 联合回归 375/375，Skill Sync+phase 74/74，Knowledge sync/merge 23/23，相关 ESLint 通过。普通非 merge Knowledge 演化入口与可信 dependency resolver 仍未统一，通用 adapter 与 revalidation 触发也尚未装配进其他产品入口，因此本项仅为“部分完成”。

后续 dependency resolver 批次由同一 `EvolvableArtifactLedgerAdapter` 提供 tenant-bound branded release resolver，只从已认证 transition history 投影每个 artifact 当前 active release；rollback 后被撤销内容立即不能再解析。Knowledge merge 不再接受调用者自报 dependency lock，而是逐项固定 source kind/digest/disposition，并要求 resolver 返回 authenticated、durable 且精确绑定的 artifactId/type/releaseId/contentDigest 后重新计算 lock digest。缺 resolver、未知 kind、跨租户、无匹配或多匹配均在 candidate persistence 与 transport 前失败；正向测试先写真实 typed Knowledge release，再证明 merge candidate 锁定同一 Ledger release。Knowledge sync/merge 26/26、resolver/adapter 1/1 通过。可信 dependency resolver 的仓库缺口由此关闭；剩余为普通非 merge Knowledge 演化入口，以及把通用 release/revalidation adapter 装配进其他产品入口。

普通 Knowledge 同步统一化的协议批次新增 branded `GovernedKnowledgeArtifactLifecycle`，构造期固定同 authority candidate/release gate、Ledger-backed release resolver 与不同 reader scope 的 transition verifier。`prepare()` 只接受 digest-only evidence、有界 operationId 和显式 `humanReviewed`，先认证当前 typed baseline 与依赖 release，再持久化 candidate 并完成 policy 预授权；非 typed 旧 baseline 不会被静默伪造成 release。`commit()` 只消费同一 lifecycle 产生的不可克隆 handle，提交后必须由独立 reader 精确回读 candidate→active transition。真实 ArtifactStore+Ledger 测试覆盖首次发布和下一版本 parent release 连续性，2/2 通过。

后续 publish 纵切已使 `GovernedKnowledgeSync` 构造期强制注入 branded lifecycle；普通发布和人工 merge 共用唯一 `publishWithArtifactEvidence()` 路径，严格执行 candidate→同步记录 durable commit→transport durable ack→active transition，merge publisher 原有平行 candidate/release 实现已删除。同步 Ledger schema 升至 v3，将原 baseline、operation、issuedAt、authorization/evidence digest 与 `humanReviewed` 精确绑定到本地发布记录；transport 响应丢失后，新实例用原绑定幂等重建 prepared handle，不能用已更新的 current record 篡改 parent。旧 v1/v2 记录仍可认证读取，但缺 v3 binding 时不能冒充可恢复的新发布。

receive 纵切继续把 accepted remote 与 concurrent conflict 纳入同一个 lifecycle：stale/未接收 replay 不创建候选，accepted 必须先持久化 candidate，再完成 remote sync commit，最后才激活；conflict 只持久化绑定当前 active baseline 的 candidate，明确 `candidateOnly=true`，不会写 active transition。同步 Ledger schema 升至 v4，新增按 envelope digest 的 authenticated reception readback，并把 receive operation、原 baseline、evidence、issuedAt 与 `activate` 决策写入 v2 artifact binding。若进程在 remote sync commit 后、transition 前失败，新控制器会用原 binding 恢复并幂等补齐唯一 transition，而不会因 current 已更新而误报 replay；重复接收不会重复 sync commit 或 transition。v1/v2/v3 历史记录仍保持只读认证兼容，不能缺少 v4 binding 冒充可恢复 reception。真实加密/RBAC/普通同步与 lifecycle 22/22、Ledger 套件 27/27 通过。至此普通 publish/receive 与人工 merge 均已统一；EVO-OPT-6 剩余工作转为其他产品 release/revalidation 入口装配。

Desktop 目标组合接线批次不再要求启动调用方直接传入三个可伪造的 reader：共享协议新增 branded runtime composition，由目标部署一次性提供 tenant、四阶段 policy、candidate/transition 持久端口与 active provider，再生成按 Skill/Prompt/Hook 类型隔离的 candidate gate、release gate 和 active reader；克隆 composition、缺字段、拼错字段或缺任一类型均失败关闭。既有 Ed25519 签名、trust-root/module digest 固定的 deployment descriptor 新增 `desktop` allowlist，认证后的单文件模块只能调用 Desktop 注入的组合工厂，不能覆盖 CLI 其他命令的内建工厂。主进程在任何 bootstrap 前加载并校验组合：Hook reader 进入 critical/deferred bootstrap context，Skill/Prompt reader 进入唯一 IPC dependency bag；未配置 descriptor 时保持显式空依赖和既有不可激活状态，部分配置、签名/摘要漂移或伪 composition 会终止启动而不回退旧 authority。Session Core、CLI loader、Desktop loader 与 Hook/Skill 联合定向回归分别 **9/9、8/8、13/13**，Desktop main build 通过。由此关闭“目标 composition 只留 seam、默认启动未注入”的仓库接线缺口；后续执行器进展见下一段，目标环境真实 KMS/PKI/数据库端口与部署验收、Prompt/Hook/Skill release/revalidation producer 仍待完成。

Governed Skill 可执行纵切随后复用现有的一次性隔离 worker，而没有把 active handler `require()` 回 Electron main：加载 active release 时重新从内存 package 构造 canonical execution manifest，逐字节固定 `SKILL.md` 与 `handler.js` 的长度和 SHA-256、Skill/version/capability，并验证 Ed25519 signature lock；同时要求共享 artifact 的 handler/signature-lock digest 和 permission capabilities 与同一内容精确一致。只有由该校验创建的私有 branded authority 才能取回闭包内 handler source，克隆对象不能读取；执行时传递精确 source、release content digest、signer fingerprint 和 capability 集给既有禁 shell、无继承环境、有界 frame/timeout/capability broker 的 worker，绝不重新打开 managed/workspace/marketplace 路径。active 替换若签名漂移会先卸载旧 governed Skill，缺隔离 executor 也在加载期失败关闭。完整 Skills 套件 **73 文件、1136/1136** 通过，相关安全/Markdown/worker 联合 **53/53**，Desktop main build 通过。该批关闭 Skill 的内容寻址执行缺口；Hook 进展见下一段。

Governed Hook 可执行纵切退役了 candidate/active 路径中的可变 shell command 与宿主脚本路径：IPC 现在只接受 `chainlesschain-hook-executable/v1` 内联 Node 制品，签名 manifest 精确固定 hookId/event、runtime、文件名、source bytes/SHA-256、capabilities 以及 SBOM/sandbox/network-egress policy digest；candidate 写入前和 active 读取后均验证 Ed25519 signature lock，且共享 runtime `codeSignatureDigest` 必须绑定同一 lock。active Hook 在内存中转为只持有 branded source authority 的 async handler，并复用同一个禁 shell、无继承环境、有界协议/超时/capability broker 的一次性隔离 worker；Electron main 不 `eval`/`require` source，也不重新打开路径。旧 command candidate、脚本路径、签名替换、缺 runner 或 runtime/permission 漂移全部失败关闭。Hook 全目录 **8 文件、303/303** 通过，新增候选/active 定向 **9/9**，Desktop main build 与精准 ESLint 通过。至此 Hook/Skill 内容寻址执行器的仓库缺口关闭；EVO-OPT-6 剩余集中到 Prompt/Hook/Skill release/revalidation producer 与目标环境真实 authority 部署验收。

三类产品 lifecycle producer 批次把 candidate gate、release gate 与 active reader 组合成同 authority-scope 的 branded producer。新增 candidate reader 只接受 authenticated+durable、tenant/type/artifact/candidate/content/artifact digest 全绑定的持久记录和原 candidate persistence receipt；CLI 的真实 `EvolvableArtifactLedgerAdapter` 已提供相同格式的 candidate provider，并从 ArtifactStore+Ledger 重新认证记录。`promote()` 的调用者只能提交 artifactId/candidateId，Eval/review/promotion receipt 和 releaseId 必须由部署侧 durable promotion authority 生成，commit 后还要由 active reader 精确回读 release/content/artifact digest；`revalidate()` 只接受 active reader 返回的持久 stale artifact 与原内容，新 dependency lock/candidateId/revalidation receipt 同样只能由部署 authority 提供，再经同 candidate gate 落盘。Desktop 签名 composition 现在强制 Skill/Prompt/Hook 三套 producer 齐全，并新增两个窄 IPC；renderer 不能上传 artifact、policy receipt、dependency lock 或 authority 标志，未配置、伪 producer、额外字段及 Knowledge 越权均失败关闭。Session Core 全量 **555/555**、真实 Ledger adapter **3/3**、Desktop deployment/IPC/Phase **54/54** 通过，主进程构建与精准 ESLint 通过。至此 Prompt/Hook/Skill release/revalidation 的仓库 producer 与产品入口已形成；目标环境仍须提供真实 KMS/PKI/数据库 candidate/transition/active provider、人工/Eval promotion authority、dependency revalidation authority，并执行签名部署与故障恢复验收。

后续 active-content 基础批次将通用 `EvolvableArtifactLedgerAdapter` 记录升级为 v2：candidate 可持久化与 `contentDigest` 精确一致的 canonical 内容，transition 明确禁止内嵌可替换内容；v1 元数据记录仍可认证读取，但不能伪称内容可用。新增 tenant-bound branded active-release reader 只从重新认证的 transition history 定位当前 release，再沿 `previousArtifactDigest` 唯一回溯 candidate 内容；未知类型、多 transition/candidate、lineage 不一致、内容缺失或替换全部失败关闭。测试证明全新 adapter 可读取 exact active 内容，第二版本激活后返回新内容，rollback 后恢复上一 release 的原内容，伪造 reader 不具品牌；adapter/Knowledge 相关 23/23 通过。该批解决 Prompt/Hook/Skill 产品装配前“只有 digest、没有可执行内容”的阻断，但尚未把 reader 接到各产品启动/激活入口。

后续 Hook 消费边界批次把 active-release reader 的品牌和输出校验上移到共享 `session-core`：reader 必须由同 tenant/type 的 release gate 构造，返回值同时携带并重新验证 active artifact、release/content/artifact digest、durable/authenticated 标记与 exact canonical 内容；普通对象、跨类型 reader、内容替换、artifactId 替换和重复 active ID 均失败关闭。Desktop `HookSystem` 默认不再扫描项目/用户配置或脚本目录，bootstrap 只有收到 branded Hook reader 才启用 active release 加载；该批当时只接受内容已绑定的 command Hook，旧 script-path 因脚本字节仍可能发生 TOCTOU 而明确拒绝，后续已由上文的签名内联制品和隔离 worker 取代 command。公开 `HookSystem.register/registerMultiple` 继续允许应用内可信函数 Hook，但 command/script 直接注册被阻断；内建监控 Hook 不受影响。共享包全量 552/552、Hook/启动联合 39/39、adapter 3/3 通过。

后续 Prompt 消费边界批次移除 `PromptOptimizer.getActiveVariant()` 对本地 `prompt_variants.is_active` 的授权依赖：该查询改为按 `prompt:<skillName>` 从 branded Prompt active-release reader 异步读取 exact 内容，缺 reader、伪 reader、跨类型 reader 或不可用内容均失败关闭，不再降级选择本地成功率最高的候选。新建表的 `is_active` 默认值降为 `0`，candidate 写入继续显式为 `0`；同步统计不再把旧投影行数冒充真实 active，而是返回 `activeVariants: null`、authority 状态和单独的 legacy 行数。Phase 20 可由宿主注入窄 reader，新增 `prompt-opt:get-active-variant` IPC 只转发该治理结果；已有单例在后注入时也会升级 reader，避免早期构造绕过。Prompt/IPC 定向 32/32、相关 ESLint 通过。该批形成 Prompt 产品读取入口；composition 与 lifecycle producer 后续已按上文接入。

后续 Skill 消费边界批次把 Desktop `skills:load-all/reload` 从四层目录扫描改为“仓库内 bundled + branded active Skill release”：`SkillLoader.loadBundledOnly()` 会先清空旧的 marketplace/managed/workspace 解析态，只读取 bundled 层；`SkillRegistry` 再从同 tenant/type reader 取得 active package，并重新校验 package 精确字段、checksum、Skill ID/version、runtime/permission manifest 和 SKILL.md 严格解析结果后才注册。该批当时只接受无 handler、无签名锁、无 capability/外部依赖的内容型 Skill；后续已由上文的签名内存字节 authority 和隔离 worker 接通可执行 handler。Phase 1/Skills IPC 可由宿主注入窄 reader，既有 Registry 单例支持后注入；Marketplace 安装发现 `SKILL.md` 时不再调用直接 hot-load，只保留 inactive 文件并明确要求治理晋级。active 集合撤销、内容失配或 reader 刷新失败都会卸载旧 governed 实例，不能继续执行内存残留。Skill loader/sync/active 定向 82/82、Skills 全量 1136/1136、Phase 50/50 和主进程构建通过，相关 ESLint 无新增错误。

## 2. 外部方案实际提供了什么

### 2.1 WikiSkill：它是研究框架，不是已发布的 Google 产品功能

本文所称 WikiSkill，是 Google Research 与 Virginia Tech 作者于 2026-08-27 发布的 v1 预印本框架，而不是 Google 已正式交付的产品功能。论文没有链接官方实现仓库，因此目前可借鉴的是论文机制和实验结果，不能把其生产成熟度、安全性或可复现性视为已经得到证明。参见 [WikiSkill 论文](https://arxiv.org/html/2608.27454)。

还需限定其适用范围：WikiSkill 验证的是**按数据集运行的离线、批量 Skill 优化**，不是从单次生产任务即时学习的在线系统。每个数据集及独立演化运行从空 Skill/Wiki 开始；每轮对训练集 rollout、提出一个单 Skill 变更，再用反复复用的留出验证集平均分决定是否接受。因此论文中的“persistent”首先指同一演化运行内跨迭代保留，尚未验证跨项目、跨部署或终身持续演化。

WikiSkill 的关键设计不是更新模型权重，而是把 Agent 经验编译为外部、可复用的程序性知识：

| 层              | 论文机制                                                                                 | 对 ChainlessChain 的启发                                                                            |
| --------------- | ---------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| `raw/`          | 保存完整且不可变的执行轨迹                                                               | 原始证据不能被 outcome 回填或后续总结原地改写；应采用 append-only event + deterministic projection  |
| `wiki/`         | 保存 pattern pages、索引、演化日志和 `skill-impact.md`                                   | 日志不等于知识；需要介于轨迹和 Skill 之间的持久、可纠错、可合并中间表示                             |
| `skills/`       | 保存 active `SKILL.md` 和回指 Wiki 模式的 `PURPOSE.md`                                   | 每个 Skill 必须回答“为何存在、由哪些证据产生、对什么运行时有效”                                     |
| Inference Agent | 演化训练 rollout 中只读取 active skills，不读取 Wiki                                     | 可作为本项目 Pilot 的默认隔离实验；是否适用于所有生产推理，应按领域对照验证，不能由一次消融直接推广 |
| Wiki Maintainer | 从成功和失败轨迹提炼、更新模式                                                           | 失败不是一次性日志，而应成为跨轮积累的反例与边界知识                                                |
| Skill Proposer  | 基于 Wiki 和选择性 Raw 证据提出单 Skill patch                                            | 一次演化只改一个可归因单元；这是变更粒度，不代表文件或部署事务原子性                                |
| Gate            | 在留出但每轮复用的 validation split 上严格比较；未提升则恢复上一轮 Skill 配置，Wiki 保留 | 可借鉴“候选不提升就不采用”；它不是统计晋级门、部署事务或外部副作用回滚                              |

WikiSkill 直接验证到的是单 Skill 提案、validation score gate 和优化工作区内的 Skill 配置回退；本报告提出的安全/权限门、人工审批、content-addressed registry、shadow/canary 与生产回滚，都是面向 ChainlessChain 的工程化扩展，不应反写成论文已交付能力。

论文结果说明了四个值得直接转化为工程约束的事实：

- Gemini-3.5-Flash 四基准消融中，在 Inference Agent 均不读 Wiki 时，启用 Maintainer 并让 Proposer 使用持久 Wiki，使最终测试平均分由 `48.7%` 提升至 `63.7%`；这支持的是**完整持久知识管线**在该实验设置中的价值，不能只归因于 Wiki 数据结构。
- 同一组消融中，让 Inference Agent 在演化训练 rollout 也读取 Wiki，平均分由 `63.7%` 降至 `60.9%`。论文把可能原因表述为假设，因此本项目应把隔离设为 Pilot 默认和可配置实验，而不是宣布生产推理永远不得访问 Wiki/Memory。
- 五基准测试平均中，Qwen-3.5-9B + WikiSkill 为 `47.4%`，高于无 Skill 的 Qwen-3.6-27B `39.4%`；这说明程序性知识在这组基准中弥补了部分模型规模差距，不能泛化为普遍替代模型规模。
- 跨模型迁移既可能增益，也可能严重负迁移：Qwen-3.5-4B 的 Spreadsheet Skill 曾把 Gemini 从 `50.5%` 降到 `18.1%`；因此“源模型有效”不能替代目标模型、工具集和环境上的重新验证。

2026-09-04 收到的对比摘要需要按论文 Table 1 校正。下表是五个 benchmark 的等权平均，不是单项得分，也不是 ChainlessChain 自测：

| 模型                 | No skill | WikiSkill | 相对自身 No skill | 相对 Qwen-3.6-27B No skill `39.4%` |
| -------------------- | -------: | --------: | ----------------: | ---------------------------------: |
| Qwen-3.5-4B-Instruct |  `26.2%` |   `38.5%` |         `+12.3pp` |                           `-0.9pp` |
| Qwen-3.5-9B-Instruct |  `29.9%` |   `47.4%` |         `+17.5pp` |                           `+8.0pp` |
| Qwen-3.6-27B         |  `39.4%` |   `63.3%` |         `+23.9pp` |                          `+23.9pp` |

摘要中的“Qwen-4B + WikiSkill `33.3%`、相对 27B 裸模型 `-6.1pp`”不是论文宏平均；正确值分别为 `38.5%` 和 `-0.9pp`。论文中的 `33.3%` 出现在 Qwen-3.5-4B + SkillOpt 的 SealQA 单项，不能移作 WikiSkill 五基准平均。论文表值来自完整演化流程的三次独立运行均值，并使用 1,000 次 paired bootstrap 做显著性检验；但作者未提供官方代码仓库、逐题原始结果、完整 seed/解码参数、vLLM 版本和硬件环境。因此这些数字只能标注为“WikiSkill 论文外部结果”，在 ChainlessChain 完成独立复现前不能用作产品性能承诺。

WikiSkill 本身也有明确边界：论文采用全量 Skill 注入，没有验证大规模检索和触发；严格 `>` gate 会拒绝中性但可能有后续价值的基础 patch；没有自动 Wiki pruning；验证集较小；没有覆盖数百步、数小时的在线适应；也没有报告隐私、prompt injection、恶意 Skill、权限扩大或供应链安全评测。

### 2.2 Claude Code 最新进展与不足

截至审计日，Claude Code 官方 changelog 最新版本为 `2.1.251`（2026-08-28）。相关能力已经形成很强的执行底座：Auto Memory、agent memory、Skills、Hooks、Plugins、`/goal`、Dynamic Workflows、Agent Teams、跨会话消息和模型切换 hooks。参见 [Claude Code changelog](https://code.claude.com/docs/en/changelog)、[Memory](https://code.claude.com/docs/en/memory)、[Goals](https://code.claude.com/docs/en/goal)、[Workflows](https://code.claude.com/docs/en/workflows) 和 [Hooks](https://code.claude.com/docs/en/hooks)。

`2.1.251` 的直接增量包括 `PreModelSwitch`/`PostModelSwitch` hooks、前台 subagent 工具调用可见性，以及多项 team/subagent 通信修复。该版本还修复了 file tool symlink-swap TOCTOU、plugin command path traversal、Workflow `scriptPath` 在权限检查前读取、Grep/Glob 经 symlink 绕过 deny，以及 project settings 绕过 managed tracing/logging policy 等问题。这些变化增强了观测、编排和路径/策略边界，但仍没有把模型切换、验证或多 Agent 结果自动连接到 Skill candidate 的强制 promotion gate。

对 ChainlessChain 最直接的借鉴不是再复制两个 hook 名称，而是把**每次模型变化变成证据分段边界**。仓库的 WS 路径已经发送 [`model-switch`](../packages/cli/src/gateways/ws/ws-agent-handler.js#L1365)，部分 headless provider fallback 也有 raw event，但独立 `--fallback-model` 路径主要通过 [`onFallback` 写 stderr](../packages/cli/src/commands/agent.js#L1395)，REPL 另有自己的日志/状态。各路径应统一为结构化事件：记录 from/to provider、model/version、原因、失败类别和预算归属；模型变化后关闭上一 trajectory segment、失效兼容性/Eval cache，并禁止把混合模型结果当成单一 Skill 的因果证据。前台 subagent 可见性则应复用到候选审阅页，展示 proposer/critic/evaluator 的实时 tool receipt 与权限，而不是只显示最终自然语言结论。

它当前更接近：

`自动记笔记 → 人工或模型编写 Skill/Workflow → 可选 skill-creator 隔离评测/版本 A/B → 用户决定提交或启用`

而不是：

`轨迹归因 → 候选 Skill → 隔离评测 → 灰度晋级 → 监控回滚`

主要不足如下：

| 能力             | 最新进展                                                                                                                                                      | 相对 WikiSkill 的差距                                                                                              | 可借鉴的优化方向                                                                         |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- |
| Memory           | 自动记录反馈、项目知识；agent 可拥有独立 memory                                                                                                               | 仍以本机 Markdown 笔记为主，缺少统一 source event、测试凭证、置信度、矛盾关系和 Skill impact                       | 将 memory 分为 episodic、semantic、procedural、policy，并为派生知识建立 evidence lineage |
| Skill / Workflow | 支持热加载、嵌套、fork 到子代理；官方 `skill-creator` 可用隔离 subagent 做 assertions grading、with/without benchmark、两个 Skill 版本盲测 A/B 和 HTML review | 已有很实用的用户触发评测迭代，但不是所有 Skill 变更必经的 candidate registry、promotion、canary 和 rollback 状态机 | 复用其用例/断言/A-B 工作流，接入强制 candidate gate 与版本固定；无需重造评测 UX          |
| `/goal` 与验证   | 独立小模型持续判断目标是否完成；部分环境可自主启动 code review                                                                                                | goal evaluator 本身不运行文件或命令；自动验证策略与 skill-creator 仍未组成强制发布事务                             | 确定性测试证据 + 独立 verifier 双门禁，模型判断不能替代真实退出码和产物哈希              |
| Hooks / Plugins  | 生命周期扩展面完整，插件可持久化数据                                                                                                                          | command hook 具有较大宿主权限；自动生成的 Hook/Plugin 缺少演化专用权限清单和原生回滚                               | restricted sandbox、capability manifest、签名、SBOM、权限 diff 和 fail-closed promotion  |
| Agent Teams      | 能并行探索和验证                                                                                                                                              | 官方仍列出实验性与恢复、状态、嵌套等限制；并行本身不会形成组织学习                                                 | 用持久 evidence ledger 做共享黑板，并用 proposer/evaluator/governor 做权力分离           |

Claude Code 对 ChainlessChain 的最大参照价值是：Skills、Hooks、Agents、Workflows、Plugins 和 Skill 评测 UX 已证明载体形态有效；差距应准确收窄为“这些能力尚未被强制串成自动 candidate/promotion/rollback 控制平面”。

### 2.3 OpenAI Codex 生态最新进展与不足

截至审计日，OpenAI 官方 changelog 最新列出的 CLI 版本为 Codex CLI `0.151.0`（2026-08-29）。Codex 生态已有 Skills、Plugins、Hooks、Memories、Goals、长任务、Subagents、Tasks、Sandbox、Approvals 和多级权限能力；其中 Computer History 与 Record & Replay 是 macOS ChatGPT desktop 产品面，不应归属于 CLI `0.151.0`。参见 [Codex changelog](https://learn.chatgpt.com/docs/changelog)、[Memories](https://learn.chatgpt.com/docs/customization/memories)、[Computer History](https://learn.chatgpt.com/docs/customization/computer-history) 和 [Record & Replay](https://learn.chatgpt.com/docs/extend/record-and-replay)。

`0.151.0` 的直接增量包括可配置的可选 MCP discovery grace period、extension 在结果进入模型前检查或替换 MCP tool result、仓库级 plugin catalog 合并，以及权限恢复、远程沙箱、模型切换和嵌套 subagent 预算等修复；它还修复了权限状态变化后陈旧 Guardian classification 继续授权操作的问题。这些变化继续补强扩展治理和执行稳定性，但仍没有定义 experience→Wiki→candidate→Eval→promotion 的演化事务。对应到本项目，permission/policy digest 改变时，历史批准、分类结果和 Eval cache 都必须失效。

这些增量给本项目三条很具体的优化：第一，在工具结果进入模型和 Maintainer 前增加统一 `ResultProjection`，分别保存受 ACL 保护的 raw evidence 与脱敏、限长、带 trust label 的 model-visible projection，并记录 transformation digest；第二，把 MCP 发现超时、权限拒绝、provider transient、sandbox/环境错误与 Skill 程序错误分开，避免系统围绕瞬时基础设施故障“优化”错误步骤；第三，所有 proposer/evaluator/subagent 消耗都必须计入根 `EvolutionRun` 的 token/cost/time/turn 硬预算。

OpenAI 官方还给出“[用带评分的改进循环迭代困难问题](https://learn.chatgpt.com/use-cases/iterate-on-difficult-problems)”的工作方式：提供评估脚本或可审阅产物，每次只做一个聚焦改动，重跑评估并记录分数直到达到目标。这仍是用户定义目标、单次任务内的 artifact 优化，不是持久 Skill 自动晋级；但它与 ChainlessChain 已有 `GoalConditionEngine + Eval + worktree/checkpoint` 高度贴合，适合先做最小闭环，再决定是否建设完整 Wiki。

它已经形成“经验采集 → 记忆/历史 → 识别重复流程 → 起草或建议 Skill/Automation → 人工确认”的半闭环，但官方文档没有描述“自动评测候选 → canary → 晋级 → 监控 → 自动回滚”的完整闭环。

| 能力                             | 最新进展                                                                   | 相对 WikiSkill 的差距                                                                                                                                                          | 可借鉴的优化方向                                                                         |
| -------------------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- |
| History / Memories               | 从会话和 Computer History 生成记忆，可识别重复操作并建议 Skill             | generated state 主要服务召回；[配置参考](https://learn.chatgpt.com/docs/config-file/config-reference)显示外部 MCP/Web/Tool Search 上下文默认也可能进入记忆生成，缺少演化隔离区 | 为来源增加 trust domain、TTL、污染标签和可撤销依赖；外部来源默认 quarantine              |
| Record & Replay（macOS desktop） | 用户发起录制，停止后系统起草 Skill，并可继续 refinement 或在新会话请求复用 | 官方没有描述强制的候选 Eval、promotion 或验证性 replay gate                                                                                                                    | ChainlessChain 可复用现有跨平台录制原语，但必须接入统一 Eval 与 Registry                 |
| Hooks / Extensions               | 可在生命周期检查、转换和观测工具结果；最新版本允许结果进模型前检查/替换    | `PostToolUse` 无法撤销已发生副作用；raw 与 model-visible 结果若不分层会污染证据                                                                                                | active write 门禁必须在副作用前；同时建立 pre-model projection、双摘要/digest 与转换审计 |
| Sandbox / Approval               | 技术沙箱、审批、auto-review 和权限 profile 较完整                          | 解决的是“能否执行”，不是“新 Skill 是否更好且可晋级”                                                                                                                            | 复用其职责分离思想，新增独立 promotion controller 和 last-known-good                     |
| Multi-agent / Goal               | 长任务和多代理已稳定化，任务与成本归属持续增强                             | 多代理提高吞吐，但没有自动产生持久 Wiki 和可验证 Skill                                                                                                                         | 将 proposer、critic、evaluator、governor 配置为不同能力角色和唯一写入者                  |
| Skill 选择                       | 渐进加载并根据 description 触发                                            | Skill 多时目录会被压缩或省略；同名和描述重叠可能造成错选                                                                                                                       | 命名空间、semver、依赖图、混合检索和 outcome-aware reranking                             |

Codex 的关键启发是：自进化需要的传感器、记忆、Skill 载体、Hooks、长任务、多代理和安全基础已经可以分别产品化；真正稀缺的是把它们连接起来的、可验证且可回滚的控制平面。

### 2.4 四方能力对照

下表中的“无”表示在本次论文、官方文档或本仓库静态审计范围内没有发现相应的完整机制，不代表厂商内部不存在未公开实现。

| 维度              | WikiSkill                                                   | Claude Code 2.1.251                                | OpenAI Codex 生态（CLI 0.151.0）                                   | ChainlessChain 当前基线                                                                             |
| ----------------- | ----------------------------------------------------------- | -------------------------------------------------- | ------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------- |
| 经验采集          | 完整训练轨迹                                                | Memory、Hooks、OTel、Workflow                      | Memories、Hooks；Computer History/Record & Replay 为 macOS desktop | Trajectory、Hooks、Rollout、Record & Replay 均有原语                                                |
| 持久中间知识      | 独立 Wiki，含模式、日志、impact                             | Memory 笔记，无独立 Skill impact Wiki              | Memories/desktop History，无官方 Skill impact Wiki                 | 无 canonical Wiki；多套 memory/knowledge/decision 模块并存                                          |
| 自动 Skill 候选   | Wiki-informed proposer                                      | 可由 Agent 编写，通常需用户固化                    | 可起草或建议，通常需用户确认                                       | Synthesizer CLI 入口不可用；Record & Replay 可形成 draft，但不是通用 proposer                       |
| Skill 验证门      | 反复复用的留出 validation split 严格提升 gate               | `skill-creator` 有隔离评测/A-B，但非自动强制发布门 | 可用 eval/hooks/auto-review 拼装，无官方完整演化门                 | Record & Replay 有 replay receipt 验证纵切；通用 Eval 尚未接 Synthesizer/Improver 的 promotion 事务 |
| 单 Skill 变更粒度 | 每轮 proposal 只改一个 Skill；不代表事务原子性              | 无原生强制约束                                     | 无官方演化级约束                                                   | Record & Replay 以单 recorded Skill 为单位；Improver 自身无 candidate 粒度约束                      |
| 回退/回滚         | candidate 未提升时恢复上一 Skill set，Wiki 保留；非部署事务 | Git/插件缓存等可辅助，无原生 Skill 生命周期        | 版本/审批基础可辅助，无官方演化 rollback 状态机                    | recorded Skill 与 Plugin 有局部 CAS/rollback；无统一 Skill active/last-known-good 控制面            |
| 跨模型兼容        | 已实验，且发现负迁移                                        | 无原生 Skill 兼容矩阵                              | 无原生 Skill 兼容矩阵                                              | 无 target model × tools × OS × runtime gate                                                         |
| 生产安全          | 论文未评测                                                  | restricted、hooks/plugins 权限仍需治理             | sandbox/approval 较强，但非演化专用                                | 已有安全基础，但学习 writer 未统一接入                                                              |

## 3. 本项目已经具备、应保留的能力

以下能力不建议推倒重做，而应作为统一演化控制面的 adapter 或底层端口：

| 能力                          | 当前证据                                                                                                                                                                                                                                                                                                                                       | 判断                                                                                                                                                                        |
| ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Canonical capability manifest | [`capability-manifest.js`](../packages/cli/src/lib/capability-manifest.js#L2) 已是 CLI、IDE negotiation、协议文档、fixture、behavior matrix 与 release diff 的单一来源；[`cc agent --capabilities`](../packages/cli/src/lib/headless-manifest.js#L127) 可机器读取                                                                              | 不应新增平行 `capability-status.json`；静态 manifest 声明 capability/gate，易变 wiring/evidence 由绑定 manifest digest 的 runtime projection 输出，保留纯函数与 drift guard |
| 目标条件与硬预算              | [`goal-condition-engine.js`](../packages/cli/src/lib/goal-condition-engine.js#L1) 已支持确定性/模型条件、outer turn、token、cost、time 预算和 snapshot/restore；[`agent.js`](../packages/cli/src/commands/agent.js#L493) 已暴露 CLI 参数                                                                                                       | 是“评分改进循环”的现成 orchestration 骨架；补 candidate/eval adapter 与 root-run budget 归集即可形成最小纵切，不必先造新 scheduler                                          |
| Worktree / Checkpoint         | [`agent.js`](../packages/cli/src/commands/agent.js#L295) 默认对 Git workspace mutation 建 checkpoint，并提供隔离 worktree；[`worktree-isolator.js`](../packages/cli/src/harness/worktree-isolator.js#L266) 对清理路径 fail closed                                                                                                              | 代码/工作区候选先复用现有隔离与恢复；Skill registry 仍需补 content-addressed candidate、active pointer 和 promotion transaction                                             |
| 轨迹存储                      | [`trajectory-store.js`](../packages/cli/src/lib/learning/trajectory-store.js#L61) 可记录 intent、tool calls、结果、response 和 outcome                                                                                                                                                                                                         | 是 Raw 层原语，但当前会通过 UPDATE 重写 `tool_chain`、score 和 synthesized 标记，不是 immutable event log                                                                   |
| Outcome Feedback              | [`outcome-feedback.js`](../packages/cli/src/lib/learning/outcome-feedback.js#L34) 支持自动和用户反馈                                                                                                                                                                                                                                           | 可作为候选信号，不可作为 correctness 或 promotion oracle                                                                                                                    |
| Reflection                    | [`reflection-engine.js`](../packages/cli/src/lib/learning/reflection-engine.js) 可生成工具统计、趋势和报告                                                                                                                                                                                                                                     | 可成为 Wiki Maintainer 的分析端口；当前 CLI 传入 null LLM，只有统计反思                                                                                                     |
| Skill Synthesizer             | [`skill-synthesizer.js`](../packages/cli/src/lib/learning/skill-synthesizer.js#L186) 可从轨迹抽取 pattern 并写 `SKILL.md`                                                                                                                                                                                                                      | 是 Raw→Skill writer 原语；应改为只生成 content-addressed candidate                                                                                                          |
| Skill Improver                | [`skill-improver.js`](../packages/cli/src/lib/learning/skill-improver.js#L207) 支持错误、纠正和更优轨迹三类触发；未发现生产构造点                                                                                                                                                                                                              | 可复用 patch 生成逻辑；必须限制 `skillsDir` 只能指向 candidate store，不能由它决定 active 目标                                                                              |
| Eval                          | [`packages/cli/src/lib/eval`](../packages/cli/src/lib/eval) 与 [`graph-kernel/eval.js`](../packages/cli/src/lib/graph-kernel/eval.js) 已有运行和趋势基础                                                                                                                                                                                       | 应升级为独立 validator，并增加模型、工具、OS、安全和成本矩阵                                                                                                                |
| Rollout                       | [`rollout-store.js`](../packages/cli/src/lib/app-server/rollout-store.js) 与 [`sqlite-rollout-store.js`](../packages/cli/src/lib/app-server/sqlite-rollout-store.js) 已有存储端口                                                                                                                                                              | 应成为 Raw event/artifact authority，而不是另建一套不可互通的轨迹库                                                                                                         |
| Record & Replay 生命周期      | [`recorded-skill-store.js`](../packages/cli/src/lib/record-replay/recorded-skill-store.js#L35) 已定义 `draft→approved→validated→enabled→revoked`；approval/replay 与 digest 绑定且 enable 用 revision CAS；[`record-replay.js`](../packages/cli/src/commands/record-replay.js#L516) 有 staged enable/revoke 失败补偿                           | 是最接近目标的 Skill 安全纵切，应抽象为通用 Registry adapter；仍缺 Wiki、通用 proposer、shadow/canary、全局 active pointer 与 last-known-good                               |
| Plugin 制品事务               | [`plugin-runtime/install.js`](../packages/cli/src/lib/plugin-runtime/install.js#L1) 已有不可变版本目录、staging、签名/SBOM、active version、rollback 与 recovery；[`scopes.js`](../packages/cli/src/lib/plugin-runtime/scopes.js#L1) 对断裂 pointer fail closed                                                                                | 可抽取共享 `VersionedArtifactActivation` 内核给 Skill Registry，避免重写锁、journal、pointer 与恢复；Skill 再叠加 Eval/lineage 专属策略                                     |
| Memory Kernel                 | [`context-memory-kernel`](../packages/cli/src/lib/context-memory-kernel) 已有 authority、privacy purge、compaction 和 adapter                                                                                                                                                                                                                  | 应承载 episodic/semantic memory；policy 与 validated procedural knowledge 仍须分层                                                                                          |
| Skill Runtime                 | CLI [`skill-loader.js`](../packages/cli/src/lib/skill-loader.js#L436) 有 execution authority lease；[`materializeSkill`](../packages/cli/src/lib/skill-loader.js#L914) 每次重读 `SKILL.md+handler` digest，内容变化需再授权/fail closed；Desktop 另有 [`skill-loader.js`](../desktop-app-vue/src/main/ai-engine/cowork/skills/skill-loader.js) | 保留 digest/lease/TOCTOU 防线并接 Registry active digest；它是执行时防御，不等于 candidate/promotion 生命周期                                                               |
| Skill metrics                 | [`skill-metrics-collector.js`](../desktop-app-vue/src/main/ai-engine/cowork/skills/skill-metrics-collector.js#L100) 的数据结构可记录 skill ID、时延、成功、token、成本和 context JSON                                                                                                                                                          | 修复 Desktop IPC/事件接线后可作为 InvocationReceipt adapter；当前既未可靠接线，也没有强制 digest、路由理由、模型/工具/权限指纹与任务 outcome                                |
| 模型选择/回退                 | WS 已发 [`model-switch`](../packages/cli/src/gateways/ws/ws-agent-handler.js#L1365)；headless runner 有 [`provider_fallback`](../packages/cli/src/runtime/headless-runner.js#L2612) raw event；独立 [`fallback-model.js`](../packages/cli/src/runtime/fallback-model.js#L250) 支持同/跨 provider 回退                                          | 已有切换原语但 WS/headless/REPL/独立 fallback 事件口径分裂；应统一结构化证据分段和成本归属，而不是新增另一套模型生命周期                                                    |
| Desktop Phase 20              | [`phase-16-20-skill-evo.js`](../desktop-app-vue/src/main/ipc/phases/phase-16-20-skill-evo.js#L399) 初始化知识图谱、Decision KB、Prompt Optimizer、Skill Discoverer、Debate Review、A/B Comparator                                                                                                                                              | 组件丰富，但目前是六个并列 manager 与 35 个 IPC handler，不是一个原子演化事务                                                                                               |
| 自改进 Skill                  | [`self-improving-agent/handler.js`](../desktop-app-vue/src/main/ai-engine/cowork/skills/builtin/self-improving-agent/handler.js#L293) 可记录错误、instinct 和 JSON skill                                                                                                                                                                       | 可复用交互；“extract-skill”只写 JSON，写入失败可被吞且仍报 success，也未发现自动消费这些 learnings 的闭环，修复前不能作为持久学习证据                                       |

### 3.1 当前实现的关键事实差距

| 对象                                   | 源码事实                                                                                                                                                                                                                                                                                                                                                     | 差距判断                                                                                                                                                              |
| -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CLI `evolution-system.js`              | [`assessCapability`](../packages/cli/src/lib/evolution-system.js#L144) 接受外部 score；[`trainIncremental`](../packages/cli/src/lib/evolution-system.js#L226) 只按公式增加 accuracy；部分 V2 governance 使用进程内 Map，而相关 CLI 操作拆成多次进程调用                                                                                                      | 指标/状态账本，不是模型训练或 Skill 演化器；跨命令生命周期需要持久 store，不能靠进程内状态                                                                            |
| Desktop 历史 `self-evolving-system.js` | 退役前的 `trainIncremental` 仅做公式递增，`selfRepair` 多数只返回动作字符串，`predictBehavior` 使用固定概率；生产扫描未找到该模块接线，源码与 8 个未注册 IPC 已于 2026-09-03 删除                                                                                                                                                                            | 历史演示和指标壳，不是“真实自进化”证据；不要把它与已接线的 cowork Phase 20 混为一体                                                                                   |
| 历史 learning hooks                    | 退役前的 `learning-hooks.js` 定义四个未接线入口，直接写旧 `TrajectoryStore`、吞掉错误且 `SessionEnd` 为 no-op；生产扫描只发现自证测试和文档引用，已于 2026-09-03 删除                                                                                                                                                                                        | 已退役，不能作为真实 Agent turn 自动产出 Raw 数据的证据；canonical Agent evidence 只走认证 ingress                                                                    |
| `learning reflect`                     | [`learning.js`](../packages/cli/src/commands/learning.js#L196) 以 null LLM 构造 ReflectionEngine                                                                                                                                                                                                                                                             | 命令可输出统计，但 LLM 根因归纳未启用                                                                                                                                 |
| `learning synthesize`                  | [`learning.js`](../packages/cli/src/commands/learning.js#L270) 以 null LLM、无 outputDir 构造；[`_extractPattern`](../packages/cli/src/lib/learning/skill-synthesizer.js#L254) 无 LLM 即返回 null                                                                                                                                                            | 当前产品入口不会真实创建 Skill，因此不会走到后述 phantom-created 分支                                                                                                 |
| Synthesizer artifact                   | 组件在“有 LLM、无 outputDir”时不写文件，却仍标记 synthesized/created；[`_persistSkill`](../packages/cli/src/lib/learning/skill-synthesizer.js#L305) 直接拼接 LLM 提供的 name                                                                                                                                                                                 | 组件存在“无 artifact 也报 created”的潜在幻影成功分支；该文件内没有确定性名称校验和 realpath containment                                                               |
| Skill Improver                         | LLM 自报 confidence 只需 `>=0.4`；[`_writeSkill`](../packages/cli/src/lib/learning/skill-improver.js#L420) 覆盖配置的 `<skillsDir>/<name>/SKILL.md`，但 `_readSkill` 已拒绝 symlink/非文件并限长；[`_logImprovement`](../packages/cli/src/lib/learning/skill-improver.js#L439) 失败被吞                                                                      | 未发现生产构造点；若 `skillsDir` 指向 runtime tree，会绕过 candidate/Eval/CAS/audit，随后 CLI loader 的 digest 再授权只能提供执行时补防，不能替代 mutation gate       |
| Improvement log                        | [`learning-tables.js`](../packages/cli/src/lib/learning/learning-tables.js#L59) 只有 skill、trigger、detail、created_at                                                                                                                                                                                                                                      | 无 evidence、diff、digest、validator、模型、环境、accept/reject、rollback 或 revocation                                                                               |
| 自动评分                               | [`outcome-feedback.js`](../packages/cli/src/lib/learning/outcome-feedback.js#L40) 从 0.5 起，按错误率、retry 和最后工具 completed 固定加减                                                                                                                                                                                                                   | 可能奖励“工具调用成功但任务答案错误”，只能用于筛选候选                                                                                                                |
| Skill 使用归因                         | [`skill_execution_metrics`](../desktop-app-vue/src/main/database/database-schema.js#L2922) 记录 skill ID、pipeline、成功、token、cost 和自由形态 context；CLI usage ledger 有 name-level Skill/model attribution                                                                                                                                             | 没有强制 Skill content digest、selector 候选/分数/理由、canonical 模型段、环境/权限指纹、terminal outcome 或 evidence digest；“执行未抛错”不能证明 Skill 改善任务结果 |
| Desktop Skill metrics 接线             | [`phase-16-20-skill-evo.js`](../desktop-app-vue/src/main/ipc/phases/phase-16-20-skill-evo.js#L30) 给 pipeline IPC 传 `{hookSystem}`、给 metrics/workflow IPC 传空依赖；[`skill-metrics-ipc.js`](../desktop-app-vue/src/main/ai-engine/cowork/skills/skill-metrics-ipc.js#L13) 实际要求 `metricsCollector`；`BaseSkill` 事件字段与 collector 监听字段也不一致 | 当前 metrics 不是“字段少但已生产可用”，而是依赖与事件 contract 均有断点；先修接线/contract tests，再扩展 Receipt schema                                               |
| Tool result 入模                       | MCP 有 [`mcp-tool-result.js`](../packages/cli/src/lib/mcp-tool-result.js#L320) 的 JSON/size/depth/node admission；Agent 随后主要 [`JSON.stringify + cap`](../packages/cli/src/runtime/agent-core.js#L14962) 后直接加入下一轮 model messages                                                                                                                  | 解决结构与上下文放大，不等于 secret/PII、prompt injection、来源 trust 的 pre-model projection；raw evidence 与 model-visible 内容也未形成可审计双层                   |
| 模型切换证据                           | WS 与部分 headless provider fallback 已有结构化 raw event，独立 `--fallback-model`/REPL 仍主要写日志或变量；Skill usage 未绑定 loader 的 exact content digest                                                                                                                                                                                                | 现有证据是碎片化事件，不是 canonical segment receipt；同一任务混用模型时仍无法稳定失效 Eval cache 或完成 Skill 版本因果归因                                           |
| Desktop A/B                            | [`ab-comparator.js`](../desktop-app-vue/src/main/ai-engine/cowork/ab-comparator.js#L349) 在 Agent 不可用时生成 placeholder；[`_benchmarkVariant`](../desktop-app-vue/src/main/ai-engine/cowork/ab-comparator.js#L362) 主要按长度、正则错误处理和可读性评分，且 winner 可进入 Decision KB 路径                                                                | 不仅不能作为生产晋级门，placeholder 还可能形成 phantom winner 并污染后续知识                                                                                          |
| Prompt Optimizer                       | [`getActiveVariant`](../desktop-app-vue/src/main/ai-engine/cowork/prompt-optimizer.js#L212) 从多个 active variant 中排序；[`optimizePrompt`](../desktop-app-vue/src/main/ai-engine/cowork/prompt-optimizer.js#L255) 只返回建议                                                                                                                               | 有统计原语，没有真正生成、验证、晋级和回滚变体                                                                                                                        |
| Skill Discoverer                       | [`skill-discoverer.js`](../desktop-app-vue/src/main/ai-engine/cowork/skill-discoverer.js#L215) 优先搜索 marketplace，失败后返回 `keyword-inference` 名称                                                                                                                                                                                                     | fallback 是关键词建议，不是可安装、可验证的真实 Skill，UI 必须明确区分                                                                                                |

## 4. 优先级总览

### 4.1 P0：真实性、安全与可恢复性

| ID       | 任务                                  | 当前差距                                                                                                  | 目标                                                                                                       | 建议投入 |
| -------- | ------------------------------------- | --------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | -------- |
| EVO-P0-1 | 能力真实性与 mutation freeze          | CLI 空入口、指标壳、phantom created、Desktop IPC/事件误接线；配置不当时 Improver 可覆盖 runtime Skill     | 扩展现有 canonical capability manifest；未接线能力显示 experimental/unavailable；默认阻止自动 active write | S～M     |
| EVO-P0-2 | Candidate、Promotion 与 Rollback 事务 | Record & Replay/Plugin 有局部 CAS 与恢复，但 Synthesizer、Improver、普通 Skill 未共用 canonical lifecycle | 推广现有事务原语；所有变更先写不可变 candidate；独立 controller 原子晋级；60 秒内回滚                      | M        |
| EVO-P0-3 | 独立真实 Eval Gate                    | heuristic score、LLM confidence、placeholder A/B 都可能误判                                               | train/validation/test 隔离；真实任务 grader、安全 hard gate、统计门槛与成本预算                            | M～L     |
| EVO-P0-4 | Raw、入模投影与 Skill 编译安全边界    | Raw 含不可信用户/工具内容；当前结构/限长 admission 不等于入模脱敏；名称和内容可进入文件                   | raw/model-visible 双层、脱敏、trust/quarantine、schema/name/realpath/capability diff、沙箱和人工 quorum    | M～L     |
| EVO-P0-5 | Fail-closed 证据与审计                | 改写后审计失败可被吞；日志缺少 diff、digest、validator 和 target runtime                                  | append-only/tamper-evident ledger；无审计即拒绝晋级；100% lineage                                          | M        |

### 4.2 P1：统一演化控制面

| ID       | 任务                                         | 当前差距                                                                                                                                                         | 目标                                                                                                     | 建议投入 |
| -------- | -------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | -------- |
| EVO-P1-1 | Canonical Raw/Wiki/Skill 架构                | 轨迹、memory、decision KB、reflection 和 Skill writer 分散                                                                                                       | 单一 EvolutionRun 状态机和三层 authority，CLI/Desktop/Graph 共用                                         | L        |
| EVO-P1-2 | Evidence-backed Wiki Maintainer              | 无持久 pattern/index/evolution-log/skill-impact                                                                                                                  | 模式可合并、矛盾、过期、撤销，并回指全部证据                                                             | M～L     |
| EVO-P1-3 | Wiki-informed Single-Skill Proposer          | Synthesizer 从单一 Raw 直接生成 Skill，缺少 PURPOSE 和多证据归纳                                                                                                 | proposer 只写单 Skill candidate；生成 PURPOSE、diff、边界和反例                                          | M        |
| EVO-P1-4 | 目标运行时 Eval 与负迁移检测                 | 没有 model × version × tools × OS × domain 兼容矩阵                                                                                                              | before/after 同种子评测，shadow/canary，跨模型重新验证                                                   | L        |
| EVO-P1-5 | Registry、生命周期与单写者治理               | recorded Skill/Plugin 已有局部生命周期；普通 Skill、CLI/Desktop/Graph 与 learning writers 没有共享 Registry                                                      | 抽取 content-addressed registry、lease/fence/CAS、唯一 promotion writer、kill switch                     | M～L     |
| EVO-P1-6 | 统一生产接线并退役重复壳                     | 真实 Agent ingress、三类 Maintainer trigger source 与 durable Registry transition adapter 已形成，两个不可达旧壳已退役；目标部署 production composition 尚未完成 | 真实 Agent 事件进入统一 adapter；所有自动 transition 由 durable trigger 驱动，公式指标不冒充训练结果     | M～L     |
| EVO-P1-7 | 结构化 Memory/Compaction 与多 Agent 权力分离 | 摘要、个人记忆和多 Agent 消息不能保证保留关键证据与职责边界                                                                                                      | episodic/semantic/procedural/policy 分层；proposer/critic/evaluator/governor 隔离                        | M～L     |
| EVO-P1-8 | SkillInvocationReceipt 与因果归因            | metrics 只有 skill ID/执行成功等弱字段；模型切换事件口径分裂                                                                                                     | 每次选择与执行固定 Skill digest、router reason、模型段、环境/权限、outcome 与成本，可反向 join Eval/Wiki | M        |
| EVO-P1-9 | 有界评分改进循环                             | 已有 GoalConditionEngine、预算、Eval、worktree/checkpoint，但未组成 candidate 优化循环                                                                           | 一次一候选、独立评分、保留 best、失败分类、根预算停止；先离线 candidate-only                             | M        |

### 4.3 P2：体验、生态与规模化

| ID       | 任务                                 | 当前差距                                               | 目标                                                                         | 建议投入 |
| -------- | ------------------------------------ | ------------------------------------------------------ | ---------------------------------------------------------------------------- | -------- |
| EVO-P2-1 | 窄域受控生产 Pilot                   | P1 离线纵切通过后，真实流量 ROI 与安全性仍未知         | opt-in、低风险 cohort，人工 review→shadow→canary；按样本量而非固定百分比扩量 | M        |
| EVO-P2-2 | Evolution Workbench                  | 用户看不到 evidence→pattern→diff→eval→promotion 全链路 | 可解释时间线、权限 diff、兼容范围、一键回滚                                  | M        |
| EVO-P2-3 | Skill Retrieval Router               | 全量注入不可扩展，description 路由会错选               | namespace + BM25/vector + outcome-aware rerank + 冲突说明                    | M～L     |
| EVO-P2-4 | 跨设备、团队和组织知识               | 当前知识多为本机或模块私有                             | 加密同步、RBAC、tenant scope、冲突合并、组织批准知识                         | L        |
| EVO-P2-5 | 跨模型 Skill 来源/目标适配与市场治理 | 迁移可能增益也可能负迁移，市场缺少可复现效果凭证       | target-specific adapter、兼容矩阵、签名 lockfile、Eval badge、分阶段升级     | L        |
| EVO-P2-6 | Wiki Pruning 与长时在线适应          | WikiSkill 本身未解决无限增长、检索和小时级在线学习     | merge/decay/TTL/tombstone；离线闭环稳定后再做无提权在线适应                  | M～L     |

## 5. P0：立即修复的真实性、安全与可恢复问题

### 5.1 EVO-P0-1：能力真实性与 mutation freeze

当前最先要解决的不是“让系统更主动”，而是让能力声明、生产接线和真实副作用一致。

建议：

1. 在现有 `CAPABILITY_MANIFEST` 增加 evolution capability ID、静态 dependency/gate 与 mutation scope；由运行时 probe/receipt 生成 `evolutionStatus`，至少含 `implemented`、`wired`、`verified`、`defaultEnabled`、`lastEvidence`、`manifestDigest`、`evidenceDigest`。`cc agent --capabilities`、IDE negotiation、文档和 fixture 继续共享 capability 定义；禁止另建平行 `capability-status.json`，也禁止让时间戳等易变状态污染 canonical manifest digest。
2. `learning synthesize` 在缺少 LLM、output registry 或 evaluator 时应明确返回 `UNAVAILABLE`，不能把空扫描包装为成功完成。
3. Synthesizer 只有在 artifact 成功持久化并取得 digest 后，才能标记 source trajectory；不允许“无 artifact 也 created”。
4. `SkillImprover` 在新 gate 上线前强制 `skillsDir` 指向 candidate-only root，或以 feature flag 强制只输出 diff；不能依赖 loader 的事后 digest 再授权代替写入门禁。
5. Desktop 未接线的 `self-evolving-system` 改名为 `evolution-metrics`、移除或明确标记 simulated；CLI `evolution-system.js` 保留真实治理/维护动作，但把公式型 `trainIncremental` 等接口单独标为 metrics-only，避免一刀切隐藏有效功能。
6. `keyword-inference` 结果必须显示为“搜索关键词建议”，不能显示为可安装 Skill。
7. 修复 Phase 16 pipeline/metrics/workflow IPC 依赖注入、`BaseSkill`↔collector 事件字段和 token 字段 contract；在真实实例与事件测试通过前，这些能力保持 `wired=false`。

验收标准：

- capability 清单中 `wired=false` 的功能无法通过正式入口产生“已创建/已进化”成功状态。
- 对所有 learning/evolution IPC 与 CLI 命令做 contract test，artifact、数据库状态、UI 状态三者一致。
- 自动 learning/proposer 身份对 active Skill 的非 promotion-controller 写入 100% 被拒绝并记录审计；人工编辑也必须通过显式 import/review 路径重新取得 digest 授权。
- 产品文档、命令 help、UI badge 与 runtime capability manifest 一致。

实施结果（2026-09-02）：**上述路线项已在当前工作区关闭。** canonical capability manifest 默认无证据即 `wired=false`；CLI `learning synthesize` 缺 LLM/candidate registry/evaluator 时返回显式 unavailable 与非零命令结果，持久化制品前不会标记 trajectory；Synthesizer、Improver、Desktop Skill Creator 与 Skill Sync import 均只产生 candidate/diff。Desktop TechLearning 不再用内存 UUID 冒充已合成 Skill，只有 `candidate-created + artifact` 才能由 IPC 返回成功；Phase 20 的正式接线已改称 Evolution Metrics，历史 `self-evolving-system` 明确为 metrics simulator 且未被该 Phase 注册。`cc evolution` 的 parent/learn/train-v2/diagnose/repair/predict help、生成命令索引、CLI reference 和产品页统一为 metrics/governance record 语义，关键词推断继续固定为不可安装的 marketplace search suggestion。writer inventory 对当前 39 个写入面保持 unknown-direct=0，其中 learning/proposer/governed-import 六个目标写入面全部为 `candidate-only`；mutation authority 对 learning/proposer/manual-import 等非 promotion-controller active mutation 继续 fail-closed 并持久记录拒绝审计。通用 coding Agent、插件安装器及旧平台 Skill 管理入口在 inventory 中仍显式标为 legacy authority，它们不以 learning/proposer 身份宣称“自动进化”，后续统一迁移由 EVO-P1-5 承担。

确定性验收：13 个定向测试文件 **295/295** 通过，覆盖 capability manifest、CLI learning contract、Synthesizer、Improver、writer inventory、mutation authority、promotion controller、Desktop TechLearning engine/IPC、Phase 接线与 keyword suggestion；真实 `learning`/`learning synthesize`/`evolution` help 已核对，command manifest、help index、CLI reference 三个生成物均无漂移；相关 ESLint 为 0 error（保留 5 个既有 warning），`git diff --check` 通过。

### 5.2 EVO-P0-2：Candidate、Promotion 与 Rollback 事务

建议把 Skill 当作不可变制品，而不是可原地编辑的 Markdown 文件。实现上不要绿地重写：以 `recorded-skill-store` 的 approval/replay/CAS 生命周期和 Plugin runtime 的 immutable version/active pointer/journal/recovery 为种子，抽取共享 `VersionedArtifactActivation` 端口，再补 Skill 专属 Eval、Wiki lineage、shadow/canary 和 last-known-good。

候选制品至少包含：

```yaml
candidateId: sha256:...
skillName: example-skill
parentDigest: sha256:...
contentDigest: sha256:...
sourceEvidenceRefs: []
derivationMode: wiki | record-replay | manual-import
wikiRevision: null
proposerModel: null
targetRuntimes: []
requestedCapabilities: []
evalRunId: null
status: draft
```

状态机建议为：

`draft → validating → validated → shadow → canary → active → deprecated / rolled-back / revoked`

关键约束：

- Proposer 只能创建 candidate，无权修改 active pointer。
- 默认一次 proposal 只改变一个 Skill；不可分割的依赖升级必须声明显式 transaction group，并在同一 gate 中全成或全败，不能把普通多文件修改伪装成一项原子证据。
- Promotion controller 校验 parent/candidate digest、真实 derivation receipt（Wiki revision 或 recording/approval/replay chain）、eval receipt、policy receipt 和签名后，用 CAS 原子切换 active pointer。
- approval、Guardian/分类结果和 Eval cache 必须绑定 permission/policy digest；权限、managed policy、模型、工具或 grader 版本变化时自动失效并重新验证。
- 运行中的会话固定到启动时的 Skill digest，不允许热加载导致半轮语义变化。
- last-known-good 和依赖 lock 必须一起回滚。

验收标准：

- 候选失败或进程在任意写入点崩溃时，active bytes 与 pointer 均保持不变。
- 100 轮并发 proposal/promotion 压测中无 lost update、双 active 或部分写入。
- 任一 active 版本可在 60 秒内回滚到字节级一致的上一版本。
- crash/restart 后能从 ledger 确定性恢复事务终态。

实施结果（2026-09-02）：**上述事务路线项已在当前工作区关闭。** Candidate 与 Release 均为 tenant-scoped content-addressed immutable artifact；promotion/rollback 必须消费绑定 exact operation、candidate/rollback target、dependency lock 与 active digest/revision 的 mutation authority，随后经 lease/fence、ledger prepare、staging fsync、active pointer CAS、ledger finalize 和 journal cleanup 收敛。`pinActive()` 固定运行中会话的 release digest；每个 release 内嵌 dependency lock、runtime manifest、target matrix 和六类 receipt digest，相关权限/策略/模型/工具/grader 上下文变化无法复用旧 transition subject。active pointer、LKG 和 dependency lock 在同一个认证状态中切换，rollback 重新激活不可变历史 release，不重新拼装字节。

确定性验收：Candidate/Release/Promotion 三个核心测试文件 **70/70** 通过。Release 矩阵覆盖 `after-prepare`、`after-staging-fsync`、`after-pointer`、`after-finalize` 等持久写入边界、finalize unavailable/commit-unknown、tamper、过期 lease、重启恢复，以及真实子进程在 pointer 写后 `process.exit(73)` 再由新实例确定性恢复；新增 promotion 压测同时提交 100 个相同 CAS 基线的完整候选，严格收敛为 1 个 committed active、99 个拒绝、revision/fence 均为 1 且无双 active/部分 ledger；rollback 断言恢复 release 的 candidate content 字节与原版本完全相同、dependency lock digest 一致并在 60 秒上限内完成。最终用户 shadow/canary 编排和唯一 production writer 构造点属于 EVO-P1-5；真实跨进程 Eval/PKI authority 属于 EVO-P0-3，不再重复作为本事务原语的未关闭项。

### 5.3 EVO-P0-3：独立真实 Eval Gate

现有 `autoScore`、LLM confidence、Prompt success rate 和 A/B 可读性分数都可以参与候选排序，但不能决定生产晋级。

最低可用 gate 应包含：

1. **数据隔离**：training 用于提炼；validation 用于选择；test 只用于最终报告。Proposer 不得读取 validation/test 答案和 grader；按时间、项目、用户和任务近重复做 group split，限制单一 workspace/user/replayed trace 的贡献，防止泄漏、投毒和反馈回路放大。
2. **真实 grader**：代码任务运行真实测试；文件任务校验 artifact；检索任务检查引用与答案；UI 任务回放真实状态，而不是只看 tool completed。
3. **同条件对照**：baseline 与 candidate 使用相同模型版本、seed、工具、权限、OS 镜像和输入集。
4. **多目标门禁**：质量、错误率、安全、权限、token、延迟和工具调用预算同时判定；安全是 hard gate。
5. **统计稳健性**：至少三次独立运行，或预注册置信区间/序贯检验；小样本不确定时进入 `needs-more-evidence`，而不是强制晋级或永久拒绝。
6. **防评测投机**：candidate 不能读取测试文件、grader 实现、隐藏答案或历史 validation 结果。

以下仅是**窄域 Pilot 默认起点**，不是跨任务通用发布标准。正式门槛应按风险等级、样本基线、统计功效、流量规模和成本目标预注册；样本不足时进入 `needs-more-evidence`：

- 安全与权限回归必须为零。
- 主要任务成功率的置信下界不得低于 baseline。
- 成功率提升至少 5 个百分点，或同等成功率下 token/延迟降低至少 10%。
- 低风险 Pilot 可先以 30 个 replay、20 个 holdout 作为最低探索量；高风险或高方差任务必须根据功效分析扩大样本，不足时只能停留 draft。

复核实施结果（2026-09-02，提交 `52427b742c`、`adde8f98e6`，本次重新验收）：**train/validation/test 的仓库内隔离协议、基础 anti-gaming 与跨 cell family-wise confidence 校正已经落地，不应继续列为“未实现”；真实生产 grader 仍未关闭。** `EvolutionEvalGate` 对 suite 强制三个预注册 split，按时间/项目/用户/近重复 group key 和 exact public-input digest 拒绝跨 split 泄漏；training partition 单独摘要并进入签名 artifact provenance，provenance 必须声明 holdout isolated、trainer authority/revision 与未撤销状态。执行器只取得单 run 新鲜 opaque task/subject handles 和混排 work plan，不获得 split、artifact role、private expected、candidate/baseline 身份或 grader 实现；stable cross-run handle、`splitBlind=false` binding、descriptor 后隐藏的 always-true dispatcher、hidden-test grader crash 和 provenance substitution 都失败关闭。validation 使用 paired seed 对照，test 保持 hidden，独立 safety receipt 是绝对 hard gate；缺任一 split 会得到 `needs-more-evidence`。matrix plan v2 预注册 family-wise error rate 与 Bonferroni 双侧校正，按 `2 × cell 数` 计算 validation/test comparisons 的最低 z；Eval receipt v4 将实际 `confidenceZ` 纳入 evidence root，matrix cell result 与 decision commitment 继续绑定该值，低于门槛时即使 aggregate receipt 被重新签名也不能维持 `accepted`。本次 Gate + target-matrix 全量 131/131 通过。仍需真实跨平台任务 grader/runner、生产数据与权限隔离部署、更广分布级 anti-gaming、持久跨进程 authority/child resolver、process-kill 与密钥撤销演练。

### 5.4 EVO-P0-4：Raw、入模投影与 Skill 编译安全边界

Raw trajectory 同时包含用户输入、工具参数、工具结果和模型输出。任何一层都可能携带秘密、个人信息、prompt injection、恶意路径、伪造成功信息或跨租户内容。把这些内容交给 Maintainer/Proposer，会把一次性攻击编译为持久 Skill。

需要明确区分三种表示：受 ACL 与保留策略保护的 raw evidence、经过脱敏/截断/来源标注后允许进入模型的 model-visible projection，以及可进入 Wiki/Skill 编译的 trusted projection。当前 MCP result admission 已限制结构、大小、深度和节点数，但这不能替代秘密/PII 清理、prompt-injection 标注和来源信任策略。每次转换都应记录 source digest、projection digest、规则版本和删改摘要，使模型看到的内容与审计原文可关联但不混存。

建议建立四道边界：

| 边界             | 必须执行的控制                                                                                                           |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------ |
| Raw 入库前       | secret/PII redaction、tenant scope、来源 trust、sensitivity、retention、加密与 ACL                                       |
| 工具结果进模型前 | 生成 model-visible projection；secret/PII redaction、trust label、注入标记、内容预算；raw 与 projection 各自 digest      |
| Wiki 归纳前      | 只读允许进入学习面的 trusted projection；untrusted/external trace quarantine；区分用户陈述、工具观察、模型推断和真实验证 |
| Candidate 写入前 | 严格 JSON schema、kebab-case 名称、realpath containment、symlink/size 限制、静态恶意指令扫描                             |
| Promotion 前     | capability/permission/network/secret diff；独立 sandbox；高风险变更双人或 policy+human quorum                            |

Maintainer 和 Proposer 默认只应拥有读取指定证据、写 candidate 的最小权限，不应拥有 shell、任意网络、secret 或 active registry write 权限。

验收标准：

- prompt injection、伪成功、secret 泄漏、目录逃逸、symlink、跨租户和恶意重复轨迹负测全部通过。
- 1,000 条对抗轨迹中，零未经审查的 active Skill 修改，零 secret 进入 Wiki/Skill 明文。
- 删除或撤销源证据后，所有派生 pattern 和 candidate 在 60 秒内被定位并降权、隔离或撤销。
- capability 扩张必须显式显示且不能由自动 proposer 自批。

实施结果（2026-09-02，工作区）：**该路线项的持久组合、Candidate plaintext 与 promotion review gate 基础已完成，整体仍为部分完成。** 既有 `EvolutionEvidenceProjector` 已执行 source verification、tenant-keyed commitment、storage/ACL/retention policy、secret/PII redaction、prompt-injection 标记、trust/quarantine、model/trusted 双投影和签名 attestation；`bc24db7a0b` 的 `ArtifactStoreEncryptedRawStore` 将 Raw 接到真实 ArtifactStore，plaintext 只交给捕获的外部 encryptor，持久层只得到 AES-256-GCM sealed bytes、cipher digest、tenant KMS ref 与无敏感 lineage；`EvolutionEvidenceArtifactAdapter` 四分片持久并 readback 后最后发布完整 derivation manifest，新实例逐项重新认证。`f668598a42` 又将同一 Unicode-aware secret/PII policy 接入唯一 Candidate canonical builder，在任何 digest/临时文件/最终文件产生前拒绝普通及混淆 canary；既有 strict schema、kebab-case、size、realpath、symlink、single-link 和 descriptor readback 继续生效。`2d7fa726f8` 进一步从认证 Candidate/active release/state/matrix binding 生成 parent-relative capability diff 和完整 reviewer packet；任何 capability 增量需两个不同真人，无增量仍需一个，签名/时效/quorum/exact packet 在 mutation authority consume 前验证，review receipt 同时进入 release policy lineage 与 procedural Memory evidence。`534ca5f4e5` 再把投影器已有的 Unicode-aware injection findings 作为稳定 `contentRiskDigest` 纳入 packet、decision 和 Memory binding；命中风险时强制双人且必须显式确认同一摘要，未确认在 mutation 前拒绝，同时允许合法安全 Skill 把攻击字符串保留为受审反例。`bd3c29b843` 扩展越权/伪造成功/凭据外传/自授权/关闭护栏等英中意图，并用 compact skeleton 阻断逐字符分隔绕过；20×10×5 形成的 1,000 条唯一合成候选全部产生 finding。Projector+review 55/55、Candidate 27/27、其余相关回归 56/56、process integration 1/1 通过。CLI Agent/tool pre-model durable ingress 与仓库内 production composition root 已由 EVO-P1-6 的 2026-09-03 批次纵切；未关闭项为 Desktop/Graph 同协议 ingress 与旧直通路径退役、目标部署的生产 KMS/HSM/PKI/policy/witness authority、60 秒删除/撤销传播、更广语义分类及生产流量攻击观测/误报校准；持久 resolver 已由 `1b30ca030e` 落地，用户可见 reviewer UI 与生产 PKI/用户身份由 P1-5/P1-6 统一验收。

### 5.5 EVO-P0-5：Fail-closed 证据与审计

建议新增 append-only `evolution_events` 和 content-addressed artifacts。每次 proposal 至少记录：

- source trace/artifact digest 与数据分片版本；
- derivation mode；Wiki 路径记录 revision/pattern/evidence counts，Record & Replay 路径记录 recording/approval/replay digests；
- parent/candidate unified diff 与 digest；
- proposer/evaluator/governor 模型、版本、prompt 和权限快照；
- target model、OS、tool/API、sandbox 和依赖 lock；
- before/after 分项分数、置信区间、成本和安全报告；
- accepted/rejected/rolled-back 原因、actor 和时间；
- 签名、hash chain 和 revocation 状态。

日志持久化不是“non-critical”。任何 promotion receipt 写入失败、校验失败、签名失败或 ledger 不可用都必须 fail closed。

验收标准：

- 100% **自动晋级的 active Skill 版本**可从 digest 反查 source evidence、derivation mode、proposal diff、Eval 和审批者；Wiki 派生版本必须回指 Wiki pattern，Record & Replay 可回指 recording/approval/replay receipts，不强迫伪造 Wiki 中转。手工/导入的 legacy Skill 至少有 owner、source/import digest、权限与 migration 状态。
- 100 次数据库异常、磁盘满、断电、hook timeout 和 evaluator crash 故障注入中零误晋级。
- rejected proposal 的非敏感 metadata、digest、原因和 tombstone 按 retention policy 可查；敏感 payload 服从 TTL、用户删除与 crypto-shredding，Proposer 仍可用 tombstone 避免无证据重复提案。
- 审计导出可离线验证 hash chain、签名和 artifact digest。

实施结果（2026-09-02，提交 `27b7e267e9`）：**离线审计导出与独立验证缺口已关闭，路线项仍为部分完成。** `EvolutionLedger.exportAuditBundle()` 在同一账本锁和认证 state 下导出 signed identity、store marker、完整 event/anchor 链、当前 durable witness，以及逐事件按 tenant 和 ref 绑定的 artifact 原字节与 resolution receipt；导出时重新解析每个 artifact，只有 validation aggregate 与原事件精确一致才返回。`verifyEvolutionLedgerAuditBundle()` 不读取原 ledger、witness 或 ArtifactStore，先校验严格 schema/capacity/canonical export digest 并捕获 plain-data graph 与同步 trust ports，再逐条重算 identity/store/event/anchor/segment/witness payload 和 artifact validation digest。真实测试在导出后删除两个在线 store 仍得到 verified 结果；空 genesis 和 legacy/domain 混合链可验证，而重算外层摘要后的尾部截断、signed 内容替换、artifact bytes/receipt 替换、排序与额外证据均失败关闭。Ledger 全量 48/48，相关六文件 104 通过、1 项按平台跳过。该批当时尚缺持久索引/snapshot，随后已由 `00686ceda8`、`9c77318e59` 关闭；路线项仍有生产 authority、迁移、生产规模/故障注入和统一 wiring，因而不升级为完成。

补充实施结果（2026-09-02，提交 `00686ceda8`）：**进程内认证增量 prefix 与查询索引缺口已关闭。** 所有公开只读入口改用同一增量 state loader；缓存不能绕过文件真实性检查，既有 segment/anchor 仍逐文件重算摘要，HEAD、identity、store marker 与独立 witness 仍重新认证。只有完整 prefix 与缓存一致时才复用已验证事件，外部合法追加只解析和验证新增尾部。每个认证 state 同时构建不暴露给调用者的 eventId/eventDigest `WeakMap` 索引，sequence 利用连续数组直接定位；`queryMany()` 最多接收 10,000 个 selector，并在一次锁、一次认证 state 下完成，默认不制造成千上万个签名 receipt。5,000 个混合查询在 `<5s` 门内完成且历史 event/domain-event 签名验证为 0，外部实例追加后只验证新增 1 个 event；Ledger 49/49，相关六文件 105 通过、1 项按平台跳过。该批尚缺的跨重启 snapshot 随后由 `9c77318e59` 关闭。

补充实施结果（2026-09-02，提交 `9c77318e59`）：**可跨重启复用的持久认证 snapshot 已完成，路线项仍为部分完成。** `checkpointState()` 将当前完整 state 与 canonical segment/anchor 文件摘要写成以 witness digest 命名的不可变 snapshot；snapshot 自身由 ledger trust root 签名，并同时绑定 identity、store incarnation/entry、HEAD/anchor 与独立 witness generation/digest。新实例仅在这些当前 authority 全部吻合后采用 snapshot，仍重算真实文件摘要、规范 schema、event/anchor/hash chain 与文件名/字节逐项关系，但省去历史 event/anchor 外部签名调用；进程内后续读取仍优先走已重新认证的 memory prefix，不会反复解析 snapshot。损坏 cache 回退到完整权威日志验证，旧 witness snapshot 改名重放也不能命中。mixed legacy/domain 负测与 100 并发事件 checkpoint→新实例 `<5s` 基准均通过；Ledger 50/50，相关六文件 106 通过、1 项按平台跳过。该批尚缺的旧 snapshot retention/清理随后由 `d6c645aefd` 关闭。

补充实施结果（2026-09-02，提交 `d6c645aefd`）：**snapshot retention 与清理边界已关闭。** checkpoint 成功后只保留当前 witness snapshot；清理只匹配 authority 根下精确命名的旧缓存，要求目录项与 `lstat` 同时为普通、非符号链接、single-link 文件，当前 snapshot 永不删除，删除后同步 authority 目录。stale-current 文件不能被静默覆盖，显式移除攻击文件后才能重建并清理旧缓存。当前剩余为生产跨进程持久 authority、生产量级/资源上限、旧 projection/journal 迁移、系统化故障注入和统一生产 wiring。

补充实施结果（2026-09-02，提交 `c96b6da007`）：**真实 v1 Candidate 到 tenant-scoped v2 的单制品迁移协议已关闭，整体迁移仍未完成。** verifier 使用历史 `chainlesschain.skill-candidate/v1\0` domain 重算精确旧 candidate/content digest 与全部 15 个字段，随后复用现行 plaintext guard；Registry-owned migration 要求可信 matrix admission 提供同 tenant execution artifacts，且 target runtimes 与旧 scope 完全相同。v2 draft 发布后必须取得 durable/authenticated migration receipt，精确绑定旧/新 ID、内容、dependency/runtime/matrix digest、tenant 与 handler；失败只留下 candidate-only draft，重试为同一 candidate/migration digest。Candidate 28/28、联合 Release/Promotion 74/74 通过。剩余迁移工作是安全枚举旧 unscoped store 的批处理与切换，以及 Release v3、state v2、journal v3 和 ledger projection 的事务迁移。

补充实施结果（2026-09-02，提交 `2c11930ef5`）：**旧 unscoped Candidate store 的安全批迁移已关闭。** `migrateLegacyStore()` 要求源/目标不重叠，对源目录和每个 artifact 做 realpath、directory/file identity、single-link、fatal UTF-8、canonical bytes、历史 digest 与聚合容量检查；先全量验证源，再逐项解析可信 execution artifacts 并复用 durable-audited `migrateLegacy()`，最后重新核对全部源 identity/bytes。源始终只读保留，便于回滚；中断后重跑只复用同一 v2 draft 和 migration digest。Candidate 29/29 通过。当前迁移剩余项收窄为 Release v3、state v2、journal v3 与 ledger projection 的事务迁移。

补充实施结果（2026-09-02，提交 `383e35a598`）：**Release v3 artifact 到 tenant-scoped v4 inactive release 的显式迁移已关闭。** verifier 按历史 `chainlesschain.skill-release/v3\0` domain 重算旧 release/content/raw dependency-lock digest 与全部 15 个字段；迁移要求其 Candidate 已由前述协议取得 authenticated/durable receipt，并核对旧 raw lock 与新 tenant-bound artifact 的内层 lock 及 lock digest，而不混淆两代摘要。新 v4 release 保留旧 mutation、transition 与 receipt lineage，同时嵌入已认证 v2 Candidate/runtime/matrix；Registry 发布后还须取得绑定旧/新 release、Candidate migration、tenant 与 handler 的 durable receipt。失败只留下 inactive release，不会修改 active state；重试保持同一 release/migration digest。Release 29/29、Candidate+Release+Promotion 76/76 通过。当前迁移剩余项收窄为 state v2、journal v3 与 ledger projection 的事务迁移。

补充实施结果（2026-09-02，提交 `ccf52dfe14`）：**state v2 的输入验证和迁移映射证明已关闭，active pointer 提交尚未关闭。** `verifySkillReleaseMigrationResult()` 可重新计算 release migration record 并复核 authenticated/durable receipt 与 tenant/Skill/current release 绑定；历史 state verifier 按原 v2 domain 重算全部字段。`buildSkillReleaseStateMigrationPlan()` 只在 active/LKG 旧 digest、旧 authority/raw lock、两份 release migration proof 与新 tenant lock 全部一致时产出 content-addressed plan，并显式声明仍需 authenticated ledger migration。API 本身不写 pointer，测试再次确认 Registry state 保持 revision 0。下一批必须由真实 EvolutionLedger adapter 持久提交该 plan、返回 current projection，再处理 journal v3 的恢复或隔离，才能把 state/journal/projection 事务迁移标为关闭。

补充实施结果（2026-09-02，提交 `065f9d277f`）：**state v2 的真实 ledger baseline、current projection 与 active pointer 提交已关闭，journal 迁移仍未关闭。** `EvolutionLedger` 现在只在空 tenant/Skill lineage 接受一条 `skill.release.state-migration` 事件，事件 subject 是 ledger-retained、durable-resolved 的完整 plan/state/authority receipt；旧 revision/fence 被保留，后续普通 finalize 必须从该 state digest 和 revision 连续延伸。同一完整请求可幂等恢复，替换 receipt、第二基线、revision gap/fork 或篡改 state 均在写入事件前或 lineage 重验时失败关闭。Registry 的 `migrateLegacyState()` 会先验证已迁移 active/LKG release 和同步 durable audit receipt，再提交 ledger、复核 current projection，最后原子写 pointer；ledger 成功而 pointer 未写时可通过显式重跑收敛。Ledger ports 12/12、artifact ports 25/25、Release 29/29 通过；相关组合共 112 通过、1 项按平台跳过，唯一暴露的慢机 stress grant 过期夹具已由 `2dc0520fea` 修复并以 18/18 复验。剩余迁移缺口是 journal v3 的认证恢复/隔离策略，以及 ledger→pointer 的迁移专用 journal 和启动期自动 reconciliation；在这些关闭前不得把旧 journal 直接翻译为当前事务成功。

补充实施结果（2026-09-02，提交 `b0d9769675`）：**journal v3 的精确历史输入验证已关闭，事务处置仍未关闭。** `verifyLegacySkillReleaseJournal()` 按历史 digest domain 和字段集合重算 journal、intent 与 tenantless state v2，并重新认证 mutation request/consumption receipt、六类 receipt、transition subject、previous→next revision/CAS 及可选 prepared ledger projection。API 刻意只读：`journaled/prepared/pointer-written` phase 只表示本地崩溃位置，不能替代 authenticated ledger resolution，verifier 因此不会 finalize、迁移 pointer 或删除旧文件。Release 30/30、ESLint、Prettier 与差异检查通过。下一批必须让独立账本决议把旧事务分类为 abort、committed-state migration 或 quarantine，再用 migration journal 支持启动期自动 reconciliation。

补充实施结果（2026-09-02，提交 `65b71c36b9`）：**journal v3 的 authenticated ledger disposition 分类已关闭，文件级执行与自动恢复仍未关闭。** 新增工厂只把 resolver 保存在模块内部 WeakMap，调用面仅得到冻结 descriptor；spread/序列化后的伪 authority 无法解析。resolver receipt 必须重算 content digest，并绑定 authority/handler、tenant、legacy journal/transaction 和严格 ledger projection。`buildLegacySkillReleaseJournalDisposition()` 对 absent、prepared、committed-current、committed-superseded 分别生成 abort、quarantine、migrate-committed-state、quarantine；迁移分支额外要求 state plan 的旧 state digest/transaction/revision/fence/active/LKG 全部对应 journal next-state，其他分支禁止夹带 state plan。计划 verifier 再次锁定 action/status/current 组合。Release 30/30 与静态检查通过。下一批应安全保留原件并执行 quarantine/abort，或调用 state migration 后记录完成，同时用持久 migration journal 让构造期自动收敛。

补充实施结果（2026-09-02，提交 `6055b210b6`）：**state migration journal 与构造期 ledger→pointer 自动 reconciliation 已关闭。** 新增独立 `state-migrations/` 安全目录和严格 journal schema，journal 在 ledger 调用前通过 staging fsync→原子 rename 持久；构造期在 recovery mutex 内按 transaction digest 有界枚举并验证路径、canonical journal、tenant/Skill/lease、plan/state/receipt 全部绑定。恢复绝不使用本地 receipt 创造账本事实：authenticated ledger 为 absent 时只清理并保持 revision 0；committed/current 时才补写 pointer、重新读取 release/state 并清理 journal/lease；prepared、non-current 或 unrelated pointer 全部失败关闭。测试覆盖 pre-commit failure、commit 后响应丢失、新实例自动恢复、journal 清理与攻击者修改 receipt 后重算外层 digest 的拒绝，Release 30/30 与静态检查通过。legacy journal 文件级 disposition 执行仍须下一批关闭。

补充实施结果（2026-09-02，提交 `76906bc88f`）：**legacy journal v3 的文件级 archive/retirement 已关闭。** disposition plan 现在内嵌完整 durable resolution receipt 和 ledger projection，archive verifier 会再次对照原 journal 重验，而不是只保留不可解释的 receipt digest。退休函数要求源路径精确为 `journals/<skill>.json`，源与 archive 目录都必须 canonical/non-symlink 且互不为祖先；源文件还须 single-link、bounded、canonical bytes 与已验证对象完全相同。archive envelope 固定完整 journal、disposition 和 committed migration state digest；临时文件 fsync 后以 hard link 原子发布，目标 readback/离线验证成功后才删除源并同步目录。目标已存在时只有逐字节相同才幂等成功，损坏 archive 会失败并保留源；committed-state 只有当前 Registry 返回的 branded migration result 才能退休。Release 30/30、ESLint、Prettier 与差异检查通过。至此 Candidate/Release/state/journal/projection 的显式迁移链已形成；P0-5 仍保留生产 authority、规模/资源上限、系统化故障注入和统一 production wiring。

补充实施结果（2026-09-02，提交 `07b0e8cd10`）：**legacy migration archive 的逐 durability 边界 fault injection 已关闭。** 归档 API 现在显式捕获 filesystem port 和同步 crash hook，并在 temp fsync、目标 publication、目标 readback、源退休四处发布不含敏感字节的 checkpoint。四阶段矩阵验证每次中断都满足“原源仍可重试”或“目标 archive 已完整持久”至少一项：fsync 后临时文件会清理；publication/readback 后目标可验且源未删；retirement 后目标可验且源已删；随后重跑均幂等收敛。Release 30/30 与静态检查通过。这关闭迁移纵切自身的系统故障演练，不把它扩大解释为 250,000-event 长账本、跨进程 authority/witness 或最终生产构造根的全系统故障验收。

## 6. P1：建立统一演化控制面

### 6.1 EVO-P1-1：Canonical Raw/Wiki/Skill 架构

建议新增唯一权威对象 `EvolutionRun`，而不是让 TrajectoryStore、Reflection、Decision KB、Prompt Optimizer、SkillImprover 和 Desktop Phase 20 各自定义一次“进化”。

```text
Agent / Tool / User events
          ↓
Canonical Rollout & Artifact Store
          ↓
Raw projection ──→ Wiki Maintainer ──→ Wiki revision
                                       ↓
Active Skill ──→ Single-Skill Proposer ──→ Candidate
                                       ↓
Independent Eval / Policy / Human Review
                                       ↓
Registry promotion / rollback
```

Wiki 是跨轨迹归纳型学习的中间表示，不应成为所有 Skill 的形式主义中转站。现有 Record & Replay 若已具备 recording、人工 approval 和 replay validation receipts，可以通过 `derivationMode=record-replay` 直接进入同一 Candidate/Registry gate；只有需要跨案例归纳的经验才走 Wiki Maintainer/Proposer。

建议目录或逻辑等价物：

```text
evolution/
  raw/events/
  raw/artifacts/
  wiki/index/
  wiki/patterns/
  wiki/evolution-log/
  wiki/skill-impact/
  registry/candidates/
  registry/releases/
  eval/runs/
  policies/
```

Raw 应是逻辑 append-only event 与 content-addressed artifact reference，查询需要的完整轨迹由 deterministic reducer 投影；outcome、tag、合成状态和删除 tombstone 都作为新事件追加，而不是修改原始事件。敏感 payload 使用独立加密对象和 key reference，使法定删除/用户删除可通过 crypto-shredding 完成，同时保留不含敏感内容的 lineage metadata。

验收标准：重复事件、乱序、crash/replay 和 compaction 后，得到相同 Raw/Wiki/Registry 投影；CLI、Desktop 和 Graph 对同一 run ID 返回相同状态与 digest。

实施结果（2026-09-02，工作区）：**该路线项已关闭。** canonical `EvolutionRun` 位于 CLI 与 Desktop 共同依赖的 `@chainlesschain/session-core/evolution-run`，定义唯一 event、projection 和 snapshot schema，并由 CLI evolution、Desktop AI engine、Graph kernel 三个薄适配器直接调用同一个 projector。状态统一包含 append-only Raw event/artifact refs、annotation、tombstone、Wiki revision、Skill candidates、Eval runs、active/LKG release 和 run lifecycle；Wiki 不是 candidate 的必经路径，`record-replay` candidate 可在 Wiki revision 为空时直接进入同一 Registry 投影。

事件只接受 sha256-bound metadata 与 artifact/key reference，递归拒绝 raw `payload|content|prompt|output|secret`；outcome、tag、synthesis status 和删除均以新事件投影，tombstone 保留 lineage 并允许外部 key crypto-shredding。projector 先按 sequence/eventId 规范排序，完全相同的重复 eventId 幂等去重，冲突重复、同 sequence 竞争、跨 tenant/run 混入全部失败关闭。compaction snapshot 固定完整 projection digest、event root、boundary 和 seen-event digest；边界前完全一致的 crash replay 被幂等忽略，替换或未知旧事件拒绝，边界后事件继续产生与未压缩全量重放完全相同的状态和 digest。7/7 跨端契约覆盖 CLI/Desktop/Graph 字节级等价、重复/乱序/进程重放、compaction 后旧事件重放与继续追加、annotation/tombstone、record-replay direct candidate、冲突/跨 run/晚到替换和敏感内容拒绝。真实 Agent ingress 切换与旧壳退役仍归 EVO-P1-6，避免在本数据模型任务中重复计算生产接线。

### 6.2 EVO-P1-2：Evidence-backed Wiki Maintainer

每个 Wiki pattern 建议至少包含：

```yaml
patternId: pat-...
kind: success | failure | constraint | anti-pattern
summary: ...
rootCause: ...
procedure: ...
appliesWhen: []
doesNotApplyWhen: []
positiveEvidence: []
negativeEvidence: []
contradicts: []
supersedes: []
confidence: 0.0
trustDomains: []
lastVerifiedAt: ...
expiresAt: ...
```

Maintainer 需要完成去重、聚类、根因归纳、成功模式提炼、反例维护、矛盾检测和 evidence count 更新。不能因为某次 Skill proposal 被拒绝就回滚 Wiki，但必须记录拒绝结果，降低或修正关联模式的可操作性。

WikiSkill 只对 Skill proposal 评分，Maintainer 生成的 Wiki pattern 本身不经过同等 gate 且不会随候选回退。本项目因此应把 pattern 定义为可纠错假设，而非已验证事实，至少区分 `hypothesis / corroborated / contradicted / revoked`；单条 LLM 总结不能直接成为可编译 procedure，必须有多源证据或真实 grader receipt。proposal 被拒绝时保留历史，但要更新关联 pattern 的 impact 与反证，防止错误 Wiki 跨轮累积。

知识不是无限追加：pattern 需要 merge、decay、stale、tombstone 和 revocation。非敏感审计 metadata 可按策略保留；敏感 payload 必须服从 retention、用户删除与 crypto-shredding，默认检索视图同时剪枝，并沿依赖图使派生对象 stale/revoked。

实施结果（2026-09-02，工作区）：**该路线项已达到仓库闭环。** `EvidenceBackedWikiMaintainer` 已建立认证 trusted-projection ingress、tenant/digest 绑定、最小权限构造策略和确定性 Wiki revision reducer。canonical state 同时维护 pattern、默认检索 index、append-only evolution log、Skill proposal impact、evidence metadata 与反向依赖；upsert 会按规范 fingerprint 去重，显式 merge 仅允许同 kind pattern，counterevidence/contradiction、TTL、半衰期 confidence decay、evidence revoke/delete、pattern revoke/tombstone 会使 pattern 进入 `contradicted/stale/revoked/tombstoned` 并从默认 index 剪枝。单源模型总结保持不可操作 `hypothesis`；多 trust-domain evidence 或真实 grader receipt 才能使 procedure 进入 `corroborated`，proposal rejection 只追加 impact/rejection count 并降低 operational confidence，不删除或回滚历史；decision candidate/outcome/pattern/reason 必须与认证 receipt digest 精确绑定。持久组合直接复用 `EvolutionArtifactPorts + EvolutionLedger`：有限 `wiki-revision` 类型仅允许 `evolution-ledger` retention，branded resolver、防 artifact substitution、head/sequence CAS、previous-revision lineage、跨 adapter 恢复和 append 响应丢失幂等均已落地；真实组合测试进一步证明 actual Ledger 文件、签名/witness 与 ArtifactStore 可由新实例恢复同一 state digest。Maintainer 13/13、adapter 6/6、真实 ArtifactPorts 25/25、EvolutionLedger 45/45 通过，另 1 项按平台条件跳过。生产 Agent evidence ingress、通用旧账本迁移和敏感 evidence 删除不属于本 Maintainer 重复实现，分别归 EVO-P1-6、EVO-P0-5、EVO-P0-4/P2-6。

### 6.3 EVO-P1-3：Wiki-informed Single-Skill Proposer

Proposer 初始只读取 Wiki index、Skill impact、active Skill digest 和训练任务结果摘要；需要时按权限选择性读取 pattern 和 Raw，避免一次性塞入全部轨迹。

每个 proposal 必须：

- 只创建或修改一个 Skill；
- 提供 `PURPOSE.md` 或等价结构，回指 pattern 和 source evidence；
- 提供适用条件、不适用条件、失败反例、回退步骤和验证方法；
- 明确 requested capabilities、target runtimes 和上下文成本；
- 生成 machine-readable diff，不直接写 active 文件；
- 如果证据矛盾或样本不足，输出 `no-proposal` 或 `needs-evidence`，不能为了完成循环强行修改。

WikiSkill 的研究结果支持在**演化训练 rollout 的 Pilot** 中默认让执行 Agent 只读 active Skill、Wiki 只对 Maintainer/Proposer 开放。ChainlessChain 应把该实验条件实现为可配置 capability policy，并做按域对照；它不是所有生产推理永远禁止访问 Wiki/Memory 的通用结论。

实施结果（2026-09-02，工作区）：**该路线项已关闭。** `WikiInformedSkillProposer` 的构造期 descriptor 固定 tenant、EvolutionRun、目标 Skill、Wiki revision、proposer model、最小样本和选择性证据上限；capability policy 要求 proposer 可读 Wiki且执行 Agent 不可读 Wiki。每次提案首先严格按 Wiki index、Skill impact、active Skill、training summary 顺序读取并验证 trusted/digest-bound envelope；矛盾或样本不足时不调用生成器，直接返回 `needs-evidence`。生成器可返回 `no-proposal`，或只请求有界的 `pattern|raw` 证据并重试一次，不能扩大到未授权数据面或为完成循环强行生成变更。

proposal validator 将 Skill name 固定为 descriptor 中的唯一目标，要求 PURPOSE summary 回指已解析的 pattern/source evidence，且必须同时给出适用条件、不适用条件、失败反例、回退步骤、验证方法、requested capabilities、target runtimes 和正数 context token/byte 上限。machine diff 仅允许 `SKILL.md`、`PURPOSE.md`、`assets/`、`references/`、`scripts/` 候选根，拒绝绝对路径、`..`、active 或 `.chainlesschain` 越界；最终 canonical proposal 连同全部 digest lineage 只发送到 `createCandidate` sink，且回读候选必须逐字段匹配 Skill、内容、Wiki lineage、capability 和 target runtime，任何适配器替换都会失败关闭。10/10 定向测试覆盖成功纵切、固定初始读取、矛盾/样本不足 abstention、pattern 选择性读取、no-proposal、第二 Skill/active path/未知 lineage 拒绝、证据篡改、capability 隔离、candidate acknowledgement failure 和 target runtime substitution。真实 Wiki authority 的统一存储与维护仍由 EVO-P1-1/P1-2 提供，不作为本 proposer 控制协议的重复阻断。

### 6.4 EVO-P1-4：目标运行时 Eval、Shadow 与 Canary

每个已验证 Skill 都应附兼容矩阵：

`model family/version × OS/arch × tool/API version × permission profile × domain/data version`

跨模型 Skill 只能先进入 target-specific validation。对于模型特有 workaround，应允许生成 adapter 或 variant，不能把源模型 Skill 静默设为全局 active。

上线流程建议：

1. offline replay；
2. hidden holdout；
3. shadow，只记录候选决策、不产生副作用；
4. canary 按 tenant/user/session 固定路由；低流量部署使用“接下来 N 个低风险会话/显式 cohort”，有足够流量时再使用预注册百分比；
5. 逐级扩大；
6. 达到预注册质量/成本回归界限、安全违规或预算越界时自动回滚；界限按风险和样本功效校准，不把固定 `+2pp` 当通用标准。

模型、工具或依赖版本变化时，受影响 Skill 自动进入 `stale-needs-revalidation`，而不是继续沿用历史徽章。

### 6.5 EVO-P1-5：Registry、生命周期与单写者治理

Skill Registry 应统一名称空间、semver、content digest、依赖 lock、owner、tenant、trust、sensitivity、allowed tools/sinks、target runtime 和 revocation。

落地时优先把 recorded Skill store 与 Plugin lifecycle 包装成同一 artifact activation port：前者提供 Skill approval/replay/expectedRevision 语义，后者提供 immutable version、active pointer、签名/SBOM、journal、lock 和 recovery。不要让新 Registry 绕过这两套已验证的防线；也不要直接复用整个 Plugin 模块而把 Plugin 专属 marketplace/source policy 强耦合进 Skill，应抽取最小公共事务内核并用 contract test 证明旧路径行为不变。

多 Agent 只用于职责分离和并行评测：

- proposer：只写 candidate；
- critic：只读，寻找反例和过拟合；
- evaluator：只运行固定评测；
- governor：只校验 policy、签名和权限；
- promotion controller：唯一 active pointer 写入者。

Registry 写入必须使用 lease/fence/CAS，避免两个 proposer 或两个客户端同时晋级导致覆盖。任何 Agent 都不能通过自修改 prompt 或 Skill 获得更高权限。

P1 必须同时交付一个最小 reviewer surface：展示 evidence 摘要、candidate diff、权限变化、Eval receipt、target runtime，并支持 approve/reject。P2 Workbench 再补完整时间线、搜索、解释、批量治理和高级可视化；不能因为完整 Workbench 排在 P2，就让 P1 人工门禁退化为盲批。

实施结果（2026-09-02，工作区）：**reviewer 控制协议、持久 decision authority 和 active mutation 人工门已落地，最终用户 surface 仍未接线，因此路线项保持部分完成。** `SkillPromotionReviewPacket` 已稳定包含本节要求的 evidence summary、candidate unified diff、parent-relative capability changes、认证 matrix Eval receipt、target runtimes，以及 `534ca5f4e5` 新增的 Unicode-aware content-risk findings/digest；所有字段及整体 packet 均有 digest。canonical policy envelope 只能按 digest 经捕获的 durable resolver 取得完整 decision，branded provider 要求独立 signature verifier、`automated=false`、唯一 human IDs、短时效、approved decision 和 exact tenant/Skill/candidate/packet；任何 capability 增量要求双人，任何 content risk 同样要求双人并显式确认摘要，无增量且无风险也不能零人通过。`1b30ca030e` 新增的 `SkillPromotionReviewLedgerAdapter` 只接受受信 builder packet，将 packet/decision 写入有限 ArtifactPorts ledger 类型和精确 lineage Ledger event；approve/reject 都须独立签名验证，同 packet 决策唯一，响应丢失可恢复，真实 Ledger/witness 新实例可重建队列并向原 provider 提供 resolver。Agent root 的 evaluated-only facade 仍固定 matrix verify→human review verify→mutation authority consume→release prepare/CAS；release policy lineage 和 procedural Memory evidence 都保留 review/content-risk receipt binding。尚未完成的是把同一持久 packet 和 approve/reject 动作呈现在 CLI/Desktop 最终用户 surface、部署生产 PKI/用户身份、让唯一产品入口构造该 root，以及 kill switch、canary/LKG 可视状态；故不能把协议和持久 adapter 当成已交付 UI。

收口复核（2026-09-05）：**本项现已达到仓库闭环。** `c24011c493` 已把认证 projection 的 list/compare、持久 packet approve/reject 和授权 rollback 接入正式 `cc evolution workbench` 与固定 App Server 方法；`ce91a2fd9e` 将同一 branded host 纵切到 Desktop main IPC/preload/AI Chat reviewer drawer，用户可查看 evidence、完整 candidate diff、权限变化、matrix receipt 与 target runtime，并提交有界人工理由；`8f704b5425` 又把 active/LKG、Canary 阶段、kill-switch 与 reconciliation 状态接入 Desktop、VS Code 和 JetBrains，非法治理投影在展示前失败关闭。当前复核的 CLI/App Server/IPC 83 项及 Desktop Vue 7 项全部通过。原段落所称“最终用户 surface 仍未接线”已经过时；生产 PKI/用户身份、唯一默认 composition root、可操作的外部 kill/rollback authority 及目标部署演练统一归 `EVO-P1-6`、`EVO-P2-1` 和 `EVO-OPT-7`，不再作为本编号的仓库内缺口重复统计。

### 6.6 EVO-P1-6：统一生产接线并退役重复壳

建议把真实生产事件接到统一 adapter：

- UserPrompt/TurnStarted → Raw start event；
- ToolRequested/ToolCompleted/ToolFailed → Raw tool events；
- ResponseCompleted/UserCorrection/TestReceipt → outcome evidence；
- SessionEnd/GoalEnd/ScheduledBatch → Maintainer trigger；
- CandidateCreated/EvalCompleted/HumanTaskSettled → Registry transition。

CLI 无人调用的 `learning-hooks.js` 与 Desktop 未接线的 `self-evolving-system` 应退役；Desktop Phase 20 的六个 manager 应作为 Maintainer、Eval 或 UI adapter，不能各自写“进化终态”。CLI evolution 模块中的真实治理动作保留，只有公式型训练指标移出“训练成功”语义。

实施结果（2026-09-03，工作区）：**CLI Agent 的 durable ingress 纵切已形成，路线项升级为部分完成。** `EvolutionRunLedgerAdapter` 将认证 evidence manifest 引用按连续 sequence、前序 artifact lineage 和 ledger head CAS 写入既有 ArtifactPorts+EvolutionLedger，并在每次重开时重新认证和重放 canonical `EvolutionRun`；响应丢失可恢复，同 baseline 竞争写、event substitution、lineage 断裂、跨租户结果和完成后追加均失败关闭。branded `AgentEvolutionIngress` 已接入交互 REPL、单轮 headless、长驻 stream headless 及 `AgentRuntime`，UserPrompt 必须在模型前持久，工具开始事件必须在 generator 恢复并实际 dispatch 前持久，工具结果与最终响应同样先进入控制面，完整会话才追加 `run-completed`。未配置显式 production authority 时保持关闭，不用本地测试密钥制造默认“已接线”。剩余工作是 CLI 命令层真实 KMS/PKI/witness composition、Desktop/Graph 同协议 ingress、SessionEnd/GoalEnd/ScheduledBatch→Maintainer 调度、Candidate/Eval/HumanTask 的 transition adapter，以及退役/降级重复 self-evolving 壳。

补充实施（2026-09-03，production composition root）：CLI 命令层现已具备单一 branded composition root，显式组合既有 encrypted Raw/evidence/ArtifactPorts/EvolutionRun/Ledger/witness 实现，并把同一 ingress 接入 interactive、headless 与 headless-stream。root 不持有默认密钥或宽松策略，要求部署方一次性注入 artifact、source、commitment、storage、attestation、ledger、witness 全部 authority，强制 ledger/witness 信任根和落盘域独立；任一端口缺失、伪对象或逐调用旁路均在副作用前失败。全链路 2/2 与受影响回归 42/42 通过，覆盖密文 Raw、连续三事件完成、新实例恢复及未品牌化对象拒绝。CLI 仓库内 composition 接线不再是剩余项；发布门改为目标环境提供真实 KMS/PKI/policy/witness authority 及其轮换运维。Desktop/Graph、Maintainer/transition 调度和重复壳退役仍未完成，因此路线项保持部分完成。

补充实施（2026-09-03，canonical Graph App Server ingress）：`AppServerGraphRuntime` 现可在 stdio/WebSocket 两种 canonical App Server transport 上接收宿主注入的同一 branded production composition factory；factory 只取得冻结的 run/revision/origin/authority 元数据，不取得 prompt、凭据或客户端 options。canonical Graph 在任何 HumanTask/Agent attempt 分派前必须先持久确认 `EvolutionRun` start，composition 未品牌化、runId 不匹配或 ingress 启动失败均以 `CC_AGENT_EVOLUTION_INGRESS_FAILED` 失败关闭且 attempt 数保持为零。Agent 节点只把宿主 ingress 传给既有 `runAgentHeadlessStream`，由该执行器继续保证 UserPrompt 在模型前、ToolRequested 在 dispatch 前持久；客户端 JSON 中同名 `evolutionIngress` 会被剥离，不能替换宿主 authority。只有 Graph 达到真实 terminal status 才追加 completion，`reconciliation_required` 明确保留未完成运行。Graph、App Server 与 WebSocket transport 定向回归 34/34 通过。由此 canonical Graph App Server ingress 不再是剩余项；Desktop 直接 Coding Agent/其他非 App Server 路径、Maintainer/transition 调度、目标部署 authority 和重复壳退役仍未完成，因此路线项保持部分完成。

补充实施（2026-09-03，legacy WebSocket Agent ingress）：Desktop 直接 Coding Agent 使用的 legacy WebSocket 服务端现沿 `serve → AgentRuntime → ChainlessChainWSServer → WSAgentHandler` 传递宿主 composition factory，并按真实请求派生稳定、不可由客户端指定的 EvolutionRun id。每个实际执行 turn 都在模型调用前创建并校验同 run 的 branded composition，先确认 run start 和 UserPrompt 持久化；`tool-executing` 等 Agent 事件则在 async generator 恢复、因而在真实工具 dispatch 前进入同一 ingress，最终响应也只有在 completion 持久确认后才发给客户端。factory 只接收冻结的 mode/run/session/request/cwd 元数据，不接收 prompt、API key 或客户端 options；未品牌化或跨 run composition 在消息历史变更和模型调用前失败关闭。相关 WS handler、runtime、server 和真实 session workflow 定向回归 192/192 通过。该批次关闭 legacy WS 子进程内部的可信接线缝，但默认 CLI/桌面包仍不内置测试密钥或本地伪 authority；目标 Desktop 启动器必须由生产部署组合并注入真实 factory 后，才可宣称最终产品入口已启用。因此剩余收窄为目标部署 authority/launcher 与 Maintainer/transition 调度。

补充实施（2026-09-03，Desktop Phase 100 模拟壳退役）：静态全仓引用复核确认 `desktop-app-vue/src/main/ai-engine/evolution/self-evolving-system.js` 与同目录 `evolution-ipc.js` 从未进入生产 IPC registry，仓库内除自身测试外没有运行时调用；真正注册的 Phase 20 `cowork/evolution-ipc.js` 使用另一组 CKG/DKB/prompt metrics/Skill suggestion/debate/A-B channels。现已删除不可达的模拟器、8 个虚构 `evolution:*` handler 及只验证模拟状态的测试，并把模块 65 与产品自进化页面改为退役说明，主设计清单不再宣称 NAS、增量训练、自修复和 8 IPC 已 100% 交付。历史内容仍可由 Git 追溯；受治理 Skill 演进与 Phase 20 metrics 保留且职责分离。

补充实施（2026-09-03，CLI learning hook 平行壳退役）：全仓生产引用复核确认 `packages/cli/src/lib/learning/learning-hooks.js` 从未接入 REPL、headless、AgentRuntime 或公共导出，只有 unit/integration/E2E 自证测试与文档引用。该 shim 会把 raw prompt、tool args/result 与 response 直接写旧 `TrajectoryStore`，对持久错误 fail-open，且 `onSessionEnd()` 始终 no-op；把它接入 canonical Agent 链会绕过已经落地的加密 Raw、trusted projection、ArtifactPorts 与 EvolutionLedger 确认边界。现已删除该文件及自证单测，将组合测试改为直接验证仍有效的 Store/Feedback/Synthesizer/Improver/Reflection 组件，并新增 truth-surface 断言防止旧 shim 回归；模块 84、CLI README 与产品页同步改为部分完成口径。学习单元/组合回归 27/27、真实命令 E2E 16/16、CLI dry-run 包 1463 个条目且旧 hook 0 命中、VitePress 全量构建通过。生产接线仍需 Maintainer/transition durable trigger 与目标部署 authority。

补充实施（2026-09-03，公式型 training metrics 语义收口）：`evolution-system.js` 的旧路径只按样本数公式递增兼容 `accuracy` 列，V2 只根据调用者提交的 before/after loss 计算留存率，却曾写入 `model-training`、返回 `completed` 并自动生成 `KNOWLEDGE_EXPANSION`。现新增诚实命名的 `recordIncrementalModelMetrics()`、`recordTrainingMetricsV2()` 与 `listTrainingMetricRecordsV2()`，统一返回 `status=metrics_recorded`、`performedTraining=false`，V2 另以 `retentionAssessment=threshold_met|retention_low` 表达阈值比较，不再从自报 loss 推断知识扩展。公开 CLI 已用 `record-model-metrics`、`record-training-metrics-v2`、`training-metrics-v2` 替代 `learn`、`train-v2`、`training-log-v2`；旧 JavaScript 函数只作为 deprecated 兼容别名，仍复用 metrics-only 结果。库/真值回归 91/91、真实 metrics truth E2E 6/6、参数 E2E 7/7 与生成物检查均通过。本项仓库内语义已关闭，P1-6 剩余为目标部署 authority/launcher、Maintainer/transition durable 调度及 production adapter 迁移。

补充实施（2026-09-03，durable Wiki Maintainer trigger）：新增 tenant-scoped `wiki-maintenance-request` / `wiki-maintenance-settlement` 有限制品和 Ledger 事件，按 event sequence 恢复 `SessionEnd` / `GoalEnd` / `ScheduledBatch` 待处理队列。enqueue 与处理前都必须由构造期固定的 source verifier 重新认证触发源；request identity 与 tenant/source/evidence digest 绑定，响应丢失和重复投递可从持久 Ledger 恢复。Maintainer 另把 request digest 纳入 revision，即使在 Wiki commit 后、settlement 前崩溃，重试也只能恢复同一 revision；settlement 反向验证真实 `wiki.revision.committed` 制品、tenant、revision/state digest 和 lineage 后才持久。真实 Ledger 文件/witness 重开、三种 trigger、源撤销、结果替换、commit/settlement 两个 crash window 及 ArtifactPorts 联合回归 55 通过、1 项按平台条件跳过。该批次完成仓库级 durable trigger 控制协议，但不虚构已上线的生产事件源：真实 Agent/scheduler producer、持久 source authority 与 worker composition 仍需在目标部署接入；并发 worker 可能重复 derive 计算，但不能重复提交 Wiki state。transition adapter 仍未完成。

补充实施（2026-09-03，Agent completion 真实 trigger source）：新增 branded `EvolutionRunWikiMaintenanceSource/Producer`，不接受调用者自报的 session/goal 结果，而是按 tenant/run id 解析已有 branded Agent production composition，每次重放其 `loadRun()` 所返回的认证 `EvolutionRun`。只有以 `run-completed` 结尾、projection digest 有效且包含有界 evidence set 的 run 才能生成 `session-end`；`goal-end` 还额外要求持久的 `goal-ended` evidence。`AgentEvolutionIngress.complete()` 可选注入该 producer，在 run commit 后 enqueue；已完成 run 的重试仍会重投幂等 request，因而 run commit 后、enqueue 响应前崩溃不会永久丢 trigger。source 在 enqueue 和 worker 处理前都重新解析同一 run，撤销/替换 composition 会在 Maintainer 前失败关闭。source/trigger 联合回归 15/15，production composition 与跨 runtime 9/9，ESLint 通过。这关闭了 CLI Agent `SessionEnd/GoalEnd`→durable queue 的仓库接线缝；目标部署仍须配置 composition resolver、trigger stream 和 worker，`ScheduledBatch` 必须使用独立 scheduler authority，transition adapter 仍未完成。

补充实施（2026-09-03，ScheduledBatch 真实 trigger source）：新增 branded `ScheduledBatchWikiMaintenanceSource/Producer`，从真实 `SchedulerStore` 重开并读取已成功结算的 tenant-scoped occurrence，而不是接受调用者自报的定时任务结果。job payload 与 occurrence result 分别使用固定 schema，绑定 tenant、evidence selector digest、完整 evidence refs 与 evidence-set digest；source 同时要求当前 job/revision 未被替换，并在建模和 Maintainer 处理前把完整 job、occurrence 与 occurrence digest 交给独立 scheduler authority verifier，只有其 durable receipt 才能成为 `sourceReceiptDigest`。真实 SQLite close/reopen、未完成 occurrence、结果替换、authority 撤销与 branded producer 共 4/4 通过，ESLint 通过。至此三类 Maintainer trigger 均已有仓库级 source/producer；目标部署仍须配置真实 scheduler verifier/PKI、store resolver、trigger worker，transition adapter 仍未完成。

补充实施（2026-09-03，durable Registry transition adapter）：新增 `SkillRegistryTransitionLedgerAdapter`，把认证 `CandidateCreated / EvalCompleted / HumanTaskSettled` 三事件链投影为 tenant-scoped `skill-registry-transition-request / attempt / settlement` 有限制品及 Ledger lineage。enqueue 与 worker 执行前都重新调用构造期固定的 source verifier，固定 candidate、matrix context、六类 receipt envelope 和三条 source ref；真实 candidate 还须与 tenant/Skill 精确匹配。worker 先按当前 active state/parent CAS 构建并持久 attempt，再调用真实 `SkillMutationAuthority.authorize()` 签发进程内一次性 capability，capability 本身从不落盘；未过期 attempt 被视为 in-flight，过期后以新 ordinal/nonce 重建，避免重用状态未知的授权。Registry commit 后、settlement 前崩溃时，新 adapter 以不可变 release 的 candidate/mutation-request/transition-subject/authority receipt 和当前 state transaction 精确恢复 settlement，不会再次晋级；settlement Ledger append 响应丢失同样可恢复。窄 evaluated control plane 现具备不增加公开字段的品牌捕获接口，ArtifactPorts 只为这三个新类型开放 ledger retention。联合回归使用两个真实隔离 Gate cell、签名 matrix receipt、双人人工审批、真实 mutation authority 与 `SkillReleaseRegistry`，覆盖 source 撤销、Registry commit crash 和 settlement 响应丢失；相关 46 项通过、1 项按平台条件跳过，ESLint 通过。该 transition 控制协议已形成仓库级纵切；目标部署仍须提供真实三事件 resolver/PKI、worker composition 与默认 launcher/其他产品入口迁移。

补充实施（2026-09-03，Registry transition 真实 source）：新增 branded `SkillRegistryTransitionSource`，不再要求 adapter 信任一个聚合后的自报结果，而是并行调用三个构造期固定且互相独立的 CandidateCreated、EvalCompleted、HumanTaskSettled durable resolver。每个 resolution 都须精确绑定 schema、tenant、source ref、candidate、Skill、时间和自身 receipt；source 再强制三方 candidate/Skill 一致、Candidate≤Eval≤HumanTask 时间单调，把 Candidate 的 candidate/actor/parent/target envelope、Eval 的 matrix context/eval envelope 与 HumanTask 的 policy envelope 汇成六类完整 receipts，并将三个 authority receipt digest 一起固定到唯一 `sourceReceiptDigest`。单元回归 4/4，连同真实 Gate→matrix→review→Registry 纵切 7/7 通过；相关控制面累计 50 项通过、1 项按平台条件跳过。仓库 source/adapter/worker method 已闭合，目标部署仍须注入三个 resolver 的真实存储/PKI 后端并运行 worker composition。

收口复核（2026-09-05）：**本项现已达到仓库闭环。** 交互 REPL、单轮/长驻 headless、AgentRuntime、canonical Graph App Server 与 legacy WebSocket Agent 已共用 branded production composition/ingress；SessionEnd、GoalEnd、ScheduledBatch 和 CandidateCreated/EvalCompleted/HumanTaskSettled 均已有从真实持久源重新认证的 producer、resolver、Ledger queue 与 worker method；两个不可达自演化壳已删除，公式训练壳只保留 metrics-only 语义。`623f5f4f66`、`d7250efac5` 已让公开 npm eager/lazy 的 `agent`、`serve`、`evolution` 入口从 Ed25519 descriptor 装载 exact-byte deployment module，并只暴露 branded 构造工厂；`e761952608` 又让 Desktop 默认 `onReady()` 在 legacy/fast-start bootstrap 和 IPC 注册前装载同一签名部署描述并注入受治理依赖，Desktop 启动的 CLI App Server 则继承同一 descriptor/trust-root 环境。当前复核的 composition、三类 source/worker、公开 loader 32 项及 Desktop loader 3 项全部通过。目标环境提供并运营真实 KMS/HSM、PKI、policy、独立 witness、scheduler/transition authority，配置 resolver/worker、执行两机恢复和密钥轮换演练，统一作为 `EVO-OPT-7` 的部署发布门，不再重复记作本编号的仓库代码缺口。

### 6.7 EVO-P1-7：结构化 Memory、Compaction 与多 Agent 权力分离

借鉴 Claude Code 和 Codex 的最新能力，但补上其共同缺口：

| 层         | 内容                                | 自动经验是否可覆盖           |
| ---------- | ----------------------------------- | ---------------------------- |
| Episodic   | 原始会话、工具、artifact、用户反馈  | 只追加或依法删除，不静默覆盖 |
| Semantic   | 稳定事实、约束、架构决定、矛盾      | 可提案，需证据和冲突处理     |
| Procedural | validated Skill、恢复步骤、workflow | 只能经 promotion gate 更新   |
| Policy     | AGENTS、组织规则、权限和合规        | 不得由自动经验覆盖           |

Compaction 必须保存 requirements、decisions、open risks、failed attempts、tests、goal state、delegated tasks 和 memory lineage。`PostCompact` 运行一致性检查；失败时恢复上一快照。

多 Agent 共享的是有证据和 scope 的事实黑板，而不是自由文本“结论”。子 Agent 的经验先进入统一提炼队列，不能直接污染组织 procedural memory。

实施结果（2026-09-02，工作区）：**该路线项部分完成。** `@chainlesschain/session-core/structured-evolution-memory` 提供唯一事件、投影和快照 schema，并导出到共享包主入口。事件只接受 digest-bound content/artifact reference 与 metadata，拒绝 raw content/secret、跨 tenant、sequence gap、冲突重放和跨 layer memoryId；完全相同的乱序/重复事件确定性投影，增量 state 必须匹配上一 projection digest。episodic 记录 append-only，删除使用 governor tombstone；child-agent/proposer 只能提交带 evidence 的 semantic proposal，接受必须同时绑定原 content/artifact/evidence、critic receipt、evaluator receipt 和 governor；procedural 只允许 promotion-controller+promotion receipt，policy 禁止自动经验并要求 human governor+policy receipt。运行时 append 仅从 tenant-bound branded authority 取得 actor 身份；高权限 transition 由 branded provider 解析 canonical digest/issuer-bound receipt，核验真实 ArtifactStore+Ledger subject、authority revision/handler、独立认证及 memory/content/artifact/evidence 绑定。Compaction 强制保留八类关键状态；CLI Hooks V2 与 Desktop HookSystem PostCompact adapters 都要求至少一个完整成功结果且无 block/prevent/error，再由 attestor 固定 outcome/candidate/projection/previous snapshot，失败或 persistence 异常不替换上一快照。持久 adapter 具备 CAS、lineage、并发冲突、响应丢失幂等、hydration 校验及实际 Ledger 文件/签名/witness reopen；同一真实组合已验证 receipt→semantic acceptance→snapshot 的跨实例恢复。`StructuredMemorySemanticReviewPipeline` 已把真实 proposal、四方独立 critic/evaluator producer/attestor/verifier、双 receipt durable acknowledgement 与 governor accept 串成同一失败关闭流程；`SkillPromotionController` 已写入 procedural authority receipt，`RemoteApprovalBridge` 已在 signed consumed lease 由 host durable store 验证后写入 policy authority receipt，并在 Memory 持久确认前阻止工具 dispatch。正式 `createEvolutionFileWitness()` 还将 witness 签名/验证交给外部同步 authority，以 owner-only 存储、严格跨进程锁、单调认证 history、discard fence 和完整 fsync/rename 链提供本机 durable witness，并由真实 Ledger 崩溃重开用例验证。控制协议 19/19、session-core 全量 538/538、Memory/authority adapter 8/8、Semantic pipeline 联合回归 12/12、CLI PostCompact 5/5、Desktop PostCompact 5/5、ArtifactPorts 25/25、PromotionController 联合回归 26/26、RemoteApproval 联合回归 17/17、FileWitness 6/6 通过，另 1 项按平台条件跳过。最终 Agent production composition、生产密钥托管/跨主机 witness 与进程级 reconciliation 验收尚未接线，故不提前标为仓库闭环。

补充验收（2026-09-02）：AgentRuntime/root 的完整 evaluated promotion 已接通 root-owned procedural append，并覆盖 release commit 后 Memory 响应丢失、显式 pending 状态、幂等 reconciliation 与新 root 恢复；同时修复 matrix envelope digest 与 receipt body digest 混用导致的必然 post-commit 失败。P1-7 继续保持“部分完成”仅因为生产密钥/跨主机 witness 和真实 OS 进程演练尚无权威证据，不再把仓库内 Agent composition 列为剩余。

补充验收（2026-09-02，file backend）：正式构造根已强制 ledger/witness authority 与存储路径分离，并由两个真实 Node PID 验证同一 ledger/witness 重开；保留 witness 而移除本地 event/authority 后，第三个 PID 失败关闭。因而“通用 Ledger OS 进程重启”不再列为缺口；仍未关闭的是生产密钥托管/部署、跨主机 witness fault domain，以及把 release commit→Memory pending→reconciliation 整条链放入真实 OS 进程退出/重启演练。

补充验收（2026-09-02，promotion reconciliation）：root 已可从持久 authority ledger 主动发现、重新认证并补齐 promotion Memory；同进程真实 release pre-commit failure 与三 PID durable receipt→Memory 恢复两组证据均通过。跨进程恢复不再依赖未认证 handoff 或上一进程保留的 JS 对象。最后的本机演练缺口只剩把“实际 release producer 执行”和“硬退出”也放入同一个进程 A，而不是由相邻的真实 release 测试与真实进程测试分别证明。

补充验收（2026-09-02，process kill）：上述最后缺口已由独立 Vitest/Node 进程中的真实 Gate→release→authority receipt→Memory pre-commit failure、父进程强制终止、两个新 Node 进程自动 reconcile/reopen 的单场景测试关闭；同步 marker 只决定何时 kill，不进入进程 B 的认证或恢复输入。P1-7 因而升级为“仓库闭环”，外部生产密钥与跨主机 witness 仍保持明确发布门。

### 6.8 EVO-P1-8：SkillInvocationReceipt 与因果归因

修复 IPC 依赖注入、`BaseSkill`/collector 事件字段和 token 字段不一致后，现有 `skill_execution_metrics` 才可作为 adapter；CLI 现有 name-level usage attribution 也应接入。即便如此，“Skill execute 未抛错”只说明局部调用完成，不能回答 Skill 是否被正确选择、是否改善最终任务、是否因模型或环境变化才成功。每次**选择、暴露与执行**至少记录：

```yaml
receiptId: ...
evolutionRunId: ...
traceId: ...
trajectorySegmentId: ...
selectedSkillDigests: []
routerCandidates: [{ digest: ..., score: ..., reason: ... }]
providerModelVersion: ...
toolSetDigest: ...
osSandboxPermissionPolicyDigest: ...
taskCohort: ...
executionStatus: ...
graderReceipts: []
userCorrectionRef: null
tokenCostLatency: {}
```

模型切换、fallback、tool set、permission/policy 或关键依赖变化都关闭当前 segment 并开启新 segment；不得把跨 segment 的结果直接归因给同一个 Skill 版本。Receipt 只保留归因所需的 bounded metadata/digest，敏感输入输出继续通过受控 artifact reference 访问。

验收标准：自动候选与 canary 运行 100% 产生可 join 的 exposure/outcome receipt；同一 trace 可确定性回答“选了什么、为何选择、在哪个环境、结果由谁判定、花费多少”；在该覆盖率达标前，不启用 outcome-aware reranking 或自动效果声明。

实施结果（2026-09-02，提交 `be547ba42f`；2026-09-03 持久化修正）：**该路线项已关闭。** `chainlesschain.skill-invocation-receipt/v1` 位于共享 `@chainlesschain/session-core`，只保留有界 metadata/digest，不写 task/prompt/output 内容。Desktop `SkillRegistry` 在执行前固定 selected Skill digest、router candidate/reason、evolution run/trace/segment、provider model、tool set、OS/sandbox/permission policy 与 cohort；`BaseSkill` 透传同一个 invocation start；`SkillMetricsCollector` 在 outcome 时补齐 execution status、grader receipt digest、user correction ref、token、cost、latency 和最终 receipt digest，并通过 `skill_execution_metrics.context_json` 持久化。CLI `run_skill` 消费同一 shared builder/verifier，把 receipt 放入 tool result；后续审计发现原 compact transcript writer 曾丢弃该字段，现已改为在 append 前验证 digest 与 `run_skill` 绑定，并由 REPL、普通/stream headless 及 canonical WebSocket 驱动写入 hash-chain `tool_call.skill_invocation_receipt`。真实 hook trace ID 已贯穿外层和隔离 child，不再退化为 session ID。

`automatic-candidate`/`canary` 缺归因字段会抛出 `CC_SKILL_ATTRIBUTION_REQUIRED`，且发生在 Desktop Skill invocation 计数或 CLI child 启动之前；普通调用可生成 `incomplete` receipt，但 `attributionEligible=false`，不能用于 outcome-aware reranking。共享 `verifySkillInvocationReceipt()` 拒绝 digest tamper，`buildSkillInvocationTraceProjection()` 按 startedAt/receiptId 确定性排序并投影 Skill 选择、router reason、provider/model、工具与策略环境、execution/grader/user-correction 判定以及 token/cost/latency 汇总。Desktop/CLI/collector/Phase/pipeline 5 文件 **208/208** 通过，ESLint 0 error，`git diff --check` 通过。上层 Eval/Wiki 后续只需按已固定的 evolutionRunId/trace/segment/grader receipt refs 领域关联，不再需要重建调用归因协议。

后续 verifier hardening 批次修正“摘要吻合即视为合法 settled receipt”的过宽边界：共享 verifier 现在要求顶层字段集合精确，逐项限制 attribution、单一 selected digest、1～64 个不重复 router candidate、唯一 grader、correction、非负安全整数 token、有限 cost/latency、canonical ISO 且单调的 started/completed time 与状态字段，并重算缺失归因列表、`attributionStatus` 和 `attributionEligible` 的一致关系；nested object 也必须是 exact plain record。`settleSkillInvocation()` 同样先验证 started receipt，不能再把重算摘要或注入字段的伪造对象升级为 settled evidence。重算 digest 后的额外 claim、字符串 score、重复候选、矛盾 attribution、重复 grader、分数 token、倒退时间和 forged started receipt 均失败关闭；session-core 定向 **5/5**、全量 **544/544**，Desktop 合法 receipt/DB authority **8/8**、CLI transcript/REPL/headless/stream/WS 消费 **131/131** 通过。该加固不改变 v1 canonical builder 的合法输出，EVO-P1-8 保持完成。

补充验收（2026-09-03，CLI transcript durability）：canonical JSONL writer 对 receipt 先验 digest、再限制只能挂接 `run_skill`，失败时不追加 event；所有正式 Agent 驱动均透传 outer 与 nested settlement 的 receipt，REPL 即使未显式开启 strict usage telemetry，也会把带 receipt 的写入失败升级为终止错误。真实 hash-chain transcript 覆盖有效写入、digest tamper、非 `run_skill` 错绑及失败前后 event count 不变；联合入口回归 4 文件 **124/124**，精准 ESLint 0 error（仅既有 warning）。

### 6.9 EVO-P1-9：复用现有底座的有界评分改进循环

建议先实现 `cc lab evolution pilot` 或等价内部入口，不先建设新调度服务：

1. 固定 baseline digest、训练/验证 split、grader、runtime fingerprint 与 root budget。
2. 在现有 worktree/checkpoint 或 candidate store 中，每轮只产生一个聚焦候选。
3. 优先运行确定性测试/产物 grader，再运行隔离模型 evaluator；记录 receipt。
4. 对比 best；满足预注册 gate 才更新本次 run 的 best candidate，否则丢弃候选，active 始终不变。
5. 将失败分类为 `procedure | model | data | infrastructure | permission/policy | security`。MCP discovery timeout、provider transient、sandbox 缺失和权限拒绝默认不记为 Skill 负样本，除非任务目标本身就是恢复这类故障。
6. proposer、critic、evaluator、subagent 的 token/cost/time/turn 全部记入根 `EvolutionRun`；任一预算耗尽、证据未知或 receipt 写入失败即停止，不允许隐藏的子任务继续优化。
7. snapshot/restore 延用 `GoalConditionEngine` 的确定性状态；代码候选复用 worktree/checkpoint，Skill 发布仍走 candidate registry，不把 Git checkpoint 当作 active Skill promotion transaction。

这一步是 P1 的离线 candidate-only 纵切，用来验证控制协议和 ROI；P2 才把通过的闭环接入 opt-in 真实流量、人工 review、shadow/canary。验收时应覆盖 budget exhaustion、provider/MCP transient、permission denial、evaluator crash、resume 和相同输入重放，并证明不会误改 active 或把基础设施失败编译成错误 Skill。

实施结果（2026-09-02，工作区）：**该路线项已关闭。** 新增 `BoundedSkillImprovementPilot` 作为 `cc lab evolution pilot` 的等价内部控制面。run descriptor 在启动前固定 tenant、EvolutionRun、Skill、baseline candidate/digest、互异的 train/validation split digest、确定性 grader、隔离 evaluator、runtime fingerprint、预注册 gate 与 root budget，并计算不可变 run digest；snapshot 恢复时 descriptor 或 digest 不匹配即拒绝。每轮 round key 只由 run digest 与轮次确定，proposer 只能返回一个候选，candidate sink 只返回 immutable candidate/content digest；确定性 grader 的可信 receipt 先于隔离 evaluator，任一 authority digest、可信标记、score 或 receipt digest 未知都会在 evaluator/best 更新前失败关闭。

best 更新要求两个 grader 均有可信证据、组合分数超过 baseline/best 的预注册增益、root token/cost/子角色 turn/工作时长与墙钟预算未耗尽、round receipt 获得 digest 一致的 durable acknowledgement，并在 receipt 前后两次确认 active state digest 未变化。控制面构造参数不含 active 或 promotion writer；即使外部端口越界改动 active，也会转为 security failure，候选不会成为 best。失败分类覆盖 `procedure | model | data | infrastructure | permission/policy | security`；provider/MCP transient、sandbox unavailable、evaluator crash 与 permission/policy denial 默认 `skillNegative=false`，只有 run 明确以故障恢复为目标时才允许归为负样本。新循环与既有 `GoalConditionEngine` 两文件 **31/31** 通过，并覆盖根预算先于成功、单候选拒绝、grader 顺序、best 保留、未知证据、receipt ack failure、provider/MCP transient、permission denial、evaluator crash、active 漂移、snapshot/resume 与相同输入 round key/快照重放。真实流量、人工 review、shadow/canary 继续由 EVO-P2-1 承担，本路线项不提前声明生产自动进化。

## 7. P2：产品体验、生态与规模化

### 7.1 EVO-P2-1：窄域、受控生产 Pilot

在 P1 离线 candidate-only 纵切通过后，选择高重复、低副作用、答案可自动验真的窄域，例如固定仓库的测试失败修复建议、结构化数据导入或 Record & Replay 稳定流程。

生产 Pilot 默认 opt-in、单租户或明确 cohort；自动阶段仍只生成 candidate。用户旅程为：

`查看 Why/Evidence → 审查 Diff/权限 → 查看 Before/After → 批准 Shadow → Canary → Active`

至少跨一个预注册观测窗口记录 adoption、success delta、cost delta、用户修订率、误晋级率、回滚率和安全事件；按所需样本量决定窗口，不机械固定 30 天或 5% 流量。未达到门槛不扩域。

实施进展：本批次新增 `ControlledSkillProductionPilot` 仓库控制协议，固定 tenant/pilot/Skill、Candidate/Baseline/Eval、Why/Evidence、Candidate/权限 Diff、Before/After 与 review packet digest，并强制显式 opt-in、有界 cohort、预注册样本量/观测窗口、硬性 Canary 流量上限及上述七类退出指标。Shadow 必须消费持久、非自动化且精确绑定的人工批准；Canary 只能沿用 Shadow 中已认证的 subject assignment，未知或重新分配的流量失败关闭；任何门槛失败均禁止扩量。阶段切换采用 durable prepare → 外部 rollout authority → durable finalize 两阶段协议，authority 回执丢失或 final commit 崩溃后只能用同一 request digest 对账；Active/Rollback 还必须与真实 active-state readback 一致。kill switch、active drift、未认证观察/恢复、未来时间戳、cohort/Canary 越界及状态替换均有定向拒绝测试，13/13 通过。该批次只建立可接生产适配器的 fail-closed 控制面；真实低风险租户/cohort、持久跨进程 authority/PKI、流量分配 worker、最终 reviewer/指标界面及实际观测窗口尚未完成，因此本项仅转为“部分完成”，不提前声明生产 Pilot 已上线。

### 7.2 EVO-P2-2：Evolution Workbench

工作台应提供一条可解释时间线：

`Raw/Recording evidence → Wiki pattern 或 direct replay derivation → Candidate diff → Eval receipt → Approval → Promotion/Rollback`

这是对 P1 最小 reviewer surface 的产品化扩展：增加检索、完整历史、冲突解释、批量治理与长期指标，而不是 P1 人工门禁的前置依赖。

用户能够回答：

- 为什么产生这个 Skill？
- 哪些成功和失败轨迹支持它？
- 哪些模型、工具和环境验证过？
- 相比上一版改变了什么权限、成本和行为？
- 为什么被接受、拒绝或回滚？
- 当前会话实际固定到了哪个 digest？

实施进展：本批次新增 branded `EvolutionWorkbenchDataSource` 与 canonical `EvolutionWorkbenchProjection`。数据源不接受 UI 直传的“历史”，而是重新消费既有 `EvolutionRunLedgerAdapter.load()`、`SkillPromotionReviewLedgerAdapter.listReviews()` 和 `SkillRegistryTransitionLedgerAdapter.list()`；三者分别负责事件重投影、review packet/decision 复核及 request→attempt→settlement 账本链验证。投影再校验并连接 `SkillInvocationReceipt` 和受控 Pilot view，形成 Raw/Wiki/Candidate/Eval/Approval/Promotion 的统一只读时间线，并按候选展示 evidence lineage、完整 diff、权限与 content-risk、target matrix、批准/拒绝原因、真实调用成本以及会话实际固定的 Skill digest。新增有界全文/状态检索、分页、expired review/high-risk/pending transition/Pilot reconciliation 冲突解释，以及最多 50 个 pending packet、绑定源 projection digest 的批量治理计划；伪造 source、事件与 projection 不一致、receipt 替换、跨 Skill transition 和已决 packet 批处理均失败关闭。该批次 7/7 定向测试通过；与受控 Pilot 合计 20/20。当前仍缺最终 CLI/Desktop/IDE reviewer surface、批量计划到逐 packet 签名决策 authority 的执行器、长期指标持久聚合与一键回滚接线，因此本项仅为“部分完成”。

后续批量治理执行批次新增 `EvolutionWorkbenchBatchExecutor`：执行前重新验证 batch plan 与完全相同的 source projection digest，任何 packet 状态或投影变化都会使整批失效；随后逐 packet 解析认证 review artifact，并为每项构造包含 plan/projection/candidate/quorum/content-risk 的确定性 request。人工 decision authority 必须逐项返回非自动化、同 request/packet/candidate/decision/reason 的签名 decision，经既有 `SkillPromotionReviewLedgerAdapter.retainDecision()` 持久验证后，再写入独立 item execution journal；不存在可一次批准 50 个 packet 的宽签名或只记部分成功的假完成。执行器与 projection 联合回归 13/13，覆盖 stale projection、非 pending、自动化/替换 decision、retention 和 item journal 未持久确认。该批关闭“批量计划只读、无法逐项执行”的仓库缺口；最终 CLI/Desktop/IDE reviewer surface、真实 human identity/PKI adapter、长期指标、版本对比和一键回滚仍未接线，P2-2 继续保持部分完成。

后续版本控制批次新增 digest-bound Workbench 版本对比和受控回滚执行器：对比只能从已验证 projection 中选择两个精确 packet，并同时展示 candidate/content/parent/diff digest、capability 与 content-risk 变化、target matrix/runtime 以及真实调用成功、失败、成本指标。回滚计划要求源版本当前 active、目标版本已有人工批准，并固定 projection/comparison、from/to content、预期 active state、请求人和原因；执行时重新加载同一 projection 并重建计划，随后只接受与精确 plan 绑定的非自动化、认证且持久人工授权，提交确定性 transition request 后再读取 active state 验证目标字节，最终 rollback receipt 也必须持久确认。未批准目标、自动授权、active readback 漂移或最终确认不持久均失败关闭，6/6 定向测试及 ESLint 通过。该批关闭版本对比与仓库级一键回滚控制协议缺口；最终 CLI/Desktop/IDE reviewer surface、生产 human identity/PKI adapter 与长期指标持久聚合仍未接线，因此 P2-2 保持“部分完成”。

后续 CLI reviewer surface 批次把上述只读与 mutation 控制协议接入正式 `cc evolution workbench` 命令树，提供 `list`、`compare`、逐 packet `review` 和精确版本 `rollback`。命令只接受构造期 branded host：projection 必须重新校验 tenant/digest，reviewer 身份只能来自部署方 identity provider 且要求 authenticated、durable、non-automated，任何 review/rollback 在执行前都必须由 projection authority 持久保存精确 projection；rollback 还会先读取 durable active state 并核对当前 content，CLI 参数不能自报 reviewer 或 active-state digest。未注入可信 host 时命令明确 unavailable，不会回落到本地 JSON 或测试密钥。Workbench 四层联合回归 26/26、CLI 全命令注册 28/28 及 ESLint 通过。该批关闭最终 CLI 命令表面与构造缝；Desktop/IDE reviewer surface、目标部署注入真实 human identity/PKI/projection/transition authority，以及长期指标持久聚合仍未完成，P2-2 继续保持“部分完成”。

后续长期指标批次新增 `EvolutionWorkbenchMetricsAggregator`：它不消费调用者直传的统计数，而是从构造期固定端口拉取 tenant/run-bound、authenticated+durable 的 `SkillInvocationReceipt` 增量；delta digest 明确承诺 source lineage、时间边界和有序 receipt digest 列表。聚合结果按 Skill content digest 保存完成/失败/阻断数、输入输出 token、成本、总延迟与最大延迟，并以 prior snapshot/source digest 形成单调链，通过 expected snapshot digest CAS 和 authenticated+durable acknowledgement 提交。跨实例可从上一 snapshot 继续，receipt 重放、跨 EvolutionRun、未来时间、delta 内容替换、来源链断裂或非持久确认均失败关闭；当前对单 snapshot 保留 100,000 个 receipt digest、单 delta 10,000 条的显式上限。Metrics 与四层 Workbench 联合回归 32/32、ESLint 通过。该批关闭仓库级长期指标确定性聚合与持久端口契约；真实 ArtifactStore+Ledger/数据库 adapter、生产规模 compaction/retention、Desktop/IDE 表面和目标部署 authority 仍未完成，P2-2 保持“部分完成”。

后续 metrics 持久组合批次新增 `EvolutionWorkbenchMetricsLedgerAdapter`，不自建第二套文件存储：metrics snapshot 作为新增的有限产品类型 `evolution-workbench-metrics-snapshot` 写入现有 `EvolutionArtifactPorts`，且只允许 `evolution-ledger` purpose 使用 ledger retention；随后以 `evolution.workbench.metrics.snapshot.committed` 类型化事件、全局 Ledger head/sequence CAS 和前一 snapshot subject lineage 提交。读取只接受 ArtifactPorts 工厂创建的 branded ledger resolver，并复核 durable record 的 artifact tenant/audience/purpose/type 与完整 snapshot tenant/run/Skill/digest；重新创建 adapter 可恢复同一 snapshot，第二 revision 会精确引用前一不可变 subject，替换 event subject 在返回状态前失败。Metrics adapter/aggregator 与 ArtifactPorts 联合 34 通过、1 项按平台条件跳过，ESLint 通过。该批关闭真实 ArtifactStore 制品与类型化 Ledger 组合；实际 `EvolutionLedger` 文件/witness 的专门重启验收、生产 receipt source/authority、规模 compaction/retention 和 Desktop/IDE 表面仍未完成。

后续真实 Ledger 验收批次把同一 metrics adapter 接到实际 `EvolutionLedger` 文件、签名和 durable witness，而不是停留在端口级 ledger fake：首个 InvocationReceipt delta 生成 snapshot 后写入真实 ArtifactStore 与 Ledger；随后保留同一外部 witness、重新创建 Ledger 和 adapter，能够恢复完全一致的 snapshot digest/revision，并验证 event count/sequence。Metrics adapter/aggregator 与 ArtifactPorts 联合 35 通过、1 项按平台条件跳过，ESLint 通过。由此关闭 metrics 的实际 Ledger 文件/witness 重启缺口；剩余为生产 receipt source/identity/PKI/transition authority、规模 compaction/retention 以及 Desktop/IDE reviewer surface。

后续 VS Code reviewer surface 批次扩展 canonical Agent/App Server 协议为四个固定方法 `evolution/workbench/list|compare|review|rollback`，并同步 TypeScript/Python/Kotlin/Swift binding、CLI schema、Agent SDK 及 VS Code/Desktop vendored SDK。`CcAppServer` 只接受 branded Workbench host，初始化能力会明确报告 available/methods；未配置时返回不可用，SDK/IDE pilot 仍不暴露 generic request。VS Code 新增正式命令 `ChainlessChain: Evolution Workbench`：从 CLI-owned App Server projection 显示候选、evidence/diff/usage，详情和对比只打开 JSON 文档；pending approve/reject 与 approved target rollback 均要求重新输入有界原因并通过 modal 明确确认，再调用固定 RPC，插件不接触底层 writer、reviewer identity 或 active-state digest。canonical 协议 19/19、SDK 4/4、App Server/Workbench 19/19、IDE/vendored SDK 15/15 及 ESLint 通过。该批关闭 VS Code 最终 reviewer surface；Desktop/JetBrains surface、生产 Workbench host/identity/PKI/transition/receipt authority 与规模 compaction/retention 仍未完成。

后续 Desktop reviewer surface 批次把同一四个固定方法沿 vendored SDK → 主进程 pilot → 固定 IPC → 窄 preload 接入 Agent 聊天页的正式“演化工作台”抽屉，不向 renderer 暴露 generic App Server request、writer、reviewer identity 或 active-state digest。抽屉支持候选状态与实际使用指标、evidence/diff、精确版本对比，以及 pending packet 的 approve/reject 和从唯一 active 版本回滚到已批准 inactive 版本；所有 mutation 都必须输入非空且不超过 2,048 字符的人工原因。renderer 在展示前复核 projection/candidate digest 与状态边界，在构造 mutation 时再次复核 packet digest、pending/approved 状态和唯一 active→target 关系；CLI-owned host 仍负责最终 authority、持久决策和 readback。主进程代理、固定 IPC、renderer boundary 与 Vue surface 编译定向回归 **62/62** 通过，ESLint 0 error。该批关闭 Desktop 最终 reviewer surface；JetBrains surface、目标部署 Workbench host/human identity/PKI/projection/transition/metrics authority、生产 receipt source及规模 compaction/retention 仍未完成。

后续 JetBrains reviewer surface 批次遵循插件现有的受控 CLI 进程边界，而不另建 IDE-owned App Server 或 writer：纯 Java `EvolutionWorkbench` 只构造固定的 `evolution workbench list|compare|review|rollback` 参数，Action 从 Tools 菜单打开候选表，支持 evidence/diff、双选精确对比、pending approve/reject 与回滚到人工批准的 inactive 版本。分页结果按真实 CLI 返回契约复核 projection digest、候选 packet/content/parent/diff/matrix digest、状态、decision 与 usage；重复 packet、非法计数、替换 comparison binding 或缺完整 execution/rollback terminal digest 均失败关闭。审批和回滚必须在 modal 输入非空且不超过 2,048 字符的原因并二次确认；插件只提交精确 packet digest，human identity、projection retention、active-state readback、transition 与持久 receipt 仍由 CLI deployment host 独占。JDK 21/IntelliJ SDK 定向 JUnit **6/6**、插件纯逻辑与 action 本地化聚合门 **1328/1328** 通过。该批关闭 JetBrains 最终 reviewer surface；P2-2 的仓库内 CLI/Desktop/VS Code/JetBrains 表面均已形成，剩余为目标部署 Workbench host/human identity/PKI/projection/transition/metrics authority、生产 receipt source及规模 compaction/retention。

后续 metrics compaction/retention 批次把 snapshot 改为“有界热集合 + 不可变冷归档”：默认最多保留 10,000 个近期 receipt digest，下一批将溢出时先把旧热集合写入新增的有限 ledger-retention 制品类型 `evolution-workbench-metrics-receipt-retention`，再以类型化 `evolution.workbench.metrics.receipts.retained` 事件、前一 retention root/count 和 batch digest 建立精确链。snapshot 只保存 retention root/count 与新的热集合，长期版本聚合值不丢失；每个后续 delta 在累计前必须通过构造期 authority 对冷归档做 digest-bound、authenticated+durable 精确 membership 查询，冷/热任一重放都失败关闭，不使用可能漏检的概率结构。归档 acknowledgement 丢失可用相同 batch digest 幂等恢复；重复 segment receipt、root/count 断链、artifact subject 替换、非持久 query/commit 或 snapshot 未引用真实 retention root 均被拒绝。真实 ArtifactStore + Ledger 文件 + durable witness 已覆盖 snapshot→归档→新 snapshot→全新 Ledger/adapter reopen→冷 receipt 重放拒绝；metrics/retention/ArtifactPorts 联合 **39 通过、1 项按平台跳过**，ESLint 0 error。该批关闭仓库内规模 compaction/retention 缺口；P2-2 剩余仅为目标部署注入真实 Workbench host/human identity/PKI/projection/transition/metrics authority 与生产 receipt source，并完成相应部署验收。

后续 metrics snapshot verifier hardening 批次修正“snapshot digest 匹配即可接受任意同摘要结构”的过宽边界：读取端现在要求 snapshot 与每个 version 都是 exact plain record；为不破坏已经落盘的同 schema v1 快照，只接受精确旧字段集（无 retention 两字段）或精确当前字段集，不接受任意混合。顶层 tenant/run/Skill、revision→prior/source/time 状态、hot receipt 唯一性与规范顺序、retention root/count、version digest 唯一性与规范顺序以及 snapshot digest 全部逐项验证。receipt/completed/failed/blocked 改为非负安全整数，token/cost/latency 仍要求有限非负数；每个 version 的 outcome 和必须等于 receipt count，所有 version count 总和还必须等于冷归档 count 加热集合长度，攻击者即使重算 snapshot digest 也不能夹带额外 claim、0.5 次调用或删减 hot receipt。新增结构负测与真实 Ledger/witness 联合回归 **15/15**；其中真实文件用例在 Windows 首次运行耗时 5.876 秒触发默认 5 秒测试超时，单独复跑 4.85 秒通过，因此只把该真实 I/O 测试的显式预算校准为 15 秒，未放宽任何产品断言。ESLint 0 error/warning。该批为后续把持久 metrics 索引复用为 retrieval outcome authority 关闭信任入口缺口；P2-2 仍只剩目标部署 authority/source 与部署验收。

后续 governance visibility 批次不再让最终用户只能从候选卡片间接推测当前控制状态：canonical list projection 新增由已验证完整投影派生的只读 `governance` 摘要，固定公开 EvolutionRun status、active/LKG release、冲突数，以及 Pilot stage/revision、kill switch 和 reconciliation-required；过滤与分页不能删除或由客户端重算这些字段。Desktop 抽屉直接展示治理状态与醒目的 kill-switch/reconciliation/conflict 标识；VS Code Quick Pick 标题和详情、JetBrains 状态栏也显示相同状态。三个客户端在展示前均对字段类型、长度、非负计数和 Pilot 枚举做 fail-closed 校验，Pilot 缺少安全布尔值时 projection 构造也会失败。CLI 投影/host **16/16**、Desktop 主进程代理/IPC/renderer 三文件 **62/62**（其中 renderer **7/7**）、VS Code **4/4**、JetBrains JDK 21 JUnit **6/6** 通过，相关 ESLint 0 error/warning。该批关闭仓库内最终 reviewer surface 对 active/LKG/Pilot 安全态不可见的窄缺口，不代表目标部署 Workbench host、human identity/PKI/projection/transition/metrics authority 或生产 receipt source 已接入，P2-2 继续保持“部分完成”。

后续 registrar/transport 接线批次关闭 “App Server transport 收到 Workbench host 后无法继续传入” 的内部断点：显式调用 `registerServeCommand(..., dependencies)` 的部署宿主可提供 branded host，registrar 将同一个捕获结果传入 stdio `runStdioAppServer()` 或 WebSocket `WebSocketAppServerHost`；WebSocket 构造在监听前验证 brand，并在每个连接的 `CcAppServer` 构造期注入。伪造普通对象不能启动，缺省 `null` 继续明确 unavailable，不从 renderer、网络请求、环境变量或本地 JSON 获取 writer/identity。App Server 主协议、WebSocket 真连接、stdio registrar 与 Workbench host 联合 **30/30** 通过，相关 ESLint/Prettier 通过。但公开 npm binary 的 lazy/eager dispatcher 都没有调用真实 deployment host loader，因而不能把这条 seam 表述为默认 `cc serve` 已接线；目标部署仍须提供并装载真实 host、human identity/PKI/projection/transition/metrics authority、生产 receipt source并完成部署验收，故 P2-2 仍为“部分完成”。

公开版故障证据进一步确认了这条边界：VS Code 扩展经 vendored SDK 固定派生 `cc serve --app-server`，CLI initialize 如实返回 `evolutionWorkbench.available=false`，`0.37.81` 却没有在调用 list 前消费该能力声明。`0.37.82` 修复候选现先协商 capability，并按 `list/compare/review/rollback` 的实际方法集裁剪动作；host unavailable 或 App Server pilot disabled 均转为明确说明而不是命令异常。该修复只消除错误 RPC 和误导性动作，默认 production host loader 仍是本项剩余工作。

### 7.3 EVO-P2-3：Skill Retrieval Router

不要照搬 WikiSkill 的 full injection。建议采用：

`namespace/tag/path filter → BM25 + vector recall → compatibility filter → outcome-aware rerank → conflict resolver`

路由结果显示选择理由、版本、上下文成本和冲突。`500` 个 prompt、Recall@5 `≥95%`、误调用率 `<2%`、本地 p95 `<100ms` 可作为低风险 Pilot 的示例起点，正式阈值按语料规模、错误成本与设备档位预注册；同名或不兼容版本不得静默选择。

实施进展：本批次新增有界 `routeSkillDescriptors()`，真实接入 CLI Agent 的 `list_skills(query)` 发现路径，替换原先只做 id/description/category substring 的无序过滤。路由先消费既有 cwd/contract allowlist，再按 namespace、tag、path、OS 与 capability 兼容性失败关闭；只允许带执行 content digest 的 Skill 进入召回。BM25 与可选 vector score 分别归一，随后结合有样本数约束的 success/correction outcome 做确定性 rerank；返回候选固定 digest、namespace、版本、上下文 token 成本、分项分数与选择理由。相同 id 的不同 digest/version、头部低 margin 歧义不会静默选中，并返回结构化 conflict；未知 digest、越界输入和非法 outcome 证据被拒绝。`run_skill` 仍保持精确名称选择，不把模糊检索直接升级为执行授权；`list_skills` 输出的候选 digest/reason 可进入既有 `SkillInvocationReceipt.routerCandidates`。路由核心及 CLI allowlist/path/执行边界相关 4 组共 161/161 通过。真实向量索引/embedding authority、InvocationReceipt 长期 outcome 聚合、跨端统一接线以及 500-prompt Recall@5/误调用率/p95 预注册评测尚未完成，因此本项仅转为“部分完成”。

后续质量基准批次新增 canonical `runSkillRetrievalBenchmark()`，在执行前固定 Skill corpus digest、case corpus digest、case count 及 Recall@5、误调用率、本地 p95 三项阈值；每个 expected digest 必须真实存在于固定 corpus，运行后只能输出 digest-bound report，不能事后改阈值。50 个 Skill × 每个 10 个查询形成 500 个唯一 synthetic prompt，预注册门为 Recall@5 `≥95%`、误调用率 `<2%`、单次本地路由 p95 `<100ms`，真实调用 `routeSkillDescriptors()` 的基准通过；故意把 p95 门降到不可达值时 report 保持失败。Router 与 benchmark 联合 9/9、ESLint 无错误。该批关闭仓库内 500-prompt 预注册基准缺口，但 synthetic corpus 不能替代真实用户分布；生产向量索引/embedding authority、InvocationReceipt 长期 outcome 聚合、Desktop/IDE 接线和真实流量重测仍未完成，P2-3 保持部分完成。

后续 Desktop canonical routing 批次把 Cowork 的真实 `SkillRegistry` 投影为 digest-bound descriptor，并通过主进程动态导入共享 `routeSkillDescriptors()`；缺失或非法 execution content digest 的 Skill 不进入召回，未复用 legacy 按关键词/使用次数排序的 recommender。正式 `skills:route` 固定 IPC 只允许 renderer 提交有界 query、namespace、tag 和 topK，OS/允许能力目标由主进程构造且 renderer 自报 target 被忽略；所有过滤器在加载 router 前 fail-fast。SkillPalette 展示版本与分项理由，canonical router 对头部得分或同名异 digest 判定冲突时只提示缩小范围，不生成可拖拽候选；空查询仍回到精确的可调用 Skill 列表。适配器与 Markdown Skill 联合 **15/15**、SkillPalette SFC 编译通过，精准 ESLint 0 error。该批关闭 Desktop Cowork 产品面的 canonical retrieval 接线；CLI-backed IDE Agent 会继续沿既有 `list_skills(query)` 消费同一路由，但生产向量/embedding 与 outcome authority、真实用户分布重测及独立 IDE 检索产品面仍未完成，P2-3 保持部分完成。

后续 CLI product search cutover 批次退役正式 `cc skill search` 中独立的 substring 筛选，把 multi-layer `CLISkillLoader` 结果直接送入同一 `routeSkillDescriptors()`。命令新增 exact category/tag/source、1～64 的有界 limit 和稳定 `--json` 输出；人类可读结果展示固定 digest、版本、总分与 BM25/vector/outcome 分项理由，发生同名异 digest 或低 margin 冲突时明确 abstain，不伪装为自动选择。JSON 直接返回 canonical schema、selected、candidates、conflicts、rejected 与 vector availability，可供 IDE 窄进程边界消费；非法或越界参数返回非零退出状态。CLI action、纯路由辅助函数及既有 router 联合 **21/21** 通过，精准 ESLint 0 error（仅既有 warning）。该批关闭 CLI 正式搜索命令仍走 legacy substring 的产品分叉；生产 outcome/vector authority、真实用户分布重测与独立 IDE 检索面仍未完成，P2-3 保持部分完成。

后续 VS Code retrieval surface 批次新增正式命令 `ChainlessChain: Search Skills`，严格通过插件现有 hardened CLI 进程边界调用固定 argv `skill search <query> --limit <1..64> --json`，不在 IDE 内复制排序器，也不提供 `run_skill` 或任何写入动作。插件对 canonical JSON 逐项验证 schema/query、候选 digest/版本/namespace/category/context cost、总分和分项分数、outcome、conflict、rejection 及 selected→returned candidate 关系；重复 digest、selected 漂移、越界结构或非零 CLI 退出均失败关闭。QuickPick 只供查看，显示 canonical score/reason；冲突时明确提示 router abstain，详情文档固定 `executionAuthorized: false`。命令贡献、注册和中英文本地化均有静态契约；CLI runner 对该有界大结果显式允许 8 MiB、其他调用仍保持 1 MiB 默认。VS Code 完整 `test:unit` **150/150** 通过，新增 surface 定向 **6/6**、精准 ESLint 0 error。该批关闭 VS Code 独立 Skill 检索产品面；JetBrains 独立检索面、生产 outcome/vector authority及真实用户分布重测仍未完成，P2-3 保持部分完成。

后续 JetBrains retrieval surface 批次复用插件统一 `AgentChatSession.runCapture()` CLI 边界，新增 Tools 菜单 `ChainlessChain: Search Skills`，仅构造固定 `skill search <query> --limit 20 --json` 参数。纯 Java `SkillRetrieval` 对 8 MiB 内 canonical 结果严格验证 schema、trimmed query、最多 64 个候选/冲突、10,000 个 rejection、候选全部 digest/版本/namespace/category/context cost、总分/分项/outcome、selected 必须存在且等于首候选，以及 conflict abstention 语义；重复 digest、selected 漂移、非法分数或缺字段均失败关闭。结果表只支持查看，详情明确 `Execution authorized: false`，无 `run_skill`、writer 或 Java-owned reranker；中英 action 资源与 plugin.xml 保持聚合一致。JDK 21 定向 JUnit **4/4**、JetBrains `smokeTest` **1334/1334**，并同步加固 VS Code 的 selected 首候选/abstention 语义，定向 **6/6** 通过。该批关闭 JetBrains 独立检索产品面；P2-3 的仓库内 CLI/Desktop/VS Code/JetBrains 表面均已形成，剩余为生产 outcome/vector authority、真实用户分布重测与部署验收。

后续 transcript outcome authority 批次把正式 `cc skill search` 与 Agent `list_skills(query)` 接到 canonical JSONL session authority，而非继续使用空统计或用户可传的原始 outcome 数字。读取端固定最多 128 个最近 session、10,000 个 receipt；每个 transcript 在 canonical writer lock 内完成 hash-chain、独立 anti-rollback anchor 与结构验证，`SkillInvocationReceipt` 再经共享 digest verifier 校验，并按 receipt digest 跨 fork 去重。入选集合中任一 blocked/damaged session、receipt tamper、非 `run_skill` 错绑或容量越界都会禁用整组 outcome 数据，不保留有利的部分聚合。只有 attribution 完整且带 grader receipt 或 user-correction evidence 的 `completed/failed` 调用进入 success/correction 样本；无判定证据的“执行未抛错”和 `blocked` 仅计覆盖率，不参与排名。输出携带只含 source digest、计数和容量的有界 authority evidence，VS Code/JetBrains 同步严格验证 verified/unavailable 状态、计数关系与上限。CLI authority/真实 transcript/route 与 Agent 接线联合 **21/21**、实际命令 **1/1**、VS Code **6/6**、JetBrains **4/4** 通过。该批关闭仓库内持久 outcome source 到正式 CLI/Agent/CLI-backed IDE rerank 的接线缺口；Desktop 自有 DB receipt authority、当前 O(N) 扫描的生产 index/snapshot/compaction/retention、生产 vector/embedding、真实用户分布重测和目标部署验收仍未完成，所以 P2-3 保持部分完成。

后续 Desktop outcome authority 批次把主进程 `SkillMetricsCollector` 已持久化到 `skill_execution_metrics.context_json` 的 canonical invocation receipt 接入同一 reranker；renderer 仍只能提交有界检索条件，不能提交 outcome 数字或数据库端口，无效 query/filter 会在扫描历史前失败。读取最近最多 10,000 行并绑定全部行 id/context digest，逐条重验 receipt digest、唯一 Skill digest、完整 attribution 状态与 grader/correction evidence，再按 receipt digest 去重；只有有判定证据的 `completed/failed` 进入 success/correction，未评分成功、blocked、未刷入数据库的内存样本均不参与。任一非法 JSON、回执篡改、重复行 id 或容量越界会禁用整组 Desktop outcome 指标，失败响应只暴露有界错误码。SkillPalette 严格校验证据 schema、计数关系和上限，并明确展示 `antiRollbackWitness: false`：当前 SQLite source 是可校验的本地持久证据，但删除或回滚数据库仍无法由独立故障域发现，不能冒充生产 authority。DB authority、Desktop adapter 与 Phase 16 接线定向 **14/14**，SkillPalette SFC 编译通过，精准 ESLint 0 error/warning。该批关闭 Desktop 自有 DB receipt 到正式检索 rerank 的仓库接线；剩余为具备独立见证的生产 outcome index/snapshot/compaction/retention、生产 vector/embedding、真实用户分布重测和目标部署验收，所以 P2-3 保持部分完成。

后续 witnessed outcome index 批次把 Workbench 的有界热 receipt、不可变冷归档和长期 version 聚合扩展为只统计 graded/corrected `completed/failed` 的 outcome counters；未评分成功和 blocked 不进入质量排名。读取端只接受由 branded metrics Ledger adapter 派生的只读 reader，并重新验证 exact snapshot、ArtifactStore subject、Ledger identity/head/sequence 与 durable witness；最多组合 128 个 source、10,000 个版本，重复 tenant/run/Skill source、任一见证替换、容量越界或旧非空快照未 backfill outcome 历史都会禁用整组索引，不回退到可能掩盖故障的 transcript 有利子集。旧空快照因没有历史可漏记可安全接纳；不可用 evidence 明确标记 `antiRollbackWitness: false`，只有验证成功的索引才能标记为 true。正式 `cc skill search` 与 Agent `list_skills(query)` 已提供 host-owned index 注入点，配置索引时优先消费持久 authority，未配置时保留原 transcript authority；VS Code 与 JetBrains 分别严格验证 transcript/index schema、状态、计数上限与 witness 语义。CLI metrics/index/resolver/命令/Agent 联合 **65/65**、VS Code **7/7**、JetBrains JDK 21 **5/5** 通过，精准 ESLint 0 error（仅既有 warning）。该批关闭仓库内具备独立见证的 outcome index 控制协议、compaction/retention 复用及三端消费边界；目标部署仍需提供真实 source catalog、完成非空旧快照 backfill 和生产 source reconciliation，且生产 vector/embedding、真实用户分布重测与部署验收尚未完成，所以 P2-3 保持部分完成。

后续 production reader composition 批次修正“检索端为了读取 outcome 而持有完整 metrics writer”的过宽能力：`EvolutionWorkbenchMetricsLedgerAdapter` 现在只能显式派生仅含 `loadOutcomeSnapshot()` 的私有 branded reader，索引拒绝完整 adapter 或结构相同的伪 reader。索引组合必须显式固定 tenant，跨租户 source 即使各自快照和 witness 都有效也整组拒绝。现有 Agent evolution production composition 复用同一真实 ArtifactStore、Ledger file backend 与独立 witness，为每个 run/Skill 创建并缓存只读 reader；`assembleAgentSkillOutcomeIndex()` 最多接收 128 个 branded composition source，拒绝重复 run/Skill、代理/额外字段和跨 tenant，再生成 branded `{tenantId, readers}`。`AgentRuntimeFactory → AgentRuntime → interactive REPL → agentLoop → list_skills` 已透传该窄组合，普通 deps 不能替换 production root 提供的 index。composition/reader/index/resolver/runtime/Agent 联合 **62/62** 通过；冷启动动态导入在 Windows 超过默认 5 秒时以命令级 15 秒预算复核通过，未改产品断言；精准 ESLint 0 error（仅既有 warning）。该批关闭仓库内真实 production composition 到交互 Agent 检索的只读组装与 tenant 隔离缺口；目标部署仍需提供历史 composition source catalog、执行旧账 backfill/reconciliation，并把同一 branded index 接入 headless/stream/WS/Graph 最终入口。生产 vector/embedding、真实用户分布重测和部署验收仍未完成，P2-3 保持部分完成。

后续 headless parity 批次把同一个 branded outcome index 从 `registerAgentCommand()` 的宿主依赖分别传入单轮 `cc agent -p` 与长驻 `--input-format stream-json`，两个 runner 在 bootstrap、模型调用和输入消费前重新捕获私有品牌，并在同时存在 evolution ingress 时强制 tenant 完全一致；随后只把窄 index 放入既有 `agentLoop` options，因此 `list_skills(query)` 与交互 REPL 消费同一 authority，调用方不能用结构相同对象替换。真实 production composition 组装的 index 已覆盖单轮和 stream 原样传递，跨 tenant ingress/index 与伪 index 均在模型调用前失败关闭，专项 **3/3** 通过。由此 headless/stream 缺口关闭；历史 source catalog、旧账 backfill/reconciliation、WebSocket/Graph 最终入口、生产 vector/embedding、真实用户分布重测和部署验收仍未完成，P2-3 继续保持部分完成。

后续 WS/Graph parity 批次把宿主提供的同一只读 index 接到 legacy WebSocket server/session handler 与 canonical App Server 的 stdio/WebSocket/Graph 三条链。Legacy handler 构造期捕获私有品牌，并在每 turn composition 启动边界校验 tenant 后才允许 prompt/model；canonical App Server 删除客户端 `options.skillOutcomeIndex`，只把构造期 capability 交给 Graph runtime、node executor 与 `CliAgentKernelAdapter → headless stream`，自定义 Graph runtime 与单独 host authority 同时出现时失败关闭，避免 capability 被静默忽略。Graph composition/index 跨 tenant 在分配 attempt 前拒绝，伪 index 在 server/runtime 构造期拒绝；Agent server factory 也只向 server surface 传 outcome index，不把单次 evolution ingress 错用为长驻 server authority。WS、Graph、App Server、transport、runtime 联合回归 **119/119** 通过，精准 ESLint 0 error（仅既有 warning）。由此仓库内交互、单轮 headless、stream、legacy WS 与 canonical App Server/Graph 消费入口均已接线；剩余为目标部署提供历史 source catalog 与实际 index 注入、旧账 backfill/reconciliation、生产 vector/embedding、真实用户分布重测和部署验收。

后续 historical catalog consumer 批次新增 `assembleAgentSkillOutcomeIndexFromCatalog()`：宿主只提交固定 tenant 下的 `{runId, skillNames[]}` 目录，并注入 production composition opener；consumer 在打开任何 run 前拒绝代理/额外字段、重复 run、同 run 重复 Skill 与超过 128 个总 source，再按 run 顺序只打开一次 composition。opener 上下文冻结且不含 prompt/密钥，返回值必须是私有 branded composition，并与请求 tenant/run 精确回绑；随后继续复用既有只读 reader 与 index assembler，错绑/伪造 composition 不会进入检索。两个真实历史 run 经全新 production composition 实例重开并组装 3 个 verified reader，专项 **4/4** 通过。仓库由此具备历史目录消费和重开协议，但不会把未认证的目录扫描当成 authority；目标部署仍须提供经认证 catalog 数据、实际 KMS/PKI opener 与 index 注入，并完成旧账 backfill/reconciliation。生产 vector/embedding、真实用户分布重测和部署验收仍未完成。

后续 outcome backfill/reconciliation 批次新增 `EvolutionWorkbenchMetricsOutcomeBackfiller`，不另建迁移存储：它只接受 authenticated+durable 的完整历史 invocation receipt envelope，精确绑定 tenant/run/Skill、旧 snapshot/source/window 与 history digest，并要求 receipt digest 唯一且 canonical 排序。hot digest 必须全部覆盖；冷 receipt 的数量必须恰好等于 retained count，且逐项经既有不可变 retention root 查询确认；随后用 canonical receipt 重算全部 execution/token/cost/latency 基线，只有与旧 snapshot 完全一致才补写 grader/correction outcome counters 和 `outcomeHistoryComplete=true`。结果通过既有 metrics adapter 的 snapshot CAS、ArtifactStore、Ledger 及提交后 readback 对账，重复执行收敛为 already-complete；缺失、替换、跨 run、统计不一致或未知持久确认均失败关闭。纯协议与真实 retained ArtifactStore+Ledger 联合定向 **19/19** 通过。仓库内非空旧快照的回填执行器由此形成；目标部署仍须提供经认证历史 receipt source、catalog/KMS/PKI opener 并实际执行迁移和审计验收。生产 vector/embedding、真实用户分布重测与部署验收仍未完成，P2-3 保持部分完成。

后续 authenticated catalog authority 批次不再允许 historical consumer 直接接收普通数组：新增私有 branded `SkillOutcomeSourceCatalogAuthority`，loader 返回的 catalog 必须具有精确 schema、tenant/catalog/revision/time、最多 128 个 run/Skill source、唯一 run/Skill 和 canonical catalog digest，并携带有界 attestation；构造期固定的独立 verifier 必须对 catalog digest 与 attestation 返回 tenant/catalog/revision 全量回绑、authenticated+durable 且带 receipt digest 的确认，内容替换、伪 authority、跨 tenant 或不可信确认均在打开任何历史 composition 前失败。`assembleAgentSkillOutcomeIndexFromCatalog()` 现在只从该窄 authority 读取 entries，再继续执行原有 composition tenant/run 品牌校验。composition/catalog 专项及全文件 **4/4** 通过，精准 ESLint 0 error。该批关闭仓库内 catalog consumer 信任普通调用方数据的缺口；目标部署仍需把 loader/verifier 接到实际持久目录与 KMS/PKI，并注入默认启动链。生产 vector/embedding、真实用户分布重测与部署验收仍未完成，P2-3 保持部分完成。

后续 CLI vector authority 批次替换了正式 `cc skill search` 只能接收裸 `vectorScores` 的信任缺口：新增 tenant-bound 私有 branded `SkillVectorAuthority`，先对最多 10,000 个 Skill 的 digest、公开检索文本和 tags 建立有界 canonical corpus 与 request digest；provider 必须返回全 corpus 唯一且完整的 `[0,1]` score、固定 model id/revision、index digest、result digest 和 attestation，随后由构造期独立 verifier 对 tenant/request/result 做 authenticated+durable 回绑并签发 receipt，任何分数替换、漏项、重复 digest、伪 authority 或不可信确认都不会进入 router。正式命令的 host dependency 配置该 authority 时才启用 vector rerank，并输出 model/index/result/receipt evidence；未配置时保留 BM25+outcome 路由且明确 `CC_SKILL_VECTOR_AUTHORITY_UNCONFIGURED`，不把降级冒充为向量可用。authority/router 定向 **13/13**、实际 CLI **13/13**、VS Code 既有严格消费回归 **7/7** 通过，精准 ESLint 0 error（仅既有 warning）。该批关闭 CLI 产品面的认证向量注入协议；剩余为将同一 authority 透传 Agent/WS/Graph/Desktop，提供目标部署的真实 embedding/index provider+独立 verifier，并让 IDE 显式校验 vector evidence。真实用户分布重测与部署验收仍未完成，P2-3 保持部分完成。

后续 Agent vector parity 批次把同一个 branded `SkillVectorAuthority` 从 `registerAgentCommand()` 和 `AgentRuntimeFactory` 贯穿交互 Runtime、单轮 headless、长驻 stream、legacy WebSocket、canonical App Server direct turn/WebSocket transport 与 Graph executor，最终由 `list_skills(query)` 调用并把 verified model/index/result/receipt evidence 返回模型。各启动边界重新捕获私有品牌；同时存在 evolution composition、outcome index 与 vector authority 时必须共享 tenant。Graph/WS 在分配 attempt、启动 ingress 或模型调用前拒绝错租户，App Server 会删除 client turn options 中的 `skillVectorAuthority` 并只注入宿主对象；伪 authority 不能借 CLI、Runtime、WS 或 Graph 进入调用链。Agent/core/runtime 联合 **40/40**、WS/App Server/Graph 联合 **91/91** 通过；runtime-factory 冷加载以既有 15 秒命令预算复核 **9/9**，精准 ESLint 0 error（仅既有 warning）。该批关闭 CLI Agent 全运行模式的 vector authority parity；剩余为 Desktop 接线、IDE 显式 evidence 校验，以及目标部署真实 embedding/index provider+独立 verifier、真实分布重测和部署验收，P2-3 保持部分完成。

后续 Desktop/IDE vector evidence 批次把同一 branded `SkillVectorAuthority` 接入 Desktop 正式 `skills:route`：renderer 仍只能提交 query/filter，主进程 Phase 1 通过宿主 getter 在每次调用时取得 authority；只有 CLI 私有工厂签发且经独立 verifier 确认的完整 corpus score 才传给 canonical router，普通 `{score()}` 对象在 rerank 前失败。未配置或有效 corpus 为空时返回规范 `CC_SKILL_VECTOR_*` unavailable evidence，不把 BM25/outcome 降级冒充为向量可用。Desktop SkillPalette、VS Code 和 JetBrains 现在都显式要求 `vectorAuthority`，严格校验 exact verified/unavailable 形状、tenant/model 标识、五类 digest、1～10,000 skill count 以及 unavailable→`vectorAvailable=false` 关系；详情面展示 model/revision/index 证据，不取得 writer 或执行能力。Desktop adapter/Phase/bootstrap **62/62**、VS Code 全量 unit **152/152**、JetBrains 纯 Java 定向兼容 harness **6/6** 通过；本机缺 JDK 21，完整 Gradle JUnit 仍由正式 JDK 21 门禁执行。本批关闭仓库内 Desktop 接线和 IDE 显式 evidence 校验缺口；目标部署仍须提供真实 embedding/index provider、独立 verifier/KMS/PKI 与默认 authority 实例，并以真实用户分布完成质量/延迟校准和部署验收，所以 P2-3 保持部分完成。

后续 process authority 批次为现有 branded `SkillVectorAuthority` 增加正式跨进程 provider/verifier 组合端口：宿主必须分别固定独立 worker identity、规范绝对 executable/entry/cwd 路径及 SHA-256，构造期和每次调用前后均重新认证文件；启动统一经过 `ProcessExecutionBroker` 的审计、凭据过滤和平台沙箱，使用 `shell:false`、绝对可执行文件、固定参数、隐藏窗口与独立显式环境，父进程 ambient 环境和秘密不继承。JSON 请求、stdout/stderr、参数、环境、超时均有硬上限，超时/超限先强制终止并等待子进程关闭，非零退出、非 JSON、文件替换或 provider/verifier 共用 entry 一律失败关闭；返回值继续由既有 authority 对完整 corpus、tenant/request/model/index/result/attestation 及独立 durable verification receipt 做 exact 校验。真实双进程 fixture 同时验证秘密不泄漏、固定身份、超时、超限和畸形输出，进程清单保持 `unreviewed: 0`。该批形成目标部署可直接注入的跨进程端口，但不把 fixture 或文件摘要检查冒充生产 embedding、PKI 或原子进程映像认证；生产 worker、KMS/PKI、默认启动链和真实分布质量/延迟校准仍待部署验收，P2-3 保持部分完成。

### 7.4 EVO-P2-4：跨设备、团队和组织知识

支持 personal/project/team/org scope、端到端加密、RBAC、租户隔离、离线冲突合并、批准与撤销。两台机器或 cloud session 只同步已授权、已批准知识；个人 memory 不因 Agent Team 协作自动升级为团队规则。

删除证据、用户行使隐私删除或组织撤销知识时，反向依赖图必须定位并处理派生 Wiki、candidate 和 active Skill。

实施进展：本批次新增演化知识专用 `GovernedKnowledgeSync` 控制协议，不复用当前会把正文直接广播且缺少入站独立认证的 legacy `OrgKnowledgeSyncManager` 作为完成证据。控制面固定 tenant/device，明确 personal/project/team/org scope；personal knowledge 不能进入共享通道，team/org 必须绑定人工 approval receipt。出站只发送经外部加密 authority 产生的 ciphertext、key ref 与按实际密文字节重算的 digest，并在 durable local commit 后才交 transport；入站依次验证 envelope digest、发送者签名、租户、密文字节、canonical 解密记录和独立 RBAC authority。每条记录携带至多 64 设备的 vector clock；旧版本/replay 幂等拒绝，并发离线修改保留为显式 conflict 且要求人工合并，不采用静默 last-write-wins。tombstone/revoke 必须绑定 revocation receipt 和有界反向依赖 disposition，active Skill 只能 rollback/quarantine，未知处置失败关闭。跨租户/伪签名/密文替换、个人自动升域、未批准共享、不安全撤销、并发冲突以及 storage/transport 非 durable 均有定向验证，6/6 通过。该批次只建立 fail-closed 同步核心；Desktop/CLI/Cloud Session 宿主切换、真实 E2EE/KMS、RBAC/approval resolver、持久 conflict queue、依赖图执行器及两机离线/撤销/隐私删除 E2E 尚未完成，因此本项仅转为“部分完成”。

后续 durable conflict queue 批次新增 `GovernedKnowledgeSyncLedgerAdapter`：local/remote/conflict 三类提交均把完整 canonical knowledge、ciphertext-only envelope、authorization receipt 与冲突基线写入新增有限产品类型 `governed-knowledge-sync-record`，只允许 `evolution-ledger` purpose 取得 ledger retention；随后以 head/sequence CAS 追加 `knowledge.sync.committed`，响应在 append 后丢失时按稳定 event identity 从账本恢复，重试时间变化不会产生第二条记录。重启读取只接受 ArtifactPorts 工厂签发的 branded resolver，重新校验 artifact tenant/audience/purpose、record/envelope/ciphertext digest、knowledge 元数据与 vector clock 绑定，并再次调用当前独立签名 verifier，因此 authority 撤销后旧记录也会 fail-closed。同步核心同时拒绝空、超过 12 MiB 或非 canonical base64 的密文，并阻止已重新签名的 scope/scopeId/action/vectorClock 元数据替换。核心与 adapter 定向 **13/13** 通过，其中实际 EvolutionLedger 文件、签名 witness 与 ArtifactStore 经全新 Ledger/adapter 实例重开后恢复同一记录；持久 conflict queue 缺口在仓库内关闭。剩余为 Desktop/CLI/Cloud Session 宿主切换、真实 E2EE/KMS 与 RBAC/approval authority、人工合并/依赖图执行器，以及两机离线、撤销和隐私删除传播 E2E，因此 EVO-P2-4 继续保持部分完成。

后续 human merge planning 批次在持久队列上增加只读 branded conflict reader 与 `GovernedKnowledgeConflictMergePlanner`。planner 不接受调用者直传或结构仿造的 conflict：每次重新从当前 Ledger/ArtifactStore reader 解析目标 envelope，并要求本地 active content 仍等于 conflict 捕获的 baseline；若已前进则拒绝陈旧裁决。merged record 必须保持同 tenant/knowledge/scope，action 固定为 upsert，vector clock 对本地和远端逐设备取最大值且只能由当前设备精确递增一次。人工 receipt 必须为 `automated:false`，在 30 天时效和 5 分钟未来偏差内，精确绑定 reviewer、冲突 envelope、本地/远端/合并 content digest、完整 merged vector 与 reason，自身 canonical digest 和有界 attestation 均须成立，随后还要由构造期固定的独立 verifier 返回 authenticated+durable 全字段确认。输出只是 digest-bound immutable merge plan，不在尚无 prepare/dispatch/finalize 崩溃恢复协议时提前发送或消除冲突。核心、持久 adapter 与 planner 定向 **14/14** 通过。人工决策与确定性合并计划缺口已关闭；持久幂等 merge executor、产品 reviewer surface 和依赖图执行器仍待后续批次。

后续 durable merge execution 批次新增 branded `GovernedKnowledgeMergePublisherAuthority` 与 `GovernedKnowledgeMergeLedgerExecutor`。首次 prepare 只接受 planner 私有品牌签发的 plan，结构相同的调用者对象不能落账；prepare artifact/event 与原 conflict subject 建立精确 lineage。进程退出后只允许从认证 ArtifactStore+Ledger prepared record 按 plan digest 恢复，不把可序列化品牌当作持久凭据。发布请求以 plan digest 派生固定 operation ID/request digest，构造期固定的 provider 必须返回 tenant/device/plan/knowledge/content/envelope 全绑定且 `durable+idempotent` 的 result，再由 authority id/revision/handler 均独立的 verifier 签发 verification receipt；不可信 result 保持 prepared 而不伪报完成。settlement 作为新增有限 `governed-knowledge-merge-operation` ledger-retention artifact 写入，event 精确引用 prepared subject；prepare/settlement 的 append 响应丢失和并发时间戳差异均按稳定内容恢复。测试覆盖伪 plan、发布响应丢失后新 executor 以同一请求恢复、settlement 响应丢失、独立 verifier 拒绝、完成后不重复发布，以及实际 EvolutionLedger 文件/签名 witness/ArtifactStore 由全新 Ledger/executor 重开；核心与联合回归 **17/17**。持久幂等 merge executor 缺口由此在仓库内关闭；产品 reviewer surface、真实发布 provider/E2EE/RBAC authority、依赖图执行器和两机部署 E2E 仍待完成。

后续 revocation dependency execution 批次新增 branded `GovernedKnowledgeDependencyAuthority` 与 `GovernedKnowledgeDependencyLedgerExecutor`。tombstone/revoke 的 canonical record 先以新增有限 `governed-knowledge-dependency-operation` 类型写入 ArtifactStore，并以 Ledger prepared event 固定完整 revocation receipt 和至多 256 个唯一 dependency disposition；每项执行请求由 operation/dependency digest 派生稳定 ID，精确绑定 tenant/device/knowledge/action/content/revocation receipt 与 kind/digest/disposition。provider 的 applied+durable+idempotent result 必须回绑构造期 authority id/revision/handler 和有界 attestation，再由独立 verifier 认证并签发 verification receipt；全部依赖完成后 settlement event 引用 prepared subject。响应丢失或进程退出后从认证 prepared record 重放相同请求，provider 以同一幂等键返回已持久结果；实际 EvolutionLedger 文件、签名 witness 与 ArtifactStore 经新 executor 重开不会再次调用 provider。`GovernedKnowledgeSync` 现在只接受 branded executor：出站撤销在 encrypt/commit/send 前完成依赖 settlement，入站只对确定更新执行；stale/replay 直接忽略，并发撤销只进入 conflict queue，绝不提前 rollback/quarantine。缺 executor、任一未持久结果或 verifier 拒绝均不会发布撤销。核心与联合定向 **21/21** 通过。仓库内依赖图持久执行和顺序缺口由此关闭；产品 reviewer surface、真实 E2EE/RBAC/merge/dependency providers 与两机部署 E2E 仍待完成。

后续 CLI reviewer surface 批次新增 branded `GovernedKnowledgeReviewHost` 与正式 `cc evolution knowledge conflicts|merge` 命令。host 构造期固定同 tenant/device 的 branded conflict reader 与 merge executor，并在内部创建 planner；CLI 只能提交 conflict envelope digest、canonical merged record 与有界 reason，不能提交 reviewer identity、receipt、签名、Ledger 或 publisher capability。receipt issuer 只获得 digest/vector/reason/request time 组成的冻结请求，返回结果仍须经 planner 的独立 verifier 全绑定认证。冲突列表只输出 scope、sender、local/remote digest、remote vector 和时间，不泄漏 ciphertext、key ref、signature 或 approval receipt；settled conflict 会从分页结果与总数剔除。executor 同时增加每 conflict 唯一 prepared plan 约束，结合 Ledger head CAS 阻断竞态双裁决；完成后重复 merge 在签发第二张 receipt 前拒绝。Commander 实际 surface、脱敏输出、durable merge、settled 隐藏、二次裁决和伪 host 均有定向验证；核心与联合回归 **23/23**。CLI reviewer surface 缺口由此关闭；Desktop/Cloud Session surface、真实 identity/PKI/E2EE/RBAC/merge/dependency providers 与两机部署 E2E 仍待完成。

后续 Cloud Session/Desktop transport 批次把 reviewer host 接入 canonical App Server，新增仅有 `evolution/knowledge/conflicts|merge` 的固定 RPC；初始化能力明确报告 availability/methods，App Server 构造期拒绝伪 host，Cloud/WebSocket 与 stdio 共用同一协议校验和 branded host。Agent SDK 只新增两项 capability-shaped 方法，不暴露 generic request；TypeScript/Python/Kotlin/Swift binding 与 CLI schema 由 canonical schema 同步生成。Desktop main pilot、IPC allowlist、preload 和 renderer 类型只转发 conflict pagination 与 merge input，renderer 仍不能接触 issuer/verifier/Ledger/publisher。协议生成检查通过，SDK、App Server durable merge 与 Desktop pilot/IPC/production-surface 联合 **78/78** 通过；知识核心与 adapter 定向累计 **25/25**。Cloud Session 固定 surface 和 Desktop 安全传输边界由此关闭；Desktop 最终 reviewer UI、真实 identity/PKI/E2EE/RBAC/merge/dependency providers 与两机部署 E2E 仍待完成。

后续 Desktop reviewer UI 批次在 AI Chat Agent mode 增加“知识冲突”正式入口和独立 drawer。renderer 对 App Server projection 进行 schema、tenant/device、数量、digest、scope/action、vector clock 与时间校验，并以 exact allowlist 拒绝 conflict 或顶层响应中夹带 ciphertext、key、signature、approval receipt 等额外字段；列表只展示来源设备、作用域、动作、本地/远端摘要、时钟和提交时间。人工合并 modal 要求粘贴完整 JSON record、填写有界原因并勾选不可变裁决确认，随后只通过固定 preload method 提交；durable 结果后刷新并隐藏已 settlement conflict，UI 不持有身份、receipt issuer/verifier、Ledger 或 publisher capability。新增 renderer 边界 **4/4**、与既有 Workbench/IPC guard 联合 **13/13** 通过，Desktop renderer 生产构建完成。Desktop 最终 reviewer surface 由此关闭；真实 identity/PKI/E2EE/RBAC/merge/dependency providers 与两机部署 E2E 仍待完成。

后续 Node crypto authority 批次新增 branded `GovernedKnowledgeNodeCryptoAuthority`，用实际 AES-256-GCM AEAD 封装 canonical knowledge：tenant/knowledge/scope/action/content/vector 全部进入 AAD，密文固定 version+96-bit nonce+128-bit tag 格式并绑定 byte digest；每个 project/team/org 只有一个 active key 用于发布，retired key 只保留历史解密，keyRef、tenant、scope 与 scopeId 必须精确匹配。envelope 使用实际 Ed25519 私钥签名 domain-separated digest，接收端只接受 tenant 内预钉扎 device→SPKI/keyId，签名结构 exact 且 base64url canonical；原始 AES/Ed25519 key 始终停留在 authority closure，不进入 envelope、App Server 或 renderer。两设备真实加解密/签名、轮换、旧密文读取、伪签名、密文篡改、跨租户 key、错私钥 pin 和未知 scope key 与同步核心联合 **12/12** 通过。真实算法 provider 缺口由此关闭；目标部署的 KMS/HSM 密钥装载与轮换控制、组织 PKI registry、RBAC/approval、merge/dependency providers 和两机部署 E2E 仍待完成。

后续 RBAC/approval authority 批次新增 branded `PermissionEngineGovernedKnowledgeRbac`、`GovernedKnowledgeRbacApprovalAuthority` 与 Ed25519 approval issuer。权限直接读取仓库真实 Permission Engine grants：permission 固定为 knowledge/scope/base64url(scopeId)/publish-or-receive/action，支持现有层级 wildcard，但不同 scopeId、operation 或 action 不会互相放权；policy digest 每次从排序后的 effective permissions 重算。team/org 进一步按 knowledge 中的 receipt digest 从独立 reader 取回 exact approval，强制 tenant/reviewer/knowledge/scope/action/content、非自动化、30 天窗口与显式到期时间，并用 tenant 内 reviewer→SPKI/keyId registry 验证 domain-separated Ed25519 签名；重复 reviewer key、跨租户 reviewer 和伪 RBAC adapter 均在构造期拒绝。project 仍只需 RBAC，不伪造人工审批。真实 Permission Engine、发布核心、权限/作用域隔离和审批替换/过期/伪签名联合 **14/14** 通过。RBAC 与组织人工 approval provider 缺口由此关闭；目标部署的 KMS/HSM 装载/轮换、动态组织 PKI registry、durable approval store、merge/dependency providers 和两机部署 E2E 仍待完成。

本次 trust ledger 批次新增 `GovernedKnowledgeTrustLedger`，将 reviewer 注册/撤销生命周期和 signed approval receipt 作为有限类型 `governed-knowledge-trust-record` 写入 ArtifactStore，并用 EvolutionLedger 的稳定 event identity、CAS append、readback 与 witness 文件形成可重开的 durable trust history。动态 reviewer registry 每次验签都会解析当前 active key；撤销会立即使历史 approval 失效，轮换必须先撤销旧 key，已撤销 key 不可重新激活。approval commit 在落盘前重算 receipt digest、校验 exact fixed shape、当前 reviewer key 和 domain-separated Ed25519 签名；响应丢失重试可恢复同一注册、撤销和 approval commit，但不会把不同 payload 误判成成功。ArtifactStore adapter 重开、真实 EvolutionLedger/witness 文件重开、撤销后的 current-trust revalidation、forged receipt 拒绝等联合 **37 passed、1 个既有平台 skip**。动态组织 reviewer PKI registry 与 durable approval store 缺口由此关闭；目标 KMS/HSM 装载/轮换控制、真实 merge/dependency providers 和两机部署 E2E 仍待完成。

后续 sync merge publisher 批次把人工 merge 的实际发布接到 `GovernedKnowledgeSync`，而不再停留在测试 provider：同步账本升级为可读取历史 v1、默认写入 v2 的 durable `operationId` 记录，并提供 tenant/device-bound branded publication reader。merge authority 使用独立 reader 对发布端与 verifier 端分别回读同一 local commit，精确绑定 plan/request、merged knowledge、envelope、设备签名、账本 record digest 与 verification receipt；同一 operationId 替换内容或 envelope 会失败关闭。local commit 后 transport acknowledgement 丢失时，新 controller/provider 会从真实 ArtifactStore+EvolutionLedger/witness 重开同一 envelope，并再次调用幂等 transport，只有重新取得 exact durable acknowledgement 才允许 merge settlement，避免把“本地已写”误报成“远端已发布”。同步、merge、真实 crypto、App Server/CLI reviewer 受影响回归 **31/31** 通过，且真实 Ledger/witness 重开已覆盖 v2 publication、旧 v1 制品继续通过认证读取。真实 merge provider 缺口由此关闭；目标 KMS/HSM 装载/轮换控制、真实 dependency provider 和两机部署 E2E 仍待完成。

### 7.5 EVO-P2-5：跨模型 Skill 来源/目标适配与市场治理

WikiSkill 实验的是 source-model-evolved Skill 文件向 target inference model 的直接迁移，不是教师/学生蒸馏。实验既有正迁移，例如 Qwen-3.6-27B 的 ALFWorld Skill 使 Qwen-3.5-9B 达到 `70.2%`、高于其 self-evolved Skill 的 `63.4%`，也有 Spreadsheet 的严重负迁移。因此可以把“强模型 proposer 为小模型起草候选”作为产品假设，但必须在目标模型上重新评测，且不能预设更强 source 必然更可迁移。

Skill/Plugin 市场增加：签名、来源 commit、SBOM、依赖 lock、权限 manifest、模型/OS/工具兼容矩阵、可复现 Eval badge、演化 lineage、分阶段更新和一键回滚。排名不能只看安装量或 LLM 自报 confidence。

实施进展：本批次新增 `GovernedSkillMarketplace` 市场控制协议。canonical manifest 将 tenant、Skill/version、source model、package digest、source commit、SBOM、dependency lock、permission manifest、target matrix、可复现 Eval badge 和完整 lineage 一并纳入签名摘要；安装前必须找到唯一且明确接受的目标 model/OS/tool/runtime cell，并由 target-specific adapter 重新认证 manifest、目标 Eval receipt 与适配输出，不能用 source model 的自报能力替代目标侧评测。更新严格按 candidate→shadow→canary→active 单阶段推进，每一步都要求精确绑定当前 state 和下一阶段的 pilot receipt；撤销必须绑定同一 manifest 的 revocation receipt 并进入 rolled-back。外部 transition 使用由 state、manifest、stage 和 authority receipt 确定性派生的 request digest，响应确认丢失后只能重放完全相同的请求；transition acknowledgement 与本地 CAS 均失败关闭。签名/摘要/目标 cell 替换、负迁移、不持久切换、陈旧 baseline、确认丢失重试和撤销回滚已有 7/7 定向测试。当前仍缺 Desktop/CLI 既有 marketplace host 的正式切换、真实 PKI/SBOM 构建器/target adapter/Eval authority、跨进程持久状态、公开 badge 与非安装量排名、部署级分阶段更新及撤销 E2E，因此本项只转为“部分完成”。

后续可信排名批次把 target cell 扩展为签名绑定的 `qualityScore`、`sampleCount` 和 safety hard gate；任何安装/适配在 target safety 未通过时即失败关闭。`GovernedSkillMarketplace.rank()` 会对每个 listing 重新执行签名、唯一目标 cell 和 target adapter 检查，再按目标 Eval `75%` 与有至少 20 个样本的真实 success/correction outcome `25%` 排序；样本不足只能取中性先验。listing 中的 install count 和模型自报 confidence 不进入 ranking schema、score 或 digest。百万安装量/满 confidence 的较弱 Skill 不能超过目标评测更强的 Skill，unsafe cell 和非法 outcome claim 均被拒绝；市场定向回归 9/9、ESLint 无错误。该批关闭仓库内“可信排名”控制缺口；公开 badge 页面、Desktop/CLI host、生产 PKI/SBOM/adapter/Eval authority、跨进程状态与部署撤销 E2E 仍未完成，因此保持部分完成。

### 7.6 EVO-P2-6：Wiki Pruning 与长时在线适应

建立 pattern 合并、重复检测、evidence decay、TTL、stale、tombstone、矛盾解析和压缩。审计证据保留，默认检索索引可清理过时知识。

只有离线闭环长期稳定后才研究单次长任务内适应；online change 不获得新增权限、不跨过 promotion gate、不跨租户，并在任务结束后重新进入离线验证。

实施进展：本批次新增 `GovernedWikiPruning`，直接消费既有 `EvidenceBackedWikiMaintainer` 的 canonical Wiki state、decay/stale/tombstone 和反向 evidence dependency，不另建第二套生命周期。确定性 plan 将超过 grace 的 stale pattern 转为 tombstone，从默认检索移除 stale/revoked/tombstoned 记录，但明确保留 Wiki revision、evolution log 和删除 receipt 审计；隐私删除必须由 tenant/source/artifact 精确绑定的认证 receipt 驱动，并先把受影响 pattern/Skill 处置为 tombstone 或 rollback，随后才允许外部 crypto-shred，最后发布新的检索投影。prepare、依赖处置、Wiki revision、crypto-shred、projection 与 finalize 均使用 plan/state-bound 确定性请求和 durable acknowledgement，陈旧 state、receipt 替换或未持久确认失败关闭。单次任务内 online adaptation 还必须持有同 tenant 的稳定离线闭环 receipt，保持 candidate-only、不可跨 promotion gate/tenant，requested capability 必须是 baseline 的子集。新控制器与既有 Maintainer 联合回归 22/22，ESLint 无错误。当前仍缺真实 ArtifactStore/KMS crypto-shred、Wiki Ledger/Skill rollback/检索索引 adapter、跨进程计划恢复、删除传播时限 E2E 和长期生产稳定性观测，因此本项仅转为“部分完成”，且不声明已启用在线自修改。

后续 Raw crypto-shred adapter 批次把抽象删除端口接到显式 KMS authority：privacy deletion receipt 现在必须额外精确绑定同 tenant 的 Raw artifact ref、cipher digest 与不可导出 key ref；`EvolutionRawCryptoShred` 在重算 pruning operation digest 后独立重验完整 deletion receipt，以确定性 destruction request 调用 KMS，要求 authenticated/durable destroyed receipt，再由独立确认端口证明同一 key 已不可用，最后持久保留只含 ciphertext lineage 和 receipt digest 的 deletion tombstone。任何跨租户 key、Raw/cipher/receipt 替换、销毁/确认/tombstone 未持久都失败关闭。真实组合测试证明 Wiki dependent disposition 先于 key destruction，随后才更新检索投影；Raw shredder、Pruning 与 Maintainer 联合 28/28、ESLint 无错误。该批关闭仓库内 KMS crypto-shred adapter 与调用顺序缺口，但生产 KMS/HSM、真实 deletion receipt/ArtifactStore tombstone Ledger、Skill rollback/检索 adapter、跨进程恢复和传播时限 E2E 仍需目标部署，因此 P0-4/P2-6 均保持部分完成。

后续 Raw deletion durable-ledger 批次把上述两个临时端口收敛到同一真实持久边界：新增 `EvolutionRawDeletionLedgerAdapter`，外部 privacy authority 签发的 receipt 必须精确绑定 tenant、evidence/source、trusted artifact、Raw ref、cipher digest、KMS key、签发时间和 trust policy，并在首次写入及每次解析时由构造期 verifier 重新认证。receipt 与 crypto-shred tombstone 分别作为新增的有限产品类型 `evolution-raw-deletion-receipt` / `evolution-raw-deletion-tombstone` 写入 `EvolutionArtifactPorts` 的 ledger retention，再以两个类型化 `EvolutionLedger` 事件串联；墓碑事件的唯一 source ref 必须指向对应 receipt artifact，任何 digest、Raw/cipher 或 lineage 替换均失败关闭。相同 receipt/tombstone 重试从 Ledger 恢复，不重复事件。真实 file-backend/witness 组合及两个独立 Node 进程的 shred→reopen 用例均通过，第二个进程重新验签并恢复同一墓碑，sequence 保持 2，整条验收在 60 秒内完成；adapter 3/3、process 1/1，连同 ArtifactPorts/Raw shredder/Pruning 回归共 42 通过、1 项按平台条件跳过，ESLint 无错误。该批关闭“真实 deletion receipt + ArtifactStore tombstone Ledger + 删除链跨进程恢复”的仓库缺口；生产 KMS/HSM/PKI 仍是目标部署门，Wiki/Skill/Memory/retrieval/Marketplace 全域传播的真实 adapter 级跨进程时限验收仍单独保留。

## 8. 90 天建议路线图

90 天目标应限定为“一个窄域、candidate-only 到受控 shadow 的纵切”，不是交付全部 P1/P2 平台。跨组织同步、市场治理、通用 Router 和长期在线适应均不进入本周期承诺。

### 阶段 A：第 1～2 周，冻结风险并校正真实性

- 扩展现有 canonical capability manifest 的 evolution 状态投影，并校正产品文案。
- 强制 `SkillImprover.skillsDir` 只能指向 candidate root，Synthesizer 改为 diff-only/candidate-only。
- 修复无 artifact 仍 created、self-improving 持久化失败仍 success、keyword fallback 冒充 Skill、metrics-only 模块命名问题。
- 修复 Phase 16 pipeline/metrics/workflow IPC 依赖注入与 Skill metrics 事件字段 contract，并用真实实例测试证明 `wired=true`。
- 建立当前所有 learning/evolution 入口、调用点和 writer inventory。

退出条件：没有未登记的 active Skill writer；所有正式入口的状态与 artifact 一致。

### 阶段 B：第 3～5 周，建立 Candidate 与安全门

- 从 recorded Skill lifecycle 与 Plugin activation transaction 抽取公共端口，补 content-addressed Skill candidate、统一 active pointer、last-known-good 和 rollback；代码/工作区候选先复用现有 worktree/checkpoint。
- 上线 Raw redaction、tenant scope、quarantine、名称/路径/schema/capability 校验。
- 建立 append-only evolution ledger 和 fail-closed receipt。
- 对目录逃逸、注入、秘密、跨租户、磁盘/数据库失败做负测。

退出条件：所有自动变更只能进入 candidate；故障注入零误晋级；60 秒回滚达标。

### 阶段 C：第 6～9 周，接入真实 Eval 与三层架构

- 复用 rollout/eval/record-replay，建立 replay、holdout、对抗和成本评测。
- 建立 Raw event projection、Wiki pattern/index/log/impact 和 `EvolutionRun` 状态机。
- 实现 Maintainer 与 Single-Skill Proposer 的 capability 隔离。
- 扩展 Skill metrics 为 `SkillInvocationReceipt`，统一 model-switch/fallback segment、failure taxonomy 和根预算归属。
- 用 `GoalConditionEngine + Eval + worktree/checkpoint` 跑通有界评分改进循环。
- 输出目标模型、工具、OS 和权限 profile 的兼容 scorecard。

退出条件：至少一个窄域 Skill 能完成 evidence→candidate→eval→reject/validate 全流程，且不修改 active。

### 阶段 D：第 10～13 周，Shadow Pilot 与统一接线

- 将真实 CLI/Desktop Agent events 接入 canonical adapter。
- 退役或降级重复 self-evolving 壳和未接线 learning hooks。
- 上线最小 reviewer surface、人工 review 与 shadow；只有样本功效和风险门满足时才进入显式低风险 cohort canary。
- 启动预注册 Pilot 观测窗口，不因演示节点或固定日期而放宽 P0 门禁。

退出条件：至少一个 Skill 在真实目标环境完成可审阅晋级和自动回滚演练；是否扩大范围由量化结果决定。

## 9. 建议验收标准

下列“零越权/零 secret/无审计不晋级”等安全性质是硬门；样本数、时延、百分比和观测窗口是 Pilot 默认值，落项时必须按风险等级与统计功效校准并写入版本化 policy，不能把示例数字当成跨域 SLA。

### 9.1 P0 发布门槛

| 类别   | 必须满足                                                                                                                                                                        |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 真实性 | artifact、数据库、UI、CLI 状态一致；未接线能力不得显示成功                                                                                                                      |
| 写入权 | active registry 只有 promotion controller 可写；其他路径 100% 阻止                                                                                                              |
| 安全   | 注入、secret、路径逃逸、symlink、跨租户、权限扩大负测全部通过                                                                                                                   |
| 审计   | 100% 自动晋级的 active 版本有完整 evidence、derivation mode、diff、eval、policy、actor 和 digest lineage；Wiki/record-replay 各保留真实来源链，legacy/imported 有来源与迁移标记 |
| 故障   | 100 次关键故障注入零误晋级；crash 后可确定恢复                                                                                                                                  |
| 回滚   | 60 秒内恢复上一版本及依赖 lock，字节级一致                                                                                                                                      |

### 9.2 P1 Definition of Done

- CLI、Desktop、Graph 对同一 `EvolutionRun` 使用相同 schema、状态和 digest。
- Raw 是 append-only event/artifact，重放结果确定；outcome 和合成状态不修改原始证据。
- Wiki 至少支持 pattern、index、evolution log、skill impact、矛盾、过期和撤销。
- 每个 candidate 默认只改一个 Skill，并有 PURPOSE/evidence、权限 diff 和 target runtime；显式 dependency transaction group 必须全成或全败。
- Proposer 无 validation/test 答案和 active write 权；Evaluator 无 proposal write 权。
- 每个可晋级 Skill 有 replay、holdout、对抗、安全、成本和兼容 scorecard。
- 每次 Skill exposure/execution 有可 join 的 InvocationReceipt；模型、工具、permission/policy 变化产生新 segment 并失效相关批准/Eval cache。
- raw evidence、model-visible projection 与 trusted learning projection 有独立 digest、ACL、转换 receipt 和删除传播。
- 有界改进循环把所有子 Agent 消耗计入根预算，并能区分 procedure 与 infrastructure/permission/provider failure。
- shadow/canary 回归越界时自动回滚，且不会切换正在运行会话的 Skill digest。

### 9.3 P2 Definition of Done

- Workbench 可从任意 active Skill 回溯完整演化时间线并一键回滚。
- Router 在真实基准达到 Recall@5、误调用率、延迟和上下文预算目标。
- 跨设备/团队同步通过 RBAC、加密、冲突和隐私删除测试。
- 市场 Skill 有签名、lock、权限、兼容和可复现 Eval 信息。
- Pilot 在成功率、成本、修订率、误晋级、回滚和安全上达到预注册样本量与门槛后才扩域。

## 10. 不建议照搬的部分

1. **不要把 WikiSkill 写成 Google 已发布产品。** 它当前是预印本研究框架；论文未链接官方实现，也未报告生产安全验证。
2. **不要把“自进化”写成模型权重训练。** 本报告讨论的是外部经验、知识和 Skill 的受控演化。
3. **不要照搬全量 Skill 注入。** 论文为排除触发误差而这样设计，生产规模下会遇到上下文、冲突和错误触发问题。
4. **不要机械照搬严格 `score > best`。** 小验证集有噪声，中性基础变更也可能为后续迭代铺路，应允许 needs-more-evidence 分支。
5. **不要让执行 Agent 同时当提案者和裁判。** 会形成自证、评测污染、权限混合和难以归因的问题。
6. **不要把 Memory 当 Policy。** 自动摘要或个人偏好不能覆盖 AGENTS、组织规则、权限和合规要求。
7. **不要把多 Agent 并行当作组织学习。** 没有 evidence ledger、scope、冲突和晋级门，多代理只会更快地产生不一致。
8. **不要自动启用生成的 Skill、Hook 或 Plugin。** 热加载能力越强，candidate/active 隔离和版本固定越重要。
9. **不要让 Wiki 永久只增不减。** 生产系统必须支持 stale、merge、tombstone、revocation 和隐私删除传播。
10. **不要重建已有 Manifest、Goal/Budget、Worktree/Checkpoint、Eval、Rollout、Memory、Record & Replay 和 Skill Runtime。** 应通过 canonical ports 收敛，而不是再复制一套。
11. **不要把所有失败都归因给 Skill。** MCP/provider transient、权限拒绝、sandbox/依赖缺失与程序步骤错误必须分开，否则自进化会固化环境噪声。
12. **不要跨模型段做伪因果归因。** model switch/fallback 后应产生新 trajectory segment；没有 InvocationReceipt 时不启用 outcome-aware reranking。

## 11. 审计范围与限制

### 11.1 审计口径

- 外部事实仅使用一手来源：WikiSkill 原论文、Anthropic Claude Code 官方文档/官方仓库、OpenAI Codex 官方文档。
- “官方未描述”表示在本次所列官方页面中没有发现该完整机制，不等于证明厂商内部绝对不存在。
- 本仓库判断基于基线提交的静态源码审计、入口与引用扫描；“未找到生产调用点”不排除运行时字符串、动态加载或外部集成，但当前仓库没有给出可验证证据。
- 审计期间工作区存在与本报告无关的 graph/CI 未提交改动；本报告未修改或依赖这些文件，所引用的 learning/Skill/runtime 事实均按上述 HEAD 基线核对。
- 本次没有启动真实 provider、执行 E2E、修改 Skill 或验证运行时性能；建议优先用 P0 inventory 和 contract tests 复核静态结论。
- WikiSkill 是 2026-08-27 的 v1 预印本，结论可能随论文修订、代码发布和更大规模实验变化。

### 11.2 主要外部来源

- [WikiSkill: Compiling Agent Experience into Persistent Knowledge for Skill Evolution](https://arxiv.org/html/2608.27454)
- [Claude Code changelog](https://code.claude.com/docs/en/changelog)
- [Claude Code memory](https://code.claude.com/docs/en/memory)
- [Claude Code goals](https://code.claude.com/docs/en/goal)
- [Claude Code subagents](https://code.claude.com/docs/en/subagents)
- [Claude Code agent teams](https://code.claude.com/docs/en/agent-teams)
- [Claude Code workflows](https://code.claude.com/docs/en/workflows)
- [Claude Code hooks](https://code.claude.com/docs/en/hooks)
- [Claude Code skills：evaluate and iterate](https://code.claude.com/docs/en/skills)
- [Claude Code plugins reference](https://code.claude.com/docs/en/plugins-reference)
- [OpenAI Codex changelog](https://learn.chatgpt.com/docs/changelog)
- [OpenAI Codex Memories](https://learn.chatgpt.com/docs/customization/memories)
- [OpenAI Codex Computer History](https://learn.chatgpt.com/docs/customization/computer-history)
- [OpenAI Codex Record & Replay](https://learn.chatgpt.com/docs/extend/record-and-replay)
- [OpenAI Codex Build Skills](https://learn.chatgpt.com/docs/build-skills)
- [OpenAI Codex：Iterate on difficult problems](https://learn.chatgpt.com/use-cases/iterate-on-difficult-problems)
- [OpenAI Codex Hooks](https://learn.chatgpt.com/docs/hooks)
- [OpenAI Codex Subagents](https://learn.chatgpt.com/docs/agent-configuration/subagents)
- [OpenAI Codex configuration reference](https://learn.chatgpt.com/docs/config-file/config-reference)

## 12. 最终建议

ChainlessChain 不缺“会记、会反思、会写 Skill”的功能点，真正缺的是把这些功能变成可信闭环的演化控制平面。

建议把近期资源集中到两个阻断式 P0：

1. **所有自动变更 candidate-only，独立评测后才能由单写者原子晋级并可回滚。**
2. **Raw→model-visible/trusted projection→Wiki→Skill 全链路建立不可信数据、权限、秘密、路径和审计边界。**

最短 P1 路径是：扩展已有 `CAPABILITY_MANIFEST` 并修复 metrics 接线 → 把 recorded Skill lifecycle 与 Plugin activation transaction 抽成通用 Registry 端口 → 用 `GoalConditionEngine + Eval + worktree/checkpoint` 跑通有界 candidate-only 改进循环 → 补 `SkillInvocationReceipt`、模型分段和失败分类 → 再接 Wiki Maintainer。这个顺序能先验证 ROI 和控制协议，也最大限度复用现有实现。

完成这些前提后，再以现有 Rollout、Memory、Record & Replay、Skill Runtime 和 Desktop Phase 20 为 adapter 扩展到受控生产 Pilot，而不是继续新增平行的“自进化”模块。这样才能把 WikiSkill 的研究启发、Claude Code 的评测/扩展载体和 Codex 的安全/编排基础，转化为 ChainlessChain 可验证、可维护、可规模化的产品能力。

## 13. 2026-09-04 增量复审：两份材料的取舍与追加任务

本节把两份材料中可借鉴的部分转换为现有路线的执行包，不另造一套“自进化”系统。`EVO-OPT-*` 是对前述 20 个主路线项剩余工作的拆解，不计入 §14 的主路线总数；完成状态必须回填到所映射的原任务，不能用执行包数量替代产品完成度。

### 13.1 可借鉴部分与必要校正

| 材料中的主张                               | 可借鉴部分                                                                       | 当前仓库基础                                                                                                                                                                                                                                                                                                                                                                                       | 必要校正与优化方向                                                                                                                                                                                                          |
| ------------------------------------------ | -------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 从“训练权重”转向“演化软件资产”             | 将经验编译成内容寻址、可审查、可签名、可回滚的外部制品                           | Candidate/Release Registry、ArtifactPorts、Ledger、Workbench 已形成较完整治理原语                                                                                                                                                                                                                                                                                                                  | 应表述为“变更面可版本化、可审计、可失败关闭”，不能宣称 Agent 行为、第三方工具和外部副作用“绝对可控”；Skill 的核心是 `SKILL.md`，还可包含 `scripts/assets/references`，不等同于“Python 代码”                                 |
| Mutation：从失败与反思生成新 Skill         | 失败聚类、成功策略和用户纠正可触发单 Skill、candidate-only 提案                  | `WikiInformedSkillProposer`、`BoundedSkillImprovementPilot` 与 Candidate Registry 已具备协议和测试                                                                                                                                                                                                                                                                                                 | 不能让一次失败直接改 Skill；先排除 provider/MCP/权限/基础设施噪声，再要求重复证据、冷却与去重。公共入口和默认生产构造点尚未接通，自动 active mutation 继续 HOLD                                                             |
| Selection：隔离环境运行历史测试            | 独立 deterministic grader、隐藏 holdout、安全 hard gate 和目标运行时矩阵值得采用 | `EvolutionEvalGate` 与 `SkillTargetMatrixEval` 已实现签名计划、全 cell 合取、deadline 和 receipt；签名 loader 与 process supervisor 已接入完整 Gate hard-kill 路径                                                                                                                                                                                                                                 | “确定性”只覆盖可确定判分的部分；模型 evaluator、真实 UI 和外部服务仍是统计/环境相关证据。目标部署仍缺真实跨平台 runner/sandbox、资源与越权攻击验收和版本化 regression corpus，不能写成“已运行全部历史测试”                  |
| Promotion：先小流量 Canary，再晋级或回滚   | Shadow、稳定分桶、预注册观测窗、渐进扩量、kill switch 和 LKG 回滚应成为强制路径  | `ControlledSkillProductionPilot` 已有 candidate→shadow→canary→active/rolled-back、人工批准、指标门和恢复协议；traffic worker 已闭环服务端 assignment、paired runner receipt、durable exclusive reservation/observation/readback 与 gate 输入；独立 watchdog 已闭环 plan-pinned Ed25519、durable latest heartbeat、带租约事故抢占、真实跨进程 kill→LKG rollback、崩溃接管和 fresh-instance recovery | 仍未把 worker 部署到目标租户真实流量入口，也未把 watchdog/store 部署到目标独立故障域并完成目标基础设施故障演练。百分比必须由风险、流量和统计功效决定；低流量用固定 N 个显式 cohort，高流量才使用 `1→5→25→100%` 等预注册阶梯 |
| Stabilization：晋升后写入 Wiki，成为新基线 | Wiki 应持久记录 proposal、评测、生产结果和回滚影响，供后续提案复用               | Maintainer 已有 `hypothesis/corroborated/contradicted/stale/revoked/tombstoned`、CAS、证据 lineage 和持久 merge                                                                                                                                                                                                                                                                                    | 晋升只能产生一条新 evidence，不能自动成为“真理”；必须经过独立结果、观测窗口和多来源佐证才转为 `corroborated/actionable`。active/LKG 仍由 Release Registry 管理，Wiki 不取得 active writer 权                                |
| Skill、Prompt、知识和 Hook 都能演化        | 共用不可变制品、证据、评测、审阅、激活和回滚协议可以减少平行系统                 | Skill 与 governed knowledge 最成熟；Desktop `PromptOptimizer` 和 Hooks 仍是独立能力面                                                                                                                                                                                                                                                                                                              | 抽取最小 `EvolvableArtifact` 协议，但保留类型专属策略；Prompt 需防数据泄漏，Hook/脚本属于高风险可执行制品，必须使用更严的 capability、签名、SBOM、沙箱和人工 quorum，不能因共用协议降低门槛                                 |

建议目标状态机在现有组件外增加一层薄编排，不重写 Registry、Eval、Pilot 或 Wiki：

```text
NEEDS_EVIDENCE → CANDIDATE → PRECHECK → EVALUATING → VALIDATED
→ REVIEW_PENDING → SHADOW → CANARY[n] → ACTIVE_PROBATION → STABLE

异常/终止分支包括（合法来源状态由 transition policy 约束）：
REJECTED | QUARANTINED | ROLLED_BACK | RECONCILIATION_REQUIRED
```

`RECONCILIATION_REQUIRED` 是等待幂等恢复的非成功状态；新版本进入 `ACTIVE_PROBATION` 后仍可快速回到上一 LKG，只有通过稳定观察窗的 `STABLE` 才能成为后续演化基线。一个签名 `EvolutionPlan` 必须一次性绑定 tenant、Skill、baseline release、candidate、Wiki revision、Eval suite、target matrix、risk tier、rollout/metric policy、permission/policy digest、预算和有效期，防止各控制器分别正确但使用了不同基线或策略。

### 13.2 可执行增量任务

2026-09-05 补充：`SkillRegistryTransitionLedgerAdapter` 现在可从已重新认证的 settlement、release/state readback 与 candidate lineage 生成 tenant/stream scoped 的 branded reconciliation source；`SkillPromotionReviewLedgerAdapter` 也会在重新验证签名、packet 与 Ledger lineage 后导出 rejected 决定；`ControlledSkillProductionPilot` 只在无 pending transition 且最终状态已持久确认时导出统计 Canary stable 或 kill-switch rollback outcome。`SkillWikiReconciler` 将这些事件解析为 digest-bound `proposal-decision` evidence，并以确定性 maintenance request 写入 Wiki：Active/stable 增加 accepted impact，reject 增加 rejection count，rollback 还将关联 pattern 置为 stale 并在同一 revision 从 Wiki retrieval index 移除。`SkillRevocationPropagation` 进一步要求独立 resolver 对 Wiki pattern、Memory、retrieval index、marketplace badge 四域作完整性声明，并以确定性 operation ID 固定执行 stale、quarantine、invalidate、revoke；四类结果全部返回 tenant/digest-bound durable+idempotent receipt 后才推进 checkpoint，进程在 effects 后退出或 checkpoint 回执丢失可安全重放。Marketplace 真实 adapter 已将旧 state、传播 request 与 rolled-back state 绑定；Structured Memory 新增仅凭持久 revocation receipt 才可执行的 procedural quarantine，CLI adapter 支持 Ledger append-response-loss 与进程重开恢复。新增外部 `SkillRetrievalRevocationAuthority`：以 tenant/Skill/content digest 固定 retrieval-index invalidation，使用持久 load/commit、CAS state digest、精确回读和响应丢失恢复，向检索面只暴露 branded 同步 reader；撤销检查在 BM25、向量与 outcome 评分前执行，并已贯穿正式 `cc skill search`、交互/单轮/流式 Agent、legacy WebSocket、App Server/Graph 与 Desktop `skills:route` 组合链。独立 `IndependentSkillRevocationSource` 进一步读取有界签名撤销记录，先重算 record digest，再要求外部 verifier 返回 tenant/stream/sequence/record-digest 精确绑定的 authenticated+durable receipt；验证后的 `revoke` 作为独立 reconciliation source 同时进入 Wiki rejected/stale 处理和四域反向传播，不依赖 Pilot rollback。最终跨进程验收使用真实 Node 子进程与磁盘持久状态，分别在 Wiki commit、四域 effect commit、propagation checkpoint commit 后立即退出，新进程均幂等恢复，四类副作用 `applyCount` 保持 1；联合既有 release pointer commit 后强退恢复，三处要求边界及 60 秒传播门联合 45/45。检索/入口回归 233/233、Desktop 9/9，Session Core 既有全套 556/556。至此 EVO-OPT-5 的仓库内交付与验收项完成；目标部署仍须按 EVO-OPT-7 提供生产 authority 并执行运营演练。

| ID        | 优先级 / 映射                       | 任务                                                               | 关键交付与验收标准                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 当前状态                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --------- | ----------------------------------- | ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| EVO-OPT-1 | P0；EVO-P0-3、EVO-P2-5              | 建立可复现 WikiSkill/ChainlessChain Benchmark 与文案 truth gate    | 固定 model checkpoint/digest、推理参数/seed、容器/vLLM/硬件、五数据集版本与逐题 split ID、工具/API、prompt、Skill/Wiki 和 runner digest；同环境运行 no-skill/skill 对照且至少 3 个独立 run；保存逐题结果、trace、grader receipt、失败分类、成本与时延；机器重算五集等权平均、delta、95% CI 和 1,000 次 paired bootstrap；报告绑定 Git SHA、runner SHA 与 dataset digest，可从零复跑。官网/README 只能从签名不可变报告生成，在此之前统一标记 `external-paper-only / HOLD` | 🟡 部分完成；canonical plan v2/report/signature envelope、五数据集/三 seed、等权统计与 1,000 次 bootstrap、公开文案 HOLD 门已落地；execution manifest 固定 Dataset Provider、Target Runner、独立 Grader、Report Attestor 和目标环境，production host 对每个 paired arm 逐级验证 authenticated+durable receipt，禁止 runner 自报 grader 分数。分块 Ledger adapter 以四种有限制品和真实文件 witness 持久化并重新认证完整 bundle；branded CLI Host 将 production execution→签名→commit→readback 收敛为唯一 `run`，正式 `cc evolution benchmark run/show` 通过 Ed25519 deployment loader 的七个内建品牌工厂取得目标 provider，缺 Host 在文件读取前失败关闭；相关 53 passed/1 条件 skip。真实五数据集、目标模型/容器、生产 grader/PKI provider 和至少三次完整目标环境运行仍待执行，公开 claim 继续 `HOLD`                                                                                                                                                                     |
| EVO-OPT-2 | P1；EVO-P1-1、EVO-P1-6、EVO-P1-9    | 增加统一 `EvolutionReleaseTrain` 编排与候选触发器                  | 将 `EvolutionRun → Wiki Maintainer → Proposer → Candidate → Eval → Review → Pilot → Promotion → Wiki impact` 串为同一 `EvolutionPlan`；只在可归因的重复 procedure failure、成功模式或用户纠正达到最小证据门后触发，排除 provider/MCP/sandbox/权限噪声；一次只改一个 Skill、默认不增权、全子任务归集根预算。验收要求任意步骤重试/重启幂等，两个新进程恢复同一 digest lineage，缺任一 receipt 时 active 字节不变                                                           | ✅ 完成；canonical plan v2、归因触发器、无提权/根预算门、八阶段 digest lineage、ArtifactStore+Ledger checkpoint、响应丢失恢复和竞争 CAS 已落地；默认 production domain composition 由同一 ArtifactStore、Ledger 与独立 witness 构造持久端口。最终真实 OS 纵切按 `Wiki Maintainer → Proposer → Candidate → 双 cell matrix Eval → durable Review → signed progressive Pilot → evaluated-only Promotion/磁盘 ReleaseRegistry → Wiki impact` 执行，matrix plan 先进入统一计划，所有下游只消费已持久的真实上游收据。进程 A 在 Review 副作用落盘但 checkpoint 前硬退出，进程 B 恢复同一 lineage 并完成 active CAS 与 Wiki checkpoint，最终为 8 个唯一阶段副作用、16 个账本事件；整组进程 3/3、相关定向 10 passed/1 条件 skip。release→Wiki→下一轮 proposal 的双向 lineage 另有真实 reconciler/proposer 20/20 证据。仓库内交付与验收已闭环；生产 PKI、runner、traffic authority 与跨主机演练归 EVO-OPT-7                                                                        |
| EVO-OPT-3 | P0；EVO-P0-3、EVO-P0-4              | 建设真实目标环境 Eval 执行面                                       | 提供 attested descriptor-to-callable loader、固定依赖环境、版本化 regression corpus、hidden holdout、独立 grader/safety/verifier 和 OS 进程或容器级 hard kill；覆盖 Linux/Windows/macOS 目标 cell。验收包含网络/文件越权、hang、迟到进程、grader crash、split 泄漏、依赖漂移和 receipt 替换，任一异常都失败关闭且 candidate 永不 active                                                                                                                                  | 🟡 部分完成；既有 Gate 已覆盖 split-blind train/validation/hidden-test、防泄漏、独立 grader/safety/verifier、deadline/AbortSignal、隔离 target 与 signed matrix 合取；Benchmark production host 又将 Dataset/Runner/Grader/Report Attestor 分权，并由 Ed25519 exact-byte deployment loader 强制 descriptor handler digest 等于已认证 callable module digest，定向 10/10。Process Supervisor 已接入正式 Gate，在不同 PID/空环境/无 shell/有界 stdio中执行，deadline `SIGKILL` 后等待 close；Node permission mode 默认拒绝文件、派生进程、Worker、原生扩展和 WASI，显式文件路径与 32–1024 MiB heap 有界。invocation/revocation evidence 与 Matrix 完整 Gate child receipt 现经 authority-root-bound branded ArtifactStore+真实 Ledger/witness 持久、fresh readback、重开解析；未品牌化 store、非 durable 确认、替换与 descriptor 漂移失败关闭，相关去重 183 passed/2 平台条件 skip。仍需真实 Linux/Windows/macOS cell、平台 sandbox 网络与 OS 级资源攻击及真实目标部署演练 |
| EVO-OPT-4 | P0/P2；EVO-P1-5、EVO-P2-1           | 让 Pilot 成为 promotion 必经门，并补统计化渐进 Canary              | 建立服务端稳定 hash 分桶与签名 assignment receipt，客户端不能自报 cohort；按 risk tier 使用 shadow→有界 Canary 阶梯→`ACTIVE_PROBATION`→`STABLE`，不硬编码 1%；同期 baseline/candidate 对照，质量使用 paired delta 置信下界或预注册序贯检验，成本、p95/p99 时延和 tool calls 做非劣门，任何 security/permission event 立即停止。外部 watchdog 在主宿主失联时仍能 kill/rollback；重复查看中间结果不能改变 alpha 或门槛                                                     | 🟡 部分完成；risk-tier v2 计划、稳定签名分桶、paired bootstrap 置信下界、成本/工具/p95/p99 非劣门和 probation→stable 投影已落地；独立 gate authority 对完整报告签名，Pilot v2 只按回执推进并复用 durable prepare/finalize/reconciliation；traffic worker 已服务端分流、验证绑定预注册 handler identity 的 paired runner receipt，以 durable exclusive reservation 防重复执行，提交观测后 fresh readback 并直接供 gate 使用；独立 watchdog 从 durable latest source 验证签名租约，只监控非终态，超时后事故抢占并严格执行外部 kill→LKG rollback、持久提交/readback/recovery，并发只发一次 effect；v2 plan 固定 Ed25519 SPKI digest，Node adapter 实现宿主私钥签发/watchdog 公钥验证和独立 rollback-key effect receipt；物理文件 store 与真实不同 PID fault drill 已覆盖 kill 后 watchdog 崩溃、租约接管、LKG readback 和 fresh-process incident recovery；联合 42/42；仍需部署到目标真实流量入口和独立故障域，完成目标观测窗、基础设施故障与扩量验收                       |
| EVO-OPT-5 | P1/P2；EVO-P1-2、EVO-P2-6           | 完成 Promotion/Observation→Wiki reconciliation 与回滚反向传播      | Active/Stable、reject、rollback、revoke 分别追加 proposal-decision 和 outcome evidence；Maintainer 再按 trust domain、样本和时间窗决定 hypothesis→corroborated，禁止单次成功直升 truth。Skill 回滚/撤销必须使依赖 pattern、Memory、retrieval index 和 marketplace badge 降级、stale、quarantine 或重评测。在 release commit、Wiki commit、settlement 三处强杀后，新进程应幂等收敛且不重复副作用                                                                          | ✅ 完成；Active/reject/stable/rollback/revoke 自动形成 Wiki impact，rollback/revoke 使 pattern stale 并移出 Wiki index；四域完整解析、幂等执行/checkpoint、Marketplace revoke、procedural Memory quarantine、持久 retrieval invalidation、独立签名 revoke source 及 CLI/Agent/App Server/Graph/WebSocket/Desktop 检索失效均已落地；真实子进程在 Wiki/effect/checkpoint commit 后强退并由新进程恢复，联合既有 release pointer 强退用例验证三处边界、零重复副作用与 60 秒门，联合 45/45，相关入口回归 233/233、Desktop 9/9、Session Core 既有全套 556/556                                                                                                                                                                                                                                                                                                                                                                                                                  |
| EVO-OPT-6 | P2；EVO-P1-5、EVO-P2-4、EVO-P2-5    | 抽取 Skill/Prompt/Hook/Knowledge 的类型化 `EvolvableArtifact` 协议 | 共用 tenant/type/content digest、parent、lineage、dependency lock、runtime/permission manifest、candidate/release、Eval/review/promotion receipt 与 active/LKG；每种类型注册独立 admission、evaluator、activation 和 rollback policy。Hook/脚本强制签名、SBOM、沙箱、网络出口策略和双人高风险审批；Knowledge 合并不获得 Skill active writer。合同测试证明任何旧 PromptOptimizer、Hook 或同步入口都不能绕过 candidate gate，依赖变化能级联 stale/revalidation             | 🟡 部分完成；共享 schema/policy/authority、两阶段 release、级联 stale→branded persistent revalidation、Ledger-backed candidate/dependency/active-content reader、通用 ArtifactStore+Ledger adapter 与单一 Knowledge lifecycle 已落地；四类 candidate 与 Knowledge 完整 lifecycle 已统一；Hook/Prompt/Skill 产品读取均只消费 branded active reader，签名 Desktop composition 已注入三类 lifecycle producer，晋级与 revalidation 只消费部署 authority 的 durable 决定并做 transition/active readback；Governed Hook/Skill 均从签名内存字节进入内容寻址隔离 worker，旧 Hook 配置/command/path、Prompt `is_active`、Skill 外部目录/marketplace hot-load 均不再具有 active authority；仓库内缺口已关闭，仍需目标环境提供真实 KMS/PKI/数据库及 human/Eval/dependency authority 并完成签名部署、恢复演练和验收                                                                                                                                                                  |
| EVO-OPT-7 | P0/P2；EVO-P0-5、EVO-P1-6、EVO-P2-2 | 交付真实 deployment composition 与运营面                           | 为公开 npm lazy/eager 入口装载 branded Workbench/Evolution host，接入生产 KMS/HSM、PKI/身份/policy、独立 witness、scheduler、runner、traffic authority、metrics source、备份与灾备；缺任一 authority 时明确 unavailable，不回退测试密钥、内存 store 或客户端配置。验收包含两机重启/断网/响应丢失、密钥轮换撤销、witness 故障、kill-switch 演练和回滚 MTTR；Workbench 显示阶段、样本覆盖、置信区间、阻断原因、active/LKG、reconciliation backlog 与证据链                 | 🟡 部分完成；branded seam、多端 Workbench、Ledger/恢复原语及公开 npm lazy/eager 的 Ed25519 descriptor + exact-byte deployment module loader 已落地；目标部署仍须提供真实 KMS/PKI/identity/witness/runner/traffic/metrics authority 并完成灾备演练                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

### 13.3 新增执行包的统一硬门

- **真实性**：`100%` active/stable release 可反查 candidate、Eval、human review、Pilot、promotion 和 Ledger receipt；外部论文结果与本项目实测永远分栏。
- **安全性**：security/permission violation 必须为 `0`，任何一个即停止扩量；未授权 active writer 命中率必须为 `0`。
- **质量**：使用同期 paired baseline、置信下界和预注册多重比较/序贯规则，不以一次平均分或 LLM 自评分晋级。
- **效率**：同时约束 token、成本、tool-call 数、p50/p95/p99 latency 与每个 `STABLE` 晋升的总 Eval/人工审核成本。
- **运营**：记录 MTTD、rollback MTTR、`RECONCILIATION_REQUIRED` backlog/age、Canary assignment 漂移和外部 watchdog 可用性。
- **知识健康**：记录 contradiction backlog/age、stale ratio、独立 trust-domain 覆盖率、回滚后依赖失效传播时延和隐私删除传播时限。

实施顺序建议为：先完成 `EVO-OPT-1` 的 truth gate，并并行推进 `EVO-OPT-2/3`；随后以一个低副作用、可确定判分的单 Skill 串通 `EVO-OPT-4/5/7`；该纵切稳定后再做 `EVO-OPT-6`，最后才讨论默认无人值守候选生成或扩大资产类型。直到目标部署通过上述硬门，production auto-promotion 保持 HOLD。

## 14. 全量任务完成情况（截至 2026-09-04）

状态口径：`✅ 已完成` 表示该编号自己的代码、确定性验证及应有生产发布边界已经全部关闭；`🟢 仓库闭环` 表示仓库实现、确定性验证和该编号自身可在仓库内完成的边界已经关闭，只剩由其他统一路线验收的生产切换；`🟡 部分完成` 表示核心底座或可复用原语已经落地，但该编号定义的生产接线、跨进程实现或外部验收尚未全部完成；`⏳ 待完成` 表示目前主要只有依赖、设计或已有系统能力可复用，关键目标尚未形成可验收纵切。

总计 20 项：**6 项已完成、4 项仓库闭环、10 项部分完成、0 项待完成**。计入本次 Raw deletion durable-ledger 后共有 **135 个基础批次**。这两个计数维度不能混用：基础提交数量不代表路线项完成数量。

| 优先级 | 编号     | 任务                                          | 状态        | 已完成与验证证据                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 剩余工作                                                                                                                                                                                                                                                   |
| ------ | -------- | --------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P0     | EVO-P0-1 | 能力真实性与 mutation freeze                  | ✅ 已完成   | canonical manifest fail-closed、CLI learning/evolution truth help、Desktop TechLearning phantom-success 移除、Phase 20 metrics 接线、旧 Phase 100 simulator/未注册 IPC 正式退役、六个 learning/proposer/import writer candidate-only、mutation authority 拒绝审计及产品文档一致性均已关闭；详见 §1.1、§5.1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 无；通用 Agent/插件 legacy writer 的单写者迁移属于 EVO-P1-5                                                                                                                                                                                                |
| P0     | EVO-P0-2 | Candidate、Promotion 与 Rollback 事务         | ✅ 已完成   | immutable tenant candidate/release、mutation authority、execution manifest、lease/fence、prepare/finalize ledger、active CAS、journal recovery、session pin、LKG+dependency lock rollback 已闭环；五阶段 crash/真实进程重启、100 并发单赢家和 `<60s` 字节级回滚均通过，核心 3 文件 70/70；提交 `23125c71e4`；详见 §1.1、§5.2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 无；最终用户 shadow/canary 与唯一 production writer wiring 归 EVO-P1-5，真实 Eval/PKI authority 归 EVO-P0-3                                                                                                                                                |
| P0     | EVO-P0-3 | 独立真实 Eval Gate                            | 🟡 部分完成 | 独立监督 Gate、train/validation/test 防泄漏与 split-blind/holdout provenance、paired validation/hidden test/safety hard gate、signed target matrix 全 cell 合取与 Bonferroni 双侧 family-wise confidence 校正、accepted receipt→promotion 窄绑定、typed resolver、evaluated-only controller、attested/bounded/fresh/rotatable/revocation-aware durability adapter、进程 hard-kill、target evidence 与完整 Matrix child receipt 的 ArtifactStore+Ledger resolver 及不暴露 direct promotion/底层 controller 的窄 control-plane facade 已提交；详见 §1.1、§5.3                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 接入真实跨平台 grader/runner 与生产隔离部署；完成更广分布级 anti-gaming、生产受限 control plane 构造接线、跨进程 authority/生产 PKI、密钥撤销演练及目标平台网络/OS 资源攻击验收                                                                            |
| P0     | EVO-P0-4 | Raw、入模投影与 Skill 编译安全边界            | 🟡 部分完成 | attested Raw/model/trusted 持久投影、ciphertext-only ArtifactStore、secret/PII + injection/quarantine、derivation manifest、Candidate strict schema/name/size/path/symlink/plaintext guard、capability diff、Unicode risk、1,000 条合成对抗候选和 signed human quorum 已落地；CLI/Graph/legacy WS durable ingress 已纵切；Raw crypto-shred adapter 要求 KMS durable destroy→独立确认；真实 deletion receipt 与 tombstone 已分别进入有限 ArtifactStore ledger-retention 类型和 EvolutionLedger source lineage，跨实例/跨进程恢复 sequence 保持 2；新增 4/4，相关联合 42 通过、1 项按平台条件跳过；详见 §1.1、§5.4、§7.6 | Desktop 默认 launcher/其他最终入口注入生产 KMS/HSM/PKI/policy/witness；完成 Wiki/Skill/Memory/retrieval/Marketplace 全域 60 秒撤销传播 E2E；更广语义分类和生产流量误报校准 |
| P0     | EVO-P0-5 | Fail-closed 证据与审计                        | 🟡 部分完成 | append-only tamper-evident ledger、typed domain events、subject-bound transition、认证制品解析与持久账本组合端口、signed identity/event/anchor/witness/artifact 自包含离线导出与无在线 store 独立验证、重新认证的增量 prefix、私有 O(1) event 索引、有界 `queryMany()`、witness-bound 持久 state snapshot、single-current retention、durable-audited v1→tenant-v2 Candidate 单制品/只读批 store migration、v3→tenant-v4 inactive Release migration、state v2 exact mapping plan 及真实 ledger baseline/current projection/active pointer 提交及 legacy journal 验证/认证处置/文件退休及 state migration journal/启动自动 reconciliation 与迁移四阶段 fault matrix 已落地；提交 `27b7e267e9`、`00686ceda8`、`9c77318e59`、`d6c645aefd`、`c96b6da007`、`2c11930ef5`、`383e35a598`、`ccf52dfe14`、`065f9d277f`、`b0d9769675`、`65b71c36b9`、`6055b210b6`、`76906bc88f`、`07b0e8cd10`；详见 §1.1、§5.5                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 真实跨进程持久 authority、生产量级/资源上限、长账本/authority/witness 系统故障注入及生产 wiring                                                                                                                                                            |
| P1     | EVO-P1-1 | Canonical Raw/Wiki/Skill 架构                 | ✅ 已完成   | shared `EvolutionRun` 统一 append-only Raw/Wiki/Registry/Eval projection；CLI/Desktop/Graph 三端调用同一 projector；duplicate/order/crash/compaction/tombstone/direct record-replay 确定性闭环，7/7 通过；详见 §1.1、§6.1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 无；生产 ingress 与旧壳迁移归 P1-6，跨进程持久 authority 归 P0-5                                                                                                                                                                                           |
| P1     | EVO-P1-2 | Evidence-backed Wiki Maintainer               | 🟢 仓库闭环 | trusted/digest-bound Maintainer 与生命周期 reducer；`wiki-revision` ArtifactPorts ledger retention、branded resolver、不可变 revision→Ledger event、head CAS、响应丢失幂等及真实 Ledger 文件/witness 新实例恢复已闭环；13/13 + 6/6 + 25/25 + 45/45 通过；详见 §1.1、§6.2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 本项无仓库内剩余；生产 ingress、通用迁移、敏感 evidence 删除分别归 P1-6、P0-5、P0-4/P2-6                                                                                                                                                                   |
| P1     | EVO-P1-3 | Wiki-informed Single-Skill Proposer           | ✅ 已完成   | 固定最小 Wiki/impact/active/training 读取；矛盾/样本不足 abstain；pattern/raw 选择性读取；单 Skill PURPOSE、digest lineage、适用/禁用边界、反例、回退、验证、capability/runtime/context cost、safe machine diff 与 exact candidate binding 已闭环；1 文件 10/10 通过；详见 §1.1、§6.3                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 无；统一真实 Wiki authority 与 Maintainer 分别归 P1-1/P1-2                                                                                                                                                                                                 |
| P1     | EVO-P1-4 | 目标运行时 Eval 与负迁移检测                  | 🟡 部分完成 | train/validation/test 隔离、paired/hidden gate、dependency/runtime/target matrix 数据模型、独立 receipt 验证、全 cell 合取与 Bonferroni family-wise confidence 校正、evaluated promotion 与 durability/revocation 组合原语、进程 supervisor、child target evidence 及完整 Matrix Gate receipt 的 ArtifactStore+Ledger resolver 已落地；详见 §1.1、§6.4                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 接入真实跨平台 runtime/grader、生产隔离部署和目标 Matrix 演练、shadow/canary，以及真实跨进程 receipt authority/生产 PKI                                                                                                                                    |
| P1     | EVO-P1-5 | Registry、生命周期与单写者治理                | 🟢 仓库闭环 | content-addressed tenant candidate/release、lease/CAS/journal/recovery；Agent-root facade 强制 matrix+human review 且不暴露 direct promotion；review packet 已含 evidence/diff/permission/Eval/runtime/content-risk；approve/reject ArtifactStore+Ledger authority、真实文件/witness 重开、provider resolver 与 signed non-automated quorum/风险摘要确认 active gate 已落地；正式 CLI 与 Desktop reviewer surface 均经固定 App Server 方法消费同一 branded host，支持持久 packet list/compare/approve/reject/rollback，并展示 active/LKG、Canary、kill-switch 与 reconciliation 状态；相关复核 90/90 通过；详见 §1.1、§6.5                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 本项无仓库内剩余；生产 PKI/用户身份、唯一默认 composition root、外部 kill/rollback authority 与目标部署演练分别归 P1-6、P2-1 和 OPT-7                                                                                                                        |
| P1     | EVO-P1-6 | 统一生产接线并退役重复壳                      | 🟢 仓库闭环 | CLI Agent durable ingress 已纵切交互 REPL、单轮 headless、长驻 stream headless 与 AgentRuntime；branded production composition root 显式组合 encrypted Raw/evidence/ArtifactPorts/EvolutionRun/Ledger/witness；canonical Graph App Server 与 legacy WebSocket Agent 均在 attempt/model/tool 前持久、拒绝客户端替换且仅在真实终态完成；Wiki Maintainer 三类 trigger 和 Registry 三事件链均已有真实持久 source、独立 resolver、Ledger queue、worker method 与 crash recovery；公开 npm eager/lazy 入口通过 Ed25519 descriptor 装载 exact-byte deployment module，Desktop 默认 `onReady()` 在 bootstrap/IPC 前装载同一部署描述并注入受治理依赖；两个不可达模拟壳已退役，公式训练壳已改为 metrics-only；当前集中复核 35/35 通过；详见 §1.1、§6.6                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 本项无仓库内剩余；真实 KMS/HSM、PKI、policy、独立 witness、scheduler/transition authority 的目标部署、resolver/worker 运维、两机恢复与密钥轮换演练统一归 OPT-7                                                                                              |
| P1     | EVO-P1-7 | 结构化 Memory、Compaction 与多 Agent 权力分离 | 🟢 仓库闭环 | shared canonical 四层 gate、真实 ArtifactStore+Ledger resolver、CLI/Desktop PostCompact、四类 producer、同 stream branded Agent root、新实例恢复、AgentRuntime semantic lifecycle、accepted matrix→真实 release CAS/reopen→root-owned procedural event、authority-ledger 自动 pending reconciliation、remote bridge、正式 durable file witness、file-backend 构造端口、通用 Ledger 重启及实际 release process-kill→新进程 Memory 恢复已落地；相关联合回归均通过；详见 §1.1、§6.7                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 本项无仓库内剩余；生产密钥托管/部署与跨主机 witness fault domain 由目标环境发布门验收                                                                                                                                                                      |
| P1     | EVO-P1-8 | SkillInvocationReceipt 与因果归因             | ✅ 已完成   | canonical schema 位于 shared session-core；Desktop DB 与 CLI hash-chain transcript 共用 builder/verifier；Skill/router/model/tool/policy/segment/outcome/grader/correction/token/cost/latency 均被 receipt digest 与 exact structural validation 固定，重算摘要不能夹带额外 claim、非法嵌套或矛盾 attribution；candidate/canary 缺字段在执行前失败；deterministic trace projection 可直接回答归因问题；verifier 定向 5/5、session-core 全量 544/544、Desktop 消费 8/8、CLI 消费 131/131；详见 §1.1、§6.8                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 无；Eval/Wiki 使用既有 refs 做上层领域 join                                                                                                                                                                                                                |
| P1     | EVO-P1-9 | 有界评分改进循环                              | ✅ 已完成   | candidate-only 内部控制面固定 baseline/splits/graders/runtime/gate/root budget；逐轮单候选、确定性 grader→隔离 evaluator、durable receipt、best gate、失败分类、active 不变式及 snapshot/resume/replay 已闭环；与 GoalConditionEngine 合计 2 文件 31/31 通过；提交 `4381aece2c`；详见 §1.1、§6.9                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 无；真实流量、人工 review、shadow/canary 明确归 EVO-P2-1                                                                                                                                                                                                   |
| P2     | EVO-P2-1 | 窄域受控生产 Pilot                            | 🟡 部分完成 | tenant/pilot/Skill 与 Why/Evidence、Diff/权限、Before/After、Eval/review 全链 digest 固定；显式 opt-in、有界 cohort、非自动化 durable review、active drift/readback、kill switch 及 durable prepare/finalize/reconciliation 已落地；risk-tier v2 计划允许预注册多级 Canary，服务端稳定万分桶 receipt 阻止客户端自报 cohort，paired 质量置信下界、成本/tool-call/p95/p99 非劣门和 security/permission 零容忍覆盖 shadow→Canary[n]→active-probation→stable；独立 gate authority 对完整报告签名，Pilot v2 只消费该回执并把每级 report/receipt digest 纳入 prepare/finalize/reconciliation；traffic worker 固定 runner/outcome handler identity，服务端 assignment、同请求 paired 执行、runner replay 验证、durable exclusive reservation、observation acknowledgement/readback 和 gate 投影已闭环；宿主外 watchdog 协议固定签名租约与 LKG，只读 durable latest heartbeat 且只监控非终态，超时后事故抢占并验证外部 kill/rollback durable receipt，持久恢复且阻止并发重复 effect；v2 plan 与 Node PKI adapter 固定 heartbeat/rollback Ed25519 SPKI digest，物理 store 和真实不同 PID 测试覆盖私钥隔离、kill 后崩溃、租约接管、LKG readback 及 fresh-process recovery；跨计划、报告替换、并发重复执行和旧 observation 绕过均拒绝；联合 42/42；详见 §7.1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 选择真实低风险租户/cohort；把 worker 接入目标流量入口；将 watchdog/durable store 部署到独立故障域并接入 reviewer/指标界面；完成实际观测窗、目标基础设施故障演练及按退出门扩量                                                                              |
| P2     | EVO-P2-2 | Evolution Workbench                           | 🟡 部分完成 | digest-bound 时间线、检索/分页、逐 packet batch、版本对比/回滚、CLI 命令和 metrics ArtifactStore+真实 Ledger/witness 已落地；canonical App Server 新增四个固定方法且只接受 branded host，SDK 无 generic RPC；VS Code、Desktop 与 JetBrains 条件式 Workbench surface 均可在宿主提供能力时显示详情、对比、modal approve/reject/rollback，且不持有 writer/identity；metrics 已形成 10,000 热 receipt + 不可变 retention root/count 冷归档、真实 Ledger/witness reopen 与冷重放拒绝，snapshot/version exact verifier 阻断重算摘要后的额外 claim、分数 count 与冷热总数漂移；新增 governance 摘要把 run、active/LKG、冲突和 Pilot kill-switch/reconciliation 状态贯穿三个客户端并在展示前校验；`serve` registrar/stdio/WebSocket transport 已贯穿显式注入的 branded host；公开 npm lazy/eager 入口新增 Ed25519 descriptor、module/trust-root digest、命令白名单与 exact-byte deployment module loader，未配置或篡改时继续 unavailable；VS Code `0.37.82` 候选在 RPC 前协商能力并安全处理默认 unavailable；loader/lazy-eager 28/28，既有协议 19/19、SDK 4/4、App Server/Workbench 19/19、IDE 15/15、Desktop 62/62、JetBrains 6/6 + smoke 1328/1328、verifier/metrics/Ledger 15/15；详见 §7.2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 目标部署签发 descriptor，并由模块提供真实 human identity/PKI/projection/transition/metrics authority 与生产 receipt source；完成目标环境部署、灾备和权限验收                                                                                               |
| P2     | EVO-P2-3 | Skill Retrieval Router                        | 🟡 部分完成 | 有界 router 已接入 Agent `list_skills(query)`、正式 `cc skill search`、Desktop SkillPalette、VS Code 与 JetBrains 只读检索命令；CLI/Agent transcript authority 具备 hash-chain/anti-rollback 验证，Desktop 本地 DB source 明确无独立 witness；Workbench persistent outcome index 复用 10,000 热 receipt、不可变 retention 与真实 Ledger/witness，只统计 graded/corrected completed/failed；新增只读 branded outcome reader、同租户多 composition 组装、只接受 branded authenticated+durable authority 的 historical catalog consumer，以及对完整 authenticated history 执行 hot/retained 覆盖校验、旧统计重算、CAS 提交与 readback 的 backfill/reconciliation；完整 writer/伪 reader/伪 catalog authority、跨 tenant、错绑 source、receipt 替换或不完整历史均拒绝，branded index 已透传交互、单轮 headless、stream、legacy WS 与 canonical App Server/Graph；query/corpus/model/index/result 全绑定并经独立 verifier 的 vector authority 已接入正式 Skill search、Agent 全运行模式与 Desktop `skills:route`，裸分数、renderer/客户端替换、漏项或错租户不能参与 rerank；Desktop/VS Code/JetBrains 显式校验 exact vector evidence，并继续严格区分 transcript/index evidence；新增跨进程 provider/verifier 组合端口固定独立 worker、可执行文件/entry 摘要、无 shell/无继承环境和输入输出/超时边界，并在调用前后重新认证文件；BM25 + 可选 vector + outcome 在歧义时 abstain，500 synthetic prompt 门已通过；新增 reader/composition 联合 62/62、headless parity 3/3、WS/Graph 联合 119/119、catalog 4/4、backfill 联合 19/19、vector/router 13/13、Agent vector parity 131/131、Desktop/Phase/bootstrap 62/62、VS Code 152/152、JetBrains 兼容 harness 6/6；详见 §7.3                                                                                                                 | 目标部署将 catalog/receipt loader 与 verifier 接到实际持久目录、KMS/PKI opener 并注入默认启动链，执行旧账迁移和审计验收；部署真实 embedding/index provider、独立 PKI verifier 与默认 Desktop authority 实例，用真实用户分布重跑并校准质量/延迟门及部署验收 |
| P2     | EVO-P2-4 | 跨设备、团队和组织知识                        | 🟡 部分完成 | 演化知识专用 fail-closed 同步核心已固定 tenant/device 与 personal/project/team/org scope；personal 禁止自动升域，team/org 强制 approval receipt；ciphertext-only envelope、密文字节 digest、签名、RBAC、durable local→transport 顺序、有界 vector clock、stale/replay、并发冲突人工合并及 revocation receipt→依赖 disposition 已落地；ArtifactStore+EvolutionLedger adapter 持久化 local/remote/conflict、支持响应丢失幂等恢复、实际 Ledger/witness 文件重开和跨 adapter conflict queue，并在读取时重新校验当前签名 authority；branded reader + human merge planner 拒绝陈旧 baseline，强制 exact joined vector，并要求非自动化、全内容绑定、独立认证且 durable 的人工 receipt；merge executor 以 Ledger prepare→独立 provider/verifier 幂等发布→settlement 关闭崩溃窗口，真实 sync merge publisher 以 v2 operationId、独立 publication reader 和 transport 重新确认完成响应丢失恢复；revocation dependency executor 对每项 rollback/quarantine/reject/tombstone 使用独立认证的稳定幂等操作，真实 Ledger/witness 重开不重复副作用，并发撤销只入冲突队列；CLI、Cloud Session 与 Desktop reviewer surfaces 已接通，均只输出脱敏摘要、不下放 authority 并阻断双裁决；真实 AES-256-GCM/Ed25519 authority 支持 scope key 轮换和 device SPKI pin，真实 Permission Engine + Ed25519 approval authority 精确隔离 scope/operation/action；trust ledger 以 ArtifactStore+EvolutionLedger/witness 持久化 reviewer 注册/撤销和 approval receipt，动态 registry 按当前 active key 验签并强制 revoke-before-rotate、禁止撤销 key 重激活；空/超限/非 canonical 密文、已重签元数据替换和伪造 receipt 均拒绝；25/25 知识定向、31/31 sync/merge/crypto、14/14 RBAC/approval、37 passed/1 existing skip trust ledger、78/78 跨端 transport、13/13 Desktop renderer 联合测试及生产构建通过；详见 §7.4 | 接入目标 KMS/HSM 装载/轮换控制与真实 dependency provider；完成两机离线、撤销和隐私删除传播 E2E                                                                                                                                                             |
| P2     | EVO-P2-5 | 跨模型 Skill 适配与市场治理                   | 🟡 部分完成 | canonical signed manifest 已固定 source commit、SBOM、dependency lock、permission、target matrix、Eval badge 与 lineage；唯一 exact target cell 必须经 adapter 重评测且通过 safety；更新按 candidate→shadow→canary→active 消费 exact pilot receipt，撤销进入 rollback；排名只消费目标 Eval 与有样本约束的 verified outcome，安装量/自报 confidence 不进入 score/digest；确定性 transition request 支持确认丢失重试；9/9 通过；详见 §7.5                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 切换 Desktop/CLI marketplace host；接入真实 PKI/SBOM builder/adapter/Eval authority 和跨进程状态；提供公开 badge 页面及部署/撤销 E2E                                                                                                                       |
| P2     | EVO-P2-6 | Wiki Pruning 与长时在线适应                   | 🟡 部分完成 | 复用 Maintainer 的 merge/decay/TTL/stale/tombstone 与 evidence reverse-dependency；plan 分离检索清理和审计保留，隐私删除先处置 dependent pattern/Skill，再经 deletion-bound Raw/cipher/key adapter 完成 KMS durable destroy、独立确认和 tombstone，最后更新 projection；各操作 exact request/durable ack；deletion receipt/tombstone 已接入真实 ArtifactStore+EvolutionLedger、逐次重新认证、幂等重试和两进程 60 秒内恢复；online adaptation 要求稳定离线闭环、candidate-only、同租户且 capability 只减不增；详见 §7.6 | 接入生产 KMS/HSM/PKI；将 Wiki Ledger、Skill rollback、Memory、retrieval 与 Marketplace 的真实 adapter 组合进同一跨进程传播时限 E2E；长期观测达标后才允许有限在线适应 |
