# ChainlessChain 项目审计报告

> 日期：2026-04-22
> 版本基线：产品 v5.0.2.49 / CLI 0.156.6 / Desktop 1.1.0-alpha / Root 0.37.8
> 审计范围：monorepo 全量（desktop-app-vue, packages/cli, backend, docs-*, mobile-app）

---

## 概览：优先级排序

| # | 问题 | 影响 | 修复成本 | 状态 |
|---|---|---|---|---|
| 1 | IPC handler 测试空白（安全关键路径） | 严重 | 高 | 进行中 |
| 2 | v-html XSS 未系统化消毒（50+ 站点） | 严重 | 中 | 待办 |
| 3 | Signal Protocol fork 长期无维护 | 严重 | 高 | 待决策 |
| 4 | Gov-V2 样板代码重复（iter16–iter28） | 中 | 低 | 待办 |
| 5 | 死代码 / 孤儿文件 | 低 | 低 | 待办 |
| 6 | CI 矩阵缺 macOS | 中 | 低 | 待办 |
| 7 | 文档版本漂移（CLAUDE.md vs 最新 bump） | 低 | 低 | 待办 |
| 8 | 超大 God Component 待拆 | 高 | 中 | 待办 |

---

## 1. 超大文件（God Component / 下一批拆分候选）

团队已完成：`SystemSettings.vue` 3444 → 1070（-69%，六个 pane）、`MainLayout.vue` 3203 → 1943（-39%，六次布局抽离）、`DIDManagement.vue` 1390 → 543（-61%，三次）。

下一批候选：

| 文件 | 行数 | 建议切分 |
|---|---|---|
| `desktop-app-vue/src/main/remote/browser-extension/background.js` | **15,509** | connection handler / message routing / command executor / stats tracking |
| `desktop-app-vue/src/main/skill-tool-system/tools/system-tools.js` | 5,743 | process exec / file ops / system monitor |
| `desktop-app-vue/src/renderer/pages/AIChatPage.vue` | **5,558** | 消息列表容器 / 输入处理 / 流式消费 |
| `desktop-app-vue/src/main/skill-tool-system/tools/ai-tools.js` | 5,414 | model call / tool invoke / result parse |
| `desktop-app-vue/src/main/skill-tool-system/tools/data-tools.js` | 4,505 | 数据读取 / 转换 / 写入 |
| `desktop-app-vue/src/main/database/database-schema.js` | 4,024 | （schema 堆积，可接受；建议迁移到声明式配置） |
| `desktop-app-vue/src/main/browser/browser-ipc.js` | 3,544 | 按 IPC 命名空间拆子 handler |
| `desktop-app-vue/src/main/remote/browser-extension-server.js` | 3,326 | — |
| `packages/cli/src/commands/cowork.js` | 3,659 | cowork god module |
| `packages/cli/src/commands/compliance.js` | 3,084 | — |

---

## 2. 死代码 / 孤儿文件

- **Backup 文件待清理**：`desktop-app-vue/src/preload/index.js.backup` —— 要么归档到 git，要么删除。
- **ESLint 大范围 disable**：desktop-app-vue/src 下 39 处 `/* eslint-disable */` 整块关闭，应改用行级 `// eslint-disable-next-line <rule>` 并注明理由。
- **TODO/FIXME**：desktop-app-vue/src 下 85+ 未入 issue 追踪。

---

## 3. 重复逻辑

### Gov-V2 样板（iter16–iter28）
136 个 V2 surface 每个都实现了等价的 `getXxxGovStatsV2()` + `_resetStateXxxGovV2()`。样例：
- `packages/cli/src/commands/*` 中 `agent-network.js`、`agent-router.js`、`aiops.js`、`a2a-protocol.js`、`community-governance.js`、`collaboration-governance.js` 等 10+ 文件

**建议**：抽 `GovernanceStatsV2Base`（或 mixin 工厂），集中：4-state / 5-state 状态机、caps 上限、auto-stale/auto-fail 回收逻辑、stats 聚合。

### Vue v-html 消毒
50+ Vue 模板使用 `v-html="renderMarkdown()"` 或 `v-html="highlightMatch()"`。`RichTextEditor` / `PluginComponentWrapper` 过了 DOMPurify，但以下直接绑定未消毒：
- `desktop-app-vue/src/renderer/components/chat/UserAssistantMessage.vue` — `renderedContent` 直出 markdown 渲染结果
- `desktop-app-vue/src/renderer/components/GlobalSearch.vue` — `highlightMatch()` 正则替换结果
- `desktop-app-vue/src/renderer/components/HelpCenter.vue` — `faq.answer` 社区内容

---

## 4. 测试空白（安全关键路径零测试）

### IPC Handlers（零测试覆盖）
- `desktop-app-vue/src/main/ai-engine/a2a/a2a-ipc.js` — agent 发现 / 分布式任务编排
- `desktop-app-vue/src/main/ai-engine/agents/agents-ipc.js` — agent 生命周期
- `desktop-app-vue/src/main/ai-engine/ai-engine-ipc.js` — AI 引擎核心事件分发
- `desktop-app-vue/src/main/ai-engine/autonomous/autonomous-ipc.js`
- `desktop-app-vue/src/main/ai-engine/code-agent/code-agent-ipc.js`
- `desktop-app-vue/src/main/ai-engine/autonomous/collaboration-governance-ipc.js`
- `desktop-app-vue/src/main/ai-engine/autonomous/tech-learning-ipc.js`

### AI Engine 并行实现（无覆盖）
- `desktop-app-vue/src/main/ai-engine/ai-engine-manager-*.js` —— P1 / P2 / optimized 三个并行版本
- `agent-coordinator.js`、`agent-templates.js`、`autonomous-developer.js`

**建议**：基于 `desktop-app-vue/src/main/utils/ipc-validator.js` 的合约加一批集成测试（至少覆盖 validate-error 路径 + happy path）。

---

## 5. 依赖健康

### 高风险
- **`@privacyresearch/libsignal-protocol-typescript@0.0.16`** — 第三方 fork，长期无更新。加密协议库停更 = 安全红线。**建议评估替换到 `libsignal-node` 官方实现或活跃维护的 fork**。
- **Handlebars 4.7.9** — 最近更新 2023，复查是否存在模板注入路径（尤其在 cowork 模板引擎中）。

### 次要
- **core-* 内部包**（`@chainlesschain/core-config` 等）始终固定 `0.1.0`，共享工具层从未发版 → 版本意图不明。
- **`koffi` / `robotjs`** 在 `optionalDependencies`，无独立审计痕迹。

### 健康
- Electron 39.2.6、Vue 3.4.0、Commander 12.1.0、chalk 5.4.1 均在合理窗口
- `overrides`: `cookie >= 0.7.0`、`axios 1.15.0` 已 pin，安全意识到位

---

## 6. 安全面

### v-html（见 §3）

### exec / child_process
- 代码内 `.db.exec(...)` 均为 SQLite 预编译语句，安全
- 未发现 `child_process.exec()` 直接拼接变量（好）
- `desktop-app-vue/src/main/browser/browser-ipc.js` 走 IPC 调度浏览器命令 → 需校验 payload schema

### IPC 校验
- `desktop-app-vue/src/main/utils/ipc-validator.js`（~262 行）存在，但是否所有 handler 都强制走 guard 未知，需要审计。

### 硬编码凭证
- 未发现明文 API key / token（好）

---

## 7. 构建 / CI

### `.github/workflows/test.yml`
- 单元测试矩阵：`ubuntu + windows`，Node 22.x —— **缺 macOS**（P2P/WebRTC 平台差异无覆盖）
- 超时 45 分钟，接近上限，可能掩盖性能退化
- 无突变测试（mutation testing）

### `.github/workflows/cli-ci.yml`
- 矩阵：`ubuntu + windows + macos`，Node 22.12.0 ✓
- unit → integration → e2e 顺序执行（无并行），~30 min 跑完
- 无显式 skipped 测试

---

## 8. 文档漂移

- `CLAUDE.md` 头部声明 `v5.0.2.43 / 7600+ Tests`，最近 commit 已经 bump 到 `v5.0.2.49 / CLI 0.156.6`（commit `fdc9df624`）
- `CLAUDE.local.md` Phase 1 仍写"已统一到 v5.0.2.43"
- `git status` 有 7 个 docs 文件 modified 未提交：
  - `deploy-docs.py` / `deploy-www.py`
  - `docs-site-design/docs/index.md`
  - `docs-site/docs/chainlesschain/overview.md`
  - `docs-site/docs/changelog.md`
  - `docs-site/docs/index.md`
  - `docs-website-v2/src/pages/index.astro`

---

## 执行计划

按依赖和影响排序（在 TaskList 中跟踪）：

1. **IPC handler 集成测试** — 覆盖 6 个零测试 handler（安全红线）
2. **v-html XSS 审计 + 统一消毒** — 封装 `safeHtml()` helper + 修 3 个最高风险站点 + lint 规则
3. **Signal Protocol 依赖评估** — 需用户决策（成本高）
4. **Gov-V2 基类抽取** — 去重 ~10 个 CLI 样板
5. **死代码清理** — 删 `.backup`、triage ESLint disable
6. **CI 矩阵加 macOS** — 一行改动
7. **文档版本回正** — CLAUDE.md + 已改未提交的 docs
8. **大文件切分** — AIChatPage.vue + browser-extension/background.js（按已有 pane 抽离模式）

---

*Generated by Claude Code audit (Opus 4.7), 2026-04-22*
