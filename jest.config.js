/**
 * Root Jest config that reuses the existing configuration under config/.
 * Keeping this stub at the repo root allows the default `jest` CLI invocation
 * (used by package scripts) to pick up the intended settings.
 */
module.exports = require('./config/jest.config.js');
