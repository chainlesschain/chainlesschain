# ChainlessChain 环境变量配置示例
# 复制此文件为 .env 并修改配置

# PostgreSQL 配置
POSTGRES_PASSWORD=chainlesschain2024

# 可选: 自定义端口 (如果默认端口冲突)
# POSTGRES_PORT=5432
# REDIS_PORT=6379
# QDRANT_PORT=6333
# OLLAMA_PORT=11434

# GPU 配置 (如果使用 NVIDIA GPU)
# 取消注释 docker-compose.production.yml 中的 GPU 配置
# NVIDIA_VISIBLE_DEVICES=all

# Ollama 模型配置
OLLAMA_MODELS=qwen2:7b,llama2:7b

# 日志级别
LOG_LEVEL=info

# 数据持久化路径 (可选)
# DATA_PATH=./data
