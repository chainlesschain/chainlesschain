# ChainlessChain Desktop - 个人AI知识库

基于U盾加密的去中心化个人知识管理系统 - 桌面版

## 快速启动 ⚡

### 1. 安装依赖

```bash
npm install
```

### 2. 启动应用

```bash
npm run dev
```

应用将在5-10秒内自动打开！

### 3. 登录

- **PIN码**: `123456`
- **模式**: U盾模拟

## 功能特性 ✨

### ✅ 已实现 (MVP v0.1.0)

- **用户认证**
  - PIN码登录
  - U盾状态检测
  - 模拟硬件加密

- **知识库管理**
  - 创建/编辑/删除笔记
  - Markdown编辑器
  - 实时自动保存
  - 预览模式
  - 全文搜索

- **AI对话**
  - 聊天界面
  - 消息历史
  - 上下文支持
  - LLM服务集成

- **现代UI**
  - 三栏布局
  - 响应式设计
  - Ant Design组件
  - 流畅动画

### ⏳ 计划中 (v0.2.0+)

- 数据持久化 (SQLCipher)
- AI向量检索 (Qdrant)
- Git自动同步
- 真实U盾集成
- 移动端支持

## 项目结构 📁

```
desktop-app/
├── src/
│   ├── main/              # Electron主进程
│   │   ├── index.ts       # 应用入口
│   │   ├── database.ts    # 数据库管理
│   │   ├── ukey.ts        # U盾管理器
│   │   ├── git-sync.ts    # Git同步
│   │   └── llm-service.ts # LLM服务
│   │
│   ├── preload/           # IPC桥接
│   │   └── index.ts
│   │
│   ├── renderer/          # React前端
│   │   ├── App.tsx        # 主组件
│   │   ├── pages/         # 页面
│   │   ├── components/    # 组件
│   │   ├── stores/        # 状态管理
│   │   └── utils/         # 工具函数
│   │
│   └── shared/            # 共享代码
│       └── types.ts       # 类型定义
│
├── package.json
├── tsconfig.json
└── vite.config.ts
```

## 技术栈 🛠️

### 前端
- React 18 + TypeScript
- Ant Design 5
- Zustand (状态管理)
- React Router (路由)
- Vite (构建工具)

### 后端
- Electron 28
- Node.js 24
- 内存数据库 (MVP)
- Isomorphic-git
- Node-forge (加密)

### AI服务 (可选)
- Ollama (LLM)
- Qdrant (向量DB)
- AnythingLLM (RAG)

## 开发命令 📜

```bash
# 开发模式 (推荐)
npm run dev              # 同时启动Vite和Electron

# 单独启动
npm run dev:renderer     # 只启动Vite
npm run dev:electron     # 只启动Electron

# 构建
npm run build:preload    # 构建Preload
npm run build:main       # 构建主进程
npm run build:renderer   # 构建前端
npm run build            # 构建全部

# 类型检查
npm run type-check       # TypeScript检查

# 打包
npm run package          # 打包为可执行文件
```

### 打包与发布

```
npm run make:win   # Windows：Squirrel 安装器 (.exe) + zip
npm run make:mac   # macOS：dmg 安装包 + zip
npm run make:linux # Linux：AppImage + deb + rpm + zip
```

- 产物默认输出在 `out/make/` 下：`squirrel.windows/ChainlessChain-Setup-<version>.exe`、`dmg/ChainlessChain-<version>.dmg`、`appImage/ChainlessChain-<version>.AppImage`、`deb/chainlesschain_<version>_amd64.deb`、`rpm/chainlesschain-<version>.x86_64.rpm`。

## 使用指南 📖

### 基本操作

1. **创建笔记**
   - 点击 "新建笔记" 按钮
   - 输入标题和内容
   - 支持Markdown语法

2. **编辑笔记**
   - 点击列表中的笔记
   - 在编辑器中修改
   - 自动保存 (1秒延迟)
   - 或按 `Ctrl+S` 手动保存

3. **预览笔记**
   - 点击 "预览" 按钮
   - 查看渲染效果
   - 再次点击返回编辑

4. **搜索笔记**
   - 在顶部搜索框输入
   - 实时过滤结果

5. **删除笔记**
   - 点击笔记右侧删除图标
   - 确认删除

6. **AI对话**
   - 点击右上角 "AI助手"
   - 输入问题
   - 按 `Ctrl+Enter` 发送
   - 可选择是否使用当前笔记作为上下文

### 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+S` | 保存笔记 |
| `Ctrl+Enter` | 发送AI消息 |
| `Ctrl+K` | 打开AI助手 (计划) |
| `Ctrl+N` | 新建笔记 (计划) |

## MVP限制 ⚠️

### 当前版本使用内存数据库

- ❌ **数据不持久化** - 重启后丢失
- ❌ **AI需要Ollama** - 可选启动
- ❌ **Git同步未启用** - UI已实现
- ❌ **模拟U盾** - 未集成真实硬件

这是**预期行为**，完整功能将在后续版本实现。

## 启动AI服务 (可选) 🤖

如需使用AI功能：

```bash
# 进入Docker目录
cd ../backend/docker

# Windows
./setup.bat

# Linux/Mac
./setup.sh

# 验证服务
curl http://localhost:11434/api/tags
```

服务启动后，AI对话功能将自动可用。

## 故障排除 🔧

### 问题: 应用未打开

**原因**: Vite服务器启动较慢
**解决**: 等待10-15秒，查看终端输出

### 问题: 白屏

**解决**:
```bash
# 清理重装
rm -rf node_modules
npm install
npm run dev
```

### 问题: 端口被占用

修改 `vite.config.ts`:
```typescript
server: {
  port: 5174  // 改为其他端口
}
```

### 问题: TypeScript错误

```bash
npm run type-check
```

### 问题: 构建失败

```bash
npm run build:preload
npm run build:main
```

## 测试 🧪

查看 `TEST_GUIDE.md` 了解完整测试流程。

基本测试:
1. 启动应用
2. 使用PIN `123456` 登录
3. 创建一个笔记
4. 编辑内容
5. 搜索笔记
6. 打开AI助手

## 文档 📚

- `TEST_GUIDE.md` - 测试指南
- `../HOW_TO_RUN.md` - 运行说明
- `../QUICK_START.md` - 快速开始
- `../IMPLEMENTATION_COMPLETE.md` - 实现报告
- `../docs/` - 详细文档

## 性能指标 📊

- ✅ 启动时间: < 5秒
- ✅ 笔记切换: < 100ms
- ✅ 搜索响应: < 200ms
- ✅ 保存延迟: 1秒 (防抖)

## 安全特性 🔒

- **U盾加密** (模拟)
  - RSA 2048位密钥
  - PIN码验证
  - 数字签名

- **IPC安全**
  - contextBridge隔离
  - 类型安全API
  - 错误处理

- **数据隔离**
  - 渲染进程无直接访问
  - 所有操作经IPC
  - 未来支持SQLCipher加密

## 贡献 🤝

欢迎提交Issue和Pull Request！

### 开发流程
1. Fork项目
2. 创建特性分支
3. 提交代码
4. 发起PR

### 代码规范
- TypeScript严格模式
- ESLint规则
- Prettier格式化

## 许可证 📄

MIT License

## 作者 👨‍💻

ChainlessChain Team

---

**立即开始**: `npm install && npm run dev` 🚀
