"use strict";

const {
  BrowserHistoryEdgeAdapter,
  BROWSER_HISTORY_EDGE_NAME,
  BROWSER_HISTORY_EDGE_VERSION,
} = require("./adapter");

module.exports = {
  BrowserHistoryEdgeAdapter,
  BROWSER_HISTORY_EDGE_NAME,
  BROWSER_HISTORY_EDGE_VERSION,
};
