/**
 * @deprecated — canonical implementation lives in
 * `../harness/jsonl-session-store.js` as of the CLI Runtime Convergence
 * roadmap. This file is retained as a re-export shim for backwards
 * compatibility and will be removed once all external consumers have
 * migrated.
 *
 * Please import from `packages/cli/src/harness/jsonl-session-store.js`
 * in new code.
 */

export {
  _sessionScaleFaultHooks,
  appendEvent,
  appendEventIfHead,
  appendWsTurnIfHead,
  claimWsTurnIfHead,
  settleWsTurnClaim,
  appendAuthorityEvent,
  appendAuthorityEventIfHead,
  startSession,
  appendUserMessage,
  appendAssistantMessage,
  appendToolCall,
  appendToolCallCompact,
  appendLlmRetryCompact,
  MAX_COMPACT_DURATION_MS,
  appendToolResult,
  appendCompactEvent,
  readEvents,
  readVerifiedEvents,
  readVerifiedWsTurnState,
  computeWsTurnInputDigest,
  createWsTurnClaimId,
  createWsTurnLifecycleReducer,
  normalizeWsTurnRequestId,
  projectWsTurnClaim,
  projectWsTurnMessages,
  projectWsTurnSettlement,
  findLatestEvent,
  rebuildMessages,
  getJsonlSessionMetadata,
  listJsonlSessions,
  listSessionIds,
  resolveSessionId,
  renameSession,
  deleteJsonlSession,
  pruneJsonlSessions,
  forkSession,
  createBranchSession,
  sessionExists,
  getLastSessionId,
  migrateLegacySessions,
  migrateLegacySessionsBatch,
  migrateLegacySessionFile,
  validateJsonlSession,
  validateAllJsonlSessions,
  sampleMigratedSessionsValidation,
  sessionPath,
  isUnsafeSessionId,
  appendTokenUsage,
  toIsoSafe,
  verifySession,
  verifyAllSessions,
  repairSession,
  TRANSCRIPT_CHAIN_STATUS,
  WS_TURN_EVENT,
  WS_TURN_CLAIM_EVENT,
  WS_TURN_SCHEMA_VERSION,
  WS_TURN_REQUEST_ID_MAX_BYTES,
} from "../harness/jsonl-session-store.js";
