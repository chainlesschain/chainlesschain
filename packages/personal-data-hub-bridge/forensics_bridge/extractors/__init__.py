"""Extractors — pull raw data off connected devices.

Phase 4.5.4-min scope: thin ADB wrappers (list_devices, pull_file).
Subsequent sub-phases add full extraction modes (adb backup / apk downgrade /
root cp), iOS itunes-style backups, and on-device decryption helpers.
"""
